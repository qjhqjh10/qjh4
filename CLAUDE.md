# CLAUDE.md — d:/4 卡牌项目的工作准则

> 这个文件**每次会话自动加载**。任务总入口是 `项目任务.md`，各条线的交接文档见它末尾的表。
> 本文件只放**跨会话必须遵守的规矩**，不放进度（进度在交接文档里）。

---

## 一、铁律（用户 2026-09-12 明确要求）

### 1. 读大量文件 / 大文件，优先派**子代理**去读

主对话窗口的上下文是**最稀缺的资源**。一个 4000 行的脚本、一份 1243 行的运行时 dump、
一个 79 KB 的交接文档 —— 直接读进来，主对话就没法干活了。

**怎么做：**

- 需要「从一堆文件里找答案」时 → 派 `Explore` 子代理，**在提示里写清楚**：
  要查什么、已知什么（别让它重复查）、**输出格式和行数上限**。
- 子代理的返回里只给**结论 + 证据（文件名:行号）**，不要贴大段原文。
- 判断标准：**这个文件我需要逐行看吗？** 不需要 → 派子代理。
  需要（比如要改的源码、要照着写的文档）→ 自己读。

**什么时候自己读**：要动手改的文件、要逐字对齐语义的权威来源（`rule_core.gd` 的某一段）、
自己刚写的文件。这些读了才有用。

### 2. 遇到不了解的事，**先读解包资源**，别猜

这个项目的优势是**原版资源基本都在本地**，绝大多数问题都有确凿答案 —— 不要凭印象设计。

| 想知道什么 | 去哪读 |
|---|---|
| 原版界面长什么样、控件叫什么、放在哪 | `d:/2/解包整理/07_场景/battlearena1/`（场景 JSON）+ `d:/4/Unity/资料/原版参照图/Unity参照管线_0825/data/runtime_ui_dump_*.tsv`（**运行时实况 dump**，有 sprite 名和 activeSelf） |
| 原版规则语义、某个关键词怎么结算 | `d:/warpforge/scripts/rule_core.gd`（权威语义来源）+ `d:/4/Unity/资料/规则书/Warpforge_Offline_Rulebook_1_5-3_中文翻译.md`（61 关键词的时机表在 :161） |
| 原版卡的数据 | `d:/4/Unity/数据/游戏数据/card_stats.json`（`desc` 是卡面效果文字） |
| 原版的美术 | `d:/4/Unity/素材/Warpforge原版/`（卡框/UI图集/卡背/战场贴图） |
| 某个原版图集切片叫什么 | `素材/Warpforge原版/UI图集/图集/<图集名>/sliced/` + 同目录 `_atlas_rects.json` |
| 原版改过哪些规则 | 各文档里的「2026-08-2x 修正」注释 —— **这些是踩过的坑，别推翻** |

**没查到就说没查到**，不要用「大概是」糊过去。

### 3. 复刻要**照解包资源里的说明**来，不能只凭截图（用户 2026-09-12 要求）

截图只能告诉你「这一帧长什么样」，**告诉不了你参数** —— 尺寸、间距、阈值、颜色、时机、
哪些元素是互斥的、显示/隐藏的条件。这些东西全在解包资源的字段里。

**做法：**

- 动手做一个原版有的东西之前，**先把它在解包资源里的对象找出来**，把数值抄下来，
  **写在代码注释里并标出处**（对象路径 + 字段名）。例：`AttackSelector.cs` 头部那一串。
- 截图只用来**验收**（做出来像不像），不用来**定参数**。
- 资源里**查不到的部分，写「查不到」**，或者明确标成「这是我们挑的，不是原版的做法」——
  绝不能把猜测写成「原版就是这样」。

**踩过的：**
- `Select Attack Background` 的 `sizeDelta` 是 780×285，但 `localScale = 0.44`、
  显示时再横向 ×1.5 → **实际 514.8×125.4**。只看截图/只看一个字段都做错。
- 我一度以为 `40K_melee_glow` 是攻击按钮的外发光（因为「看起来像」），
  实际是**场上单位身上的高亮** —— 在预制体目录里，不在场景里。**凭印象认图会认错。**

### 4. 🔴 **跑起原版游戏，对着实况验证**（用户 2026-09-12 要求）

**本地有能跑起来的原版游戏**（`d:/2/unity_run_ref/`，装了 MelonLoader + SceneJumpShot mod）。
解包资源是**静态快照**，能告诉你字段值，**告诉不了你运行时实际用哪个值、什么时候用、长什么样**。
所以复刻任何一处，**能跑到实况就要跑到实况**：

**做法：**

- 复刻某个界面/交互/数值之前，**先进原版游戏把那一处跑出来**：
  看真实的对象层级、真实的 activeSelf、真实的数值、（能拍就拍）真实的画面。
- 自己的实现做完之后，**再进一次原版，对着同一处比**。自我感觉「像」不算数。
- **实况与解包字段冲突时，以实况为准**，并在代码注释里写明「实况是 X，字段是 Y，按实况」。
- 跑不起来 / 那个界面进不去时，**如实说「没跑到实况」**，别拿静态字段冒充运行时结论。

**为什么单开一条**：解包资源里同一个场景常有**多份同类实例、值互相矛盾**
（`CardsHorizontalLayout` 的 MB 5271 vs MB 4053，`m_scale` 是 0.73 vs 0.54）；
而且 `activeSelf`、显示条件、运行时才赋的 `buttonIcon` 这些东西**只有跑起来才知道**。
参照图也踩过 —— `shots_ui/ui_prefabs/` 那 218 张 PNG **全是黑的**，当参照用就全错了。

**能验的和验不了的**（2026-09-12 实测，别去撞墙）：

| 能做 | 做不到 |
|---|---|
| 跳**任意 bundle 场景**（含主菜单） | **手牌注入** —— `inj FAIL: empty Data`，根因是原版已关服、卡数据在远程 CCD；`BattleDebugController` 在正式版被裁掉，**mod 修不了** |
| **dump UI 树**（path / name / **activeSelf** / rect / pos / text / image） | **卡组编辑界面** —— 实例化即黑，`解包资源使用地图.md` 已记「deck 链路无原版视觉证据，以 Unity JSON 为准」 |
| 拍**真渲 RT 图**（战场/火焰/烟/碎片都是活的） | 主菜单只拍得到顶栏（97% 靠运行时实例化） |
| 点 `DebugButtons` 里的调试按钮 | 改游戏数据/存档；`ScreenCapture` 截图会崩 |

配置：`d:/2/unity_run_ref/UserData/sjs_dump_cfg.txt` 四行 —— ①bundle 文件名 ②等几秒 ③输出目录
④动作（`drive` / 空 / 秒数列表）。启动前要设 `DOTNET_ROOT`，副本路径必须纯 ASCII。
详见 `项目任务.md` 第八节「常用操作速查」。

---

## 二、环境

```bash
# ⚠️ 本机带了 ELECTRON_RUN_AS_NODE，启动 Unity 前**必须 unset**，否则静默失败
UNITY="D:/Unity/Hub/Editor/6000.3.23f1/Editor/Unity.exe"
unset ELECTRON_RUN_AS_NODE && "$UNITY" -batchmode -quit \
  -projectPath "D:\4\Unity\MyGame" -executeMethod <入口> -logFile -
```

常用 `-executeMethod` 入口（详见 `资料/规则引擎_进度与交接.md` 第一节）：
`RuleEngineTest.Run` / `BattleScene.Run` / `CardBaseDemo.Run` / **`DeckScene.Run`** /
`BattleScene.BuildAndSaveScene` / **`DeckScene.BuildAndSaveScene`** /
`VfxPicker.Run` / `ArtBaker.BakeFromCLI` / `DistortProbe.Run`

**动手改完东西，至少复跑这三条**：`RuleEngineTest.Run`（规则/卡组）、`BattleScene.Run`（对战）、
`DeckScene.Run`（卡组编辑）。**改了版面还要看截图** —— 断言测不出「压暗没铺满」这种问题。

---

## 三、这个工程反复强调的几条（血换来的）

- **改动要加断言，别只看截图。** 截图看不出「播的是不是该播的那个」。
  改版面之后 `CardBaseDemo.Run`（四档分辨率）**必须复跑**。
- **不许静默失败。** 没实现的东西要**说出来**（`ErrUnimplemented` / 卡面打 `*` / 日志警告），
  不能让玩家以为它有作用。
- **对局必须可复现。** 引擎里只用 `System.Random(seed)`，不用 `UnityEngine.Random`。
  定死的规则（比如「取槽号最小的那个」）**不要改成随机**。
- **批处理下没有帧循环**：`Object.Destroy` 不生效（用 `DestroyImmediate`）、
  粒子要手动 `Simulate`、特效播放器靠 `Update` 自毁所以不会自己消失。
- **两处写同一条规则 = 迟早不一致。** 判据要共用一份（例如「近战还是远程」只有
  `SimpleAI.UseRanged` 一处），表现层要用就转发。
- **🆕 原版材质上带着内置 Standard shader 的残留值。** 原版 shader 换过之后，
  旧材质里的 `_SrcBlend/_DstBlend/_ZWrite/_Surface` 等**不会被清掉**。原版 shader 属性表里
  没有这些名字时它们是**死值**；但自建 shader 一旦用 `Blend [_SrcBlend][_DstBlend]` 间接寻址，
  就会把死值当活值用（实测把抓屏扭曲变成了不透明覆盖 + 写深度）。
  **动手改自建 shader 前先跑 `工具/dump_shader.py` 读原版的属性表和 pass 状态**：
  属性表里没有的，一律硬编码。
  ⚠️ 原版的 **HLSL 源码**确实被剥了（`Shader.m_Script` 是 null），但**编译字节码在** ——
  以前我写成「源码和字节码都被剥掉了」，那是**读错了属性**（`m_SubProgramBlob` 是 None，
  真数据在 `Shader.compressedBlob`）。解出来是 DXBC，**资源名是明文**，
  「这个 shader 采样哪张纹理」能查证。工具：`工具/dump_shader_blob.py`。
- **🆕 判定「效果是否还原」要先确认尺子有意义。** 抓屏扭曲这类效果在**没有内容可扭曲**的
  空场景里本来就该是空的；曾经把这种「尺子的假象」当成「导出整个丢了」，白记了几十条。

---

## 四、当前阶段与任务顺序（2026-09-12）

**目标：把原版的战斗、卡组编辑和周边功能尽可能还原到位。**

**联网味的功能也要**（用户 2026-09-12 明确）：倒计时 / 出牌超时 / 投降 / 换牌倒计时 ——
原版有的都尽量做，不要以「单机用不上」为由砍掉。

**美术资源以后再说** —— 等原版复刻得差不多了再动手。机制早就位
（`Resources/Art/` 换同名文件即可、删目录退回占位美术），**不用现在操心、不用反复提**。

**还差什么，看 `项目任务.md` 的「九、距离完整还原原版还差什么」** —— 那里是 2026-09-12
把反编译代码 + 运行时 UI dump 挖了一遍之后的对账结果，分四类：
①玩法骨架（卡组编辑 / 对局结算 / 换牌 / 战斗日志 / 卡牌放大详情 —— **整块没有**）
②表现与手感（发牌入场 / 攻击位移 / 命中反馈 / AnimFX 脚本层）
③内容深度（战术卡 478 张 / 关键词差 48 个 / 对手 AI）
④特效还原质量（183 个高置信问题 / 构建后 player 从没验过）
