# CLAUDE.md — d:/4 卡牌项目的工作准则

> 这个文件**每次会话自动加载**。任务总入口是 `项目任务.md`，各条线的交接文档见它末尾的表。
> **卡牌阵营这条线的下一个会话从哪开始**：`资料/阵营推进_清单与交接.md`。
> 本文件只放**跨会话必须遵守的规矩**，不放进度（进度在交接文档里）。

---

## 一、铁律（用户 2026-09-12 明确要求）

### 1. 读大量文件 / 大文件，优先派**子代理**去读

主对话窗口的上下文是**最稀缺的资源**。一个 4000 行的脚本、一份 1243 行的运行时 dump、
一个 79 KB 的交接文档 —— 直接读进来，主对话就没法干活了。

**怎么做：**

- 需要「从一堆文件里找答案」时 → 派 `Explore` 子代理，**在提示里写清楚**：
  要查什么、已知什么（别让它重复查）、**输出格式和行数上限**。
- 子代理的返回里只给**结论 + 证据（文件名:行号）**，不要贴大段原文。
- 判断标准：**这个文件我需要逐行看吗？** 不需要 → 派子代理。
  需要（比如要改的源码、要照着写的文档）→ 自己读。

**什么时候自己读**：要动手改的文件、要逐字对齐语义的权威来源（`rule_core.gd` 的某一段）、
自己刚写的文件。这些读了才有用。

### 1·b 🔴 **简单、重复的活也要派子代理 —— 多路并行**（用户 2026-09-13 明确要求）

> 「要利用好子代理，多派子代理去完成简单、重复的任务。这样可以**多路并行**，加快项目进度。」

**不等号**：不是「只有大文件才派子代理」。**顺手就能切块、块之间互不依赖**的活，
**一律派出去并行做**，主对话只留「要动脑、要动手改」的部分。

**什么样的活该派（典型形状）：**

| 形状 | 例子 |
|---|---|
| **逐张 / 逐阵营 / 逐文件普查** | 「1118 张卡图逐张抄一遍再对账」（29 个子代理）· 「13 个战场各盘一遍」 |
| **批量提取 / 统计** | 从一堆 `.md`/JSON 里抽某类数据做成表 |
| **批量验证一条判据** | 「每个 subtype 的卡是不是都满足 …」 |
| **逐条对账** | 两份数据源逐条比，列出差异 |
| **同一调查的多条线索并行** | 「原版怎么做 X」→ 一个查规则书、一个查反编译、一个查解包资源 |

**派的时候必须写清四样**（少一样就会拿回一堆没用的东西）：
① **要查什么** ② **已知什么**（别让它重复查） ③ **输出格式** ④ **行数上限**。

📋 **现成的清单**：**`资料/可并行任务清单.md`** —— 2026-09-13 盘点出 **16 条**能派的活，
每条都写了「切几块 / 每块产出什么 / 数据在哪」，**还带一节「不建议派的」**（那些看着能派、实际不能的）。
**开工前先扫它一眼。**

**能并行的与不能并行的（本机实测）：**

- ✅ **读文件、写 python 脚本、跑分析** —— 可以多路并行
- ❌ **Unity 批处理** —— **同一工程同一时刻只能跑一个实例**
  （第二个进程 `Exiting ... return code 1`，一行测试都没跑）。所以 `RuleEngineTest.Run` 那四条
  **必须串行**，「派子代理并行跑测试」在这台机器上**不成立**。
- ⚠️ 派之前先看一眼**有没有别的子代理正在动同一批文件** —— 撞车会写出两份不一致的结论。

### 2. 遇到不了解的事，**先读解包资源**，别猜

这个项目的优势是**原版资源基本都在本地**，绝大多数问题都有确凿答案 —— 不要凭印象设计。

| 想知道什么 | 去哪读 |
|---|---|
| **原版资源（首选，2026-09-12 起）** | **`d:/2/新解包资源/assets_full/`**（**4.4 GB / 24.7 万文件**，本地缓存、只读）—— 按 **bundle 分包**、每包下按资产类型分目录（`Texture2D/` `Sprite/` `MonoBehaviour/` `GameObject/` …）。⚠️ **图一定用这一份**：旧的 `d:/2/解包整理/` 里**650 张同名 PNG 内容不一样**（例：卡牌插图旧的是 660×1024、新的是 1024² 原图）。⚠️ 它还覆盖了旧的没解的基础文件（`resources.assets` / `level0` / `globalgamemanagers.assets`），`Sprite/*.json` 里有**旧解包没有的 `textureRect`**（sprite 在图里的真实矩形）—— 定尺寸全靠它 |
| 原版界面长什么样、控件叫什么、放在哪 | `d:/2/解包整理/07_场景/battlearena1/`（场景 JSON）+ `d:/4/Unity/资料/原版参照图/Unity参照管线_0825/data/runtime_ui_dump_*.tsv`（**运行时实况 dump**，有 sprite 名和 activeSelf） |
| **原版有哪些环节 / 节点叫什么**（最快入口） | **`d:/4/Unity/资料/说明书/`** —— `01_战斗_对战/` 有**13 个战场各一份的 2D 全树**（1200+ 行/份）、`04_界面UI/菜单全树.md`、`05_游戏数据/脚本定义.md`（2294 个类名索引）。⚠️ 这份目录 2026-09-12 之前一直没被用起来 |
| **战斗规则 / 战斗数值**（五层各答什么、标准查法） | **`d:/4/Unity/资料/战斗规则与数值_出处.md`** —— 先查它，别一层没找到就说「查不到」 |
| **规则书说了什么 / 我们做到哪**（复制·选择·随机部署·时机·结算顺序…逐条对账） | **`d:/4/Unity/资料/规则书_实现指南对账.md`** —— 含**最该马上改的三条**；⚠️ 也写了附录 B/D **不能当原版语义源** |
| 原版规则语义、某个关键词怎么结算 | `d:/warpforge/scripts/rule_core.gd`（权威语义来源）+ `d:/4/Unity/资料/规则书/Warpforge_Offline_Rulebook_1_5-3_中文翻译.md`（61 关键词的时机表在 :161） |
| **某个词算不算督军**（**单位 ≠ 部队**） | 规则书中文版 `:70-75`：「**单位（Units）**：任何有攻击与生命值的卡（**含督军与衍生物**）· **部队（Troops）**：仅部队卡，**不含督军**」⇒ 卡面写 `units` 时**督军要算**、写 `troops` 才不算。**判据只此一处**：`EffectResolver.AddSide` 的 `troopOnly`（`= spec.Kind == "troop"`）。⚠️ **这条踩过两次**：解析层把 `for each friendly unit` 归成 `troop` ⇒ **14 张卡静默少算一个督军**；而且一条老断言把这个错口径**钉死了**（注释写着「督军不算 —— 卡面写的是 `unit` 不是 `any`」）—— **自检绿不等于口径对** |
| **类名 / 字段名 / 枚举值** | `d:/2/Warpforge_code/Scripts/Assembly-CSharp/`（**2294 个类的签名桩**：字段/枚举完整，**方法体是空的**） |
| **反编译 `.c` 里的 `_DAT_xxxxxxxx` 常量** | 那些是**能读的**字面量：`RVA = 地址 − ImageBase(0x180000000)`，再查 PE 节表换成文件偏移，从 `d:/2/unity_run_ref/GameAssembly.dll` 读 4 字节。脚本和自检办法见 `资料/战斗规则与数值_出处.md` §三 |
| **原版卡**拼好的成品图**（卡面装配的尺子，2026-09-12 用户指出）** | **`d:/2/Warpforge部队卡片/<阵营>/<1督军·2天赋·3部队·4计策·5防御卡>/*.png`** —— 900×1200 的 PnP 卡图，**卡名白 / 阵营橙 / 效果文字米白 / 兵种橙**，费用在右上、**数值沿卡底一排四个**（左圆红剑=近战 · 右圆紫枪=远程 · 银盾=护甲 · 最右绿框=生命）。另有 `card.html`（打印用模板）与三份规则书。⚠️ **对着它比才发现我们原来把卡名画成了橙、阵营行和兵种行根本没画** |
| 原版卡的数据 | `d:/4/Unity/数据/游戏数据/card_stats.json`（`desc` 是卡面效果文字） |
| 原版的美术（我们导入工程的那一份） | `d:/4/Unity/素材/Warpforge原版/`（卡框/UI图集/卡背/战场贴图）；**导出缓存** `d:/2/Warpforge_tools/data/ui_extract/`（5557 张切好的图） |
| 某个原版图集切片叫什么 | `素材/Warpforge原版/UI图集/图集/<图集名>/sliced/` + 同目录 `_atlas_rects.json` |
| 原版改过哪些规则 | 各文档里的「2026-08-2x 修正」注释 —— **这些是踩过的坑，别推翻** |

**没查到就说没查到**，不要用「大概是」糊过去。

### 3. 复刻要**照解包资源里的说明**来，不能只凭截图（用户 2026-09-12 要求）

截图只能告诉你「这一帧长什么样」，**告诉不了你参数** —— 尺寸、间距、阈值、颜色、时机、
哪些元素是互斥的、显示/隐藏的条件。这些东西全在解包资源的字段里。

**做法：**

- 动手做一个原版有的东西之前，**先把它在解包资源里的对象找出来**，把数值抄下来，
  **写在代码注释里并标出处**（对象路径 + 字段名）。例：`AttackSelector.cs` 头部那一串。
- 截图只用来**验收**（做出来像不像），不用来**定参数**。
- 资源里**查不到的部分，写「查不到」**，或者明确标成「这是我们挑的，不是原版的做法」——
  绝不能把猜测写成「原版就是这样」。

**踩过的：**
- `Select Attack Background` 的 `sizeDelta` 是 780×285，但 `localScale = 0.44`、
  显示时再横向 ×1.5 → **实际 514.8×125.4**。只看截图/只看一个字段都做错。
- 我一度以为 `40K_melee_glow` 是攻击按钮的外发光（因为「看起来像」），
  实际是**场上单位身上的高亮** —— 在预制体目录里，不在场景里。**凭印象认图会认错。**

### 4. 🔴 **跑起原版游戏，对着实况验证**（用户 2026-09-12 要求）

**本地有能跑起来的原版游戏**（`d:/2/unity_run_ref/`，装了 MelonLoader + SceneJumpShot mod）。
解包资源是**静态快照**，能告诉你字段值，**告诉不了你运行时实际用哪个值、什么时候用、长什么样**。
所以复刻任何一处，**能跑到实况就要跑到实况**：

**做法：**

- 复刻某个界面/交互/数值之前，**先进原版游戏把那一处跑出来**：
  看真实的对象层级、真实的 activeSelf、真实的数值、（能拍就拍）真实的画面。
- 自己的实现做完之后，**再进一次原版，对着同一处比**。自我感觉「像」不算数。
- **实况与解包字段冲突时，以实况为准**，并在代码注释里写明「实况是 X，字段是 Y，按实况」。
- 跑不起来 / 那个界面进不去时，**如实说「没跑到实况」**，别拿静态字段冒充运行时结论。

**为什么单开一条**：解包资源里同一个场景常有**多份同类实例**，值还各不相同
（`CardsHorizontalLayout` 在 battlearena1 里就有**四份**）；
而且 `activeSelf`、显示条件、运行时才赋的 `buttonIcon` 这些东西**只有跑起来才知道**。
参照图也踩过 —— `shots_ui/ui_prefabs/` 那 218 张 PNG **全是黑的**，当参照用就全错了。

> ⚠️ **2026-09-14 更正**：这里原来写的是「多份同类实例、**值互相矛盾**」，
> 举的例子是 `CardsHorizontalLayout` 的 **MB 5271 vs MB 4053**（`m_scale` 0.73 vs 0.54）。
> **那两份不是矛盾，是两份不同用途**：5271 = **我方手牌**（父链
> `BottomAnchor/PlayerArea/HandArea`，`PlayerHand.isPlayer=1`）；
> 4053 = **敌方手牌**（父链 `UpperAnchor/EnemyArea/HandArea`，`isPlayer=0`，
> `m_invertRotation=1` 也和「敌方手牌从上方垂下来」自洽）。**各自都对。**
> **错因**：只比字段值、**没解 `m_Father` 父链**（`isPlayer` 也没看）。
> 当时用来「判定 5271 权威」的三条论据因此全塌 —— 好在结论（我方手牌取 5271）**蒙对了**。
> **正确做法**：看到多份，**先解父链 / 读用途字段**（`isPlayer` 这类），
> 各自都对就**按用途取**；真判不出用途，再谈「谁权威」。
> **实据**：`d:/2/新解包资源/assets_full/bundle_scenes_scenes_battlearena1/MonoBehaviour/`
> 的四份（4052=选牌面板 · 4881=换牌面板 · 4053=敌方手牌 · 5271=我方手牌）；
> 而 `资料/战斗规格/战斗重建_0827/子代理读报_back左区_0827.md:117,120,122,125`
> **2026-08-27 就已经写清了**「敌 4053 / 我 5271」—— 是后面那份文档**推翻了自己已有的正确记录**。

**能验的和验不了的**（2026-09-12 实测，别去撞墙）：

| 能做 | 做不到 |
|---|---|
| 跳**任意 bundle 场景**（含主菜单） | **手牌注入** —— `inj FAIL: empty Data`，根因是原版已关服、卡数据在远程 CCD；`BattleDebugController` 在正式版被裁掉，**mod 修不了** |
| **dump UI 树**（path / name / **activeSelf** / rect / pos / text / image） | **卡组编辑界面** —— 实例化即黑，`解包资源使用地图.md` 已记「deck 链路无原版视觉证据，以 Unity JSON 为准」 |
| 拍**真渲 RT 图**（战场/火焰/烟/碎片都是活的） | 主菜单只拍得到顶栏（97% 靠运行时实例化） |
| 点 `DebugButtons` 里的调试按钮 | 改游戏数据/存档；`ScreenCapture` 截图会崩 |
| **自己加探针** —— mod **源码就在本地**（`d:/2/Warpforge_tools/scenejumpshot/SceneJumpShot.cs`，924 行 + 编译环境），想量哪个运行时字段都能加；它本来就读 TMP 的 `m_text` | |

配置：`d:/2/unity_run_ref/UserData/sjs_dump_cfg.txt` 四行 —— ①bundle 文件名 ②等几秒 ③输出目录
④动作（`drive` / 空 / 秒数列表）。启动前要设 `DOTNET_ROOT`，副本路径必须纯 ASCII。
详见 `项目任务.md` 第八节「常用操作速查」。

### 5. 🔴 **发现文档记错了，就地改掉**（用户 2026-09-12 要求）

文档是**下一个会话唯一的记忆**。一条写错的记录会一直误导下去 —— 已经吃过好几次：

| 文档原来写的 | 实际 | 代价 |
|---|---|---|
| `Card Frame Cost Icon` / `40K_display`「本地只有 sprite 元数据、没有导出的 PNG，做不了」 | **两张图早就切好了**，我们的索引里白纸黑字写着「✅ 在用」，而且 `Card_Frame_Cost_Icon.png` **已经在 `Resources/` 里** | 一整轮把它当「做不了」，对账表上挂着两条假的待办 |
| 「解包里也没有 `EndBattleDoors` 的预制体或 clip」 | 没有的是**独立 prefab**；**组件实例每个战场场景里都有**，字段齐全，我们甚至已经 dump 出来了 | 结论方向错了（「拿不到」vs「拿得到、只是没接」） |
| 「TMP Settings 资产没被解出来」 | 资产在 `resources.assets` 里，是**解包流程漏了这个文件** | 把「流程缺口」记成了「资源不存在」 |

**做法：**

- 发现某条记录与事实不符 → **当场改掉那条记录本身**。不要只在对话里说一句，
  也不要只在别处补一句新的 —— **两份说法打架比没有更糟**（索引说「在用」、对账表说「没有」，
  这次就是这么来的）。
- 改的时候写成「⚠️ 日期 更正：原来写 X，实际是 Y，错因是 Z」——
  **保留更正痕迹**，下一个会话才知道这条被核过、不必重查。
- 顺手 `grep` 那句话的关键词，**把同一句话被复制到别处的地方一起改**（这次同一句错在 3 个文件里）。
- 拿不准是「**真没有**」还是「**没找到**」时，按这个顺序再下结论：
  ① 查我们自己的索引 `d:/4/Unity/资料/索引与盘点/解包资源使用地图.md`（标了每个资源的状态）
  ② 搜导出缓存 `d:/2/Warpforge_tools/data/ui_extract/`（**5557 张切好的图**）
  ③ 全盘按文件名搜 `d:/2 /d:/4`
  **翻过一个镜像目录（`素材/…/Sprite/`，那里常常只有 .json 元数据）之后就写「本地没有」= 一定会错。**

### 6. 🔴 **交接时把旧的交接内容删掉**（用户 2026-09-13 要求）

交接文档是给**下一个会话**看的，不是流水账。**它还留着的旧内容 = 下一个会话的第一号误导源** ——
已经吃过好几次：任务文件里「下次接着做」有两套并存、编号还冲突；同一个自检数字一处写 590 一处写 837；
对账表一行说「图没进 `Resources/`」另一行说「✅ 在用」。新会话**分不清哪句是现在、哪句是过去**，
只能从头核一遍（2026-09-13 那次就白核了两条，还差点照着一份**抄错了实例**的坐标去改 UI）。

**做法（每次收工交接时）：**

- **只留四样**：① 当前状态（一句话 + 数字）② 待办（按优先级）③ 踩过的坑 ④ 出处（`文件:行号`）。
- **删掉**：上一轮/上几轮的进度叙述、状态快照、「已完成」清单、被推翻的结论、
  以及任何「当时是对的、现在已经不对」的话。**这些不是知识，是噪声。**
- **数字与清单只留一处**：别的文档要引用就写「见 X 文档某节」，**不要抄第二份**
  （两份迟早不一致，而不一致比没有更糟）。
- **要留历史就挪走**：真值得留的（比如「某一轮为什么这么设计」）→ 挪进
  `资料/历史/<线>_第N轮.md`，或者干脆依赖 `git log`，**别留在交接文档正文里**。
- ⚠️ **「坑」和「出处」不算旧交接内容** —— 那是知识，删了下一个会话还会再踩一遍，**要留**。
  区分标准只有一条：**「现在还成不成立」** —— 成立的留，不成立的删。

### 7. 🔴 **拿不准的卡牌效果 / 插图 / 数值 —— 直接去看那张卡的成品图**（用户 2026-09-13 明确要求）

> 「关于你拿不准的卡牌的效果、插图、数值等信息，需要**直接去 `D:/2/Warpforge部队卡片`**，
> 这里看看**对应的完整的卡牌图片**。」

**位置**：`d:/2/Warpforge部队卡片/<阵营>/<1督军 · 2天赋 · 3部队 · 4计策 · 5防御卡>/*.png`
—— **900×1200 的 PnP 成品卡图**（另有 `card.html` 打印模板与三份规则书）。

**什么时候必须去看**（不是「有空看看」，是**判据不够时就去**）：

- **效果文字**写得含糊 / 我们的数据源两处不一致 / 怀疑 OCR 抄漏抄错
- **插图**对不上（哪张图配哪张卡）
- **数值**（费用 / 攻击 / 生命 / 远程 / 关键词的数字）拿不准
- **兵种**（`subtype`，卡面那行橙字）拿不准
- 要**验收**卡面做出来像不像

**卡面上什么长什么样**（2026-09-13 实测更正）：
**卡名白 / 阵营橙 / 效果文字米白 / 兵种橙**，**费用在右上**，
**数值沿卡底一排四个** —— **左圆（红/剑）= 近战 · 右圆（紫/枪）= 远程 · 银盾 = 护甲 · 最右绿框 = 生命**。
> ⚠️ **2026-09-13 更正：原来这里写「下方两圆一盾（左圆近战 · 右圆远程 · 中间的盾是生命）」，
> 错了两处 —— ① 不是三个而是一排四个，② **盾是护甲、不是生命**，生命在最右边的绿框里。**
> **错因**：这条是照较早的较模糊卡图印象写的，没逐张核。
> **实据**：`Dark Angels/3部队/Warpforge_20_Deathwing-Terminator.png` 亲读 —— 左圆红剑 3 · 右圆紫枪 3 ·
> 银盾 1 · 绿框 4；而我们的卡表 `health=4`、`armour=1` **与后两者逐一吻合**，可反证。
> **代价**：`_对账.md` 的「远程 7 处」里有 1 处（`Deathwing Terminator` 旧值 1）就是**把护甲读成了远程**
> —— 和 `Baneblade` 是同一个 OCR 坑。详见 `资料/卡表核对_卡图提取/_裁定_三围.md`。

**为什么单开一条**：这份卡图是**最终真相**，其它数据源（OCR 表、`card_stats.json`、
`card_stats` 之外的旧表）都可能抄错。**已经吃过一次**：对着它比才发现我们原来把**卡名画成了橙**、
**阵营行和兵种行根本没画**。而 **1118 张卡的 `subtype` 与关键词数值**也是靠逐张看它才对出来的
（修掉 `subtype` 串列 40 处、关键词数值被吃掉 72 处，见 `资料/卡表逐张核对_与对账.md`）。

**做法**：
- 看图得到的结论**写清是「照卡图」**，并记下阵营与目录（那是可复查的坐标）。
- ⚠️ 卡图是**成品拼图**，**拿不到参数**（尺寸/间距/颜色值）—— 那些照旧去解包资源里读字段（铁律 2、3）。
- 🔴 **方括号 token 的名字 ≠ 语义，而且同一个 token 在不同搭配里可能指不同东西**（2026-09-14 A7 实测）：
  · `[weapon]` **不是**「武器值」，是**紫圈枪 = 远程**（4 张卡图逐张核过）；
  · 反过来 `[Armor]`/`[Armour]` 在 **`+N Attack … +N Armour` 这个固定搭配里也是枪**（5 张核过），
    而**真护甲卡面一律写裸词 `Armour 1`**（不带方括号）。
  ⇒ **别按 token 名猜属性**（那会静默把远程加在近战上，数值还「看着对」）；`GivePayload.ReAttr`
    那张图标词表是**唯一判据**，加词前照卡图核。
- 🔴 **夹在「效果文字」里的多义词，先去读中文（`descZh`）—— 英文那个 `or` 有两种含义**（2026-09-15 用户点名）：
  🔴 **中文一直都有，而且卡面印的就是它**（2026-09-15 实测）：`cards_engine.json` 的
  **`nameZh` 1130/1130 · `descZh` 1124/1124**（`desc` 非空的都有，没有一条是占位），
  源头 `数据/卡牌翻译/zh_cards.json`（由 `工具/gen_cards_engine.py` 读进来）；
  **游戏里卡面走的就是它**（`BattleDriver.FaceTextFull:2585`：`DescZh` 有就用中文，没有才英文）。
  ⇒ **看中文 = 看玩家实际看到的东西**，不是「翻译参考」。别只读英文。
  · `Give Flank to a friendly troop **or** Stun an enemy troop` → **二选一**（卡面没写 `choose` ⇒ **随机**）
  · `Deal 3 damage to an enemy, **or** 6 if it has Hunt Mark` → **条件换数值**（**不是二选一**！
    中文写的是「**若其带猎杀标记，则造成 6 点**」）
  两条在英文里长得**一模一样**。**拿不准就先看中文**，中文也含糊再去查卡图（下一条）与解包资源。
  ⚠️ 我为这个错绕了整整一轮：把 `Vindicator` / `Wulfen Pack Leader` / `Monster Hunters` /
  `Disruption Blades` 读成「二选一、待裁」，其实它们 2026-09-15 一次就接完
  （`EffectText.TryOrAltIf` + `EffectResolver.AltHolds`）。审计与更正痕迹见
  `资料/卡牌效果or句_审计.md`。
- 🔴 **图标被剥掉之后只剩裸数字的（`Adjacent units have +1`）—— 语义在文本层不可恢复**：
  实测两张**同一个写法、属性不同**（`Genestealer Familiar` 卡面是粉拳=近战、`Cadre Fireblade` 是紫枪=远程，
  `desc` 与 `descZh` 都只剩 `+1` / `+2`）。**兜底猜一个 = 静默错一张** ⇒ 宁可**认不出**
  （项目红线），要修得走数据侧 `cardface_fixes.json` 的 `_manual_*` 列，**不是**解析层能猜的。

---

## 二、环境

```bash
# ⚠️ 本机带了 ELECTRON_RUN_AS_NODE，启动 Unity 前**必须 unset**，否则静默失败
UNITY="D:/Unity/Hub/Editor/6000.3.23f1/Editor/Unity.exe"
unset ELECTRON_RUN_AS_NODE && "$UNITY" -batchmode -quit \
  -projectPath "D:\4\Unity\MyGame" -executeMethod <入口> -logFile -
```

常用 `-executeMethod` 入口（详见 `资料/规则引擎_进度与交接.md` 第一节）：
`RuleEngineTest.Run` / `BattleScene.Run` / `CardBaseDemo.Run` / **`DeckScene.Run`** /
`BattleScene.BuildAndSaveScene` / **`DeckScene.BuildAndSaveScene`** /
`VfxPicker.Run` / `ArtBaker.BakeFromCLI` / `DistortProbe.Run`

**动手改完东西，至少复跑这三条**：`RuleEngineTest.Run`（规则/卡组）、`BattleScene.Run`（对战）、
`DeckScene.Run`（卡组编辑）。**改了版面还要看截图** —— 断言测不出「压暗没铺满」这种问题。

⚠️ **用脚本（python / sed）改 `项目任务.md` 与 `资料/*.md` 之后，确认行尾没被翻** ——
🔴 **2026-09-15 又踩一次，这次是 `.cs` 源码：`sed -i` 会把整个文件的行尾翻成 LF。**
`CardPresentation/Core/CardView.cs` 本来是 **CRLF**，用 `sed -i` 改了一行常量之后整篇变 LF，
`git diff` 从 **212/29** 变成 **2091/1908**（= 整个文件重写）。还原办法同上（把 `\n` 换回 `\r\n`）。
⇒ **改 `.cs` / `.meta` / `.tsv` 这类行，一律用 Edit 工具或 python 的 `wb`，别用 `sed -i`**；
真要用了，**改完立刻 `git diff --numstat` 看一眼** —— 数字接近文件行数就是翻了。
⚠️ **`file` 命令对「混行尾」不报警**（只会不显示 CRLF 字样）——
**判据是数出来**：`b.count(b'\r\n')` 对比 `b.count(b'\n')`。
⚠️ **行尾是混的，别一刀切**（2026-09-15 更正：原文写「那些文件是 LF」，实测不成立）：
大多数是 **LF**，但 **`资料/特效还原_进度与交接.md` 是 CRLF** —— 那天按「统一 LF」写完，
`git diff` 直接变成**整篇 1758 行**。**改之前先 `git show HEAD:<路径> | file -b -` 看一眼**；
改完再 `file <路径>` 对一眼，翻了就还原：
`io.open(p,'wb').write(io.open(p,'rb').read().replace(b'\r\n', b'\n'))`（反向就把 `b'\n'` 换成 `b'\r\n'`）。
（Windows 上的 python `open(..., 'w')` 默认写 CRLF ⇒ 2026-09-14 一天里连踩两次。）

---

## 三、这个工程反复强调的几条（血换来的）

- **改动要加断言，别只看截图。** 截图看不出「播的是不是该播的那个」。
  改版面之后 `CardBaseDemo.Run`（四档分辨率）**必须复跑**。
- **不许静默失败。** 没实现的东西要**说出来**（`ErrUnimplemented` / 卡面打 `*` / 日志警告），
  不能让玩家以为它有作用。
- **对局必须可复现。** 引擎里只用 `System.Random(seed)`，不用 `UnityEngine.Random`。
  定死的规则（比如「取槽号最小的那个」）**不要改成随机**。
- **批处理下没有帧循环**：`Object.Destroy` 不生效（用 `DestroyImmediate`）、
  粒子要手动 `Simulate`、特效播放器靠 `Update` 自毁所以不会自己消失。
- **两处写同一条规则 = 迟早不一致。** 判据要共用一份（例如「近战还是远程」只有
  `SimpleAI.UseRanged` 一处），表现层要用就转发。
- **🆕 原版材质上带着内置 Standard shader 的残留值。** 原版 shader 换过之后，
  旧材质里的 `_SrcBlend/_DstBlend/_ZWrite/_Surface` 等**不会被清掉**。原版 shader 属性表里
  没有这些名字时它们是**死值**；但自建 shader 一旦用 `Blend [_SrcBlend][_DstBlend]` 间接寻址，
  就会把死值当活值用（实测把抓屏扭曲变成了不透明覆盖 + 写深度）。
  **动手改自建 shader 前先跑 `工具/dump_shader.py` 读原版的属性表和 pass 状态**：
  属性表里没有的，一律硬编码。
  🔴 **2026-09-15 又踩了一次同一个坑，这次是 `_EmissionColor`**：往自建粒子 shader 里接
  `_EmissionColor` 时写成了 `col = tex * _Color * _EmissionColor * IN.color`，
  而原版那批材质里它是**黑的默认值**（URP/内置粒子 shader 属性表就是 `(0,0,0)`，
  还要 `_EmissionEnabled`/`_EMISSION` 才采它）⇒ **整个材质被乘成黑**，13 个效果同时变暗
  （`Cut Wulfen SW` 亮度比 0.033）。**接一个原版属性之前先问三句：默认值是什么、
  原版在什么条件下才采它、原版是加法还是乘法** —— 它多半不是乘法。全过程见
  `资料/特效还原_进度与交接.md` §〇·一。
  ⚠️ 原版的 **HLSL 源码**确实被剥了（`Shader.m_Script` 是 null），但**编译字节码在** ——
  以前我写成「源码和字节码都被剥掉了」，那是**读错了属性**（`m_SubProgramBlob` 是 None，
  真数据在 `Shader.compressedBlob`）。解出来是 DXBC，**资源名是明文**，
  「这个 shader 采样哪张纹理」能查证。工具：`工具/dump_shader_blob.py`。
- **🆕 棋盘是裸数组，光环靠「手挂的钩子」重算 —— 新增写入点必须补钩子。**
  `PlayerState.Board` 没有「写入即触发」这回事，而光环（`CardDef.AuraSpecs` → `Auras.Recompose`）
  是**持续加成**：来源在场就有效、离场就收回。现在有 **9 个棋盘写入点 + `BeginTurn`** 各挂了一次
  （清单在 `Core/Aura.cs` 的 `Recompose` 注释里）。**往 `Board[..]` 写新代码时，顺手调一次
  `Auras.Recompose(ctx)`** —— 漏了不会报错，只会让光环停在**上一个棋盘状态**上（静默）。
- **🆕 判定「效果是否还原」要先确认尺子有意义。** 抓屏扭曲这类效果在**没有内容可扭曲**的
  空场景里本来就该是空的；曾经把这种「尺子的假象」当成「导出整个丢了」，白记了几十条。

---

## 四、当前阶段与任务顺序（2026-09-12）

**目标：把原版的战斗、卡组编辑和周边功能尽可能还原到位。**

**联网味的功能也要**（用户 2026-09-12 明确）：倒计时 / 出牌超时 / 投降 / 换牌倒计时 ——
原版有的都尽量做，不要以「单机用不上」为由砍掉。

**美术资源以后再说** —— 等原版复刻得差不多了再动手。机制早就位
（`Resources/Art/` 换同名文件即可、删目录退回占位美术），**不用现在操心、不用反复提**。

**下一步的详细计划（含「数据在哪」）看 `资料/卡牌效果管线_计划与交接.md`。**
**还差什么，看 `项目任务.md` 的「九、距离完整还原原版还差什么」** —— 那里是 2026-09-12
把反编译代码 + 运行时 UI dump 挖了一遍之后的对账结果，分四类：
①玩法骨架（卡组编辑、对局结算、卡牌放大详情 **已做完**；**还差**换牌 mulligan / 战斗日志 / 倒计时 / 投降）
②表现与手感（发牌入场 / 攻击位移 / 命中反馈 / AnimFX 脚本层）
③内容深度（战术卡**已能打**；覆盖率/载荷/关键词的**数字只在** `资料/阵营推进_清单与交接.md` §一 一处，别在这儿抄第二份；
   ⚠️ **2026-09-14 更正**：这里原来抄了一个「A3/A4 收工时完全解析 425/449」的**旧数** ——
   既是**第二份**（违反「数字只写一处」）、又**已经过期**。**只留指针，不抄数**
   （A4 整条线 2026-09-14 收工，只剩 3 种句子不认识、全有硬理由）。
   ⚠️ **2026-09-16 更正**：把残留的「**449 张**」也删掉了 —— 实际是 **445** 张
   （`资料/阵营推进_清单与交接.md` §一）。同一天把上面那句里的「§五」改成「**§一**」：
   **当前值在 §一那张表里**，§五 是**跑自检的命令节**，指过去会白跑一趟。
   另：对手 AI 是贪心、不搜索）
④特效还原质量（183 个高置信问题 / 构建后 player 从没验过）

> 🔴 **2026-09-16 用户定的下一步：先做「构建后 player 验证」**（就是 ④ 那条 —— 三条线都只验过**编辑器**，
> **真包从来没跑过**）。**对手 AI 的优化排在它之后**（现在只贪心、不搜索）。
> 这一条为什么是**唯一**的：编辑器里跑绿 ≠ 真包里能跑 ——
> `Resources/` 的加载路径、`#if UNITY_EDITOR` 包掉的东西、批处理下没有帧循环那几件事，
> **只有真包能证伪**。做法与线索见 `项目任务.md` §九 ④。
