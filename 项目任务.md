# 项目任务文件 —— 自研卡牌对战游戏

> 建立：2026-09-10　|　更新：**2026-09-18**　|　面向：**新会话开工前必读**
>
> **当前状态一句话**：打开 `Assets/CardPresentation/Scenes/Battle.unity` 按 Play 就能和**打分型 AI**（照原版重写、设置面板里可调难度）打一局 ——
> **打的是原版卡**（原版卡池按阵营凑，张数见 `资料/阵营推进_清单与交接.md` §一），**卡面按原版规格组装**
> （立绘 + 卡框 + 文字底板 + 材质描边 + 原版 `Pragati` 字形），**效果文字按原版语义结算**
> （**「不认识的句子」双双归零**；**「会执行的那一层」的机制缺口也归零** —— 判据是 `EffectText.WillRunOps`
> **按卡类型问对的层**，卡面那个 `*` 也不再只给战术卡打；原来记成「终态」的 6 张逐张开图查完 ⇒
> **0 张真终态、全部落地**，正本 `资料/普查产出_0916/静默桩家族_0916.md`）。
> 完整流程：**开局换牌 → 拖牌上场（付不起拖不上去）→ 攻击方式三选一 / 技能与触发 / 特效与手感补间 →
> 轻点卡牌弹放大展示窗 → 墓地·战斗日志面板 → 倒计时与投降 → 播原版 Victory/Defeat/Draw 视频 → 结算面板**；
> 🆕 还有**单位语音条**（原版 `Unit Chat`，单位部署/攻击/阵亡时说一句**原版语音**，工程从 2026-09-17 起有音频）
> · **回放条**（原版 `ReplayButtons`：重开 / 暂停 / 继续 / 单步）· **设置面板里可调对手难度**（原版没有这个入口）
> · 🆕 **敌方手牌**（原版 `PlayerHand` MB 4350，**显示卡背、从屏幕上方垂下来**；规格 `资料/敌方手牌_原版规格.md`）
> · 🆕 **挨打震镜头**（原版是 Cinemachine 震**主相机、不震 UI**；我们单相机 ⇒ 落地成「相机 + `HudRoot` 同向等量平移」）。
> **自检跑哪几条、跑出来多少 —— 只看 `资料/阵营推进_清单与交接.md` §一 一处**（本文档不抄第二份；
> 写死的数字在这里只会误导 —— 这一格先后记过三个版本的数、都被后来的断言淘汰了）。
> ⚠️ **判断工作区状态只看 `git status`**，别信文档里写死的「干净」（这一格害过人）。
> **五条线各有独立交接文档** —— 见 §三 那张入口表。**本文件只放：待办 + 坑 + 速查。**
> 📌 **四处「动手之前先查它」**：**战斗 UI** → `资料/战斗UI_原版对账表.md` ·
> **卡表数据 / 卡面**（`card_stats.json` / `cardface_fixes.json` / `STAT_FIXES`）→
> `资料/PnP卡图_逐张对账_0915.md`（**§六末** = 逐张并排验收的结论 + 这一轮新查出的坑）·
> **特效逐效果状态** → `资料/特效还原台账.tsv`（+ `资料/特效还原_进度与交接.md` §〇·一）·
> 🆕 **要新增卡牌 / 复用特效 / 自制特效** → `资料/自制特效_加入流程.md`（**三条硬规矩在那儿**：
> 自制 prefab 别放进 `Assets/WarpforgeVFX/`（会被 `ClearGenerated` 删）、别写进 `effect_index.json`
> （会被重生成抹掉）、**名字就是库的键**）。
> 🆕 **2026-09-17 新增两处**：**原版那套动画/时限数值**（`deathTimeMinionDuration` / `mulliganTimeLimit` /
> `timeToDrawPlayerCard` … 48 项）→ **`资料/VarsGlobal_原版数值.md`** ·
> **「哪些是我们自己挑的、原版到底有没有答案」** → **`资料/自设计清查_0917.md`**
> （用户 2026-09-17 口径：**一切按原版，不做多余的自我设计**；标「我们挑的」之前先确认真查不到）。
> 🆕 还有两份：`资料/反编译工具链_重建记录.md`（**要补反编译时照它跑**）·
> `资料/说明书_未读14份_索引.md`（11 个 arena 全树 + 菜单要点第 2/3/4 块的补读结论）。
> ⚠️ **两条跑不了的实况，别再撞**：**卡面**（原版已关服、卡数据在远程 CCD，dump 出来的卡面节点全是空壳）·
> **战斗 UI 的「内容」**（要对局数据才出得来；3D 战场与 UI rect 是真的）。
> ⚠️ **一条已被推翻的旧记录**：卡框**不是**「按稀有度分四档」（`CardTier` 与 `CardRarity` 是两个独立枚举）——
> 详见 §七 那条坑（**修法还没拍板**）。
> 🔴 **一条跨轮判据（开工前先读）**：**覆盖率绿 ≠ 卡面语义对**。
> 四条反馈（①不认 ②半懂 ③载荷没机制 ④打得比卡面宽）**全都看不见「某半句被吞掉、从来没变成 op」**——
> 那种句子判「认了」、载荷有机制、也不宽，四个桶一个都不响，卡面还不打 `*`。
> ⇒ 那一族 = **静默桩家族**（6 个成员，**2026-09-16 全部修掉**）。
> **逐条判据 / 修法 / 坑只看正本** `资料/普查产出_0916/静默桩家族_0916.md`（从句级的账在 `吞句候选裁定_汇总.md`）。
> 📌 **两把尺子都进自检了**：从句级（`RuleEngine/Editor/CardProbe.cs` + `工具/scan_swallowed_clauses.py`）·
> 中文对账（`RuleEngineTest.TestZhAgreement` J1–J6 + `工具/zh_crosscheck.py`）。
> ⚠️ **从句尺子天生看不见三类**（整句凭空消失 · 解析干净但语义错 · 解析得出有机制但**没有触发点**）⇒
> **「这一族修完了」只对这一类成立**，别当全局结论。
> **治本建议（还没做）= 待办表第 11 行 ①**。

> **⏭ 下次接着做（按优先级；**2026-09-17 更新**）**
>
> 🔴 **这张表是全项目唯一的待办正本**（各线交接文档里的「下一步」都指回这里）。
> ✅ **开工前先 `git status` 看一眼**（**别信文档里写死的「干净」**）。
> 🔴 **卡牌线已收工**（逐卡数字见 `资料/阵营推进_清单与交接.md` §一）；「解析缺口归零」说的是
> **①不认/②半懂**那两个桶，**不等于**卡面语义全对（见文首那条 2026-09-16 的判断）。
> ✅ **第 1 / 2 / 4 / 5 行都已收工**（第 5 行 = 手感三件，2026-09-17）。
> ✅ **第 3 行（特效线）· 第 6 行（AnimFX）· 第 7 行（卡实例身份）· 第 13 行（多张放大）2026-09-17/18 都已收工**；
> 第 15 行做掉了 ①（放大窗语音按钮）与 ⑦b（阵亡消散分档），**还剩 ②③④⑤⑥⑦a**；
> 🔴 **接着做第 11 / 15 行剩下的，然后第 8 / 10 / 12 / 14 行**；
> 可穿插的候选与耦合只在下面「剩下的任务能不能随便排」那**一处**写，别在这儿再来一份。
>
> | # | 做什么 | 现状 / 从哪开始 | 体量·阻塞 |
> |---|---|---|---|
> | ✅ **1 · 构建后 player 验证** | ✅ 2026-09-17 收工（抓出 5 条「编辑器里好、真包坏」· 两种自动对局 `-wfdrive`/`-wfhuman`）。⚠️ **`-wfhuman` 偶发卡死没收口**（已装 `-wfwatch` + `-wfstall`）· 🚫 **跑批别并发**。**唯一出处 = `资料/特效还原_进度与交接.md` §七 步 5** | — |
> | ✅ **2 · 对手 AI（按原版重写）** | ✅ 2026-09-17 收工（动作表 + 打分族 + 难度旋钮 + 换牌规则）。🔴 两件原以为是缺口的**是原版死代码**（留牌 / 不自杀）⇒ 不实现。**唯一出处 = `资料/AI_原版反编译_0917.md`** | — |
> | ✅ **3 · 特效线四步** | ✅ 2026-09-17 收工（拖尾贴图那种真因是 `EffectExporter.MapProp` **无条件改属性名** ⇒ 静默丢 207 个材质的贴图；四处修法 + `ParticleModuleProbe` 验证）。⚠️ **「黑方块那批」没验**（要收口得跑一次全量 sweep，两趟各约 25 分、**不能并发**）；⚠️ 172 条「替代 shader 认不出源属性」**待逐个拍板**。**唯一出处 = `资料/特效还原_进度与交接.md` §〇之三 / 三之补** | 大·软 |
> | ✅ **4 · 界面剩余件** | ✅ 2026-09-17 四件全清（等待提示 · 牌堆张数 · **回放条** · **单位语音条**：工程首次有音频，1787 条）。**唯一出处 = `资料/战斗UI_原版对账表.md` §三之〇** | — |
> | ✅ **5 · 手感三件** | ✅ 2026-09-17 三件落地（发牌 0.3/0.15 · 重排 0.2 · 拿起 0.1 · `dragScale` 1.1 · `TapThreshold` 0.3 · **震镜头 = 相机 + `HudRoot` 同向平移**）。🔴 **仍是我们挑的**登记在 `CardFeel.Catalog` 的 `Src` 里 | — |
> | ✅ **6 · AnimFX 18 类** | ✅ 2026-09-18 实现完毕（15 个模块类 + 数据链 895 效果/2313 模块 + 工厂自动登记）。⚠️ **下游钩子还剩 8 个没接**；⚠️ 没还原的（`ChangeShapeAngle` / `Tween` 本体 / `PostProcess` 上屏 / `Cardback` / **原版音效 `sounds` 937 条未接线**）都**标了 + 报警**。**唯一出处 = `资料/AnimFX_实现与接线.md`** | 大·软 |
> | ✅ **7 · 卡实例身份** | ✅ **2026-09-18 收工**（手牌/牌库/弃牌堆/场上/移出游戏都存 `CardInstance`；🎯 一号验收靶 `Master of Manoeuvre` 绿：减费**只落在回手那一份**上，同名另一张一分钱没降）。<br>🔴 **接手必看三条**：① **算费用要传实例**（`CostOf(卡模板)` 看不见按份修正；配方 `Key="*"`+`HandInstanceId`）② 发号只有 `BattleContext.NewInstance`（无 ctx 走 `Detached`，负数号）③ **「新造一张」vs「挪一份」**（`RemoveFromSource` 返回摘走的那一份，动作分支一律 `?? pickedInst`）。**唯一出处 = `资料/卡实例身份_爆炸半径.md` §八** | — |
> | **8 · 阵营资源那一半** | `useWaystone` **主动「收集」**没做（现在是「一死直接 +1」）；灵魂石「**打出时付 vs 回合内随时**」没坐实 → `资料/阵营推进_清单与交接.md` §一之三 | 中·硬 |
> | **9 · `BattleScene.Run` 退出段错误** | exit 139；**断言全过**、崩在 `EditorApplication.Exit` **之后**；当前代码 **复现不出来** ⇒ **别追**，判据只看 `BT === 合计` | 中·硬 |
> | **10 · 卡组编辑的尾巴** | 拖拽加牌 / 卡面放大 / **卡组串分享**（= 卡组码）—— ⚠️ 原版格式拿不到 ⇒ **用户 2026-09-17 定：自己设计**（带版本号与校验，坏了要能说出「读不出来」）；规格·校验·存档·卡组库已通 | 中·软 |
> | **11 · 卡牌线残件** | ① **从句级尺子做成自检报表** ② 光环那一侧的机制账（`Aura.Recompose`）**没有报表** ④ `Company Master` 的 `it` 指代时序**未跑实局** ⑥ **四个数值圈从 1126 张卡面图里程序化读出来**（悬案 §十一 #2）<br>✅ 2026-09-18 做完 ③（中文对账整脚本修好：A 0 · B 5 · C 1090 · ? 0）与 ⑤（退化支 = **333 处目标 / 361 卡名、全 `×1`** ⇒ 只做事「取槽号最小的那个」）。<br>🔴 **⑤ 顺手量出一条真差异 —— 用户 2026-09-18 定：先记着、之后再改**：**45 处带 `Auto` 的 `deal`** 卡面语义是「自动选敌方**最弱**单位」（原版 `rule_core.gd:2692` → `_auto_fx_target:3206-3216` 逐个比 health 取最小），而我们**打的是槽号最小的敌人** ⇒ **要动引擎，等拍板**；证据在 `资料/普查产出_0918/11⑤_...md` §五 | 小·无 |
> | **12 · 界面 / 战场的还原度** | 用户 2026-09-16：「战场布置和编辑界面好像都很粗糙，不像原版」；**做法**：动手前先通读 `资料/说明书/` 对应那一份。**差在哪 = `资料/战场还原度_差距清单_0917.md`**（唯一出处）：2D 层照原版字段摆的、**3D 层只摆了一张烘平的背景图**；差四大类 = 3D 装饰/光照/透视 · 战场粒子（原版 60 个）· 卡牌软光/影 · **13 个战场只搬了 1 个**。另两件：**卡面按展示场景分层**（原版阵营行只在放大窗印、场上连框和文本都没有）· **卡面高亮 5 个状态色与时长**（字段在卡预制体上，旧解包 0 命中 ⇒ 按新解包路径再找一次）。⚠️ 那份**没覆盖卡组编辑界面** | 大·软 |
> | ✅ **13 · 卡牌放大——多张一起看** | ✅ **2026-09-18 收工**：`CardPresentation/Battle/MultiCardDisplay.cs`（原版 `UIMultiCardDisplay`）。**版面逐条来自解包**（窗口带 `y[131,949]` · 标题 1192×63 fs38 · 遮罩 α0.7725 · Continue 条 577.5×63.84 + 圆钮 80.47）。🔴 **四处是我们挑的**（卡间距 · 卡高 · 滚动改成「缩到装得下」· **入口 = 点我方牌堆**），**都写在文件头** | — |
> | **14 · 补「没有 type tree 的 .assets」的对象** | `VarsGlobal` 整表当年就是这么漏掉的（`sharedassets0.assets` 没有 type tree ⇒ 按字段名 grep 全树 0 命中 ⇒ 误判「资源不存在」）。做法照 `工具/read_varsglobal.py`（签名桩给字段顺序 + 原始字节给值 + 锚点自检）把那一族 .assets 扫一遍 | 中·无 |
> | **15 · 语音 / 音频的后续件** | ✅ 2026-09-18 做完 **①放大窗语音按钮**（`Voices Over Button` 88.655² + `40k_UI_bt_voicelines`）与 **⑦b 阵亡消散分档**（小兵 0.2 / 督军 0.5）。<br>**还剩**：② `ChatPopup` 预设台词面板（⚠️ 先核清 `ChatButton` 是「语音开关」还是「开面板」）③ `RadioChat` ④ 剩下 **922 条语音接触发点**（接法 = `BattleDriver.SpeakFor` 多接几个事件，⚠️ 每个时机都是「我们挑的」）⑤ 波形等化器（先跑 `工具/dump_shader_blob.py` 看有没有 DXBC）⑥ **三根音量滑块**（music/soundFX/voiceOver）⑦a **ASR 把 1787 条音频转成文本**（用户点名；转出来要**人工抽检**） | 小~中·软 |
> | **16 · 阶段二：游戏外壳（彻底复刻）** —— **排在剩下全部做完之后** | 开场动画 → 主菜单 → 战斗/卡组/商店/锻造厂/日常。**三条边界**（用户 2026-09-17）：① 美术**留原版不再换**（发布前仍要删 `Resources/Art/`）② **不做真实经济**（资源固定 9999 · 商店可买 · 日常做、过期的活动不做 · 全解锁）③ 多人**入口与界面照原版做**、功能留空（将来 P2P），**点了要如实说明「暂无服务器」**。开工入口：`资料/说明书/04_界面UI/菜单全树.md` + `资料/可并行任务清单.md` ⑫（已切好 4 块） | 大·软 |
> | **17 · 加时（Overtime）** | **判据**（用户 2026-09-17 给）：每回合开始判一次 —— **后手 `ctx.Players[1].MaxEnergy >= 10`**，判过不再判。**效果**：经典模式每回合多抽 1 张。**表现**：`OvertimeIndicator` 68.6×71 已摆上（默认关，原版行为）+ 淡入1s/停1s/淡出1s + `OvertimeStart` 音效；全屏 `OvertimeSplashText`（`OVERTIME!` fs80）**还没建**。常量 `DeckRules.OvertimeEnergy/OvertimeDrawPerTurn` 已有、**引擎一次没读**。**唯一出处 = `资料/加时与冲突模式_原版规格.md`** | 中·软 |
> | ✅ **已收工（只为「别重做」）** | 敌方手牌（`资料/敌方手牌_原版规格.md`）· 卡面逐张验收 A1–A7 · PnP 两批对账 · 灵魂石 · 选牌面板 · 特效接线三件 · **换牌 mulligan / 战斗·墓地日志 / 倒计时 / 投降** · 静默桩家族（`资料/普查产出_0916/静默桩家族_0916.md`）· 原来记成「终态」的 6 张 · W 组 190 个分类 | — |
> | ⛔ **原版拿不到 / 用户决定不做** | ① **奖励 / 评分数据** —— 用户 2026-09-17 定：**不做** ② **加时 `overtimeTurn` 数值**（服务器下发）—— 判据已给（见第 17 行）⇒ **不再缺数** ③ **「印不印阵营行」**：跟的是**印次版式**、不是卡字段 ⇒ 要逐张一致只能逐卡记录，我们无条件印（与原版约 1/3 不符）⇒ **待拍板**，见 `资料/PnP卡图_逐张对账_0915.md` §六B ④ **掉兜底的 30 个 shader 要不要补映射** —— 🔴 原版 shader 本体就在随包补充包里 ⇒ 更贴「一切按原版」的做法可能是**改走原件** ⇒ **待拍板**，见 `资料/普查产出_0917/README.md` 线索 #1 | — |>
>
> **🔢 剩下的任务能不能随便排？** —— **大部分可以，但有两处耦合，乱排会返工**：
> ① **特效线（第 3 行）与 AnimFX（第 6 行）是同一条链** —— AnimFX 是「让特效在对的时机播」那一层，
>    而 **W 组分类刚做完、正好是它的输入**（结论：触发源是**动画挂点**，不是关键词表）⇒ **连着做**。
> ② ✅ **卡实例身份（第 7 行）2026-09-18 已做完** —— 它动手牌的数据结构，做完之后**那套绕开它的近似
>    （`Ephemeral` 按份数记 / 手牌加成的「卡 + 份数」/ `CostMod.Key` 按卡 id）已经**全部拆掉**，
>    换成「一份一个标记 · 一份一条额度 · `HandInstanceId` 钉份」。详见第 7 行那一格与
>    `资料/卡实例身份_爆炸半径.md` §八。**这一格以后不用再读**。
> 📌 **真包是唯一的「真包」尺子**（编辑器全绿 ≠ 真包里能跑）⇒ 大改完**都要跑一遍** `-wfdrive`，
>    做法与结果出处见 `资料/特效还原_进度与交接.md` §七 步 5。
> **彼此不依赖、可自由穿插的**：第 8（阵营资源）· 第 10（卡组编辑尾巴）·
> 第 11（卡牌线残件）· 第 12（界面还原度，**软、量大**）· 第 13（卡牌放大）· 第 14（没 type tree 的 .assets）· 第 15（语音/音频后续件）。
> **推荐顺序**：**（11、13、15 穿插）→ 3 + 6 → 7 →（8、10、12、14）** —— ✅ **3 / 6 / 7 / 13 都已做完**
> （第 1、2、4、5 行 2026-09-17 收工；第 3、6 行 2026-09-17/18；**第 7、13 行 2026-09-18**；
> 第 15 行的 ①⑦b 2026-09-18 做完）
> ⇒ **现在做 11 与 15 剩下的，然后 8 / 10 / 12 / 14**。
> 📌 **第 16 行（阶段二：游戏外壳）排在「当前剩下的任务全部做完」之后** —— 用户 2026-09-17 明确。

>    ✅ **共用判据（**要加新东西之前先看这几处，别写第二份**）**：
>    格位相邻 = `BoardSpec.AdjacentSlots` · 伤害公式 = `RuleCore.DamageAfterReduction` ·
>    「这条 op 有没有机制」= `EffectText.OpHasMechanism` · 「这句是不是光环」= `Auras.TryParse`（**只此一处**；报表另用 `Auras.LooksLikeAura` 判形状）·
>    「**这张卡算不算某一类**」= `CreatePool.MatchesKind` · 「**单位 ≠ 部队**」= `EffectResolver.AddSide` 的 `troopOnly` ·
>    「**属性词 → 字段名**」= `RuleCore.NormalizeAttr`（`melee`→`attack`；**不过它会静默丢属性**）·
>    「**立绘取哪张图**」= `BattleDriver.ArtKey`（原版卡给 id、自造 26 张给卡名）·
>    「**哪些关键词进棋盘徽标**」= `Core/Badges.cs`（含别名表）· 「**卡面关键词段怎么拼**」= `CardText.KeywordSegment`（**不动 `desc`**）。
>
>    📁 **原版选牌/选效果面板的实况**（截图 + 运行时节点树 + `GetPosition` 布局数学）
>    在 `资料/原版参照图/Unity参照管线_0825/data/panel_0914b/报告.md`（**唯一出处**）。
>    ⚠️ 两条仍然成立的事实：**卡没拿到**（候选 list 元素全 null、卡的 SO 在远程 bundle）⇒ 卡的观感仍无实况；
>    **离线态 UI 相机一张都不渲**（UI 拍不到的真因，不是 URP）。
>
>
> **⚠️ 规则书审计③还剩一半**：「费用修正按卡名」修了（卡表 v6 稳定 id + `CostMod.Key` 改 id），
> 但**卡实例身份**没做（= 上表第 7 行）。状态见 `资料/规则书_实现指南对账.md` §一。
>
> **🔧 可以并行派的活** —— `资料/可并行任务清单.md`（**表里带 ✅ 的别再重做**）。
> 方针继续用：**一次多派几个**，但**派之前先把要发出去的数据核对清楚**（有实测教训），
> 而且**子代理的证据比你的印象可靠时照它的改**。
> 🔴 **派之前先核「那件事是不是真没做」** —— **清单本身也会过期，表里的 ✅ 不是唯一判据**
> （吃过一次：照清单第 ⑭ 条派活，任务书写「`special` 卡框从没实测过」，**实际 9-13 就做过**，
> 见 `资料/普查产出_0913/special_tier_实测_前半/后半.md`；那条已就地更正）。
> ✅ **两条边界**（照做，别再试）：
> **① 「查证 / 裁定 / 写独立文档」可以派**（`Explore` 只读 · `general-purpose` 有全工具）——
> 产出落成独立文档，**下次接着做不用重查**。
> **② 「改引擎代码 / 跑自检」不能派** —— 引擎集中在 3 个文件（块间必撞），
> 而且**同工程同一时刻只能起一个 Unity 实例** ⇒ 这两件留在主对话串行做。
> 给编辑型子代理的活要**输出文件互不重叠**（各写各的新文件，主对话最后合并）。
>
> 📁 **只读普查的产出（都只到「建议」为止，动手前照旧先核）**：`资料/普查产出_0913/`（菜单盘点 ·
> 语音索引 · 卡表三堆裁定 ⚠️**已被 PnP 逐张对账取代、只剩个别待核项** · `special→tier` 实测 ·
> 效果→shader 对账）+ 🆕 `资料/普查产出_0916/`（**静默桩家族** · **吞句裁定汇总** · **W 组分类汇总** · 三份 `待修_*.md`）
> + 🆕 `资料/普查产出_0917/`（**135 个原版 shader 属性表** · **313 个界面菜单盘点** · **958 效果→shader 对账** ·
> **33 个补充 shader 引用清单** · **172 个材质丢掉的源属性**）
> —— 🔴 **先读它自己的 `资料/普查产出_0917/README.md`**：那是**索引 + 10 条未结线索**（含「原版 shader 本体就在我们随包里，
> 「近似替代」其实可以改走原件」这条**待拍板**的），比逐份翻快得多。
> `资料/战术卡剩余7条_语义查证.md`（**逐句表**）· `资料/单位卡desc与光环_批次划分.md` ·
> `资料/选牌与选效果面板_原版数值.md` · `资料/卡表核对_卡图提取/_裁定_*.md`。
> ⚠️ **W 组只看 `普查产出_0916/W组分类_汇总.md`** —— 0824 那份接线台账**覆盖面很窄**、
> `普查产出_0913/W组191_分类_前半/后半.md` 已因此删除（真证据是原版四张「动画 → VFX root」表，见汇总 §二）。

>
> **📌 美术：原版美术一直留着、不再换自己的**（用户 2026-09-17 定，**取代原来那句「美术资源以后再说」**）。
> 机制照旧（`Assets/CardPresentation/Resources/Art/` 换同名文件即可、删目录退回占位美术），只是**不再计划换**。
> 🔴 **红线不变**：原版美术是 Everguild / GW 的资产 ⇒ **个人使用、不上传、不商用**，发布前整个 `Resources/Art/` 仍要删。
> **五条线各有独立交接文档**（`规则引擎_进度与交接` / `卡牌基座_进度与交接` / `对战排版_原版数值与改造方案` /
> `原版复刻_场景与美术` / `特效还原_进度与交接`）· 仓库 https://github.com/qjhqjh10/qjh4.git
> ⚠️ **工作准则见仓库根目录的 `CLAUDE.md`**（**每次会话自动加载**，七条铁律）—— **本文件不抄第二份**。
> 🔴 **项目目标（用户 2026-09-17 拍板）**：**从「学完 warpforge 就换自己的美术资源」改为「彻底复刻 warpforge」**——
> 最终要能**像进原版一样**：开场动画 → 主界面 → 从主界面进**战斗 / 卡组 / 商店**等功能界面。
> **三处明确简化**（用户定的）：**不做多人** · **资源（游戏内货币）改为无限** · **过期的活动不做**。
> ⇒ 见待办表**第 16 行「阶段二：游戏外壳」**（**当前剩下的任务做完再开**）。
> （阶段一仍是「按原版把战斗与卡组编辑还原到位」；**美术留原版、不再换**。）

---

## 一、项目背景

**要做的东西**：一个**自己的卡牌对战游戏**，玩法模仿《战锤40K Warpforge》——**美术由用户自己提供**，
我们要的是「**让卡牌动起来**」的机制（发牌/翻面/上场/攻击位移/命中反馈/特效播放）。用原版解包资源当
**参考蓝本**：这类动效从零设计很费劲，原版有一套成熟的机制和调好的手感数值。

**技术栈**：Unity 6.3.23f1 + URP 17.3.0，项目在 `d:/4/Unity/MyGame`。

---

## 二、项目准则

### 🔴 红线

1. **原版资源只做参考，不直接用于发布版本。**
   Warpforge 的资源归 Everguild，战锤 40K 的 IP 归 Games Workshop。**游戏机制本身不受版权保护**（模仿玩法没问题），但具体的**美术、特效参数、动画数据、音频**是他们的资产。
   - ✅ 可以：学结构、抄数值（手感参数）、照搬设计思路、自己做一套
   - ❌ 不可以：把解包的贴图/模型/音频直接打进发布版本
   - ⚠️ `MyGame/Assets/WarpforgeArena1/` 和 `MyGame/Assets/WarpforgeVFX/` 都是**研究产物**，发布前必须整个移出构建
   - 用户已明确：特效还原是**个人使用，不上传、不商用**

2. **不要重新解析 Unity JSON。** 战斗规格已经整理成权威文档（`资料/战斗规格/战斗重建_0827/审查更正清单_0827.md`），**以它为准**，重新解析既慢又容易出错。

3. **改 `d:/2/` 前先问。** 那边是档案库（44.9 万文件），有工具和文档依赖具体路径。要改先确认。

4. **🆕 仓库只放代码 / 工具 / 文档。** 原版解包资源、第三方自购素材包、以及它们的直接衍生物
   **一律留在本地**，`.gitignore` 已排除。原因是版权（Warpforge 的美术/特效归 Everguild，
   战锤 40K 的 IP 归 Games Workshop；自购素材包的授权也不允许再分发）。
   留在本地的大件：`Unity/素材/`（4 GB）、`Unity/MyGame/Assets/WarpforgeVFX/{Prefabs,Materials,Textures,Meshes}`（1.8 GB）、
   `StreamingAssets/WarpforgeVFX/*.bundle`（原版 shader 字节码）、`Unity/资料/{原版参照图,说明书,规则书,原版实拍}`。
   **这些随时可以用导出器重建**，不需要进仓库。

### 🟡 工作方式

6. **每个改动都要能实拍验证。** 血泪教训：审计报告说「OBJ 是 X 镜像的，进 Unity 会左右反」，数据比对完全正确，**但结论是反的** —— 因为 Unity 的 OBJ 导入器会再做一次反向转换，两次抵消。顶点级数值比对不足以下结论，**必须实拍比对**。

7. **一次只改一个变量。** 场景重建时同时改了网格朝向和材质判据，出问题时分不清是哪个导致的，白绕了一轮。

8. **比对工具本身要先验证。** 本轮踩到：原版 vs 导出的并排渲染，两边各自按包围盒自动构图，**取景不同导致亮度差没有可比性**，据此得出过错误结论。统一取景后结论才成立。

---

## 三、新会话从这里开始 —— **文档总入口**

> ⚠️ **路径约定**：下表 `资料/…` `工具/…` `_tmp_view/…` 都是相对 **`d:/4/Unity/`** 的
> （少数带 `d:/4/` 前缀或 `d:/2/` 前缀的照原样看）。
> 🔴 **本文件只留这一张表** —— 别处不再抄第二份。

**先想好这次做哪条线** —— 常走的三条线各自的交接文档是首选读物（完整入口见下面那张表）：
- **卡牌线（主战场）** → `资料/阵营推进_清单与交接.md` §一之三（**每次自检会重写工作清单**）
- **特效线** → `资料/特效还原_进度与交接.md`
- **玩法线** → `资料/规则引擎_进度与交接.md` · `资料/卡牌基座_进度与交接.md`

### 必读（开工前过一遍）

| # | 文件 | 作用 |
|---|---|---|
| 1 | `资料/资源使用手册.md` | **资源总入口**：目录结构、每个目录怎么用、常用操作速查、已知的坑 |
| 2 | `资料/原版复刻_场景与美术.md` | 美术怎么接进来的、怎么换成自己的（换同名文件即可）、图集切片与烘背景的做法、六个坑 |
| 3 | `资料/规则引擎_进度与交接.md` | **玩法线交接**：游戏已能玩、怎么跑自检、自设计的两套卡、下一步 |
| 3b | `资料/卡牌效果管线_计划与交接.md` | **卡牌→打出→战斗→发动效果这条链的下一步计划**：每件事的**数据在哪**、反编译能用的部分 |
| 4 | `资料/特效还原_进度与交接.md` | **特效线交接**：已验证的事实、三层 shader 架构、产物、已知 bug、下一步 |
| 5 | `资料/卡牌基座_进度与交接.md` | **表现层交接**：布局/卡/拖拽/落位，设计决策与踩过的坑 |
| 5b | `资料/对战排版_原版数值与改造方案.md` | **版面数值出处**：原版手牌布局器字段+曲线、棋盘坐标（含镜像）、卡面数值实测位置 |
| 6 | `资料/规则引擎_设计.md` | **规则引擎设计**：边界、数据结构、**逐条规则出处**；§6b = 事件流与效果文法规格 |
| 6b | `资料/战斗UI_原版对账表.md` | **战斗 UI 逐元素对账**：原版实测值 + 出处 + 我们的现状 + 还差的 + 悬案。**改战斗 UI 前先查它** |
| 7 | `资料/特效还原台账.tsv` | **逐效果状态**（判定/亮度比/`\|ln\|`/置信度/时段/技术构成）。**最权威的特效现状清单，数字只写这一处** |
| 8 | `资料/自研游戏_特效与卡牌基座_设计.md` | **设计草案**：特效怎么接进游戏、卡牌基座的模块划分与接口 |
| 9 | `Unity/_资源评估_场景特效动画.md` | **开工蓝图**：场景/特效/动画三类的可用性判定、逐项工时 |
| 10 | `资料/战斗规格/战斗重建_0827/审查更正清单_0827.md` | **战斗权威规格**（首选，不要重新解析 JSON） |
| 11 | `资料/PnP卡图_逐张对账_0915.md` | **卡牌数据的尺子**：拿原版**成品卡图**逐张对账我们的卡表。**§六末 = 逐张并排验收的结论**（判据 + 坑都在那节）。**改卡表数据 / 动卡面之前先读它** |
| 12 | `资料/阵营推进_清单与交接.md` | **卡牌线交接**：§一 状态表（**自检数 / 卡池张数 / 未实现关键词的唯一出处**） · §一之三 待办 · §一之四 坑与出处<br>⚠️ **2026-09-16 更正**：这里原来写「§五 自检数（唯一出处）」，**指针指错节** —— §五 自己写着「当前值只看 §一那张表」，**实际值在 §一**（`:15` 战术卡 · `:19` 卡池 · `:20` 未实现关键词 · `:22` 自检） |
| 13 | `资料/战斗规则与数值_出处.md` | **战斗规则 / 战斗数值去哪查**（五层资料各答什么 + 反编译 `_DAT_xxxxxxxx` 常量的读法） |
| 14 | `资料/规则书/Warpforge_Offline_Rulebook_1_5-3_中文翻译.md` | **原版离线规则书**：**61 个关键词的完整技能表**（每个都写明触发时机）。做卡牌效果/关键词/AnimFX 先读它 |

### 数据与出处（要查具体值时走这几处）

| 要找什么 | 去哪 |
|---|---|
| **原版资源（首选）** | **`d:/2/新解包资源/assets_full/`**（按 bundle 分包；`Sprite/*.json` 里有 `textureRect`）。⚠️ 旧的 `解包整理/` 里 **650 张同名图内容不对** |
| 一张贴图 / 库里的名字 | `数据/索引/texture_index.json` |
| 一个类的字段名 | `d:/2/Warpforge_code/`（**仅签名桩**：方法体是空的，字段名/类型/`[SerializeField]` 完整） |
| **真实控制流** | `d:/2/Warpforge_tools/data/decomp_il2cpp_0827/decomp_out/`（1800 个 `.c`） + `decomp_out2/`（224 个） + 🆕 **`decomp_out_ai/`（127 个：`AI` 整类 41 + `AnimFX*` 86，2026-09-17 补）**。⚠️ 不是所有类都反编译了，先确认在不在；**不在就自己补**（`资料/反编译工具链_重建记录.md`，几分钟） |
| **读原版 shader 的属性表 / pass 状态** | `工具/dump_shader.py`（**动手改自建 shader 之前先跑它**） |
| **原版的运行时 UI 树（最新一份）** | `资料/原版参照图/Unity参照管线_0825/data/runtime_ui_dump_drive_0912.tsv`（1253 行） |
| **原版有哪些战斗环节 / 节点叫什么** | `资料/说明书/01_战斗_对战/`（13 个战场各一份 2D 全树）+ `04_界面UI/菜单全树.md` |
| **卡面各层的原版尺寸/位置** | `资料/战斗规格/战斗重建_0827/子代理读报_2dcard_0827.md`（A2 表） |
| 事件名 → 特效名 / **挑特效** | `Assets/CardPresentation/Core/VfxMap.cs` · `Editor/VfxPicker.cs`（**挑特效必须用它**） |
| 卡组编辑的规则与素材从哪来 | `资料/卡组编辑_原版数值与实现方案.md` |
| **关键词三列对账**（原版 `DefinedTrait` **134 个带值成员** / 规则书 / 我们） | `资料/关键词三列对账.md` |
| 原版预组牌 vs 我们卡池 | `资料/原版预组牌_核对.md`（脚本 `工具/check_prebuilt_decks.py`） |
| 特效逐效果根因 / AnimFX 成表 | `资料/特效E组_逐效果根因表.md` · `资料/特效E组_根因初判.md` · `资料/AnimFX_18类成表.md` |
| **一个特效的参数 / 原版 bundle 原件** | `素材/Warpforge原版/特效共享资源/` 或 `d:/2/解包整理/08_预制体特效/战斗预制体/` · bundle 原件在 `d:/2/Warhammer 40k Warpforge/Warpforge_Data/StreamingAssets/aa/StandaloneWindows64/` |
| **原版 UI 图（按钮/宝石/名牌）** | `d:/2/Warpforge_tools/slice_battle_atlas.py`（切好的 60 张在 `素材/Warpforge原版/UI图集/图集/battleatlasui/sliced/`） |
| **截原版真渲图 / 从 D:\2 同步资源** | 跑 `d:/2/unity_run_ref/Warpforge.exe`（见 §八）· `工具/sync_from_d2.py --batch <名>`（幂等，不覆盖） |
| 具体某条线做过的批次设计 | `资料/事件层_数据与设计.md` · `资料/临时卡Ephemeral_设计与实现计划.md` · `资料/回手与指代_数据与设计.md` · `资料/常驻效果_数据与设计.md` · `资料/选牌Choose_数据与设计.md` |
| 某次排查的证据图（**不是结论**） | `资料/留档_排查证据/`（`卡面组装_0912/` · `特效_抓屏扭曲/` · `卡组编辑/`） |
| **每次自检重写的全量清单** | `_tmp_view/`：`tactic_unparsed.txt` · `unit_desc_unparsed.txt` · `when_unparsed.md` |
| 原版真渲图 | `资料/原版参照图/`（⚠️ **不能整目录当参照用**，见 §七） |
| 解包资源总索引 | `资料/索引与盘点/解包资源使用地图.md` |
| 🆕 **加时（Overtime）/ 冲突模式的规格与参数** | **`资料/加时与冲突模式_原版规格.md`** —— 触发点 · 效果 · 表现 · 音效 · 原版两种模式的 **18 字段参数总表** · 还缺什么 |

---

## 四、资源在哪、怎么用

> **目录结构、每个目录怎么用 → 只看 `资料/资源使用手册.md`**（这里不再抄第二份）。
> 要点：仓库根是 `d:/4`，Unity 侧的一切在 **`d:/4/Unity/`**（`MyGame/` 工程 · `素材/` · `数据/` ·
> `资料/` · `工具/`）；带 ★ 的（`素材/ 数据/ 资料/ 工具/`）是**项目过程中新建的**，不是原版解包资源。
> ⚠️ **本文件 `项目任务.md` 在 `d:/4` 根目录，不在 `Unity/` 下。**

### 关键访问方式

> **逐项访问方式**（贴图索引 / 特效参数位置 / 截原版真渲图 / 类字段名 / 真实控制流 / bundle 原件 /
> UI 图集切片 / 同步器）**→ 见 §三「数据与出处」那张表与 §八 速查**，这里不抄第二份。
> 只留两条**原创做法**：**原版立绘/卡框/卡背**走 `工具/import_original_art.py`（按卡名拷进
> `Resources/Art/cards/`，`--check` 只查不写）· **战场背景**走 `ArtBaker.BakeFromCLI`
> （打开重建好的 `WarpforgeArena1` 场景 → 渲 1920×1080 → 存 `Resources/Art/arena1_bg.png`）。

### 五个关键认知

1. **原版的卡牌动效不是 AnimationClip，是 DOTween 代码。**
   证据：反编译的 `DrawCard` 里直接写着 `DOScale` + `SetEase`；`CardScript` 有 423 个方法。
   全项目只有 **98 个唯一 clip**，用在 DOTween 补不出来的地方（着色器参数、粒子开关、后处理）。
   → **好消息**：代码驱动路线被验证可行，而且**74 个 `UnitTweenSO` 的 ease/duration/loops 数值可以整套搬**。

2. **没有 Spine。** 盘点说的「Spine 卡牌动画 2334 文件」是**误标** —— 那 2334 个全是 `CardAnim` 的 `AnimInfo` JSON。三层穷举 0 命中。

3. **原版战场就是 URP，特效不会变粉红。** 之前担心的管线兼容问题在原版素材上不存在。

4. **原版特效不需要服务器。** 本轮实测：本地加载战场 bundle 进战斗，火焰/烟/碎片都是活的；catalog 里 84 个 bundle 全部解析为本地路径，一个不缺。服务器只影响卡牌数据和对局逻辑。**详见 `特效还原_进度与交接.md` 第二节。**

5. **🆕 解包资源里没有卡牌数值 —— 只有图和 ID。**（2026-09-12 全库验证）
   立绘/卡框/卡背/语音/动画 ✅ 在；卡牌 **ID**（哪个卡包有哪些卡）✅ 在；
   但**费用/攻/血/护甲** ❌ **不在**（原版是联网游戏，数值在服务端）。
   全库 grep `"(attack|health|cost|damage)"\s*:\s*\d` **0 命中**；客户端只有
   `RawCardScript.cs` / `CardData.cs` 这样的**类定义**，没有数据资产。
   - **数值的唯一来源** = `D:\2\Warpforge部队卡片` 的**卡面照片 OCR**
     （`d:/warpforge/tools/ocr_cards.py` → `card_stats_raw.json` → `merge_card_stats.py` → `card_stats.json`）
     ⚠️ **是 OCR，会有错**：1136 条里 87 张没读到值、47 处靠编辑距离模糊匹配、个别 `desc` 有重复词。
     **拿不准的回查 `ocrSrc` 指向的原图**。
   - **但效果文字有更权威的来源**：`d:/warpforge/data/i18n/zh_CN.csv`（6369 条键），
     第一列英文原文、第三列中文翻译 —— **98% 的卡能对上**，卡面 `desc` 就是这些键拼起来的。
     中文文案顺带就有了。

---

## 五、当前进度

### ✅ 已完成

> 整张「已完成」表已按铁律 6 **挪进历史** → `资料/历史/项目任务_已完成清单_0916.md`
> （**逐轮完整历史用 `git log`**）。本节只留**仍然成立**的两条判断：

- **`WarpforgeArena1`（战场重建）是「摆位预览级」，不是特效库** —— 见下。
- **特效还原：权威清单 = `资料/特效还原台账.tsv`**（每次重跑 sweep 后重生）；
  **验收指标 = `|ln(亮度比)|` 的中位/均值，不是「对得上多少个」**；
  **数字只看 `资料/特效还原_进度与交接.md` §〇·一**（本文档不抄）。
  ⚠️ **Z 计数在带宽边界上极其敏感**（边界挪 0.05 就摆动 22–54 个）⇒ 拿它当门槛会把噪声读成退步。

### ⚠️ `WarpforgeArena1` 的真实水平：**摆位预览级，不是特效库**

**0 个 prefab**（全烘在场景里）· 39/39 材质用 `URP/Unlit`（应该用 `URP/Particles/Unlit`）且全 Transparent ·
`SizeModule`/`ColorModule`/`SubModule`/`TrailModule` **全部 enabled:0** · **加法混合完全没处理**
（1142 个 VFX 材质里 **344 个（30%）是 `SrcAlpha/One` 加法混合**）。它的价值是**证明流水线可行**，不是可用资产。
> 后续的 `WarpforgeVFX/` 是另一条更完整的路线（从 bundle 直接导出，粒子模块完整），见交接文档。

### ❌ 还没做

> **完整待办只看文首「⏭ 下次接着做」那张表** —— 本文件不抄第二份；明细与出处分布在
> `资料/阵营推进_清单与交接.md` §一之三 · `资料/战斗UI_原版对账表.md` ·
> `资料/规则引擎_进度与交接.md` · `资料/特效还原_进度与交接.md` §〇·一 四处。
> ⚠️ **一处「原版有、我们故意不做」的**（理由记着，**别再翻案**）：换牌的**四种模式**
> （服务器开关 `GameplayVariablesData.showMulligan` 选的，本地没有那份配置）。
> ✅ 换牌**倒计时** 2026-09-17 已做（总秒数 = `VarsGlobal.mulliganTimeLimit` = **25 s**）。

---

## 六、待办任务

> 🔴 **待办的正本在文件顶部那张「⏭ 下次接着做」表**（2026-09-17 更新、14 行 + 已收工 + 永久拿不到）——
> **本节不抄第二份**，只写**各线特有的细节与出处**。

> 📌 **结算「开门」那段是带出处的知识，别丢**：原版那套**不是动画、是播视频** ——
> `EndBattleDoors.SetupDoor`（反编译 `decomp_out/EndBattleDoors__SetupDoor.c`，193 行）建一张
> **1920×1080 的 `CustomRenderTexture`** 贴给 `Video Image` 的 `RawImage`，按枚举
> `==10` victory / `==20` defeat / `==30` draw 选片，**返回 `VideoClip.get_length()`** 给调用方
> `_CloseBattleDoors_d__393__MoveNext` 去 `WaitForSeconds`；那个类里**没有 Animation/Animator/DOTween**。
> **奖励是和视频同时出的**（`SetupDoor` 自己就调了 `ShowRewards`，`:105`）；98 个唯一 clip 里
> 结算/开门/奖励相关的是 **0 个**。⚠️ **2026-09-12 更正**：没有的是**独立 prefab**，**组件实例每个战场
> 场景里都有**（字段齐全：`skullsObjs`/`rewardsHolderObj`/`victoryVideo`…，已 dump 到
> `07_场景/battlearena1/MonoBehaviour/MonoBehaviour_5238.json`）——「解包里没有」这个说法有误导性。

### 玩法线：卡牌基座 —— **还差两条**

- **出牌区 / 卡组 / 弃牌堆**：⚠️ **半做** —— 两边牌堆已有原版卡背 + 「DECK n DISC n」；
  **还差**实体牌堆的厚度感、弃牌堆的正面展示。
- **手牌→战场落地节奏**：✅ **2026-09-17 收工**（三处「我们挑的」都换成了原版值：
  `MoveTime` = `minionToConversionPointTime` · 重排 `timeToPositionCard` · 拿起 `timeToScaleIntoMoving`）。
  **仍属「我们挑的」的只剩中段抬升的形状** —— 归属正本 = `Core/CardFeel.cs` 的 `Catalog`（`Src` 三档）。
- ~~换自己的美术~~：**2026-09-17 用户定：不换了 —— 原版美术一直留着**（机制仍在：换 `Resources/Art/cards/`
  同名文件即可；删目录退回占位美术）。

### ✅ 已收工（留一段只为「别重做」）

> 已收工清单**只看文首那张表最后两行**（本节不抄第二份）。
> ⚠️ 特效侧**白板自检只挑了 20 个常用效果做样本、不是全部 958 个** —— **别把白板绿灯当发布绿灯**。

**三条仍然成立、别推翻的结论**：**关键词未实现 0 个**（⚠️ 数字以自检打印的
`RuleCore.UnimplementedKeywords()` 为准）· **原版没有「事件级间隔表」**（`Core/EventTiming.cs` 的时长是从
单特效的 `UnitTweenSO` 里挖的；~~阵亡/触发两条没有出处、是拍的~~ —— 🔴 **2026-09-18 更正：阵亡那条
2026-09-18 已从 `VarsGlobal` 补上出处（小兵 `deathTimeMinionDuration 0.2` / 督军 `deathTimeWarlordDuration 0.5`），
`EventTiming.IsSourced` 现在**每条都返回真**；触发那条仍是有出处的 `sec5FractionDelay`。**别再把「拍的」当结论**。）· **「原版牌堆是个多张叠起来的堆」
这个前提不成立**（dump 里 `PlayerDeck` = 底板 + 卡背(217×314) + 回合灯 + 张数）。

**逐项落点与踩过的坑**：`资料/规则引擎_进度与交接.md` · `资料/卡牌基座_进度与交接.md` ·
`资料/特效还原_进度与交接.md` · `资料/阵营推进_清单与交接.md` —— **本文件不再逐条重抄**。

### 玩法线：规则引擎 + 可玩对战 —— **能玩了**

完整交接：`资料/规则引擎_进度与交接.md`　|　设计：`资料/规则引擎_设计.md`（§6b = 事件流与效果文法规格）


⚠️ **两条结构事实**（改的时候别踩）：`CardPresentation/Battle/BattleDriver` 是**引擎 ↔ 画面唯一连接点**
（拖拽校验 / 弹出攻击方式选择器 / 回合流转 / HUD）· `RuleEngine/Core/EffectSpec` 的**封闭效果文法
只服务自设计的那 26 张卡**，原版卡一律走 `EffectText` 那一整套。

**验收**：`BattleScene.Run`（**验「接得上」**）与 `RuleEngineTest.Run`（**验「算得对」**）**两条都必须绿**。

### 道具箱（不用自己做，直接拿）

| 资产 | 用途 |
|---|---|
| **`Card Hand To Board` clip（0.92s）** | 「手牌落到战场」的完整蓝图，17 条曲线拍清了时序 |
| **74 个 `UnitTweenSO`（86 KB）** | ease/duration/delay/loops/punch 数值全在（含具体类型名如 `PunchTween`） |
| `Card Display Open`、`BattleHudShow/Hide` | 发牌与手牌展开 |
| 13 个 `Battle Arena X Camera Intro` | 进战斗镜头 |
| `card_anim_map.json` / `card_vfx_tree.json` / `atk_vfx_map.json` | 卡牌→特效映射（591 条定义 → 400 个预制品） |
| 反编译的 34 个卡牌类 / 2024 个方法名 | 当需求规格说明书用 |
| `数据/游戏数据/animfx_components.json` | **🆕** 2346 个 AnimFX 组件的**参数值**（哪个 prefab 挂哪个模块、参数多少） |

---

## 七、已知的坑（踩过的，别重踩）

### 通用

| 坑 | 真相 |
|---|---|
| **OBJ 手性** | 审计说"是 X 镜像的，要先还原"——**错的**。Unity 的 OBJ 导入器会反向转换，原样搬才对。实拍验证过 |
| **`_Blend` 判材质透明** | 恒为 0，不可用。用 `_SrcBlend`/`_DstBlend` + `_AlphaClip`。但也别优先用 Alpha 裁剪（贴图是共享图集，整图统计不代表单个网格）。**取"优先混合"最稳** |
| **粒子翻页图集** | 网格在 `ParticleSystem` JSON 的 **`UVModule`**（不是 `TextureSheetAnimationModule`） |
| **文件数口径** | 解包目录里**大量是精确字节重复的副本**。2026-09-12 实测：`11_着色器` 527→232（**56%** 是重复）、`07_场景/battlearena1` 10020→5347（**47%**）、`01_卡牌/Ultramarines` 777→591（24%）；全库口径见 `_审计报告_0910.md` §B1（215,809 / 48%）。**所有「文件数」都要打折看**。⚠️ 旧文档里写的「约 21%」**复现不出来**，别再用那个数 |
| **文件数 ≠ 对象数** | `_<pathID>` 后缀**不是重复标记**，是提取器的原名格式 |
| **`ELECTRON_RUN_AS_NODE`** | 本机环境带这个变量，会让 Unity Hub / 团结引擎 Hub **启动即静默退出**。跑 Unity 批处理前也要 unset |
| **`ParticleSystem.Burst`** | 构造参数带下划线前缀（`_time`/`_minCount`），**不能用命名参数** |
| **`ShapeModule`** | 属性是 `shapeType` 不是 `type`；`arc` 单位是**度**，JSON 里存的是弧度 |
| **批处理下粒子不动** | 预览渲染前要 `ps.Simulate(3f, true, true, false)` |
| **重跑 `classify_move.py` / `extract_full.py`** | **绝对不要** —— 前者分类会覆盖且不可逆，后者会再加一遍重复 |
| **`convert_unity_anim.py`** | 输出 Godot `.tres`，**Unity 用不了**，别用 |
| 🔴 **行尾（LF/CRLF）** | 仓库里**两种混着**：`.cs` 是 CRLF，好几份 `.md` 是 **LF**（例：`项目任务.md`、`资料/阵营推进_清单与交接.md` —— ⚠️ 而 `资料/卡牌效果管线_计划与交接.md` 是 **CRLF**，别按扩展名猜）。**用 python 的 `open(p,'w')` 或 `sed -i` 批量改文件会把整篇行尾翻掉** —— `git diff` 会变成「全文件重写」（2026-09-13 撞到：一个 4323 行的文件报 8457 行改动）。<br>⚠️ **2026-09-13 第二次踩，形态不一样**：那次只**改一行**，却翻了两整份文件（`战斗UI_原版对账表.md` 报 266/265、`规则引擎_进度与交接.md` 报 1397/1397）。**错因是「读」不是「写」** —— `open(p, encoding='utf-8').read()`（默认 `newline=None`）会做 universal-newline 翻译，**把 CRLF 读成 LF**，再写回去就成了 LF。<br>⇒ **三条规矩**：① **别按扩展名猜行尾** —— `资料/战斗UI_原版对账表.md` 与 `资料/规则引擎_进度与交接.md` 都是 **CRLF**，而别的 `.md` 是 LF，**逐个文件查**（`git show HEAD:<f> \| grep -c $'\r$'`）；② 改文档**一律用 Edit 工具**（它保留原有行尾），**别用 python 读写**；非要用就全程 `open(p,'rb')` / `open(p,'wb')`；③ **提交前必跑**下面那条自查。<br>**自查**：`git diff --numstat` 和 `git diff --ignore-cr-at-eol --numstat` 两个数不相等 = 行尾被动了。<br>**修**：按 `git show HEAD:<file>` 里占多数的那种，把整篇换成同一种再提交。 |

### 🆕 玩法线（规则引擎 + 卡牌基座，详见 `资料/规则引擎_进度与交接.md` 第五节）

| 坑 | 真相 |
|---|---|
| 🔴 **🆕 谁按「卡名」建索引，谁就会在同名卡上串数据**（2026-09-15 一次逮到 6 处） | 卡池里有 **5 组同名跨阵营**的卡（`Terminator` / `Terminator Champion` / `Aggressor` / `Maulerfiend` / `Bladeguard Veteran`）。**同一个坑在 6 个地方各犯了一遍**：立绘文件名（`art_<卡名>.png` ⇒ 后写的覆盖前写的，实测 `art_aggressor.png` 是太空野狼那张、暗黑天使挂着别人的画）· `cardface_fixes` 的修正表查询 · `STAT_FIXES` · 覆盖率报表 · 战斗日志小头像 · PnP 对账脚本。**判据一律改成「引擎卡 `id`（或 `阵营+卡名`）」**：`BattleDriver.ArtKey` · `load_cardface_fixes` 的 `"<阵营>/<卡名>"` 键 · `STAT_FIXES` 的 `RARITY_BY_FACTION` 式特判。**加新东西时先问一句：这个键在跨阵营重名时唯一吗？** |
| 🔴 **🆕 跑 `ArtBaker.ApplyImportSettings` 之后，卡面会变「彩色马赛克」**（2026-09-15 踩到） | 两个工具**在同一条判据上打架**：`工具/import_original_art.py` 的 `fix_art_meta()` 把 `.meta` 的 `alphaIsTransparency` **关掉**（开着时 Unity 会用**最近邻填充**补透明区的 RGB，填充边界是**多边形格子** ⇒ 整张立绘糊成马赛克，而**我们恰恰要用透明区的 RGB**：底层 `ArtOpaque` 拿它补卡框拱窗的背景），而 `ArtBaker.ApplyImportSettings` 又把它**打开**。现在两处一致设 `false`。⚠️ **改完立绘必须跑 `ArtBaker.ApplyImportSettings`，跑完必须渲一张卡面看一眼** —— 断言查不出马赛克 |
| **🆕 `ImageQuad` 的宽度是从贴图宽高比推出来的** | 它只接一个「高度」，宽度 = 高度 × 贴图宽高比 —— **不是给两个尺寸**。做「铺满屏幕」的遮挡层时拿一张正方形贴图去铺，会得到一个方块，屏幕左右两边没盖住（抓屏扭曲的暗底、结算界面的压暗都踩过）。要铺满就**按当前可见区的宽高比造那张贴图** |
| **🆕 `Label.SetText` 在未激活的物体上跑，字形网格不会重建** | 面板先 `Hide()` 再 `Show()` 时，如果 `Show()` 里**先写文字后激活**，标题会是一片空白（结算界面踩过）。顺序必须是**先激活、再 `SetText`** |
| **🆕 同一层的两个 UI 图会互相压住，谁压谁看渲染顺序** | 面板/行/按钮如果 z 相同，压盖关系是不确定的（侧栏前 3 行的背景被面板盖掉过）。**显式分层**（面板 z=0.30 / 行 z=0.10 / 其余 0）。已做成通用断言：`DeckScene.Run` 里「同层的图不许重叠」，能挡住整类版面错误 |
| **🆕 `default(Struct)` 的数值字段不是「未设」，是 0** | `DeckFilter` 的 `Cost` 默认 0，而 **0 是个合法费用** —— 不显式初始化的话，筛选默认变成「只看 0 费卡」，**整池被筛成 103 张**（池子张数见 §一 的指针）。**表示「不限」要用 -1 这种哨兵值，并且构造函数里显式赋上** |
| **🆕 断言管行为，版面还是得看图** | 本轮三个版面 bug（压暗没铺满、卡格没居中压到筛选栏、侧栏按钮压在卡表上）**断言全绿**，都是截图看出来的。所以「改动要加断言」和「改版面要看图」是两条并行的规矩，不能只做一条 |
| **🆕 结算视频是「左右拼 alpha」，Unity **不会**替我们拆** | 三条结算片实为 **matroska/webm + VP8、3840×1080 / yuv420p**（没有 alpha 通道）：左半彩色正片、右半白色剪影（明度即 alpha）。原版 `VideoClip` 元数据是 `Width:1920 m_HasSplitAlpha:true` —— 那是**原版导入时拆好的产物**；我们把同一份字节丢进自己的工程，Unity 报的是 **3840×1080**（`VideoProbe.Run` 实测）。所以 RT 要开 3840×1080、显示区 1920×1080，**合成自己写**（`Shaders/VideoSplitAlpha.shader`）。⚠️ 手册坑 6 原来写的「用前要 crop」对我们不适用，已更正 |
| 🆕 **批处理下 `VideoPlayer` 能用，但不能阻塞主线程** | 这个工程的批处理自检是 `-executeMethod` 调静态方法、**没有 play 循环**（`BattleDriver.Update` 不跑）。`Thread.Sleep` 泵循环拿到的永远是 `isPrepared=False / frame=-1`；改成注册 `EditorApplication.update`（每 tick 调一次 `QueuePlayerLoopUpdate`）、**让方法立刻返回**，`Prepare` 就过了、`Play` 也真出帧。⚠️ 调用时**不能带 `-quit`**，退出要自己 `EditorApplication.Exit`。见 `VideoProbe.Play` |
| **🆕 「尺子的假象」第二例：对照组的底板和被测面板同层** | 拍结算界面时给了一块假底板当「战场」，它和面板的压暗层（`EndPanel.Build` 里建 dim 用的是 `Vector3.zero`）**z 都是 0** → 底板盖住整块面板，面板根本没渲。而「看得见画面」那条断言因为**底板自己就是亮的**空过了。**搭台子的东西也要摆在正确的位置上**，不然量的是台子不是被测物 |
| **🆕 同一个位置在两处算，迟早只剩一处是对的** | 换分辨率时要把 HUD 重贴一遍（`ReanchorHud`）。建的时候 END TURN 按钮改到了右侧，**重贴那段还写着旧坐标**（`0.952f - EndTurnTextDx()`），结果按钮在右边、文字留在右下角。修法是把它抽成一个常量（`EndTurnX01/Y01`），建的、重贴的、文字的三处都读它 —— **和「判据只有一处」是同一条规矩，只是对象从规则换成了坐标** |
| **🆕 跨局的状态要显式清，不会自己归零** | 接「按 R 再来一局」时一口气撞出三个：`CardInteraction._placed`（重开一局后**上一局摆过的格子再也放不了牌**）、`BattleDriver.BuildHud()` 每开一局建一份（**HUD 叠两层**，而且旧的那个结算面板成了孤儿**永远不 Hide**）、`interaction.OnDeployed += ...` 每局多挂一份（落位回调跑 N 遍）。**凡是「这一局才有」的状态，重开那条路上都要有对应的清/复用** |
| **🆕 界面上写着的话要有人接** | 结算面板写着「按 R 再来一局」，**没有任何代码监听 R** —— 面板承诺了一件事、什么都没发生，正是红线禁止的静默失败。加一行 `Keyboard.current.rKey.wasPressedThisFrame` 就好，但**得有人去发现它**（这次是因为要写重开的自检才发现的） |
| **🆕 阵营名传错池子会静默给一套别的卡** | `StarterCards.Of(faction)` 原来是「不是 Tide 就当 Ember」——传 `"Ultramarines"` 会拿到一整套 Ember 卡、还打得挺像样，**完全看不出错**。已改成认不出就报错 + 空表。同理 `DeckBuilder.StarterDeck` 在空池子上会 `deck[0]` 越界，也补了守卫 |
| **编辑器模式下 `Object.Destroy` 不生效** | 它要等下一帧，而编辑/批处理里**没有帧循环**。批处理自检里旧的手牌视图全留在画面上，看起来像「手牌莫名其妙多出好几张」（实拍踩到）。用 `Application.isPlaying ? Destroy : DestroyImmediate` |
| **换边之后必须再 `BeginTurn`** | `EndTurn` 只换 `Active`，**发能量/抽牌/解疲劳都在 `BeginTurn` 里**。只调 EndTurn 的话对手整个回合 0 能量、一张牌出不来（表现：AI 场上永远只有督军） |
| **格位底片要用「落位后」的尺寸** | 战斗场景格位间距比手牌紧得多，用整卡尺寸画底片，9 个底片会**糊成一整条灰带**。要用 `placedScale * LayoutSpace.Scale` |
| **落位动画要推进到「落定」为止，别写死时长** | 引擎调用在 `DeploySequence` 的完成回调里。自检写死 `Step(0.8f)` 不够，断言全挂而画面其实是对的 |
| **`LayoutSpace.Cam` 是静态字段，不进 Play 模式** | 编辑器里建场景时设过，运行时是 `null` → 归一化坐标退回默认宽高比，超宽屏/4:3 全错位。场景里要有个 `Start()` 重新 `Apply(cam)` |
| **`TryResolveSlot` 只比 x、不看 y** | 拖到战场**正上方空白处**，只要 x 恰好落在某格附近就算合法落点。7 格时槽 0 在 0.236 恰好躲过，改成 9 格才暴露。横纵容差**不是同一个物理尺度**（可见宽 17.78 / 高 10），是两个独立参数 |
| **接口成员是属性，字段实现不了** | `ICardProvider` 声明 `string Id { get; }`，用 `public readonly string Id;` **字段**会编译报 `CS0535`。要改成只读自动属性 |
| **能量按「每方自己的回合数」算** | 不是全局回合数。按全局算的话后手首回合 2 能、先手第 2 回合 3 能，**整局能量曲线偏高**。`rule_core.gd` 有明确修正记录 |
| **远程攻击默认也吃反击** | 直觉会写成「远程不受反击」，**那是错的**。只有 `Long Range` 免反击 |
| **Flying 检查的是「目标」不是「攻击者」** | 语义是「飞行单位不能被近战打到」。`rule_core.gd` 修正过方向，正好反了 |
| **出牌校验顺序：先费用、后格位** | 和 `rule_core.play_card` 一致。顺序反了会出现「判了格位却已经扣过费」的中间态 |
| **关键词值 = 整串里第一个数字** | 不是「尾随数字」。`"Companion 2: Missile Drone"` 取 2。无数字的关键词取 **1**（存在即真），取 0 的话护甲和 Shield 会一起失效 |
| **测试夹具的垫牌要放在「反转后的手牌」前面** | 抽牌是 `pop_back`；垫牌若放末尾，起手会全是 `filler0` |
| **棋盘格数两边各写一份** | `RuleEngine.BoardSpec` 和 `CardPresentation.BoardLayout` 各有一份常量，**靠 `RuleEngineTest.TestBoardMatchesPresentation()` 断言相等**。**改格数必须两个一起改再跑自检** |
| **🆕 汉字渲染走通了（2026-09-12）** | 最坑的一条：**`AssetDatabase.ImportPackage()` 在 `-batchmode -quit` 下根本不落地** —— 它只是把导入排进队列，进程在队列被处理前就退出了，**日志里连报错都没有**。得自己解 `.unitypackage`（gzip+tar，每条一个 GUID 目录，含 `pathname`/`asset`/`asset.meta`；⚠️ 条目名带 `./` 前缀要剥）。另一条：**`TMP_Text.textBounds` 是「行盒」不是「字形框」**，按它定字号字会小 ~30%（汉字只占 1 em，行高 1.437 em）。详见 `资料/规则引擎_进度与交接.md` 第六节 |
| **🆕 开局那几行写在 `Start()` 里 = 自检永远验不到** | 批处理下 `AddComponent` **不触发 `Start`**，所以「按 Play 的入口」和「自检跑的入口」很容易变成**两条路** —— 而真会出问题的一定是前者。修法：把开局抽成一个方法（`BattleDriver.BeginFromDeckLibrary`），`Start()` 和自检**调同一个**。和「判据只有一处」是同一条规矩，只是对象从规则换成了**入口** |
| **🆕 「重开一局」漏带的那半边** | `Restart()` 原来只带阵营和种子、**不带卡组**（那时也没有卡组）。接上卡组库之后，按一次 R 就从「你编的那副」变回自动凑，而且**画面上没有任何变化能看出来**。规矩：`Begin()` 的参数里凡属「这一局用什么」的，`Restart()` 都要原样带过去 |
| **🆕 提示行是 NoWrap 的，长名字会横着铺出屏幕** | `Label` 走 TMP 且 `textWrappingMode = NoWrap`（`Label.cs`）——**卡组名是玩家自己起的、长度不可控**，直接塞进提示行会横着长出可见区。要**截断**，并且用 `Label.WorldW` 加一条量宽度的断言挡着 |
| **🆕 「玩家看不见的警告 = 没警告」** | 卡组里的战术卡/防御卡会被丢掉这件事，原来只写在 `Debug.LogWarning` 里 —— 打包之后玩家一个字都看不到，而画面上**牌确实少了**，看起来就像卡组没生效。要玩家知道的事必须画在**画面上**（这次放在提示行的**休息态文字**上） |
| **🆕 卡框的「透空窗口」是拱形，不能拿来当矩形裁剪框** | 立绘原来被裁进一个「从卡框 alpha 量出来的透空窗口」里 —— 那个区是**拱形**，立绘是**矩形**，直角会**顶出拱形**（症状：「卡框没包住插图」），而且窗口以外不画立绘。原版是**立绘铺满、由卡框的 alpha 当遮罩**（`子代理读报_2dcard_0827.md:310`）。**别再把「量出来的形状」当矩形用** |
| **🆕 卡框 rect ≠ 该画的尺寸** | `CardFrame` 的 rect 是 2.2452×3.2572，但 PNG 的金属 bbox 只有 **608×936**（宽高比 0.6496），左右各约 18% 是透明留白。**拉满 rect 会把框横向撑宽 6%**；按 sprite 自身比例 fit 出来是 2.1159×3.2572，宽度正好落在卡本体（2.0927）那条线上，也和原版卡面量出来的「金属基本顶到卡边（0～98% 卡宽）」一致 |
| **🆕 卡图贴图的 alpha 通道是解码残渣** | 这批卡牌插图的 alpha **97% 的像素≈0**（只有一小块不规则的斑），**RGB 才是真数据**。用 UnityPy 直接读 bundle 结果一样（`m_TextureFormat=BC7`）→ 不是导出工具的锅。**导入时必须把 alpha 写成 255**，否则整张卡全透明。（`工具/import_original_art.py` 的 `write_portrait`） |
| **🆕 旧解包的卡图是「裁过的」** | `解包整理/01_卡牌/*/Texture2D/` 里的插图是 660×1024（从 1024² 里裁的，而且**裁的位置和原版 sprite 的 `textureRect` 对不上**：旧 x180 w662 vs 原版 x176.5 w670.5）。**650 张卡图新旧内容不一致，全在卡图这一批**（UI 图/卡背不受影响）。→ 一律用 `d:/2/新解包资源/` + 按 `textureRect` 裁 |
| **🆕 新导入的 PNG 没有 `.meta` 导入设置 → 运行时读不了像素** | 刚拷进来的贴图用的是 Unity 默认导入设置，**`GetPixels32()` 直接抛**（没开 Read/Write）。`CardView.FrameUv` 量不到 alpha 包围盒就退回「整张 1024²」，**卡框的透明留白一起铺上去**，UV 裁切全失效（自检里那条「卡框 UV 裁到了卡本体上」就是这么红的）。**加完美术要跑一次 `-executeMethod ArtBaker.ApplyImportSettings`** |
| **🆕 稀有度→卡框档位没实测过就别写死** | 原版卡框分 tier1–4，但**哪一档对哪个稀有度资源里没有明说**（`CardFramesSO` 只给 `cardTier` 0..3 的槽）。这次是**拿 4 张不同稀有度的原版卡面和 4 张 tier 框逐一比对**定下来的（common→1 / rare→2 / epic→3 / legendary→4，对照图 `资料/留档_排查证据/卡面组装_0912/tier_map.png`）。⚠️ **2026-09-13 更正**：`special` 那 39 张**后来实测了** —— tier1 24 / tier2 15，**没有一张 tier4**；
而且 **tier 是「按阵营」的常数、不是逐卡属性**（`资料/普查产出_0913/_裁定_special卡框.md`） |
| **🆕 卡框分档 ≠ 底部那颗宝石** | 分档改的是**框的形制**（tier1 素 → tier4 华丽），而卡框纹理上那颗菱形在四档里**都是空的暗色凹槽** —— 稀有度的**颜色**是另外叠一张宝石（`1_40k_cardframe_rarity_*` 5 张，浅蓝/绿/紫/金/橙红）。只看卡框换没换就以为稀有度做完了，会漏掉这颗。对照图 `资料/留档_排查证据/卡面组装_0912/gems.png`（上排原版 / 下排我们的框） |
| **🆕 卡池里混着「不是卡」的条目** | `card_stats.json` 自己标了 16 条 `noise=true`（`Normal Conditions`/`Normal`/`v2` 这类**占位索引**，加 `HB`=Imotekh 的异画重复、`Threnodic Choir Flawless`=同图重复），还多一条 ` Iron Priest`（**前导空格**造成的重复）。而 `gen_cards_engine.py` **原来不过滤** → 它们是「没有卡面的假卡」，卡组编辑器和自动凑牌都可能抽到。**现在生成器已过滤掉。** ⚠️ 判重必须按 **(阵营, 归一化名字)** —— 原版本来就有跨阵营同名卡（Aggressor / Terminator 那几个），只按名字会误删。⚠️ 卡池**当前张数**看 `资料/阵营推进_清单与交接.md` §一（本文档不写死） |
| **🆕 按卡名找图，模糊兜底不能跟「整个文件名」比** | 贴图名带前缀（`CSM_BlackLegion_strat_Helfire Torch`），拿卡名跟整串比相似度只有 0.55 —— 「Hellfire Torch」和「Helfire Torch」这种**只差一个字母**的也被判成配不上（18 张就是这么漏的）。要**在文件名里滑一个跟卡名等长的窗**取最高分。另外**按卡名从长到短认领**：短名会先到先得（`Deffkopta` 抢走 `Mega Blasta Deffkopta` 的图） |
| 🔴 **🆕 「配不上 PnP」≠「原版没有这张卡」—— 而且「数卡图」本身就是个坑**（2026-09-15/16） | 那 32 张里**没有一张是「原版没印」**：9 张（DA `6秘密` 5 + GSC `6破坏卡` 4）是**手机翻拍的 `.jpg`**，而 `load_pnp_files()` 当时**只认 `.png`** ⇒ 它们**从没进过验收管线**，被误判成「原版没印」。剩下的是**两边写法不同**（`stormlord` ↔ `Imotekh the Stormlord`、`Poisoned Supplies` ↔ `Pilfered Supplies`…）与**天赋卡的 `(…Talent)` 后缀**（PnP 印裸名）。最后 1 张 `SW_Aggressor` 是**重复行**不是缺卡。⇒ 判据表只有一处：`check_pnp_cards.py` 的 **`PNP_NAME_ALIAS`**。⚠️ **改名要拍板**：PnP 印的是「当年写法」，**不一定是原版游戏名**（`Veteran Flyboy` 的 `zh_CN.csv:2565` 就站另一边），而且「PnP 文件名 / `ocrName` / `card_ids.json`」**三处全是 PnP 派生的、不是独立来源** |
| 🔴 **🆕 断言全绿 ≠ 版面对 —— 一次连「四条自检」都全军覆没**（2026-09-15） | `PragatiDigits.Blit` 少了**一次 y 翻转**（卡面缓冲 row 0 在**底部**，`CardView.FillHexagon` / `TextCanvas` 都翻了，只有这个 09-15 晚新加的文件没翻）⇒ **全池每一张卡**的费用/近战/远程/生命数字都画到了**上下镜像**的位置，而 `RuleEngineTest` / `BattleScene` / `DeckScene` / `CardBaseDemo` **全绿**。更隐蔽的是**验收用的拼版早于这处改动**（21:35 vs 23:49）⇒ 整轮 A1–A7 验收**没照到它**。⇒ **改了卡面数字/版面的代码，必须重渲一张卡亲眼看**；`_tmp_view/cardface_all/` 那批量尺也要跟着重渲，否则它自己就是坏的 |
| **🆕 正则 / 子代理报的「数据不符」都不是结论，判据是卡图**（铁律 7） | 两个方向的误报都吃过：用「中英属性序列比对」扫出 61 张候选，子代理逐张开图后**否掉十几张**；反过来子代理报 `UM47` 费用「原版 1」，裁原图一看是 **0**。⇒ **四个代理独立得出的原版规律（可直接当规则用）**：**`±N` 后只出现粉拳(近战) / 紫枪(远程)；「生命」一律写英文 `Health` 不配图标；「护甲」从不跟在 `±N` 后面** |
| 🔴 **🆕 卡面属性的 token 名不可信，判据只有一张逐卡表**（2026-09-16） | 同一个 `[Attack]`/`[Armor]`，**这张卡是拳、那张卡是枪**（`AM22` 的 `[Attack]` 是枪、`DA6` 的是拳）。**唯一的逐卡判据**是 `工具/gen_icon_plan.py` 的 **`TOKEN_BY_CARD`**（101 条，每条带证据）。**改某张卡的属性 token 要连它的键一起换**，否则旧键成**死条目**、新 token 掉进「锚点没对上」**没有图标**（实测 `GOF87` 被自动配成了**盾**）。生成器会打印「逐卡表里的死条目」。⚠️ **裸 `+N`（token 被 OCR 丢掉）与 token 写错是两回事** |
| 🔴 **🆕 原版卡面的「印次版式」不是我们的数据**（2026-09-16 实证） | **阵营行印不印**、**卡名是白还是橙**，跟的是**原版印次**（橙名版 / 白名版），**不是任何卡字段** —— 13 阵营开图 76 张 + 全库 1112 张扫描，唯一稳定的相关是单向的「橙卡名 ⇒ 一律不印阵营行」（23/23）。我们**无条件印一行**，与原版约 **1/3 的卡**不符。**要改就得逐卡记录版式**，推不出来。出处：`资料/PnP卡图_逐张对账_0915.md` §六 B 后面那节 |
| 🔴 **🆕 自检断言也会「靠巧合通过」**（2026-09-16 抓到两条） | `Check(notice.Contains(n + " 张"))` 在 `n = 0` 时被提示行里别的数字（`10 张`）**含子串 `0 张`** 蒙混过去 —— **行为变好了它反而变红**。⇒ 断言要**按分支精确比对那句话本身**，别用 `Contains(数字 + 单位)` 这种形状 |
| 🔴 **🆕 改卡表数据只有一条路：走生成链，别手改产物**（2026-09-13 起） | 卡表的 `keywords` / `subtype` **是算出来的**（源 = `资料/卡表核对_卡图提取/_合并总表.md`），`gen_cardface_fixes.py` 一跑就**静默抹掉**手改。要手工改某张卡 → 走 **`_manual_keywords` / `_manual_subtype`**（脚本原样保留、**最后应用**；说明写在同名的 `_` 键里；⚠️ 加条目要**写全该卡的整份关键词表，不是增量**）。完整链四条按顺序：`merge_cards_new.py` → `compare_cards.py` → `gen_cardface_fixes.py`（⚠️ 基准是 `card_stats.json`，不是产物）→ `gen_cards_engine.py`。⚠️ **核差异别只信 `--check`**（它只说「一致 / 已过期」、不列差异）—— 生成前后拿 `git diff --numstat` 逐字段比，**键要用「卡名+faction」或数组下标**，光用卡名会撞上**同名跨阵营**的卡、报出一堆假差异。<br>🔴 **2026-09-16 第二次踩**：`gen_cardface_fixes.py` 跑一次会**删掉顶层的 `_` 开头说明键**（白名单已改成「**顶层凡是 `_` 开头的一律带走**」） |
| 🔴 **🆕 改了 `desc` / `descZh` 必须重跑 `gen_icon_plan.py`**（两步：`--write` 落盘 + `gen_icon_doc.py`） | `card_icon_plan.json` 是**按每张卡的 `desc`/`descZh` 逐 token 算出来**的（`CardIcons.cs` 运行时读它画图标）⇒ 改了 desc 不重跑，**首图标会错**（实测 `DA43 Repulsor` 的首图标还停在 `Rally:`，而它 `desc` 已改成 `Armour 1.` 开头）。⚠️ **漏跑过**：重跑时计划表里还带着 `BL_Helfire_Torch` / `SOR_Sister_Dogmata` / `UM_2nd_Company_Terminator`（已改名）与 `SW_Aggressor`（已删）四个**幽灵条目**。落盘会**同时写两份**：`数据/游戏数据/card_icon_plan.json`（源）+ `Resources/` 那份（运行时） |
| 🔴 **🆕 夹在「效果文字」里的多义词，先读中文 `descZh`**（英文的 `or` 有两种含义） | `Give Flank to a friendly troop **or** Stun an enemy troop` → **二选一**（卡面没写 `choose` ⇒ **随机**）；`Deal 3 damage to an enemy, **or** 6 if it has Hunt Mark` → **条件换数值**（**不是二选一**）。两条在英文里长得**一模一样**，中文一眼分得开（后者写「若其带猎杀标记，则造成 6 点」）。⚠️ **中文一直都有、而且卡面印的就是它**（`cards_engine.json` 的 `descZh` 1124/1124；`BattleDriver.FaceTextFull:2585`：`DescZh` 有就用中文）。审计在 `资料/卡牌效果or句_审计.md`。⚠️ 另有**图标被剥掉只剩裸数字**的（`Adjacent units have +1`）—— 语义**在文本层不可恢复**（`Genestealer Familiar` 是拳、`Cadre Fireblade` 是枪，`desc` 与 `descZh` 都只剩 `+1`）⇒ **宁可认不出**，要修走数据侧 `cardface_fixes.json` 的 `_manual_*` |

### 特效还原线（详见交接文档第九节，那里更全）

| 坑 | 真相 |
|---|---|
| **🆕 原版材质上带着内置 Standard shader 的残留值** | 原版 shader 换过之后，旧材质里的 `_SrcBlend/_DstBlend/_ZWrite/_Surface/_Glossiness/_Metallic…` **不会被清掉**。如果新 shader 的属性表里**没有**这些名字，它们就是**死值**（Unity 忽略）；但我们自建的 shader 一旦用 `Blend [_SrcBlend][_DstBlend]` 这种间接寻址，就会把死值当活值用。实测：`Everguild/FX/Particle Distortion Affect Transparents` 属性表里混合相关的一个都没有（写死在 pass 状态：SrcAlpha/OneMinusSrcAlpha、ZWrite Off），而材质上是 `_SrcBlend=1/_DstBlend=0/_ZWrite=1` → 导出侧变**不透明覆盖 + 写深度**，把背后的东西整块抠掉。**判据：先读原版的 `m_PropInfo.m_Props`，属性表里没有的就一律硬编码，别做间接寻址**（`WFSpritesAdditive` 是同一个坑的另一例） |
| **🆕 特效在「没有内容可扭曲」的场景里本来就该是空的** | 抓屏扭曲这类效果读的是屏幕贴图。测量用的空场景只有纯色 clear，重采样纯色还是纯色 → 一个亮点都没有。**这会被读成「导出整个丢了」（C 组）**，其实原版在同样条件下也只是画一块恒定灰方块（采样到没绑定的默认灰贴图）。判定这类效果必须**给背景放真实内容**（`DistortProbe` 用不透明棋盘），而且要么把材质排进透明队列、要么确认 `_CameraOpaqueTexture` 已经装上内容 |
| **🆕 同一场景里常有多份同类组件 —— 先解父链判「用途」，别急着判「矛盾」** | ⚠️ **2026-09-14 更正**：这一行原来写「**值会互相矛盾**」，举 `CardsHorizontalLayout` 的 MB 5271 vs MB 4053（`m_scale` 0.73 vs 0.54）为例，还说 4053 是「另一套预设（小屏档或旧版本）」。**这是错的** —— 5271 = **我方手牌**（父链 `BottomAnchor/PlayerArea/HandArea`、`PlayerHand.isPlayer=1`），4053 = **敌方手牌**（父链 `UpperAnchor/EnemyArea/HandArea`、`isPlayer=0`、`m_invertRotation=1`），**两份各自都对**，`−330` vs `+74.5` 正负相反正是因为一方从下往上弧、一方从上往下垂。**错因**：只比字段值、没解 `m_Father` 父链（`isPlayer` 也没看），于是把「两个用途」误读成「一个权威一个作废」，还编出三条「判权威」的论据（结论蒙对了，论据全塌）。**正确做法**：解父链（`d:/2/新解包资源/assets_full/bundle_scenes_scenes_battlearena1/` 里逐层解 `Transform.m_Father`）或读用途字段，按用途取各自那份。**实据**：`Unity/资料/战斗规格/战斗重建_0827/子代理读报_back左区_0827.md:117,120,122,125`（2026-08-27 就写清「敌 4053 / 我 5271」了） |
| **🆕 挑特效不能只看台账，要实拍** | `effect_index.json` 判定「对得上 + 高置信」的效果里，`Axe_Slash_User_SW` / `Attack_Stomp` / `BulletImpactBurst_Blood` 在我们自己的战斗场景里渲成**一整块不透明黑方块**（材质 alpha 没还原对）。用 `Editor/VfxPicker.cs` 实拍：**每个效果拍 3 个时刻**（同一个效果早期是尘环、晚期是黑菱形，只看一帧结论完全相反）+ **背景用中灰**（用近黑背景会把黑方块和正常效果混在一起看不出来） |
| **🆕 批处理能跑 ≠ GUI 编辑器能开** | 工程 manifest 是瘦的、没装 `com.unity.modules.physics2d`，DOTween 的模块「默认全开」→ `DOTweenModulePhysics2D.cs` 编译不过。`DOTweenSetup` 只给 Standalone/Android/iOS 设了宏，**编辑器那套编译用不上** → 「批处理全绿、GUI 一打开满屏报错」。修法：`Assets/csc.rsp` 写 `-define:DOTWEEN_NOPHYSICS2D`。**rsp 里不能写中文注释**（会被当成源文件参数报 `CS2001`） |
| **🆕 静态字段不进 Play 模式** | `CardEffects.Play` 是静态委托，编辑器建场景时接了也白接 —— 按 Play 时是 null，特效只剩日志。用 `[RuntimeInitializeOnLoadMethod]` 自己装（`WarpforgeEffectHost.cs`） |
| **🆕 组件上的私有引用不进序列化** | `BattleBackdrop` 的 `_mr/_tex` 存场景后是 null，背景不刷新。`Build()` 里要从子节点重新找一遍 |
| **🆕 相机的宽高比必须在建东西之前定死** | HUD 文字/格位底片的世界坐标**建的时候算一次**，而 `LayoutSpace.VisibleWidth` 跟着 `aspect` 走。批处理下相机默认 4:3 → 建完再拍 16:9，HUD 整片偏左 |
| **🆕 Unity 导入会把非 2 次幂的图缩到最近的 2 次幂** | 1920×1080 的背景图会被拉成 **2048×1024**（宽高比 1.78→2.0），一「铺满」就整体放大 12.5%，和原版量出来的槽位坐标对不上。`textureImporter.npotScale = None` |
| **🆕 粒子材质要用 `Particles/Unlit`，不能用 `URP/Unlit`** | 后者**不乘粒子顶点色**，`startColor` 的灰度/透明度全丢 —— 烟和蒸汽渲出来是一坨**白方块** |
| **🆕 共享材质上改 `mainTexture` 会串** | 所有卡共用同一张贴图。每层都要 `new Material(模板)` |
| **🆕 画布数组 row 0 在底部** | 本项目的贴图绘制里，「从顶部数」的 y 和数组行号差一次翻转。`FillHexagon` 忘了翻 → 费用六边形画到了**右下角**（盖住生命宝石）。写画布图元时先问一句「这个 y 是哪套坐标」 |
| **🆕 UI 图集里 sprite 的坐标只在 `SpriteAtlas.m_RenderDataMap`** | `Sprite.m_Rect` / `m_RD.textureRect` 全是 `(0,0,w,h)`（那是 sprite 自己的尺寸），`uvTransform` 是枢轴偏移。切图集用 `d:/2/Warpforge_tools/slice_battle_atlas.py` |
| **🆕 粒子系统的随机种子不钉死，尺子就不确定** | `useAutoRandomSeed` 默认**开**，每次重播换种子 → 粒子分布每次不同。实测**同一进程连渲三次** 537/518/512 个亮点（亮度和却几乎不变 = 分布变了），两遍扫描 ~60% 的采样行会变 —— **噪声和要测的改动一样大**。修法（`EffectSweepBatch.SeedAndReset`）：关自动种子 + 显式 `randomSeed` + 先 `Simulate(0, restart:true)` 复位。修完 2968 行只剩 11 行差 1 个像素。**改东西之前先确认这条** |
| **🆕 `SpriteRenderer`/`SpriteMask` 的 sprite 不会被导出** | 精灵是 bundle 资产，引用落不下来（Unity 写成 guid 全 0 的伪引用）。后果不止精灵自己不显示：`VisibleInsideMask` 的粒子全靠它裁形，遮罩一空**整块效果渲成空**。实测 **78 个 prefab** 有被遮罩的粒子。修法见交接文档 P0-h |
| **🆕 原版 prefab 的根节点有的停在场外 `x≈100`** | `Atk_GrotGrenade`(99.59,1.24,-0.37)、`Attack_Stomp`(100,0.725,0)。播放时**位置必须归位**，否则效果播在场景外；但**旋转/缩放不能动**（是作者定的，`Antimatter Explosion` 是 X=-90°、`Atk_GrotGrenade` 是 scale 0.5） |
| **🆕 取景要量「要渲染的那个实例」** | 量原样实例化的副本 → 相机看 x=100、效果在原点 → 全 0 亮点。**看着像还原失败，其实是尺子错了** |
| **🆕 「一大团粉色糊满屏」未必是坏 shader** | 可能是效果本身就是全屏类：`Color change overlay` 半径 **245.95 单位**（战场环境叠加）。判据用半径：普通卡牌特效 3–15 / 大范围闪电 74–82 / 环境叠加 245+ |
| **🆕 `sweep_*.tsv` 的 `*_srgb` 两列是坏的** | 恒为 65536。台账用的 `lit`/`sum` 没受影响，但那两列别再拿去算东西 |
| **🆕 看到「一大片效果同时坏掉」，先怀疑尺子，别怀疑资产** | 本轮实测**五次**：①280 个「两边都空」是单帧采样点不对 ②749 个「严重偏暗」是扫描工具两边加载条件冲突 ③达标数掉 36 个是兜底逻辑覆盖了真值 ④「Z 计数从 500 掉到 470」是 Z 计数的带宽边界抖动（边界挪 0.05 就摆动 22–54 个）⑤白板 10/24「没渲染」是取景量错了实例。**五次都是工具/代码的坑，一个资产都不用改**。直接当资产问题去修会白干好几天 |
| **🆕 `资料/原版参照图/` 不能整目录当参照用** | 239 张里**只有 11 张可信的真渲图**（`shots_battle/` 4 + `data/` 4 + `pitch_matrix/` 3）。`shots_ui/ui_prefabs/` 那 218 张**全是黑的**（同一 MD5、都是 27260 B）—— 生成它的 SceneJumpShot v2.2 挑了 `clearFlags=Nothing` 的 UI Camera、又把 Canvas 强制成 `ScreenSpaceOverlay`（Overlay 不过相机，当然拍不到）。**不是「进不去原版」**：同一轮另一张相机拍的图是好的（mean 95.55），v3.6 改法之后就正常了 |
| **🆕 `AssetBundle.LoadFromFile` 对「内容已在进程里」的文件返回 null** | 同一份内容、两个路径也算重复。编辑器工具先把 84 个源 bundle 全量加载后，`wf_shaders.bundle` 就再也加载不进来 → 所有只靠 bundle 兜底的 shader 解析失败 → 渲染成纯色块。这是 P0-d 的真因 |
| **🆕 读材质属性要连「值」一起从序列化数据读** | `Material.GetFloat()` 对「shader 没声明」的属性返回 0，而原版材质的 shader 来自 bundle、声明表是残缺的。只读名字不读值，重建出来的 `_SrcBlend` 全是 0 |
| **🆕 UnityPy 重打 bundle 必须保留 `AssetBundle` 对象并重写它的 `m_Container`** | 删掉它 → Unity 报 `not compatible with this newer version of the Unity runtime`（**这句话极具误导性，跟版本毫无关系**）；留着但不重写 → 包能加载但一个资产都暴露不出来 |
| **🆕 拿不准判定的效果，加「置信度」而不是硬给结论** | 两个小数字相除得出的比值没有意义（实测有个效果原版只有 22 个亮点、导出 114 个，比值 5.38 纯属噪声） |
| **`com.unity.modules.assetbundle` 默认没装** | Editor 程序集能用 `AssetBundle`，**运行时程序集编译不过**（`CS1069`）。已在 manifest 补上 |
| **`Shader.Find` 打包后找不到未被引用的 shader** | 自建 shader 必须注册进 Graphics Settings 的 Always Included Shaders |
| **legacy 粒子 shader 在 URP 里变粉** | `Mobile/Particles/Additive` 这类是内置管线的；且混合写死在 shader 里、材质上没有 `_SrcBlend`，必须按 shader 名推断 |
| **`ParticleSystemRenderer.mesh` 不是 `MeshFilter`** | 漏了这条，Mesh 模式发射的粒子（弹体/光环）导出后整个消失 |
| **比对工具取景必须统一** | 各自自动构图会让亮度差失去可比性，曾据此得出错误结论 |
| **`-executeMethod` 一次只能给一个** | 给两个只跑最后一个 |
| **不清旧产物会累积副本** | `GenerateUniqueAssetPath` 不断产出 `X 1.mat`；清理时别把 `WarpforgeVFX/Runtime` 和 `Shaders` 一起删掉 |
| **`Warpforge_code` 是签名桩** | 方法体**直接留空**（不是抛异常），0 个文件含桩标记。字段名/类型/`[SerializeField]` 完整可用 |
| 🔴 **🆕 往自建 shader 里接原版属性之前，先问三句**（2026-09-15 踩到，13 个效果同时变暗） | 三句 = **默认值是什么 · 原版在什么条件下才采它 · 原版是加法还是乘法** —— **它多半不是乘法**。实测：给 `WFParticlesExtraColor.shader` 接 `_EmissionColor` 时写成 `col = tex * _Color * _EmissionColor * IN.color`，而原版那批材质里它是**黑的默认值**（URP/内置粒子 shader 属性表就是 `(0,0,0)`，还要 `_EmissionEnabled`/`_EMISSION` 才采它）⇒ **整个材质被乘成黑**（`Cut Wulfen SW` 亮度比 0.033）。改回**加法发光项**后 13 个效果全复原。**这跟「原版材质带内置 Standard 的残留值」是同一条规矩的两个方向** |
| 🔴 **🆕 「尺子漏了」：`BlendProbe` 的 shader 解析那段覆盖不全**（2026-09-15 亲手验过） | 它是**从 `导出报告.tsv` 的 `原 shader:` 行抓名字**来试的（`BlendProbe.cs:128-150`），所以**报告里没出现过的 shader 它一条都不验** —— 把三张 ShaderGraph（`Fx_ParticleDissolve_apb` / `Fx_RockDissolve` / `Eclipse Tau`）从映射表里 stash 掉，它**照样报 70/70 / 0 失败**。那三条因此能烂到 C 组。⇒ 新增**点名验**的探针 `-executeMethod ShaderResolveProbe.Run`（`WarpforgeArena1/Editor/ShaderResolveProbe.cs`，带对照组、**不依赖任何报告**）。⇒ **断言绿不等于覆盖面够**，跟「白板 20/20 不是 958 个」是同一类坑 |
| **🆕 W 组的「两边全程空」是噪声门假阳性** | 判据是「亮点数 < 60」，**不是「都为 0」** ⇒ 这批得重判。⚠️ 手头有两份**不同口径**的台账别混：`d:/2/Warpforge_tools/data/vfx_wiring_j1..5.tsv` 是 2026-08-24 的**独立「效果→触发事件」台账**（可用它重判 W 组，别逐条找触发条件）；`资料/特效还原_进度与交接.md` §〇·一 记的是**验收指标**那一路 |

---

## 八、常用操作速查

```bash
# Unity 批处理构建战场
unset ELECTRON_RUN_AS_NODE && "D:/Unity/Hub/Editor/6000.3.23f1/Editor/Unity.exe" \
  -batchmode -quit -projectPath "D:\4\Unity\MyGame" -executeMethod BuildArena1.BuildFromCLI -logFile -

# 渲染战场预览图（批处理下需模拟粒子）
... -executeMethod BuildArena1.RenderPreview -logFile -

# === 特效还原 ===
... -executeMethod EffectExporter.Run -logFile -                 # 导出效果（全量导出前把 EffectExporter.Limit 改成 -1）
... -executeMethod EffectCompare.Run -logFile -                  # 原版 vs 导出 并排渲染比对 → d:/4/_tmp_view/cmp/
... -executeMethod WarpforgeSetup.RegisterShaders -logFile -     # 新增/改名自建 shader 后，注册进 Always Included

# === 特效「能用」这一侧（详见 资料/特效还原_进度与交接.md 第五节）===
"D:/2/Warpforge_tools/py312/python.exe" d:/4/Unity/工具/gen_effect_index.py   # 导出集变了 → 重建索引 → 重建效果库（两步都要跑）
... -executeMethod EffectLibraryBuilder.Run -logFile -
# 白板自检：播→消失闭环 / 渲染与台账一致 / 反复播不泄漏（grep "^WB " 看结果）
... -executeMethod WhiteboardTest.Run -logFile "d:/4/_tmp_view/whiteboard.log"
# 🆕 抓屏扭曲定位（三种纯色背景 + 一块不透明棋盘，量「相对空场景的贡献」）；图在 _tmp_view/distort/
... -executeMethod DistortProbe.Run -logFile "d:/4/_tmp_view/distort.log"
#    筛输出：grep "^DP "；退出码 0 = 断言全过；**断言条数看自检打印的那一行，别写死**

# 🆕 挂上抓屏 Feature（**幂等**，重复跑不会加两份）—— 把 GrabPassTransparentFeature 加进两个 Renderer 的
#    rendererFeatures；手动在 Inspector 里加也行，但那是「下个会话不知道做过没有」的改动 ⇒ 用这个入口
... -executeMethod WarpforgeSetup.RegisterRendererFeature -logFile -
# 🆕 混合状态 + shader 解析的**常驻守卫**（**改 shader/binder 之后必跑**）
... -executeMethod BlendProbe.Run -logFile "d:/4/_tmp_view/blend.log"
#    筛输出：grep "^BP "；退出码 0 = 断言全过；报告 _tmp_view/blend_report.tsv
# 🆕 **点名验「每个原版 shader 名都解析得到」**（`BlendProbe` 那段是拿报告里的名字试的，会漏）：
... -executeMethod ShaderResolveProbe.Run -logFile -

# 🆕 读任意原版 shader 的属性表 + pass 状态（**动手改自建 shader 之前先跑它**）
"D:/2/Warpforge_tools/py312/python.exe" d:/4/Unity/工具/dump_shader.py "Particle Distortion"
# 生成人看的白板场景 → Assets/WarpforgeArena1/Scenes/VFXWhiteboard.unity
... -executeMethod WhiteboardBuilder.Run -logFile -

# 重跑场景清单生成器
"D:/2/Warpforge_tools/py312/python.exe" d:/2/Warpforge_tools/scripts/gen_unity_arena_manifest.py

# === 规则引擎（详见 资料/规则引擎_进度与交接.md 第三节）===
# ⚠️ 下面所有 python 工具**必须带 PYTHONIOENCODING=utf-8**（脚本里有 ⚠️ 这类字符，
#    GBK 控制台会在**写文件之前**抛 UnicodeEncodeError）
# 卡表精简（改了 card_stats.json 或 数据/卡牌翻译/zh_cards.json 之后重跑；--check 只对账不写文件）
PYTHONIOENCODING=utf-8 "D:/2/Warpforge_tools/py312/python.exe" d:/4/Unity/工具/gen_cards_engine.py
# 🆕 卡面图标的计划表（改了任何卡的 desc / descZh 之后**必须跟着重跑**，两步）—— 判据与坑见 §七
PYTHONIOENCODING=utf-8 python Unity/工具/gen_icon_plan.py --write   # 落盘（不加 --write 是干跑）
PYTHONIOENCODING=utf-8 python Unity/工具/gen_icon_doc.py           # 人看的对照文档
# 原版美术同步进 gitignore 的 Resources/Art/（幂等；卡框/卡背/插图的做法见 §七）
"D:/2/Warpforge_tools/py312/python.exe" d:/4/Unity/工具/import_original_art.py
#   ⚠️ **导完美术必须再跑一次这个**（否则新 PNG 没开 Read/Write —— 见 §七）
... -executeMethod ArtBaker.ApplyImportSettings -logFile -
# 配不上的插图摊成人能看的（原版卡面 + 候选插图 + README）→ Unity/资料/待人工对_插图/
#   ⚠️ 那个目录在 .gitignore 里（全是原版资产）
"D:/2/Warpforge_tools/py312/python.exe" d:/4/Unity/工具/collect_unmatched_art.py
# 规则引擎自检（纯逻辑，很快）
... -executeMethod RuleEngineTest.Run -logFile "d:/4/_tmp_view/ruleengine.log"
#    筛输出：grep "^RE " d:/4/_tmp_view/ruleengine.log；退出码 0 = 全过，1 = 有失败
# 🆕 逐句解析探针（改 EffectText 的判据之前/之后拿来量）
#   写 d:/4/_tmp_view/probe_in.txt（一行一句，`#` 是注释）→ 跑 → 读 probe_out.txt
#   （卡级判定 + 逐段判定 + 每条 op 摊平；**只读文件、不碰工程**，可以反复跑）
... -executeMethod EffectParseProbe.Run -logFile -
#    筛输出：grep "^EP "；退出码 0
# 卡牌基座自检（**棋盘格数改过就要复跑**）
... -executeMethod CardBaseDemo.Run -logFile -
#    筛输出：grep "^CBD "；🆕 **判据看 `=== 合计：N 通过 / M 失败 ===`，退出码 0 = 全过**
#    （⚠️ 这两样是 2026-09-17 才有的 —— 在那之前它**一个断言都没有**，只是截图生成器）

# === 可玩对战（详见 资料/规则引擎_进度与交接.md 第一节）===
# 重建「能玩的那个场景」→ Assets/CardPresentation/Scenes/Battle.unity，打开按 Play
... -executeMethod BattleScene.BuildAndSaveScene -logFile -
# 卡组编辑自检（规则/筛选/加删牌/翻页/版面 + 截图）
... -executeMethod DeckScene.Run -logFile "d:/4/_tmp_view/deck.log"
#    筛输出：grep "^DK "；断言条数看自检自己打印的那一行（**别在这里写死**）
# 重建卡组编辑场景 → Assets/CardPresentation/Scenes/DeckEditor.unity
... -executeMethod DeckScene.BuildAndSaveScene -logFile -

# 对战自检：开局/拖拽/费用拒绝/出牌/攻击/护卫/技能与触发/打完整局/原版卡牌/卡组库那套牌/战术卡拖出去打
#   （断言数见 `资料/阵营推进_清单与交接.md` §一 ⚠️ 2026-09-16：原写 §五，实为 §一；另出 16 张截图）
... -executeMethod BattleScene.Run -logFile "d:/4/_tmp_view/battle.log"
#    筛输出：grep "^BT "；图在 d:/4/_tmp_view/battle/
#    ⚠️ 它**退出时可能 segfault**（崩在 Unity 关停之后）—— **判据看 `BT === 合计` 那一行，别看退出码**

# === 结算「开门」视频（详见 资料/规则引擎_进度与交接.md）===
# 视频元数据自检（阻塞、秒回；断言源尺寸 3840×1080 / 片长 / 帧数与原版元数据一致）
... -executeMethod VideoProbe.Run -logFile "d:/4/_tmp_view/videoprobe.log"
# 真播 + 左右拼 alpha 合成 + 像素断言 + 结算界面实拍（~30 秒）—— ⚠️ 不能带 -quit
... -executeMethod VideoProbe.Play -logFile "d:/4/_tmp_view/videoplay.log"
#    筛输出：grep "^VP "；图在 d:/4/_tmp_view/video/
#    视频本体在 gitignore 的 Resources/Art/videos/，从 素材/Warpforge原版/视频/videos/ 拷（改名 .webm）

# 从档案库同步资源（幂等）
"D:/2/Warpforge_tools/py312/python.exe" d:/4/Unity/工具/sync_from_d2.py --batch all

# === 进原版游戏（看"原版到底长什么样"的标准答案）===
# ⚠️ 必须先设 DOTNET_ROOT，不设起不来；副本路径必须纯 ASCII（MelonLoader 的 plthook_open 怕中文）
taskkill //IM Warpforge.exe //F
cd /d d:\2\unity_run_ref && set DOTNET_ROOT=<本机 dotnet 根> && start "" Warpforge.exe
# mod 配置：d:/2/unity_run_ref/UserData/sjs_dump_cfg.txt  四行：
#   第1行=bundle 名（写 scenes_scenes_mainmenuwarpforge.bundle 就进主菜单）
#   第2行=进场景后等几秒   第3行=**输出目录（归档位，别改回 `_tmp_view/orig`，那儿会被当垃圾清）**
#   第4行=动作：drive＝战斗驱动全套 / 空＝只 dump 一次+截一张 / 秒数列表如 45,75,105,135＝多时间点各一份
# 产出：runtime_ui_dump_*.tsv（UI 树，带 activeSelf 和 sprite 名）+ 真渲 PNG
# ⚠️ 战斗 UI 的「内容」出不来（要对局数据）—— 但 3D 战场是对的，UI 树里的 rect 也全是真的。
# ⚠️ 验不了的（实测，别再撞）：手牌注入（原版关服、卡数据在远程 CCD）、卡组编辑界面（实例化即黑）、
#    主菜单只拍得到顶栏、改游戏数据、ScreenCapture 截图会崩
```

**Python 一律用 `D:/2/Warpforge_tools/py312/python.exe`** —— 系统的 Python 3.14 缺 UnityPy 纹理转换模块，导出会失败。

---

## 九、距离「完整还原原版」还差什么

> 2026-09-12 对账结果。派子代理把**反编译代码（1800+224 个带方法体的 `.c`）+ 运行时 UI dump +
> 离线规则书**挖了一遍，列出原版「战斗 + 卡组编辑」到底有哪些环节，再拿我们的代码逐条比。
> 原版那侧每条都有类名/场景对象名出处，我们这侧是实测的代码状态。**按「还差多少」排序。**
> 🔴 **下一件事只看文首那张表**（本节不再排优先级 —— 它只留「原版有什么、我们做到哪」的对账结论）。

### 🟡 UI 对齐 / 玩法骨架 / 卡牌覆盖率 —— **只看三份指针**

> 本节 2026-09-12 的对账结果**逐项都已收口**（玩法骨架 · UI 元素 · 战术卡解析 · 阵营资源），
> 逐项实测值、现状与残留悬案**只看这三份，本文件不抄第二遍**：
> ① **战斗 UI 逐元素**（原版实测值 + 出处 + 现状 + 还差什么 + 悬案）→ `资料/战斗UI_原版对账表.md`
> ② **玩法 / 卡牌覆盖率 / 自检数（唯一出处）** → `资料/阵营推进_清单与交接.md` §一 表 + §一之三
> ③ **特效逐效果状态**（判定 / 亮度比 / 置信度，**数字只有这一处**）→ `资料/特效还原台账.tsv` +
>    `资料/特效还原_进度与交接.md` §〇·一
> ⚠️ **仍然成立的结论**：卡池张数与「双射零剩余」**看 `资料/阵营推进_清单与交接.md` §一**
> （本文档不写死）；**「卡框分档 ≠ 底部那颗宝石」**见文首那条被推翻的旧记录。
> ⚠️ **一条已知的如实偏离**（不是静默失效）：`useWaystone` 的**主动「收集」**没做，
> 现在是单位一死直接 +1（`RuleCore.cs:845-846`）。

**移植方案与实测数据**（句型普查、`_resolve_text` 的 handler 顺序表、两条决定性结论）
见 `资料/战术卡效果_移植方案.md`。

> 📌 **倒计时 / 投降 / 出牌超时** ✅ 全做完。秒数出处**是知识，留着**：**离线练习 600 s · EventAI 240 s ·
> 换牌 25 s**（三处的原版出处见 `资料/战斗规则与数值_出处.md` §二 与 `资料/VarsGlobal_原版数值.md`）。

**验收规矩**：`BattleScene.Run` 的断言数**只写在 `资料/阵营推进_清单与交接.md` §一 一处**（⚠️ **2026-09-16 更正**：原写 §五，实为 §一），
**每改一项加一条，别只看截图**；改布局后 `CardBaseDemo.Run`（四档分辨率）**必须复跑**；
碰特效 shader 前先跑 `工具/dump_shader.py`。

### 🅱 特效线（把「能用」推到「好看」）

- ✅ **`_GrabPassTransparent` 已查明并落地**（原悬案）：原版 HLSL 源码被剥了，**但编译字节码在** ——
  `Shader.compressedBlob` 分块 LZ4 解压出来是 DXBC、**资源名是明文**（工具 `工具/dump_shader_blob.py`）。
  名字里的 *Affect Transparents* 说明它拷的是**含透明物**的颜色缓冲（URP 的 `_CameraOpaqueTexture` 不含）
  ⇒ **`GrabPassTransparentFeature` 已实现**（把颜色缓冲拷进那个全局纹理），抓屏扭曲那一族 41 条读的就是它。
  ⚠️ **这一条 2026-09-18 更正过**：原文写「**下一步（真该做的是它）**」—— 那是**过时**的，早就做完了，
  留着只会让下一个会话去做一件已经做完的事。
- **验收注意**：指标用 `|ln(亮度比)|` 的中位/均值，**别用「对得上多少个」**；尺子已经修成确定性的
  （粒子随机种子钉死）。**P1-a0（尺子的假象 + 那个真 bug）见 `资料/特效还原_进度与交接.md` P1-a0。**

### 🗣 开新会话时怎么说

直接说「**继续 <线名>，<要做的事>**」，让它先读对应的交接文档。最常用的三条开场：
「**继续卡牌线，做 …**」→ `资料/阵营推进_清单与交接.md` §一之三（**先跑一次自检**）·
「**特效线，查 sweep 退步**」→ `资料/特效还原_进度与交接.md` §〇·一 ·
（原「最后换美术」那条**已作废**：美术留原版，见文首。）

> 📌 **「要什么 → 看哪份文档」的完整索引在 §三**（本文件只留那一处）。
> 🔴 **特效线 / AnimFX 的当前状态与「下一步」只看两处**：`资料/特效还原_进度与交接.md` §〇
> + `资料/AnimFX_实现与接线.md`（模块层：实现清单 · 钩子表 · 验收）。

---


## 十、文档维护

**为什么单开一节**：文档是「下一个会话唯一的记忆」。一条写错的记录会一直误导下去 ——
已经吃过好几次（见 `CLAUDE.md` 铁律 5）。

- **搬去 `资料/历史/` 的逐轮流水账**（正文里一律换成了指针）：`规则引擎_逐轮记录_0911-0913.md` ·
  `规则引擎_第七节_已做完记录.md` · `项目任务_第七～十六轮小结.md` · `项目任务_已完成清单_0916.md`。
- 🔴 **别以为瘦身就等于每份都变短**：`原版复刻_场景与美术.md` 过铁律 6 时是**净增**的 ——
  它没有「该删」项，改的全是**更正 + 补出处**。
- 🔴 **行尾（LF/CRLF）**：完整判据、修法与踩过的两次见 **§七「行尾（LF/CRLF）」那条**。

**还没结清的几处**（都是「两条规矩打架」或跨文件的数，**没敢自作主张**，列在这儿等拍板）：
- `排版` §三 Tooltip 原文 / §八「真未知」：体检清单说「该删」，但同一份文件的「不许动」名单又列着它们 ⇒
  里面是 Tooltip 原文与「`Z_OFFSET` 原版查不到、自定 0.004」这类**如实声明**，按铁律 6 **留了**
- **参照图张数三处不一致**：磁盘实测 **241** · `卡组` §四 写 239 · `排版` 原写 257
- `基座` §六 出处表仍称 `rule_core.gd` 为「**权威**语义来源」，与「权威 = 规则书 + 反编译、
  `rule_core.gd` 降为对照」的裁定不符
- `排版` 手牌尺寸「165×263」与「165.0×262.7」是同一个量的两种精度，未统一
- `数据源` §二「只有我们有 24 张 / 文档有 48 个名字」是拿**旧分母**跑的，换分母没重跑

**那条规矩不变**：**该瘦，但只能删「已完成的欠账」和「同一件事的第二份摘要」——
凡带出处的行（`:行号`、`MB 5272`、反编译 `:105`）一律原样留着**，
删了结论就变成不可复核的断言。

🔴 **一条实测出来的教训（照它改你写的每一份文档）**：
**自检数 / 覆盖率 / 名单，一律只能写成指针，不能写死。**
第三十四轮核对过 17 条记忆，**5 条栽在写死的数字上**，全部在 1～2 轮内过期；
本轮又在交接文档里抓到同样的病（同一件事在 4～6 处各写一个数、互相打架）。
⇒ 写数字之前先问一句：**这个数的权威在哪一份？** 有权威就写「见 X 某节」，别抄第二份。

**别动**（删了会丢东西）：`资源使用手册.md` §3 速查 + §4 十坑 ·
`特效还原_进度与交接.md` §八 的 47 条坑表 · `规则引擎_设计.md` §6b 的规格书 ·
`战斗UI_原版对账表.md` 全篇 · `战术卡效果_移植方案.md` §一/二 的普查数据。

## 十一、悬案与「拿不到的东西」（2026-09-12 收工时整理）

> **为什么单开一节**：项目里有几件事**卡在「本地确实没有」上**，不是「没找到」。
> 这些如果散在各文档里，下一个会话会**重复挖一遍**（本轮就重复挖了两条：
> 骷髅阈值其实写在规则书、回合时限其实是字面量能读）。
> 已经查实的见 `资料/战斗规则与数值_出处.md`；**这里只放「还没解决的」**。

### B. 还没解决的悬案（按「影响什么」排）

| # | 悬案 | 卡在哪 | 明细在哪 |
|---|---|---|---|
| 1 | **攻击方式选择器三钮的位置** | dump（三钮 `activeSelf=False`，近战在槽左下、Active/Range 叠在中心）与 `AttackSelector.cs` 文件头（「以横排为准」）**冲突**，而战斗 UI 要对局数据才出得来 | `资料/战斗UI_原版对账表.md` 第四节 |
| 2 | **七张表的数值只剩 OCR 一条来源** | 全池有多少张像 Baneblade 那样「把盾牌读成远程」，**只抽 43 张量过**（5 张错）。根治要**把四个数值圈从全池卡面图里程序化读出来**（位置是固定的，可行；池子张数见 `资料/阵营推进_清单与交接.md` §一） | `资料/卡牌数据源对账_0912.md` §六 |
| 3 | **牌堆底板的绝对位置** | 只有静态坐标表一个来源，dump 里是 stretch 读不出 —— **单一来源** | `资料/战斗UI_原版对账表.md` |
| 4 | **关键词字号没有原版出处** | `DescTextUnit 23.55` 是**请求值不是渲染值** → 已标「我们挑的」 | `资料/规则引擎_进度与交接.md` |
| 5 | **卡组导入导出格式 / 中文文案表** | 原版 `CardDeck.Serialize` 未反编译；中文文案是我们起的名字（版权红线） | 同上 |
| 6 | **`m_elemetsParent` 父节点变换 / `allowSelectCardLayoutOverFlow`** | 排版文档里标着「真未知」 | `资料/对战排版_原版数值与改造方案.md` |
| 7 | **奖励 / 评分数据** | **服务器侧，永久拿不到**（原版已关服）→ 只能占位或自创 | `资料/规则引擎_进度与交接.md` |

> ⚠️ **第 2 条单独提一句**：它是唯一一条**会影响玩法正确性**的 —— 数值错了，卡的强度就错了。
> 其余几条都是「还原度」层面的。
