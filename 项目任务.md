# 项目任务文件 —— 自研卡牌对战游戏

> 建立：2026-09-10　|　更新：**2026-09-14（卡牌线 A5 全线收工 + A7 光环收工〔含「回合到期」族〕；
> 另更正「单位 / 部队」口径、盘点卡面图标）**　|　面向：**新会话开工前必读**
> 前六轮：Unity 环境 → 解包资源审计与修复 → 战场重建 → 特效还原 → 特效「能用」+ 卡牌基座
> → 对战排版 + 规则引擎 + 可玩对战
>
> **当前状态（2026-09-14 收工）一句话**：打开 `Assets/CardPresentation/Scenes/Battle.unity` 按 Play
> 就能和贪心 AI 打一局 —— **打的是原版卡**（Ultramarines vs Goff，从原版卡池按阵营凑，**1130 张**），
> 卡面是**按原版规格组装的原版卡**（立绘铺满 + 卡框 alpha 当遮罩 + 卡框 + **底部稀有度宝石** +
> 中文卡名/效果），HUD 的每个元素都按原版实测尺寸摆过。
> ⚠️ **2026-09-13 更正**：这格原来写「**卡框按稀有度分四档**」—— **不成立**。
> 实测 39 张 `special` 卡用的是 tier1/tier2（**没有一张 tier4**），而且 `CardTier` 与 `CardRarity`
> 是**两个独立枚举**（框档跟的是升级档/印刷批次，不是稀有度）。详见
> `资料/普查产出_0913/special_tier_实测_前半.md`（两半独立收敛）。**修法要拍板。**
> 现在这一局的完整流程是：**开局换牌（回车或点「完成换牌」）** → 拖牌上场（付不起拖不上去）→
> 攻击方式三选一 / 技能与触发 / 特效与**手感补间**（发牌入场 / 出手 / 命中 / 阵亡 / 伤害飘字都能看出来）→
> **轻点卡牌弹放大展示窗** → 墓地·战斗日志面板 → 倒计时与投降 → 打完播原版的 Victory/Defeat/Draw 视频再出结算面板。
> **自检跑哪几条、跑出来多少 —— 只看 `资料/阵营推进_清单与交接.md` §五 一处**，本文档不抄。
> ⚠️ 这一格先后记过三个版本的数、都被后来的断言淘汰了；**写死的数字在这里只会误导**。
> 逐轮做了什么见各线交接文档；**本文件只放待办、坑和速查**。
>
> ⏭️ **新会话从哪开始**：看上面那段「**下次接着做**」，或者按线走 ——
> **卡牌线是主战场**，入口是 `资料/阵营推进_清单与交接.md` §一之三（**每次自检会重写工作清单**）。
> 「战斗界面 / 玩法骨架」那条线做满了；两条线的待办都在「下次接着做」里。
>
> ⚠️ **判断工作区状态只看 `git status`，不要信文档里写死的「干净」** ——
> 这一格写死过一次、害过人（文档说干净，实际攒了四轮没提交）。
>
> **📜 第七～十六轮的轮次小结已挪走** → `资料/历史/项目任务_第七～十六轮小结.md`
> （按铁律 6：交接文档不放流水账。**要看逐轮完整历史用 `git log`。**）

> **📊 战斗 UI 改了哪些、还剩哪些：看 `资料/战斗UI_原版对账表.md`** ——
> 一个元素一行，原版多少 / 我们多少 / 出处是哪，还带着「悬案」和「量出来的中间值」。
> **改战斗 UI 之前先查它，不要重新去挖 dump。**

> **⏭ 下次接着做（按优先级，2026-09-14 卡牌线 **A5 + A7 全部收工**）**
>
> 🔴 **用户定的顺序：先做卡牌线的 A 组。** 下面 1 就是它。
>
> ✅ **开工前先 `git status` 看一眼**（**别信文档里写死的「干净」** —— 这一格写死过一次、害过人；
> 也**别信这里列的提交号**，那个会过期）。
>
> 1. **卡牌线 A 组**（**主战场**）—— 入口只有 `资料/阵营推进_清单与交接.md` §一之三。
>    逐项的数字都以**自检每次重写的那几份**为准（`_tmp_view/tactic_unparsed.txt` ·
>    `tactic_by_faction.md` · `when_unparsed.md` · `adjacent_report.md` · `unit_desc_unparsed.txt`），**别抄这里的**。
>
>    **📌 一句话状态（2026-09-14 · A5 全线收工 + A7 光环收工）**：战术卡**完全解析 443/449** ·
>    单位卡 desc **570/586** · 督军 desc **55/56**（**① 栏 0 种** —— 一句不认识的都没有了）·
>    未实现关键词 **3 个**（`destroyer` / `ecstasy` / `sabotage`）· 自检 `RuleEngineTest` **2432/2432**。
>    ⚠️ **数字只写一处** —— 权威是 `资料/阵营推进_清单与交接.md` §一 + 自检每次重写的 `_tmp_view/*`，
>    **别抄这里的**。
>    🔑 **A7 收工的样子**：**30 张光环全部生效**，`[unit]` ① 栏里**一条光环句都不剩**
>    （最后 2 张「卡面裸 `+N`」的从**数据侧**修掉了）。
>
>    **接着做（按优先级，每条都写了从哪开始）** —— **表里只有第 1~4 行是待办**，
>    下面打 ✅ 的是**已收工**的记录，留着只为「**别重做**」：
>
>    | # | 做什么 | 从哪开始 |
>    |---|---|---|
>    | ✅ **灵族灵魂石收工**（2026-09-14） | **实测 28 张**（**不是 17 张** —— 那个数只数了 `【绿圈N】` 一个 token，`Aeldari__1.md` 里同一件事记成裸数字、另有天赋卡那一类）补上 `N [Spirit Stone]: …` + **接上那个缺掉的触发点** ⇒ 付费句真的结算、`Bright Lance Vyper` 的监听器真的响 | 逐卡名单 `资料/灵魂石卡_逐张核.md` · `useWaystone` 语义 `资料/查证_useWaystone_语义.md` · **完整记录（含 4 条坑）见 `资料/阵营推进_清单与交接.md` §一之三**（**别在这儿抄第二份**） |
>    | ✅ **选牌/选效果面板收工**（2026-09-14） | **玩家真的能选了** —— 原来三个「本该问玩家」的点**全是 `ctx.Rng` 等概率自动挑**。现在 `choosecard` 那 **52 张**进面板（引擎侧 `ChoosePicks` + 表现层 `ChoosePanel`）；`RuleEngineTest` **2512/2512** · `BattleScene` **388/0** | 落点与 **4 条坑**（含「`U(px)` 除错 10 倍 ⇒ 4 张候选叠在屏幕中央，断言全绿、截图才发现」）见 **`资料/阵营推进_清单与交接.md` §一之三**（**别在这儿抄第二份**）。⚠️ 剩余 `chooseone` 7 / `chooseeffect` 3 张**没做**；⚠️ 有 1 个**未解决**的退出段错误（断言全过，崩在 `Exit` 之后） |
>    | 🔴 **1 · 下一件 = A6 按阵营铺** | 剩余战术卡 | `_tmp_view/tactic_by_faction.md`（自检每次重写） |
>    | **2 · 选牌面板的尾巴** | `chooseone`（7 张）· `chooseeffect`（3 张）两族**还没有面板** ⇒ 仍由引擎等概率挑；`Hrolf the Ironhowl` 的 `become A or B` 是**第 4 个同类点** | `资料/选牌_受影响卡普查.md`（62 张完整清单 + 逐卡候选域）。⚠️ **别静默**：`ChooseSites > ChooseAnswered` 要一直报着 |
>    | ⛔ **3 · A4 批 3 的两条** | 偷控制权（`Telephatic Domination`）· 易伤死亡传染（`Spreading Corruption`）**后半** | `资料/战术卡剩余7条_语义查证.md` §二/§三 —— **有硬理由**（三层权威全没有 / 缺「非棋盘监听者」容器），**别当成漏做重挖** |
>    | ✅ **A7 光环收工**（2026-09-14） | **30 张全部生效**。五步全做完：设计稿 → 解析 + 数据模型 → **结算**（`Auras.Recompose`，整份摘掉再重加）→ 自检 → 并入的「回合到期」族（`Stealth` 回合到期 + 3 张督军的「自己回合里会飞」+ 顺带修掉那 3 张的 `keywords` 数据错）。**`[hero]` ① 栏归零**、`[unit]` ① 栏一条光环句不剩 | **9 条坑与出处只看** `资料/单位卡desc与光环_批次划分.md` §6.3/§6.4（**一处**） |
>    | ✅ **A5 全线收工**（2026-09-14） | 批 1~4 全做完（批 3 六条 · 批 4 十条）+ 3 条漏网 + **2 条静默失效**（`Backlash` 回手会把全场单位收进手牌 · `This card costs 1 less…` 认不出）| **坑与出处只看 `资料/阵营推进_清单与交接.md` §一之四**（10 条判据与坑，一处） |
>    | ✅ **A4 全部收工** | 战术卡「不认的句子」44 → **3 种**、完全不懂 **1 张** | `资料/战术卡剩余7条_语义查证.md` §六～§八（⚠️ §七 那两条结论**已被推翻**） |
>    | **表现层 · 卡面图标**（🆕 2026-09-14 盘点） | 🔴 **卡面上一枚效果图标都没有** —— 效果文字是**纯文本原样输出**，连 `[攻击]` 这种占位符都照字面印。**素材基本齐**（78 张 trait 图已导 73 张），缺的是「画上去」 | `资料/卡面图标_现状与缺口.md`（现状 / 素材 / 缺口清单 / 一处**两种说法打架未定**） |
>
>    ✅ **共用判据（**要加新东西之前先看这几处，别写第二份**）**：
>    格位相邻 = `BoardSpec.AdjacentSlots` · 伤害公式 = `RuleCore.DamageAfterReduction` ·
>    「这条 op 有没有机制」= `EffectText.OpHasMechanism` ·
>    🆕「**属性词 → 字段名**」= `RuleCore.NormalizeAttr`（`melee`→`attack`；**不过它会静默丢属性**）·
>    🆕「**这句是不是光环**」= `Auras.TryParse`（**只此一处**；报表另用 `Auras.LooksLikeAura` 判形状）·
>    🆕「**这张卡算不算某一类**」= `CreatePool.MatchesKind` · 「**单位 ≠ 部队**」= `EffectResolver.AddSide` 的 `troopOnly`。
>
>    ✅ **用户点名的那件 2026-09-14 晚已做完**：**「选牌 / 选效果面板展开后长什么样」拍到了** ——
>    换牌 `Mulligan`（原版自然弹）· 选牌/选效果 `ChooseCardMenu`（探针 `SetActive` 催的）。
>    截图 + 运行时节点树 + 原版 `GetPosition` 布局数学全在
>    **`资料/原版参照图/Unity参照管线_0825/data/panel_0914b/报告.md`**（唯一出处）。
>    ⚠️ **卡还是没拿到**（候选 list 元素全 null、卡的 SO 在远程 bundle）⇒ 卡的观感仍无实况。
>    ⚠️ 顺带把旧根因推翻了：UI 拍不到不是 URP 的事，是**离线态 UI 相机一张都不渲**（拍法见报告 ①）。
>    ⚠️ **引擎侧已经做完了**（`chooseeffect`，2026-09-14）—— **欠的只剩面板**。
>
>    ⏸ **三笔「有理由地没做」的小账**（**别当成漏做重挖**）：`destroyer`（规则书只说
>    「优先攻击可被摧毁的单位」；判据 `RuleCore.WouldKill` 已现成，**但那条管的是普通攻击、还没接**）·
>    `ecstasy`（两条卡点没解）· `sabotage`（机制留到基因窃取者那一轮）。逐条理由见
>    `资料/阵营推进_清单与交接.md` §一之三·候选 E。
>    ⏸ **`Infinite Biomorphologies` 的「给手牌」作用域**也没做 —— 要**卡实例身份**（独立一轮）；
>    **三处都如实报着**（结算日志 / `unresolved` / 覆盖率 ③ 栏 + 卡面 `*`）。
>
> 2. 🔴 **特效线：查「sweep 重跑后指标变差」** —— `|ln(亮度比)|` 中位 **0.155 → 0.202**；
>    **C 组好转**（30→15 ✅）但 **D 组**变差（11→32）、E 组高置信 176→169。
>    「可比」已核过三步（`analyze_sweep.py` 没改过 / **导出产物没变** / 变的是 09-13 的
>    `Shaders` + `Runtime`）⇒ **退步只可能来自第三十三轮那批改动**。
>    做法：那 4 类改动（混合判据 61 · 抓屏 Feature 41 · 未映射 shader 16 · `_EmissionColor` 11）
>    **逐类单独回退**，每退一类重跑 sweep。⚠️ `资料/特效E组_逐效果根因表.md` 上的锅名单
>    （A 35 / F 16 / G 13）要按新台账**重核** —— 那张表是按**旧台账**分的组。
>    详见 `资料/特效还原_进度与交接.md` §〇·一。
>    🆕 **另有一条口径要扶正**：W 组的「两边全程空」**是噪声门假阳性**（判据是「亮点数 < 60」，
>    不是「都为 0」）⇒ 该拿现成的 `d:/2/Warpforge_tools/data/vfx_wiring_j1..5.tsv`
>    （2026-08-24 的独立「效果→触发事件」台账）**把 W 组重判一遍**，别逐条找触发条件。
> 3. **AnimFX 脚本层**（成表在 `资料/AnimFX_18类成表.md`，**建议顺序在里面**）
> 4. **战斗界面那几条边角**（这条线本身已做满，界面对账见 `资料/战斗UI_原版对账表.md`）：
>    **挨打震镜头** · **小队件**（选卡菜单 / 回放条 / 等待提示 / **点开牌堆看张数**）·
>    **单位语音**（本地 **1698** 条 `VO_*.ogg`；工程里 0 条 —— **是没同步，不是没资源**；
>    权威映射见 `资料/普查产出_0913/语音索引.md`）·
>    **手牌→战场落地节奏**（`Card Hand To Board` 时间轴早读出来了，`DeploySequence` 还没接）
> 5. **对手强一点**（`SimpleAI` 是贪心，不搜索、不算斩杀、不为治疗留技能）
> 6. **跑原版游戏 dump 内存里的 ScriptableObject**（用户 2026-09-12 拍板）——
>    `ScenarioVariables` 的 `clockTimeLimit`/`clockCountdownSec`、`VarsGlobal` 的动画时长。
>    ⚠️ 它们所在的 bundle **是远程包、本地没有** —— **先在能跑的场景里确认对象非空，再谈拿值**。
>    做法见**第十一节 A 段**。
> 7. **构建后 player 验证** —— 三条线都欠着，到现在**只验过编辑器**
>
> **⚠️ 规则书审计③还剩一半**：「费用修正按卡名」修了（卡表 v6 稳定 id + `CostMod.Key` 改 id），
> 但**卡实例身份**没做 —— 手里两张同名卡仍是同一个 `CardDef` 对象，「只降这一张复制品」做不到。
> 要让手牌存**实例**，是独立的一轮。状态见 `资料/规则书_实现指南对账.md` §一。
>
> **🔧 可以并行派的活** —— `资料/可并行任务清单.md`（**表里带 ✅ 的别再重做**）。
> 方针继续用：**一次多派几个**，但**派之前先把要发出去的数据核对清楚**（有实测教训），
> 而且**子代理的证据比你的印象可靠时照它的改**。
> 🔴 **2026-09-14 补的一条教训（吃过一次）**：**派之前先核「那件事是不是真没做」** ——
> 我照清单第 ⑭ 条派活，任务书写「`special` 卡框从没实测过」，**实际 9-13 就做过**
> （`资料/普查产出_0913/special_tier_实测_前半/后半.md`），白让它绕了一圈。
> 那条已就地更正；**清单本身也会过期，表里的 ✅ 不是唯一判据**。
> ✅ **2026-09-14 实测的两条边界**（照做，别再试）：
> **① 「查证 / 裁定 / 写独立文档」可以派**（`Explore` 只读 · `general-purpose` 有全工具）——
> 本轮派了 5 个，产出落成两份新文档（见下），**下次接着做不用重查**。
> **② 「改引擎代码 / 跑自检」不能派** —— 引擎集中在 3 个文件（块间必撞），
> 而且**同工程同一时刻只能起一个 Unity 实例** ⇒ 这两件留在主对话串行做。
> 给编辑型子代理的活要**输出文件互不重叠**（各写各的新文件，主对话最后合并）。
>
> **📁 2026-09-13 第三十五轮普查产出** —— 11 路子代理（只读）的成果在 **`资料/普查产出_0913/`**：
> 菜单盘点（143 个根界面）· 语音索引 · **卡表三堆裁定**（🔴 查出「一卡两行」3 组）·
> **special→tier 实测**（🔴 推翻悬案 #4）· **W 组 191 分类**（🔴 噪声门假阳性）· 效果→shader 对账 958 行。
> ⚠️ **只读普查，「该怎么修」是建议，动手前照旧先核。**
> ⚠️ **引用前先看清口径**：`W组191_分类_前半.md` 里的 84 条「(c)」**不是结论** ——
> 那份分片**没用上** `d:/2/Warpforge_tools/data/vfx_wiring_j1..5.tsv`（0824 的独立接线台账），
> 用上它的另一半判出了完全不同的结果。**该台账在 `索引与盘点/_资产盘点_0910.md:156` 有登记、没人用**
> （和「`说明书/` 一直没被用起来」同一类问题）。
>
> **📁 2026-09-14 子代理产出（批量，查证 + 数据裁定 + 方案）** —— **这是下一轮的直接输入**：
> - **`资料/战术卡剩余7条_语义查证.md`** —— 批 3 / 批 4 七条**逐条**：语义 · 出处(file:行号) ·
>   落点 · 风险 · **判「挂起」的硬理由**（哪一层都查不到、缺什么容器）。
> - **`资料/单位卡desc与光环_批次划分.md`** —— A5 的 161 张**收敛成 ≈20 句型族 → 4 批 + 3 挂起**
>   （**四批全做完**）；光环 **30 张**逐卡表 + **A7 的五步方案（§六 —— ✅ 五步全做完）**。
>   🔴 里面一条**推翻了旧记录**：`BattleManager__ShouldTriggerAdjacentDamage.c` **不是光环**，
>   是**爆裂**的判据（早实现过）。⚠️ 两条**假缺口**：`Talent:`（35 次）+ `Companion N:`（8 次）
>   **都已实现**，缺的只是**自检白名单**。
> - 🆕 **`资料/选牌与选效果面板_原版数值.md`** —— 原版**只有一个面板**（`ChooseCardMenu`，
>   选效果复用它），对象树 / 尺寸 / 交互 / 文案全扒齐；**实况数值核过、观感没拍到**。
> - 🆕 **`资料/卡面图标_现状与缺口.md`** —— 🔴 **我们卡面上现在没有任何效果图标**
>   （效果文字纯文本原样输出，连 `[攻击]` 都照字面印）+ 素材在哪 / 缺哪几张 / 字体缺字那个坑。
> - **`资料/卡表核对_卡图提取/_裁定_数值第二轮.md`** + `_tmp_view/stat_fixes.json` ——
>   **结论是「空操作」**（那 7 张在当前引擎表里已全对）；价值是**独立复核** + 那条旁支发现
>   （5 张 Aeldari 督军的费用是**阵营徽记**被读成数字 5，**已修**）。
> - **`资料/卡表核对_卡图提取/_裁定_special卡框.md`** —— 复核 0913 那份，补了客观量：
>   **tier1 24 / tier2 15**，而且 **tier 是「按阵营」的常数、不是逐卡属性**；
>   🔴 **「4 档框 ↔ 4 档稀有度」这个前提本身不成立**（卡框是升级档/批次，稀有度的尺子是**底部菱形宝石**）。
>
> **⏸ 配不上立绘的卡（16 张）** —— 根因是**原版改过名**（`Helfire_Torch` / `Awakening Obelisk` /
> `Lord Exultant`…），我们拿**新卡名**反查自然落空。⏭️ 下一步：把别名加进
> `Unity/工具/import_original_art.py` 再重跑。逐张对照表在 `资料/卡表核对_卡图提取/_裁定_立绘.md`
> （含两张重复卡 + 一张可能配错的立绘）。

>
> 📌 **美术资源以后再说**：等原版复刻得差不多了再动手。机制早就位
> （`Assets/CardPresentation/Resources/Art/` 换同名文件即可、删目录退回占位美术），**不用现在操心**。
>
> **五条线各有独立交接文档**：`资料/规则引擎_进度与交接.md`、`资料/卡牌基座_进度与交接.md`、
>   `资料/对战排版_原版数值与改造方案.md`、`资料/原版复刻_场景与美术.md`、`资料/特效还原_进度与交接.md`
> 仓库：https://github.com/qjhqjh10/qjh4.git
>
> ⚠️ **工作准则见仓库根目录的 `CLAUDE.md`** —— 每次会话**自动加载**，**七条铁律**（子代理读大文件 /
> 先查解包资源 / 复刻照资源字段来 / 跑原版游戏对实况 / 文档记错就地改 / 交接删旧内容 / 拿不准看成品卡图）。
> **本文件不抄第二份。**
>
> **当前阶段（2026-09-12）**：**框架先按原版搭，不断优化到原版水平，最后再换美术资源。**

---

## 一、项目背景

**要做的东西**：一个**自己的卡牌对战游戏**，玩法模仿《战锤40K Warpforge》。

**分工**：
- **美术由用户自己提供** —— 卡牌插图、文字、效果等
- **需要的是"让卡牌动起来"的机制** —— 发牌、翻面、上场、攻击位移、命中反馈、特效播放这类交互/过渡表现

**为什么用 Warpforge 的解包资源**：这类"卡牌动效"从零设计很费劲，原版有一套成熟的机制和调好的手感数值，可以当**参考蓝本**。

**技术栈**：Unity 6.3.23f1 + URP 17.3.0，项目在 `d:/4/Unity/MyGame`。

---

## 二、项目准则

### 🔴 红线

1. **原版资源只做参考，不直接用于发布版本。**
   Warpforge 的资源归 Everguild，战锤 40K 的 IP 归 Games Workshop。**游戏机制本身不受版权保护**（模仿玩法没问题），但具体的**美术、特效参数、动画数据、音频**是他们的资产。
   - ✅ 可以：学结构、抄数值（手感参数）、照搬设计思路、自己做一套
   - ❌ 不可以：把解包的贴图/模型/音频直接打进发布版本
   - ⚠️ `MyGame/Assets/WarpforgeArena1/` 和 `MyGame/Assets/WarpforgeVFX/` 都是**研究产物**，发布前必须整个移出构建
   - 用户已明确：特效还原是**个人使用，不上传、不商用**

2. **不要重新解析 Unity JSON。** 战斗规格已经整理成权威文档（`资料/战斗规格/战斗重建_0827/审查更正清单_0827.md`），**以它为准**，重新解析既慢又容易出错。

3. **改 `d:/2/` 前先问。** 那边是档案库（44.9 万文件），有工具和文档依赖具体路径。要改先确认。

4. **🆕 仓库只放代码 / 工具 / 文档。** 原版解包资源、第三方自购素材包、以及它们的直接衍生物
   **一律留在本地**，`.gitignore` 已排除。原因是版权（Warpforge 的美术/特效归 Everguild，
   战锤 40K 的 IP 归 Games Workshop；自购素材包的授权也不允许再分发）。
   留在本地的大件：`Unity/素材/`（4 GB）、`Unity/MyGame/Assets/WarpforgeVFX/{Prefabs,Materials,Textures,Meshes}`（1.8 GB）、
   `StreamingAssets/WarpforgeVFX/*.bundle`（原版 shader 字节码）、`Unity/资料/{原版参照图,说明书,规则书,原版实拍}`。
   **这些随时可以用导出器重建**，不需要进仓库。

### 🟡 工作方式

5. **先做最小可玩原型（12–17 人天）验证玩法，再决定要不要投第二阶段（再 10–18 人天做特效框架）。** 不要一上来就追求特效华丽。

6. **每个改动都要能实拍验证。** 血泪教训：审计报告说「OBJ 是 X 镜像的，进 Unity 会左右反」，数据比对完全正确，**但结论是反的** —— 因为 Unity 的 OBJ 导入器会再做一次反向转换，两次抵消。顶点级数值比对不足以下结论，**必须实拍比对**。

7. **一次只改一个变量。** 场景重建时同时改了网格朝向和材质判据，出问题时分不清是哪个导致的，白绕了一轮。

8. **比对工具本身要先验证。** 本轮踩到：原版 vs 导出的并排渲染，两边各自按包围盒自动构图，**取景不同导致亮度差没有可比性**，据此得出过错误结论。统一取景后结论才成立。

---

## 三、新会话从这里开始（必读十份）

**先想好这次做哪条线**，三条线各自的交接文档是首选读物：
- **特效线** → `d:/4/Unity/资料/特效还原_进度与交接.md`
- **玩法线（卡牌基座）** → `d:/4/Unity/资料/卡牌基座_进度与交接.md`
- **玩法线（规则引擎）** → `d:/4/Unity/资料/规则引擎_进度与交接.md`

| 顺序 | 文件 | 作用 |
|---|---|---|
| 1 | `d:/4/Unity/资料/资源使用手册.md` | **资源总入口**：目录结构、每个目录怎么用、常用操作速查、已知的坑 |
| 2 | `d:/4/Unity/资料/原版复刻_场景与美术.md` | 美术怎么接进来的、怎么换成自己的（换同名文件即可）、图集切片与烘背景的做法、六个坑 |
| 3 | `d:/4/Unity/资料/规则引擎_进度与交接.md` | **玩法线交接**：游戏已能玩、怎么跑自检、自设计的两套卡、下一步 |
| **3b** | `d:/4/Unity/资料/卡牌效果管线_计划与交接.md` | 🆕 **卡牌→打出→战斗→发动效果这条链的下一步计划**：每件事的**数据在哪**（规则书哪一节 / 哪个文件）、反编译代码能用的部分、下一步做什么。**接着做战术卡/战斗之前先读它** |
| 4 | `d:/4/Unity/资料/特效还原_进度与交接.md` | **特效线交接**：已验证的事实、三层 shader 架构、产物、已知 bug、下一步（**P1-a0 的遗留：原版抓屏扭曲读哪张屏幕贴图** + **P1-a1 关键词触发表**） |
| 5 | `d:/4/Unity/资料/卡牌基座_进度与交接.md` | **表现层交接**：布局/卡/拖拽/落位，设计决策与踩过的坑 |
| 5b | `d:/4/Unity/资料/对战排版_原版数值与改造方案.md` | **版面数值出处**：原版手牌布局器字段+曲线、棋盘坐标（含镜像）、卡面数值实测位置、MB 5271/4053 谁是权威 |
| 6 | `d:/4/Unity/资料/规则引擎_设计.md` | **规则引擎设计**：边界、数据结构、**逐条规则出处** |
| 6b | `d:/4/Unity/资料/战斗UI_原版对账表.md` | **战斗 UI 逐元素对账**：原版实测值 + 出处 + 我们的现状 + 还差的 + 悬案。**改战斗 UI 前先查它** |
| 7 | `d:/4/Unity/资料/特效还原台账.tsv` | **逐效果状态**：957 行，含判定/亮度比/`\|ln\|`/**置信度**/时段/技术构成。**最权威的特效现状清单** |
| 8 | `d:/4/Unity/资料/自研游戏_特效与卡牌基座_设计.md` | **设计草案**：特效怎么接进游戏、卡牌基座的模块划分与接口 |
| 9 | `d:/4/Unity/_资源评估_场景特效动画.md` | **开工蓝图**：场景/特效/动画三类的可用性判定、逐项工时 |
| 10 | `d:/4/Unity/资料/战斗规格/战斗重建_0827/审查更正清单_0827.md` | **战斗权威规格**（首选，不要重新解析 JSON） |

补充：
- 原版真渲图 257 张：`d:/4/Unity/资料/原版参照图/`（判断"原版到底长什么样"的标准答案）
- 解包资源总索引：`d:/4/Unity/资料/索引与盘点/解包资源使用地图.md`
- **🆕 原版离线规则书**：`d:/4/Unity/资料/规则书/Warpforge_Offline_Rulebook_1_5-3_中文翻译.md`（`d:/2/Warpforge部队卡片/` 下有同一份）
  —— **61 个关键词的完整技能表**（每个都写明触发时机），做卡牌效果/关键词/AnimFX 时先读它
- **🆕 原版布局数据**：`d:/2/Warpforge_code/Scripts/Assembly-CSharp/CardsHorizontalLayout.cs`（手牌布局器，字段带 Tooltip）
  + `d:/2/解包整理/07_场景/battlearena1/MonoBehaviour/`（手牌/棋盘的实例数值）

---

## 四、资源在哪、怎么用

```
d:/4/Unity/
├── MyGame/                    Unity 工程
│   └── Assets/
│       ├── WarpforgeArena1/     battlearena1 研究产物（构建器 + 场景）
│       └── WarpforgeVFX/        特效还原产物（导出器 + prefab + 自建 shader）
├── 素材/
│   ├── <10 个自购素材包>         第三方商店包，已逐个扫描（`素材清单.md` 里的包数已滞后）
│   │   └── 素材清单.md           每个包的 URP 可用性、特效清单、导入步骤
│   ├── Warpforge原版/     ★      1316 MB，从解包复制的原版素材
│   └── Blender图集导出/   ★      383 MB，flipbook 精灵图集
├── 数据/                  ★      25.5 MB，引擎中立的 JSON
├── 资料/                  ★      说明书 84 份 + 战斗规格 + 规则书 + 参照图 + **各线交接文档**
├── 工具/                  ★      3.1 MB，同步器 + 13 个关键脚本快照
└── 项目任务.md                   ← 本文件（在 d:/4 根目录，不在 Unity/ 下）
```

**★ = 项目过程中新建的（不是原版解包资源）。**

### 关键访问方式

| 要找什么 | 去哪 |
|---|---|
| 一张贴图 | `数据/索引/texture_index.json`（3160 个名字 → 路径） |
| 一个特效的参数 | `素材/Warpforge原版/特效共享资源/` 或 `d:/2/解包整理/08_预制体特效/战斗预制体/` |
| 截原版真渲图 | 跑 `d:/2/unity_run_ref/Warpforge.exe`（MelonLoader + SceneJumpShot mod 已装好） |
| 一个类的字段名 | `d:/2/Warpforge_code/`（**仅签名桩** —— 方法体是空的，但字段名/类型/`[SerializeField]` 完整） |
| 真实控制流 | `d:/2/Warpforge_tools/data/decomp_il2cpp_0827/decomp_out/`（**1800 个 .c，有方法体**）+ `decomp_out2/`（224 个）。⚠️ 不是所有类都反编译了 —— 先确认在不在。DLL 里的浮点常量可从 `d:/2/unity_run_ref/GameAssembly.dll` 的 PE 段表解出（imagebase `0x180000000`） |
| 从 D:\2 同步新资源 | `工具/sync_from_d2.py --batch <名>`（幂等，不覆盖） |
| 原版 bundle 原件 | `d:/2/Warhammer 40k Warpforge/Warpforge_Data/StreamingAssets/aa/StandaloneWindows64/` |
| **🆕 原版立绘/卡框/卡背** | `工具/import_original_art.py`（按卡名拷进 `Resources/Art/cards/`，`--check` 只查不写） |
| **🆕 原版 UI 图（按钮/宝石/名牌）** | `d:/2/Warpforge_tools/slice_battle_atlas.py`（从 `BattleAtlasUI` 图集切，切好的 60 张在 `素材/Warpforge原版/UI图集/图集/battleatlasui/sliced/`） |
| **🆕 战场背景怎么来的** | `ArtBaker.BakeFromCLI` —— 打开重建好的 `WarpforgeArena1` 场景 → 渲 1920×1080 → 存 `Resources/Art/arena1_bg.png` |

### 四个关键认知

1. **原版的卡牌动效不是 AnimationClip，是 DOTween 代码。**
   证据：反编译的 `DrawCard` 里直接写着 `DOScale` + `SetEase`；`CardScript` 有 423 个方法。
   全项目只有 **98 个唯一 clip**，用在 DOTween 补不出来的地方（着色器参数、粒子开关、后处理）。
   → **好消息**：代码驱动路线被验证可行，而且**74 个 `UnitTweenSO` 的 ease/duration/loops 数值可以整套搬**。

2. **没有 Spine。** 盘点说的「Spine 卡牌动画 2334 文件」是**误标** —— 那 2334 个全是 `CardAnim` 的 `AnimInfo` JSON。三层穷举 0 命中。

3. **原版战场就是 URP，特效不会变粉红。** 之前担心的管线兼容问题在原版素材上不存在。

4. **原版特效不需要服务器。** 本轮实测：本地加载战场 bundle 进战斗，火焰/烟/碎片都是活的；catalog 里 84 个 bundle 全部解析为本地路径，一个不缺。服务器只影响卡牌数据和对局逻辑。**详见 `特效还原_进度与交接.md` 第二节。**

5. **🆕 解包资源里没有卡牌数值 —— 只有图和 ID。**（2026-09-12 全库验证）
   立绘/卡框/卡背/语音/动画 ✅ 在；卡牌 **ID**（哪个卡包有哪些卡）✅ 在；
   但**费用/攻/血/护甲** ❌ **不在**（原版是联网游戏，数值在服务端）。
   全库 grep `"(attack|health|cost|damage)"\s*:\s*\d` **0 命中**；客户端只有
   `RawCardScript.cs` / `CardData.cs` 这样的**类定义**，没有数据资产。
   - **数值的唯一来源** = `D:\2\Warpforge部队卡片` 的**卡面照片 OCR**
     （`d:/warpforge/tools/ocr_cards.py` → `card_stats_raw.json` → `merge_card_stats.py` → `card_stats.json`）
     ⚠️ **是 OCR，会有错**：1136 条里 87 张没读到值、47 处靠编辑距离模糊匹配、个别 `desc` 有重复词。
     **拿不准的回查 `ocrSrc` 指向的原图**。
   - **但效果文字有更权威的来源**：`d:/warpforge/data/i18n/zh_CN.csv`（6369 条键），
     第一列英文原文、第三列中文翻译 —— **98% 的卡能对上**，卡面 `desc` 就是这些键拼起来的。
     中文文案顺带就有了。

---

## 五、当前进度

### ✅ 已完成

| 项 | 产物 |
|---|---|
| Unity 环境 | Unity 6.3.23f1 + 团结引擎双引擎、VS 2022 + Unity 工作负载、许可证已激活 |
| battlearena1 战场重建 | `MyGame/Assets/WarpforgeArena1/`（28 网格 + 34 粒子，**摆位预览级**，见下） |
| 解包资源审计与修复 | 字体还原、197 张场景贴图补齐、文档更正 |
| 资源整理 | 1.38 GB 复制到 `d:/4/Unity/`，同步器可重跑 |
| 评估 | `_资源评估_场景特效动画.md`（697 行，含逐项工时和优先级） |
| **卡框去盾牌** | 去盾牌版卡框（四个 RGBA PNG，尺寸与 alpha 与原图一致）—— 产物已进 `Resources/Art/cards/frame_*.png`；原始工作目录在桌面、可能已被清理 |
| **特效还原管线** | `MyGame/Assets/WarpforgeVFX/` —— **958 个效果全量导出**（prefab/材质/贴图/网格 + 5 个自建 shader），运行时可重建材质 |
| **🆕 特效「能用」** | `WarpforgeEffectPlayer`（播放/生命周期/Exit/Kill/ActiveCount）+ `WarpforgeEffectLibrary`（958 条效果库）+ `effect_index.json` + 白板自检 + 白板场景。**三条自检全绿**（见下） |
| **🆕 卡牌基座（第 1–7 步全通）** | `Assets/CardPresentation/` —— 归一化坐标布局（1080p/2K/超宽/4:3 都成立）、`CardView`（占位卡面程序生成，不依赖美术）、`HandLayout` 扇形+让位、`CardInteraction`（悬停/拖拽/落位/回弹，新输入系统）、`CardHighlight` 6 状态描边、`DeploySequence` 落位动画、`CardEffects` 特效钩子。**批处理自检每步都有断言 + 截图**，详见 `资料/卡牌基座_进度与交接.md` |
| **🆕 尺子确定性** | `EffectSweepBatch.SeedAndReset` —— 钉死粒子随机种子；两遍扫描 2968 行只剩 11 行差 1 像素（原来 ~60% 的行会变） |
| **🆕 DOTween 已装** | 1.3.030，从官方仓库经 `codeload.github.com` 下载安装（Asset Store 要编辑器 GUI，命令行做不了）。重装脚本 `工具/install_dotween.py`；验证 `DOTweenSmokeTest.Run` |
| **🆕 规则引擎（简单版）** | `Assets/RuleEngine/` —— Core（纯 C#，零 Unity 依赖）/ Data（卡表+凑牌组+自设计卡+AI）/ Editor（自检）。断言条数**见 `资料/阵营推进_清单与交接.md` §五**（每轮都在涨，别抄数）。含 `ICardProvider`、`BoardSpec`（9 格）、`KeywordTable`（卡面 146 种脏写法归一化） |
| **🆕 游戏能玩了** | `Assets/CardPresentation/Scenes/Battle.unity` **按 Play 就能打**：拖牌上场（付不起拖不上去）→ 点单位选中 → 点敌方目标攻击 → END TURN → 贪心 AI 回应。`BattleDriver` 是引擎↔画面**唯一连接点**。对战自检的断言数**见 §五** + 12 张截图 |
| **🆕 自己设计的卡** | `RuleEngine/Data/StarterCards.cs` —— **Ember Legion**（猛攻）/ **Tide Swarm**（消耗），各 13 张，纯数值 + 已实现关键词。**不用原版卡**（版权 + v1 不解析 desc） |
| **🆕 卡面黑卡白字** | `CardView.FaceTexture()` 程序生成：费用/卡名/关键词/近战·远程·生命，**未实现的关键词加 `*` 标出**。文字走自写的 5×7 点阵 ASCII 字库 |
| **🆕 棋盘 9 格** | `BoardLayout` 7 → 9（督军槽 4），四档分辨率实测放得下且不重叠；**顺带修掉「拖到战场上方空白处也算合法落点」的真 bug**（只比 x 不看 y） |
| **🆕 对战排版（第七轮）** | 手牌弧线/张角/间距、棋盘间距/缩放全部换成**原版实测值**（出处见 `资料/对战排版_原版数值与改造方案.md`）；**敌方镜像**；**拖拽插槽**（边拖边让位 + 松手按插入位置重排）。对战自检 22 → **48 条断言**（含存档重开检查） |
| **🆕 原版复刻（第七轮）** | 画面 = 原版战场实拍图（`ArtBaker` 从 `WarpforgeArena1` 场景烘的 1920×1080）+ 原版卡框 + **原版立绘 1137 张**（全卡池，按卡名从新解包反查；原来只配了自设计那 26 张）+ 原版卡背（两边牌堆）+ 原版 HUD（END TURN / 能量宝石 / 名牌）。**美术全在 `Resources/Art/`，换同名文件就是换美术、删目录退回占位美术**。做法与坑见 `资料/原版复刻_场景与美术.md` |
| **🆕 特效接进战斗（第七轮）** | `Core/VfxMap.cs` = 「事件 → 特效名」表。已接：**出牌 / 登场（分阵营）/ 近战 / 远程 / 挨打 / 阵亡**。后两个是 `RefreshAll` 对比同步前后的战场推出来的（引擎不发事件）。**发动技能 / 触发效果接不了** —— 规则引擎还没有这两种事件（VfxMap 里位置留好了） |
| **🆕 特效试片台（第七轮）** | `Editor/VfxPicker.cs` —— 把候选特效**在战斗场景的相机下**播一遍、每个拍 3 个时刻。**挑特效必须靠它**：台账判定「对得上」的效果里有几个在我们场景里渲成**不透明黑方块**（材质 alpha 没还原对），只看台账会踩 |

### ⚠️ `WarpforgeArena1` 的真实水平：**摆位预览级，不是特效库**

- **0 个 prefab**（全烘在场景里）
- 39/39 材质用 `URP/Unlit`（应该用 `URP/Particles/Unlit`）且全 Transparent
- `SizeModule` / `ColorModule` / `SubModule` / `TrailModule` **全部 enabled:0**
- **加法混合完全没处理** —— 1142 个 VFX 材质里 **344 个（30%）是 `SrcAlpha/One` 加法混合**

它的价值是**证明流水线可行**，不是可用资产。
> 后续的 `WarpforgeVFX/` 是另一条更完整的路线（从 bundle 直接导出，粒子模块完整），见交接文档。

### 特效还原的真实水平 → **看 §〇·一 与台账，别引用本节**

**权威清单** = `资料/特效还原台账.tsv`（**每次重跑 sweep 后重生**）；**当前数字只写在
`资料/特效还原_进度与交接.md` §〇·一 一处**（2026-09-13 第三十四轮重跑：**指标变差**）。

**两条口径要记住**（这两条是知识，不是数字）：
- **验收指标 = `|ln(亮度比)|` 的中位/均值**，**不是**「对得上多少个」；
- **Z 计数在带宽边界上极其敏感**（边界挪 0.05 就摆动 22–54 个）⇒ 拿它当门槛会把噪声读成退步。
  这是 P0-f 结案时实测出来的。

### ❌ 还没做

> ⚠️ **这一节原来挂着四件「玩法骨架」，2026-09-13 全做完了**（换牌 mulligan / 战斗日志·墓地日志 /
> 倒计时·投降 / 卡牌放大详情）—— **别再当待办**。明细在 `资料/规则引擎_进度与交接.md`。

**仍然没做的**（**按优先级排在文首「⏭ 下次接着做」**，这里只列名）：
- **点开牌堆查看** —— 原版 `DeckManager.ToggleCardbackPreview` 是「**牌背预览 + 剩余张数**」，
  ⚠️ **不是逐张翻看牌堆**（这条以前容易想当然）。
- **加时机制** —— 标记已摆、`DeckRules.OvertimeEnergy` 常量在，但 `overtimeTurn` 只在服务器下发的
  LiveOps JSON 里、**本地确证查不到** ⇒ 要做就得照规则书「双方能量到 10」或自挑一个并标明。
- **对手强一点**：`SimpleAI` 是纯贪心，没有留牌 / 保命 / 交换算计。
- **战斗界面的边角**：选卡菜单 · 单位语音条 · 回放条 · 等待提示 · 挨打震镜头 ·
  **单位语音**（本地 **1698** 条 `VO_*.ogg`（⚠️ 2026-09-13 更正，原来写 1857 —— 那是**文件总数**，
  含 115 个 `.wav` 与 44 条不带 `VO_` 前缀的；见 `资料/普查产出_0913/语音索引.md`），
  工程里 0 条 —— **是没同步，不是没资源**）。
- **手牌→战场的落地节奏** —— `Card Hand To Board` 的时间轴**早就读出来了**；
  **差的不是资源，是「把它接上」**（我们的 `DeploySequence` 仍是自己挑的节奏）。
- **关键词补全** —— ⚠️ **别抄数字**，以自检打印的 `RuleCore.UnimplementedKeywords()` 为准；
  **做哪个按频次排**（`资料/关键词频次表.md` + `_tmp_view/tactic_unparsed.txt`）。
- **特效还原线** —— 当前数字见 `资料/特效还原_进度与交接.md` §〇·一；另有 **AnimFX 脚本层**
  （18 个类 / 2346 个组件，成表在 `资料/AnimFX_18类成表.md`）。
- **构建后 player 验证**（三条线都欠着，到现在**只验过编辑器**）。

**两处「原版有、我们故意不做」的**（理由都记着，**别再翻案**）：
- **换牌的四种模式** —— 服务器开关（`GameplayVariablesData.showMulligan`）选的，本地没有那份配置；
- **换牌独立倒计时** —— 离线练习模式下原版 `_MulliganCountdown` 直接 `return`（单机没有）。
---

## 六、待办任务

### 🔴 玩法线 —— 按优先级（**「还原原版」这条线的主战场**）

| 优先级 | 任务 | 说明 |
|---|---|---|
| **P0** | ~~**卡组编辑界面**~~ | ✅ **2026-09-12 做完**：`CardPresentation/Scenes/DeckEditor.unity` 打开按 Play 就能用。侧栏卡表 + 卡池网格（4×3 翻页）+ 五路筛选 + 费用曲线数据 + 合法性实时显示。规则/校验/存档在 `RuleEngine/Core/DeckRules.cs` + `Data/DeckStore.cs`。**自检 `DeckScene.Run` 61/61**。✅ 卡组库（多套新建/改名/复制/删除/选中）+ 本地存档 + 导入导出串都已接通，**编辑完关掉再打开还在**。⚠️ 还欠：拖拽加牌、卡面放大、卡组串分享（原版 `shareButton`，单机没分享对象）。规格见 `资料/卡组编辑_原版数值与实现方案.md` |
| ~~**🔴 P0**~~ | ~~**把原版卡牌接进对战（先两个阵营）**~~ | ✅ **2026-09-12 做完**（Ultramarines vs Goff，中文卡名+原版卡框+卡池组卡）。详见第九节 |
| ~~**🔴 P0**~~ | ~~**接上「卡组编辑器编的那套牌」**~~ | ✅ **2026-09-12 做完**：`Start()` 现在读 `DeckLibrary` 里当前选中的那套（`BattleDriver.BeginFromDeckLibrary`），**督军决定阵营**（原版就是这么判的）；不合法 / 展不开 / 存档坏了都**在屏上说明白**再退回自动凑；「按 R 再来一局」不再丢卡组。⚠️ 引擎只收单位卡 → 一副合法的 30 张卡组里**战术卡和防御卡上不了场**（⚠️ **2026-09-13 起不成立**：战术卡 09-12 通、防御卡 09-13 通），所以开局的**提示行**会明说丢了几张（**那一行是我们加的**，原版没有）。自检第 9 节 21 条 |
| ~~**对局结算界面**~~ | ✅ **2026-09-12 做完**：`CardPresentation/Battle/EndPanel.cs`。暗底盖满 + 胜负标题 + 回合数与敌方督军最低生命 + **三个骷髅**（判据照规则书:36，只在 `DeckRules.SkullsFor` 一处）+ 奖杯/评分 + 再来一局提示。尺寸取自运行时 dump 的 `EndBattlePanel` 子树。⚠️ 一处是**我们排的**（未点亮骷髅用半透明），文件头注明了。✅ 纵向位置和奖杯图在加开门视频那轮都**改成照原版了**：奖励块的锚点用 dump 里 `AllRewardsHolder` 的 `(0,-260)`（屏幕 y≈800），奖杯用原版那张 `40k_UI_icon_ranked_Skirmish`（原来那张 `WF_UI_Trophy_Gold` **在 `ui_deck/` 里根本不存在**，一直在静默不画）。✅ **「开门」已做完**（2026-09-12）：原版那套根本不是动画，是**播视频** —— `Battle/BattleDoors.cs` + `Shaders/VideoSplitAlpha.shader`。**奖励是和视频同时出的**（判据：`EndBattleDoors.SetupDoor` 自己就调了 `ShowRewards`，反编译 `decomp_out/EndBattleDoors__SetupDoor.c:105`），播完留最后一帧当底图；**只有结果文字藏起来** —— 那段视频自己带 VICTORY/DEFEAT/DRAW 字样，原版那棵树里也没有结果文字节点。
- `EndBattleDoors.SetupDoor`（反编译 `decomp_out/EndBattleDoors__SetupDoor.c`，193 行）：
  建一张 **1920×1080 的 `CustomRenderTexture`** → 贴给 `Video Image` 的 `RawImage` →
  `VideoPlayer.set_targetTexture(renderTex)` → `AssetReferenceTyped<VideoClip>.Load()` 选片
  （按枚举 `==10` victory / `==20` defeat / `==30` draw）→ **返回 `VideoClip.get_length()`**，
  调用方 `_CloseBattleDoors_d__393__MoveNext` 拿它 `WaitForSeconds`。
- 那个类里**没有 Animation / Animator / DOTween** —— 字段桩只有
  `VideoPlayer videoPlayer` + `RawImage videoImage` + 三条 `AssetReferenceTyped<VideoClip>`。
- 运行时 dump 里 `BattleDoors` 子树**是单链**：`BattleDoors`(activeSelf=**False**) → `Canvas` →
  `EndBattlePanel` → `Background` / **`Video Image`** / `AllRewardsHolder`。**没有别的节点** ——
  「开门」视觉 100% 是这段视频。
- 98 个唯一 AnimationClip 里**结算/开门/奖励相关的是 0 个**；**没有** `EndBattleDoors` 的**独立 prefab**，也没有 clip
  —— ⚠️ 2026-09-12 更正：**组件实例在每个战场场景里都有**（字段齐全：`skullsObjs`/`rewardsHolderObj`/`victoryVideo`…），
  已 dump 到 `07_场景/battlearena1/MonoBehaviour/MonoBehaviour_5238.json`。「解包里没有」这个说法有误导性。
- 视频在 `素材/Warpforge原版/视频/videos/`：**Victory 3.71s / Draw 3.40s / Defeat 6.79s**（1920×1080，24–25fps）。
  ⚠️ 三条都带 **split alpha**（`HasSplitAlpha=true`）—— mp4 存成 **3840×1080 左右拼**、
  右半边是 alpha 遮罩，**播的时候要用 shader 把右半边当 alpha 合回去**（手册坑 6 说的「要 crop」就是这个）。
  ⚠️ **原版资产，只能当参考、不能进发布版本**。
  ⚠️ **奖励/评分是服务器侧数据、永久拿不到**（原版已关服，手册坑 7）→ 只能占位或自创。 |
| ~~**P1**~~ | ~~**换牌 mulligan**~~ | ✅ **2026-09-13 做完**：引擎 `RuleCore.Mulligan`（弃回牌库 → 重洗 → 补抽，规则书 :46）+ 表现层 `Battle/MulliganPanel.cs`（结构照运行时 dump）。⚠️ **四种模式与独立倒计时故意不做**（前者是服务器开关选的、后者离线练习模式原版就 return）。见第二十五轮 |
| **P1** | **三件「点了有反应」的交互** | 卡牌放大详情（`CardDisplayWindow`）/ 战斗日志·墓地日志（`CemeteryLogManager`）/ 点开牌堆看张数（`DeckManager`）。原版类名和场景对象都挖到了，见第五节「还没做」 |
| **P2** | 倒计时 / 投降 / 出牌超时 | ✅ **2026-09-13 两件都做完了**：投降（`RuleCore.Forfeit` + **原版入口**：设置按钮 → 设置面板 → 投降钮）+ 回合倒计时（60 s / 超时 15 s / 自动结束回合，数值出自 `DefaultScenario.json:19-21`）。**还欠**：加时（`overtimeTurn` 只在服务器下发的 LiveOps JSON 里，本地查不到 —— 要做得照规则书「双方能量到 10」或自己挑数并标明）、**换牌那套独立倒计时**（⚠️ 2026-09-13 更正：这里原来写「mulligan 本身也还没做」是**旧话**，mulligan 第二十五轮已做完；倒计时是**故意不做** —— 原版 `_MulliganCountdown` 在离线练习模式下直接 return） |
| ⚠️ **P1** | **把「黑方块」那一批特效修掉**（特效线的活） | `Axe_Slash_*` / `Attack_Stomp` / `BulletImpactBurst_Blood` 在战斗场景里渲成**不透明黑方块**（材质 alpha 没还原对）|
| ~~**P0**~~ | ~~**给引擎加「技能/触发」事件**~~ | ✅ **2026-09-12 做完了**。引擎现在发六种结构化事件（`BattleEvent.cs`），加了五类触发 + 主动技能；表现层删掉了「对比战场快照」那套猜测。见 `资料/规则引擎_进度与交接.md` 第七节 |
| ⚠️ **P1-a0 已重查（2026-09-12）** | **抓屏扭曲那条「证据链」是尺子的假象，不是资产差异** | 原版在我们那套空场景里输出的是**一块恒定的半透明灰方块**（采样到没绑定的默认灰贴图），跟背景、跟队列都无关 —— 台账里「C 组：导出整个丢了」那批抓屏扭曲效果判的是假象。**但同一次探针查出一个真 bug 并已修**：原版 shader 把混合**写死在 pass 状态里**，而原版材质带着内置 Standard 的残留 `_SrcBlend=1/_DstBlend=0/_ZWrite=1`，我们那版用 `Blend [_SrcBlend]` 间接寻址 → 被污染成**不透明覆盖 + 写深度**。详见 `资料/特效还原_进度与交接.md` P1-a0 |
| ~~P1~~ | ~~原版抓屏扭曲读的是哪张屏幕贴图~~ | ✅ **2026-09-12 查明：`_GrabPassTransparent`**。从 `Shader.compressedBlob` 解出 DXBC，资源名是明文（工具 `工具/dump_shader_blob.py`）—— 它**没有**采 `_CameraOpaqueTexture`。**下一步**：给 URP 加个 Renderer Feature 把颜色缓冲（含透明物）拷进这个全局纹理，然后把 `WFDistortion` 的采样源换过去 |
| ~~P1~~ | ~~出牌/攻击的动画时序~~ | **✅ 2026-09-12 做完**：`Core/EventTiming.cs`。⚠️ 原版**没有事件级间隔表**，时长是从单特效的 `UnitTweenSO` 里挖的，阵亡/触发两条**没有出处、是拍的**（表里标了） |
| ~~P2~~ | ~~牌堆/弃牌堆做成实体牌堆~~ | **✅ 2026-09-12 做完**。⚠️ **「原版是个多张叠起来的堆」这个前提不成立** —— 运行时 dump 里 `PlayerDeck` 是「底板 + 卡背(217×314) + 回合灯 + 张数」，没有多张叠放。我们按这个补齐了（原来卡背只有 140 px 高） |
| ~~P2~~ | ~~战术卡效果（449 张卡的空间）~~ | ✅ **能打，正在推覆盖率**（A4 已推三批，**批 1 收尾 2026-09-13**）。**数字只写一处** —— `资料/阵营推进_清单与交接.md` §五；**还差哪些句子**看 `_tmp_view/tactic_unparsed.txt` ① 栏与候选 H |
| P2 | 关键词补全 | ⚠️ **数字以自检打印的 `RuleCore.UnimplementedKeywords()` 为准**（**2026-09-13 A3 收工实测：未实现 3 个** —— `destroyer` / `ecstasy` / `sabotage`，三个都有理由，见 `资料/阵营推进_清单与交接.md` §一之三）；`UnimplementedKeywords` 可查；触发类的口子已经开好，加关键词 + 在正确时机调 `FireTriggerOnBoard` 即可。**做哪个按频次排**（`_tmp_view/tactic_unparsed.txt`）。⚠️ 这格原来写死「23 个 / 还差 38 个」是**旧数** |
| P2 | 对手强一点 | `SimpleAI` 是贪心，没有留牌/保命/交换算计 |
| ~~P2~~ | ~~表现层补动效~~ | ✅ **2026-09-13 做完**（发牌入场 / 攻击位移抬手 / 命中抖动 / 阵亡消散 / 数值过渡 / 手牌重排）。见 `资料/规则引擎_进度与交接.md` 第二十五轮 |

### 特效线：**「能用」这一侧已经打通** —— 还剩两件

`WarpforgeEffectPlayer`（播放入口/生命周期/兜底销毁）· `WarpforgeEffectLibrary` + `effect_index.json`
（**958 条**，`工具/gen_effect_index.py` → `EffectLibraryBuilder`）· 白板场景实拍
（`WarpforgeArena1/Scenes/VFXWhiteboard.unity`）**都已做完**。

| 任务 | 状态 |
|---|---|
| **构建后 player 的验证** | ⬜ **只在编辑器里验过**。做法见 `资料/特效还原_进度与交接.md` 第八节末 |
| AnimFX P0 两个模块（`AnimFXController` + `AnimFXModuleTween`） | ⬜ 补原版节奏；数据全在（`数据/游戏数据/animfx_components.json`） |

> ⚠️ **一条关于「尺子」的警告**：白板自检那个**闭环 20/20 · 渲染与台账一致 20/20 · 5 轮无泄漏**，
> 只挑了 **20 个**常用效果做样本，**不是全部 958 个**。**别把白板绿灯当发布绿灯。**

### 玩法线：卡牌基座 —— **第 1–7 步已做完**

分辨率/安全区 + 9 格布局 · `CardView` + 扇形手牌 · 悬停抬起与邻牌让位 · 拖拽回弹 ·
落位判定 + `DeploySequence` · `CardHighlight` **6 状态（用描边表达，整卡着色读不出来）** ·
接特效（`CardEffects` 钩子 → `WarpforgeEffectPlayer`）—— **全部做完**。
完整交接（文件清单、设计决策、怎么跑自检、踩过的坑）：`资料/卡牌基座_进度与交接.md`

**还差的**：
- **出牌区 / 卡组 / 弃牌堆**：⚠️ **半做** —— 两边牌堆已有原版卡背 + 「DECK n DISC n」；
  **还差**实体牌堆的厚度感、弃牌堆的正面展示。
- **手牌→战场落地节奏**：`Card Hand To Board` 那条时间轴**早就在本地读出来了**，
  **差的不是资源、是「把它接上」**（我们的 `DeploySequence` 仍是自己挑的节奏）。
  ⚠️ 手牌重排的补间**时长仍是「我们挑的」0.18s** —— 原版的 `timeToPositionCard` 在没被反编译的调用方里。
- **换自己的美术**：机制已就位（卡是**四层**：立绘/卡框/数值/描边，换 `Resources/Art/cards/`
  里的同名文件即可）。**现在装的是原版美术** —— 换美术的事按 §📌 那条「以后再说」。

### 玩法线：规则引擎 + 可玩对战 —— **能玩了**

完整交接：`资料/规则引擎_进度与交接.md`　|　设计：`资料/规则引擎_设计.md`

**打开 `Assets/CardPresentation/Scenes/Battle.unity` 按 Play 就能用鼠标打一局。**
拖牌上场（**付不起拖不上去**）→ 点自己的单位选中（合法目标亮起来）→ 点敌方目标攻击 →
END TURN → 贪心 AI 出牌、打你 → 督军掉到 0 判负。

| 模块 | 内容 | 状态 |
|---|---|---|
| `RuleEngine/Core/` | 棋盘 9 格 / 回合状态机 / 出牌 / 攻击 / 伤害 / 胜负 / 疲劳 | ✅ 纯 C#，零 Unity 依赖，断言数**见 `资料/阵营推进_清单与交接.md` §五** |
| `RuleEngine/Core/BattleEvent` | **结构化事件流**（六种，带格位/卡名/数值）→ 表现层照它播特效 | ✅ 🆕 2026-09-12 |
| `RuleEngine/Core/EffectSpec` | **效果文法**（`Damage/Heal/Draw N 目标`），解析不出来就标 `*` | ✅ 🆕 |
| 技能与触发 | 五类触发（Rally / Strike / Slay / Backlash / Penitence）+ 主动技能 `Ability:` | ✅ 🆕 26 张卡里 11 张用上了 |
| `RuleEngine/Data/` | `StarterCards`（**自己设计的两套卡**）/ `DeckBuilder` / `SimpleAI` | ✅ |
| `CardPresentation/Battle/BattleDriver` | **引擎 ↔ 画面唯一连接点**：拖拽校验 / **弹出攻击方式选择器** / 回合流转 / HUD | ✅ 断言数**见 §五** + 16 张截图 |
| **`CardPresentation/Battle/AttackSelector`** | **原版 `Drag Attack Selector`**：近战/远程/主动技能三选一，数值全来自解包场景 | ✅ 🆕 |
| `CardPresentation/Battle/Label` | 世界空间文字（没有 TMP，走自写点阵字库） | ✅ |
| `CardView` 卡面 | **黑卡白字**：费用/卡名/关键词+效果/三围；未结算的加 `*` | ✅ |
| 战术卡效果 | ✅ **能打（449 张），正在推覆盖率** —— 数字只写一处，见 `资料/阵营推进_清单与交接.md` §五 | ✅ |
| 关键词补全 | 机制数**以自检打印的 `RuleCore.UnimplementedKeywords()` 为准**（§九 那张表指过来了）。⚠️ 这格原来写死「已做 23 / 共 61、还差 38」是**旧数**，2026-09-13 更正 | ⬜ |

**验收要点**：`BattleScene.Run` 会跑「开局 → 拖拽上场 → 费用拒绝 → 出牌 → 攻击 → 护卫限制 →
**技能与触发（事件流 → 特效）** → 打完整局」并逐条断言 + 截图；
`RuleEngineTest.Run` 单独验规则本身。两条都必须绿 —— 前者验「接得上」，后者验「算得对」。

> **⛔ 已删除两段过时内容**（2026-09-12）：原来的「第 1 阶段：修基座」和「第 2–4 阶段」两张表
> 针对的是 `WarpforgeArena1` 那条**旧路线**（摆位预览级），早就被 `WarpforgeVFX` 新路线取代；
> 里面的模块名（`HandHover` / `HandDrag` / `CardAnimator`）在仓库里也不存在。
> 真要追溯去看 git 历史。当前的模块划分看 `资料/自研游戏_特效与卡牌基座_设计.md` 与实际代码。

### 道具箱（不用自己做，直接拿）

| 资产 | 用途 |
|---|---|
| **`Card Hand To Board` clip（0.92s）** | 「手牌落到战场」的完整蓝图，17 条曲线拍清了时序 |
| **74 个 `UnitTweenSO`（86 KB）** | ease/duration/delay/loops/punch 数值全在（含具体类型名如 `PunchTween`） |
| `Card Display Open`、`BattleHudShow/Hide` | 发牌与手牌展开 |
| 13 个 `Battle Arena X Camera Intro` | 进战斗镜头 |
| `card_anim_map.json` / `card_vfx_tree.json` / `atk_vfx_map.json` | 卡牌→特效映射（591 条定义 → 400 个预制品） |
| 反编译的 34 个卡牌类 / 2024 个方法名 | 当需求规格说明书用 |
| `数据/游戏数据/animfx_components.json` | **🆕** 2346 个 AnimFX 组件的**参数值**（哪个 prefab 挂哪个模块、参数多少） |

---

## 七、已知的坑（踩过的，别重踩）

### 通用

| 坑 | 真相 |
|---|---|
| **OBJ 手性** | 审计说"是 X 镜像的，要先还原"——**错的**。Unity 的 OBJ 导入器会反向转换，原样搬才对。实拍验证过 |
| **`_Blend` 判材质透明** | 恒为 0，不可用。用 `_SrcBlend`/`_DstBlend` + `_AlphaClip`。但也别优先用 Alpha 裁剪（贴图是共享图集，整图统计不代表单个网格）。**取"优先混合"最稳** |
| **粒子翻页图集** | 网格在 `ParticleSystem` JSON 的 **`UVModule`**（不是 `TextureSheetAnimationModule`） |
| **文件数口径** | 解包目录里**大量是精确字节重复的副本**。2026-09-12 实测：`11_着色器` 527→232（**56%** 是重复）、`07_场景/battlearena1` 10020→5347（**47%**）、`01_卡牌/Ultramarines` 777→591（24%）；全库口径见 `_审计报告_0910.md` §B1（215,809 / 48%）。**所有「文件数」都要打折看**。⚠️ 旧文档里写的「约 21%」**复现不出来**，别再用那个数 |
| **文件数 ≠ 对象数** | `_<pathID>` 后缀**不是重复标记**，是提取器的原名格式 |
| **`ELECTRON_RUN_AS_NODE`** | 本机环境带这个变量，会让 Unity Hub / 团结引擎 Hub **启动即静默退出**。跑 Unity 批处理前也要 unset |
| **`ParticleSystem.Burst`** | 构造参数带下划线前缀（`_time`/`_minCount`），**不能用命名参数** |
| **`ShapeModule`** | 属性是 `shapeType` 不是 `type`；`arc` 单位是**度**，JSON 里存的是弧度 |
| **批处理下粒子不动** | 预览渲染前要 `ps.Simulate(3f, true, true, false)` |
| **重跑 `classify_move.py` / `extract_full.py`** | **绝对不要** —— 前者分类会覆盖且不可逆，后者会再加一遍重复 |
| **`convert_unity_anim.py`** | 输出 Godot `.tres`，**Unity 用不了**，别用 |
| 🔴 **行尾（LF/CRLF）** | 仓库里**两种混着**：`.cs` 是 CRLF，好几份 `.md` 是 **LF**（例：`资料/卡牌效果管线_计划与交接.md`、`资料/阵营推进_清单与交接.md`）。**用 python 的 `open(p,'w')` 或 `sed -i` 批量改文件会把整篇行尾翻掉** —— `git diff` 会变成「全文件重写」（2026-09-13 撞到：一个 4323 行的文件报 8457 行改动）。<br>⚠️ **2026-09-13 第二次踩，形态不一样**：那次只**改一行**，却翻了两整份文件（`战斗UI_原版对账表.md` 报 266/265、`规则引擎_进度与交接.md` 报 1397/1397）。**错因是「读」不是「写」** —— `open(p, encoding='utf-8').read()`（默认 `newline=None`）会做 universal-newline 翻译，**把 CRLF 读成 LF**，再写回去就成了 LF。<br>⇒ **三条规矩**：① **别按扩展名猜行尾** —— `资料/战斗UI_原版对账表.md` 与 `资料/规则引擎_进度与交接.md` 都是 **CRLF**，而别的 `.md` 是 LF，**逐个文件查**（`git show HEAD:<f> \| grep -c $'\r$'`）；② 改文档**一律用 Edit 工具**（它保留原有行尾），**别用 python 读写**；非要用就全程 `open(p,'rb')` / `open(p,'wb')`；③ **提交前必跑**下面那条自查。<br>**自查**：`git diff --numstat` 和 `git diff --ignore-cr-at-eol --numstat` 两个数不相等 = 行尾被动了。<br>**修**：按 `git show HEAD:<file>` 里占多数的那种，把整篇换成同一种再提交。 |

### 🆕 玩法线（规则引擎 + 卡牌基座，详见 `资料/规则引擎_进度与交接.md` 第五节）

| 坑 | 真相 |
|---|---|
| **🆕 `ImageQuad` 的宽度是从贴图宽高比推出来的** | 它只接一个「高度」，宽度 = 高度 × 贴图宽高比 —— **不是给两个尺寸**。做「铺满屏幕」的遮挡层时拿一张正方形贴图去铺，会得到一个方块，屏幕左右两边没盖住（抓屏扭曲的暗底、结算界面的压暗都踩过）。要铺满就**按当前可见区的宽高比造那张贴图** |
| **🆕 `Label.SetText` 在未激活的物体上跑，字形网格不会重建** | 面板先 `Hide()` 再 `Show()` 时，如果 `Show()` 里**先写文字后激活**，标题会是一片空白（结算界面踩过）。顺序必须是**先激活、再 `SetText`** |
| **🆕 同一层的两个 UI 图会互相压住，谁压谁看渲染顺序** | 面板/行/按钮如果 z 相同，压盖关系是不确定的（侧栏前 3 行的背景被面板盖掉过）。**显式分层**（面板 z=0.30 / 行 z=0.10 / 其余 0）。已做成通用断言：`DeckScene.Run` 里「同层的图不许重叠」，能挡住整类版面错误 |
| **🆕 `default(Struct)` 的数值字段不是「未设」，是 0** | `DeckFilter` 的 `Cost` 默认 0，而 **0 是个合法费用** —— 不显式初始化的话，筛选默认变成「只看 0 费卡」，1127 张池子被筛成 103 张。**表示「不限」要用 -1 这种哨兵值，并且构造函数里显式赋上** |
| **🆕 断言管行为，版面还是得看图** | 本轮三个版面 bug（压暗没铺满、卡格没居中压到筛选栏、侧栏按钮压在卡表上）**断言全绿**，都是截图看出来的。所以「改动要加断言」和「改版面要看图」是两条并行的规矩，不能只做一条 |
| **🆕 结算视频是「左右拼 alpha」，Unity **不会**替我们拆** | 三条结算片实为 **matroska/webm + VP8、3840×1080 / yuv420p**（没有 alpha 通道）：左半彩色正片、右半白色剪影（明度即 alpha）。原版 `VideoClip` 元数据是 `Width:1920 m_HasSplitAlpha:true` —— 那是**原版导入时拆好的产物**；我们把同一份字节丢进自己的工程，Unity 报的是 **3840×1080**（`VideoProbe.Run` 实测）。所以 RT 要开 3840×1080、显示区 1920×1080，**合成自己写**（`Shaders/VideoSplitAlpha.shader`）。⚠️ 手册坑 6 原来写的「用前要 crop」对我们不适用，已更正 |
| 🆕 **批处理下 `VideoPlayer` 能用，但不能阻塞主线程** | 这个工程的批处理自检是 `-executeMethod` 调静态方法、**没有 play 循环**（`BattleDriver.Update` 不跑）。`Thread.Sleep` 泵循环拿到的永远是 `isPrepared=False / frame=-1`；改成注册 `EditorApplication.update`（每 tick 调一次 `QueuePlayerLoopUpdate`）、**让方法立刻返回**，`Prepare` 就过了、`Play` 也真出帧。⚠️ 调用时**不能带 `-quit`**，退出要自己 `EditorApplication.Exit`。见 `VideoProbe.Play` |
| **🆕 「尺子的假象」第二例：对照组的底板和被测面板同层** | 拍结算界面时给了一块假底板当「战场」，它和面板的压暗层（`EndPanel.Build` 里建 dim 用的是 `Vector3.zero`）**z 都是 0** → 底板盖住整块面板，面板根本没渲。而「看得见画面」那条断言因为**底板自己就是亮的**空过了。**搭台子的东西也要摆在正确的位置上**，不然量的是台子不是被测物 |
| **🆕 同一个位置在两处算，迟早只剩一处是对的** | 换分辨率时要把 HUD 重贴一遍（`ReanchorHud`）。建的时候 END TURN 按钮改到了右侧，**重贴那段还写着旧坐标**（`0.952f - EndTurnTextDx()`），结果按钮在右边、文字留在右下角。修法是把它抽成一个常量（`EndTurnX01/Y01`），建的、重贴的、文字的三处都读它 —— **和「判据只有一处」是同一条规矩，只是对象从规则换成了坐标** |
| **🆕 跨局的状态要显式清，不会自己归零** | 接「按 R 再来一局」时一口气撞出三个：`CardInteraction._placed`（重开一局后**上一局摆过的格子再也放不了牌**）、`BattleDriver.BuildHud()` 每开一局建一份（**HUD 叠两层**，而且旧的那个结算面板成了孤儿**永远不 Hide**）、`interaction.OnDeployed += ...` 每局多挂一份（落位回调跑 N 遍）。**凡是「这一局才有」的状态，重开那条路上都要有对应的清/复用** |
| **🆕 界面上写着的话要有人接** | 结算面板写着「按 R 再来一局」，**没有任何代码监听 R** —— 面板承诺了一件事、什么都没发生，正是红线禁止的静默失败。加一行 `Keyboard.current.rKey.wasPressedThisFrame` 就好，但**得有人去发现它**（这次是因为要写重开的自检才发现的） |
| **🆕 阵营名传错池子会静默给一套别的卡** | `StarterCards.Of(faction)` 原来是「不是 Tide 就当 Ember」——传 `"Ultramarines"` 会拿到一整套 Ember 卡、还打得挺像样，**完全看不出错**。已改成认不出就报错 + 空表。同理 `DeckBuilder.StarterDeck` 在空池子上会 `deck[0]` 越界，也补了守卫 |
| **编辑器模式下 `Object.Destroy` 不生效** | 它要等下一帧，而编辑/批处理里**没有帧循环**。批处理自检里旧的手牌视图全留在画面上，看起来像「手牌莫名其妙多出好几张」（实拍踩到）。用 `Application.isPlaying ? Destroy : DestroyImmediate` |
| **换边之后必须再 `BeginTurn`** | `EndTurn` 只换 `Active`，**发能量/抽牌/解疲劳都在 `BeginTurn` 里**。只调 EndTurn 的话对手整个回合 0 能量、一张牌出不来（表现：AI 场上永远只有督军） |
| **格位底片要用「落位后」的尺寸** | 战斗场景格位间距比手牌紧得多，用整卡尺寸画底片，9 个底片会**糊成一整条灰带**。要用 `placedScale * LayoutSpace.Scale` |
| **落位动画要推进到「落定」为止，别写死时长** | 引擎调用在 `DeploySequence` 的完成回调里。自检写死 `Step(0.8f)` 不够，断言全挂而画面其实是对的 |
| **`LayoutSpace.Cam` 是静态字段，不进 Play 模式** | 编辑器里建场景时设过，运行时是 `null` → 归一化坐标退回默认宽高比，超宽屏/4:3 全错位。场景里要有个 `Start()` 重新 `Apply(cam)` |
| **`TryResolveSlot` 只比 x、不看 y** | 拖到战场**正上方空白处**，只要 x 恰好落在某格附近就算合法落点。7 格时槽 0 在 0.236 恰好躲过，改成 9 格才暴露。横纵容差**不是同一个物理尺度**（可见宽 17.78 / 高 10），是两个独立参数 |
| **接口成员是属性，字段实现不了** | `ICardProvider` 声明 `string Id { get; }`，用 `public readonly string Id;` **字段**会编译报 `CS0535`。要改成只读自动属性 |
| **能量按「每方自己的回合数」算** | 不是全局回合数。按全局算的话后手首回合 2 能、先手第 2 回合 3 能，**整局能量曲线偏高**。`rule_core.gd` 有明确修正记录 |
| **远程攻击默认也吃反击** | 直觉会写成「远程不受反击」，**那是错的**。只有 `Long Range` 免反击 |
| **Flying 检查的是「目标」不是「攻击者」** | 语义是「飞行单位不能被近战打到」。`rule_core.gd` 修正过方向，正好反了 |
| **出牌校验顺序：先费用、后格位** | 和 `rule_core.play_card` 一致。顺序反了会出现「判了格位却已经扣过费」的中间态 |
| **关键词值 = 整串里第一个数字** | 不是「尾随数字」。`"Companion 2: Missile Drone"` 取 2。无数字的关键词取 **1**（存在即真），取 0 的话护甲和 Shield 会一起失效 |
| **测试夹具的垫牌要放在「反转后的手牌」前面** | 抽牌是 `pop_back`；垫牌若放末尾，起手会全是 `filler0` |
| **棋盘格数两边各写一份** | `RuleEngine.BoardSpec` 和 `CardPresentation.BoardLayout` 各有一份常量，**靠 `RuleEngineTest.TestBoardMatchesPresentation()` 断言相等**。**改格数必须两个一起改再跑自检** |
| **🆕 汉字渲染走通了（2026-09-12）** | 最坑的一条：**`AssetDatabase.ImportPackage()` 在 `-batchmode -quit` 下根本不落地** —— 它只是把导入排进队列，进程在队列被处理前就退出了，**日志里连报错都没有**。得自己解 `.unitypackage`（gzip+tar，每条一个 GUID 目录，含 `pathname`/`asset`/`asset.meta`；⚠️ 条目名带 `./` 前缀要剥）。另一条：**`TMP_Text.textBounds` 是「行盒」不是「字形框」**，按它定字号字会小 ~30%（汉字只占 1 em，行高 1.437 em）。详见 `资料/规则引擎_进度与交接.md` 第六节 |
| **🆕 开局那几行写在 `Start()` 里 = 自检永远验不到** | 批处理下 `AddComponent` **不触发 `Start`**，所以「按 Play 的入口」和「自检跑的入口」很容易变成**两条路** —— 而真会出问题的一定是前者。修法：把开局抽成一个方法（`BattleDriver.BeginFromDeckLibrary`），`Start()` 和自检**调同一个**。和「判据只有一处」是同一条规矩，只是对象从规则换成了**入口** |
| **🆕 「重开一局」漏带的那半边** | `Restart()` 原来只带阵营和种子、**不带卡组**（那时也没有卡组）。接上卡组库之后，按一次 R 就从「你编的那副」变回自动凑，而且**画面上没有任何变化能看出来**。规矩：`Begin()` 的参数里凡属「这一局用什么」的，`Restart()` 都要原样带过去 |
| **🆕 提示行是 NoWrap 的，长名字会横着铺出屏幕** | `Label` 走 TMP 且 `textWrappingMode = NoWrap`（`Label.cs`）——**卡组名是玩家自己起的、长度不可控**，直接塞进提示行会横着长出可见区。要**截断**，并且用 `Label.WorldW` 加一条量宽度的断言挡着 |
| **🆕 「玩家看不见的警告 = 没警告」** | 卡组里的战术卡/防御卡会被丢掉这件事，原来只写在 `Debug.LogWarning` 里 —— 打包之后玩家一个字都看不到，而画面上**牌确实少了**，看起来就像卡组没生效。要玩家知道的事必须画在**画面上**（这次放在提示行的**休息态文字**上） |
| **🆕 卡框的「透空窗口」是拱形，不能拿来当矩形裁剪框** | 立绘原来被裁进一个「从卡框 alpha 量出来的透空窗口」里 —— 那个区是**拱形**，立绘是**矩形**，直角会**顶出拱形**（症状：「卡框没包住插图」），而且窗口以外不画立绘。原版是**立绘铺满、由卡框的 alpha 当遮罩**（`子代理读报_2dcard_0827.md:310`）。**别再把「量出来的形状」当矩形用** |
| **🆕 卡框 rect ≠ 该画的尺寸** | `CardFrame` 的 rect 是 2.2452×3.2572，但 PNG 的金属 bbox 只有 **608×936**（宽高比 0.6496），左右各约 18% 是透明留白。**拉满 rect 会把框横向撑宽 6%**；按 sprite 自身比例 fit 出来是 2.1159×3.2572，宽度正好落在卡本体（2.0927）那条线上，也和原版卡面量出来的「金属基本顶到卡边（0～98% 卡宽）」一致 |
| **🆕 卡图贴图的 alpha 通道是解码残渣** | 这批卡牌插图的 alpha **97% 的像素≈0**（只有一小块不规则的斑），**RGB 才是真数据**。用 UnityPy 直接读 bundle 结果一样（`m_TextureFormat=BC7`）→ 不是导出工具的锅。**导入时必须把 alpha 写成 255**，否则整张卡全透明。（`工具/import_original_art.py` 的 `write_portrait`） |
| **🆕 旧解包的卡图是「裁过的」** | `解包整理/01_卡牌/*/Texture2D/` 里的插图是 660×1024（从 1024² 里裁的，而且**裁的位置和原版 sprite 的 `textureRect` 对不上**：旧 x180 w662 vs 原版 x176.5 w670.5）。**650 张卡图新旧内容不一致，全在卡图这一批**（UI 图/卡背不受影响）。→ 一律用 `d:/2/新解包资源/` + 按 `textureRect` 裁 |
| **🆕 新导入的 PNG 没有 `.meta` 导入设置 → 运行时读不了像素** | 刚拷进来的贴图用的是 Unity 默认导入设置，**`GetPixels32()` 直接抛**（没开 Read/Write）。`CardView.FrameUv` 量不到 alpha 包围盒就退回「整张 1024²」，**卡框的透明留白一起铺上去**，UV 裁切全失效（自检里那条「卡框 UV 裁到了卡本体上」就是这么红的）。**加完美术要跑一次 `-executeMethod ArtBaker.ApplyImportSettings`** |
| **🆕 稀有度→卡框档位没实测过就别写死** | 原版卡框分 tier1–4，但**哪一档对哪个稀有度资源里没有明说**（`CardFramesSO` 只给 `cardTier` 0..3 的槽）。这次是**拿 4 张不同稀有度的原版卡面和 4 张 tier 框逐一比对**定下来的（common→1 / rare→2 / epic→3 / legendary→4，对照图 `资料/留档_排查证据/卡面组装_0912/tier_map.png`）。**`special` 那 39 张没实测，先按 tier4** —— 别当成原版结论 |
| **🆕 卡框分档 ≠ 底部那颗宝石** | 分档改的是**框的形制**（tier1 素 → tier4 华丽），而卡框纹理上那颗菱形在四档里**都是空的暗色凹槽** —— 稀有度的**颜色**是另外叠一张宝石（`1_40k_cardframe_rarity_*` 5 张，浅蓝/绿/紫/金/橙红）。只看卡框换没换就以为稀有度做完了，会漏掉这颗。对照图 `资料/留档_排查证据/卡面组装_0912/gems.png`（上排原版 / 下排我们的框） |
| **🆕 卡池里混着「不是卡」的条目** | `card_stats.json` 自己标了 16 条 `noise=true`（`Normal Conditions`/`Normal`/`v2` 这类**占位索引**，加 `HB`=Imotekh 的异画重复、`Threnodic Choir Flawless`=同图重复），还多一条 ` Iron Priest`（**前导空格**造成的重复）。而 `gen_cards_engine.py` **原来不过滤** → 它们是「没有卡面的假卡」，卡组编辑器和自动凑牌都可能抽到。**现在生成器已过滤掉。** ⚠️ 判重必须按 **(阵营, 归一化名字)** —— 原版本来就有跨阵营同名卡（Aggressor / Terminator 那几个），只按名字会误删。⚠️ 卡池**当前张数**看 `资料/阵营推进_清单与交接.md` §五（本文档不写死） |
| **🆕 按卡名找图，模糊兜底不能跟「整个文件名」比** | 贴图名带前缀（`CSM_BlackLegion_strat_Helfire Torch`），拿卡名跟整串比相似度只有 0.55 —— 「Hellfire Torch」和「Helfire Torch」这种**只差一个字母**的也被判成配不上（18 张就是这么漏的）。要**在文件名里滑一个跟卡名等长的窗**取最高分。另外**按卡名从长到短认领**：短名会先到先得（`Deffkopta` 抢走 `Mega Blasta Deffkopta` 的图） |

### 特效还原线（详见交接文档第九节，那里更全）

| 坑 | 真相 |
|---|---|
| **🆕 原版材质上带着内置 Standard shader 的残留值** | 原版 shader 换过之后，旧材质里的 `_SrcBlend/_DstBlend/_ZWrite/_Surface/_Glossiness/_Metallic…` **不会被清掉**。如果新 shader 的属性表里**没有**这些名字，它们就是**死值**（Unity 忽略）；但我们自建的 shader 一旦用 `Blend [_SrcBlend][_DstBlend]` 这种间接寻址，就会把死值当活值用。实测：`Everguild/FX/Particle Distortion Affect Transparents` 属性表里混合相关的一个都没有（写死在 pass 状态：SrcAlpha/OneMinusSrcAlpha、ZWrite Off），而材质上是 `_SrcBlend=1/_DstBlend=0/_ZWrite=1` → 导出侧变**不透明覆盖 + 写深度**，把背后的东西整块抠掉。**判据：先读原版的 `m_PropInfo.m_Props`，属性表里没有的就一律硬编码，别做间接寻址**（`WFSpritesAdditive` 是同一个坑的另一例） |
| **🆕 特效在「没有内容可扭曲」的场景里本来就该是空的** | 抓屏扭曲这类效果读的是屏幕贴图。测量用的空场景只有纯色 clear，重采样纯色还是纯色 → 一个亮点都没有。**这会被读成「导出整个丢了」（C 组）**，其实原版在同样条件下也只是画一块恒定灰方块（采样到没绑定的默认灰贴图）。判定这类效果必须**给背景放真实内容**（`DistortProbe` 用不透明棋盘），而且要么把材质排进透明队列、要么确认 `_CameraOpaqueTexture` 已经装上内容 |
| **🆕 同一场景里常有多份同类组件 —— 先解父链判「用途」，别急着判「矛盾」** | ⚠️ **2026-09-14 更正**：这一行原来写「**值会互相矛盾**」，举 `CardsHorizontalLayout` 的 MB 5271 vs MB 4053（`m_scale` 0.73 vs 0.54）为例，还说 4053 是「另一套预设（小屏档或旧版本）」。**这是错的** —— 5271 = **我方手牌**（父链 `BottomAnchor/PlayerArea/HandArea`、`PlayerHand.isPlayer=1`），4053 = **敌方手牌**（父链 `UpperAnchor/EnemyArea/HandArea`、`isPlayer=0`、`m_invertRotation=1`），**两份各自都对**，`−330` vs `+74.5` 正负相反正是因为一方从下往上弧、一方从上往下垂。**错因**：只比字段值、没解 `m_Father` 父链（`isPlayer` 也没看），于是把「两个用途」误读成「一个权威一个作废」，还编出三条「判权威」的论据（结论蒙对了，论据全塌）。**正确做法**：解父链（`d:/2/新解包资源/assets_full/bundle_scenes_scenes_battlearena1/` 里逐层解 `Transform.m_Father`）或读用途字段，按用途取各自那份。**实据**：`Unity/资料/战斗规格/战斗重建_0827/子代理读报_back左区_0827.md:117,120,122,125`（2026-08-27 就写清「敌 4053 / 我 5271」了） |
| **🆕 挑特效不能只看台账，要实拍** | `effect_index.json` 判定「对得上 + 高置信」的效果里，`Axe_Slash_User_SW` / `Attack_Stomp` / `BulletImpactBurst_Blood` 在我们自己的战斗场景里渲成**一整块不透明黑方块**（材质 alpha 没还原对）。用 `Editor/VfxPicker.cs` 实拍：**每个效果拍 3 个时刻**（同一个效果早期是尘环、晚期是黑菱形，只看一帧结论完全相反）+ **背景用中灰**（用近黑背景会把黑方块和正常效果混在一起看不出来） |
| **🆕 批处理能跑 ≠ GUI 编辑器能开** | 工程 manifest 是瘦的、没装 `com.unity.modules.physics2d`，DOTween 的模块「默认全开」→ `DOTweenModulePhysics2D.cs` 编译不过。`DOTweenSetup` 只给 Standalone/Android/iOS 设了宏，**编辑器那套编译用不上** → 「批处理全绿、GUI 一打开满屏报错」。修法：`Assets/csc.rsp` 写 `-define:DOTWEEN_NOPHYSICS2D`。**rsp 里不能写中文注释**（会被当成源文件参数报 `CS2001`） |
| **🆕 静态字段不进 Play 模式** | `CardEffects.Play` 是静态委托，编辑器建场景时接了也白接 —— 按 Play 时是 null，特效只剩日志。用 `[RuntimeInitializeOnLoadMethod]` 自己装（`WarpforgeEffectHost.cs`） |
| **🆕 组件上的私有引用不进序列化** | `BattleBackdrop` 的 `_mr/_tex` 存场景后是 null，背景不刷新。`Build()` 里要从子节点重新找一遍 |
| **🆕 相机的宽高比必须在建东西之前定死** | HUD 文字/格位底片的世界坐标**建的时候算一次**，而 `LayoutSpace.VisibleWidth` 跟着 `aspect` 走。批处理下相机默认 4:3 → 建完再拍 16:9，HUD 整片偏左 |
| **🆕 Unity 导入会把非 2 次幂的图缩到最近的 2 次幂** | 1920×1080 的背景图会被拉成 **2048×1024**（宽高比 1.78→2.0），一「铺满」就整体放大 12.5%，和原版量出来的槽位坐标对不上。`textureImporter.npotScale = None` |
| **🆕 粒子材质要用 `Particles/Unlit`，不能用 `URP/Unlit`** | 后者**不乘粒子顶点色**，`startColor` 的灰度/透明度全丢 —— 烟和蒸汽渲出来是一坨**白方块** |
| **🆕 共享材质上改 `mainTexture` 会串** | 所有卡共用同一张贴图。每层都要 `new Material(模板)` |
| **🆕 画布数组 row 0 在底部** | 本项目的贴图绘制里，「从顶部数」的 y 和数组行号差一次翻转。`FillHexagon` 忘了翻 → 费用六边形画到了**右下角**（盖住生命宝石）。写画布图元时先问一句「这个 y 是哪套坐标」 |
| **🆕 UI 图集里 sprite 的坐标只在 `SpriteAtlas.m_RenderDataMap`** | `Sprite.m_Rect` / `m_RD.textureRect` 全是 `(0,0,w,h)`（那是 sprite 自己的尺寸），`uvTransform` 是枢轴偏移。切图集用 `d:/2/Warpforge_tools/slice_battle_atlas.py` |
| **🆕 粒子系统的随机种子不钉死，尺子就不确定** | `useAutoRandomSeed` 默认**开**，每次重播换种子 → 粒子分布每次不同。实测**同一进程连渲三次** 537/518/512 个亮点（亮度和却几乎不变 = 分布变了），两遍扫描 ~60% 的采样行会变 —— **噪声和要测的改动一样大**。修法（`EffectSweepBatch.SeedAndReset`）：关自动种子 + 显式 `randomSeed` + 先 `Simulate(0, restart:true)` 复位。修完 2968 行只剩 11 行差 1 个像素。**改东西之前先确认这条** |
| **🆕 `SpriteRenderer`/`SpriteMask` 的 sprite 不会被导出** | 精灵是 bundle 资产，引用落不下来（Unity 写成 guid 全 0 的伪引用）。后果不止精灵自己不显示：`VisibleInsideMask` 的粒子全靠它裁形，遮罩一空**整块效果渲成空**。实测 **78 个 prefab** 有被遮罩的粒子。修法见交接文档 P0-h |
| **🆕 原版 prefab 的根节点有的停在场外 `x≈100`** | `Atk_GrotGrenade`(99.59,1.24,-0.37)、`Attack_Stomp`(100,0.725,0)。播放时**位置必须归位**，否则效果播在场景外；但**旋转/缩放不能动**（是作者定的，`Antimatter Explosion` 是 X=-90°、`Atk_GrotGrenade` 是 scale 0.5） |
| **🆕 取景要量「要渲染的那个实例」** | 量原样实例化的副本 → 相机看 x=100、效果在原点 → 全 0 亮点。**看着像还原失败，其实是尺子错了** |
| **🆕 「一大团粉色糊满屏」未必是坏 shader** | 可能是效果本身就是全屏类：`Color change overlay` 半径 **245.95 单位**（战场环境叠加）。判据用半径：普通卡牌特效 3–15 / 大范围闪电 74–82 / 环境叠加 245+ |
| **🆕 `sweep_*.tsv` 的 `*_srgb` 两列是坏的** | 恒为 65536。台账用的 `lit`/`sum` 没受影响，但那两列别再拿去算东西 |
| **🆕 看到「一大片效果同时坏掉」，先怀疑尺子，别怀疑资产** | 本轮实测**五次**：①280 个「两边都空」是单帧采样点不对 ②749 个「严重偏暗」是扫描工具两边加载条件冲突 ③达标数掉 36 个是兜底逻辑覆盖了真值 ④「Z 计数从 500 掉到 470」是 Z 计数的带宽边界抖动（边界挪 0.05 就摆动 22–54 个）⑤白板 10/24「没渲染」是取景量错了实例。**五次都是工具/代码的坑，一个资产都不用改**。直接当资产问题去修会白干好几天 |
| **🆕 `资料/原版参照图/` 不能整目录当参照用** | 239 张里**只有 11 张可信的真渲图**（`shots_battle/` 4 + `data/` 4 + `pitch_matrix/` 3）。`shots_ui/ui_prefabs/` 那 218 张**全是黑的**（同一 MD5、都是 27260 B）—— 生成它的 SceneJumpShot v2.2 挑了 `clearFlags=Nothing` 的 UI Camera、又把 Canvas 强制成 `ScreenSpaceOverlay`（Overlay 不过相机，当然拍不到）。**不是「进不去原版」**：同一轮另一张相机拍的图是好的（mean 95.55），v3.6 改法之后就正常了 |
| **🆕 `AssetBundle.LoadFromFile` 对「内容已在进程里」的文件返回 null** | 同一份内容、两个路径也算重复。编辑器工具先把 84 个源 bundle 全量加载后，`wf_shaders.bundle` 就再也加载不进来 → 所有只靠 bundle 兜底的 shader 解析失败 → 渲染成纯色块。这是 P0-d 的真因 |
| **🆕 读材质属性要连「值」一起从序列化数据读** | `Material.GetFloat()` 对「shader 没声明」的属性返回 0，而原版材质的 shader 来自 bundle、声明表是残缺的。只读名字不读值，重建出来的 `_SrcBlend` 全是 0 |
| **🆕 UnityPy 重打 bundle 必须保留 `AssetBundle` 对象并重写它的 `m_Container`** | 删掉它 → Unity 报 `not compatible with this newer version of the Unity runtime`（**这句话极具误导性，跟版本毫无关系**）；留着但不重写 → 包能加载但一个资产都暴露不出来 |
| **🆕 拿不准判定的效果，加「置信度」而不是硬给结论** | 两个小数字相除得出的比值没有意义（实测有个效果原版只有 22 个亮点、导出 114 个，比值 5.38 纯属噪声） |
| **`com.unity.modules.assetbundle` 默认没装** | Editor 程序集能用 `AssetBundle`，**运行时程序集编译不过**（`CS1069`）。已在 manifest 补上 |
| **`Shader.Find` 打包后找不到未被引用的 shader** | 自建 shader 必须注册进 Graphics Settings 的 Always Included Shaders |
| **legacy 粒子 shader 在 URP 里变粉** | `Mobile/Particles/Additive` 这类是内置管线的；且混合写死在 shader 里、材质上没有 `_SrcBlend`，必须按 shader 名推断 |
| **`ParticleSystemRenderer.mesh` 不是 `MeshFilter`** | 漏了这条，Mesh 模式发射的粒子（弹体/光环）导出后整个消失 |
| **比对工具取景必须统一** | 各自自动构图会让亮度差失去可比性，曾据此得出错误结论 |
| **`-executeMethod` 一次只能给一个** | 给两个只跑最后一个 |
| **不清旧产物会累积副本** | `GenerateUniqueAssetPath` 不断产出 `X 1.mat`；清理时别把 `WarpforgeVFX/Runtime` 和 `Shaders` 一起删掉 |
| **`Warpforge_code` 是签名桩** | 方法体**直接留空**（不是抛异常），0 个文件含桩标记。字段名/类型/`[SerializeField]` 完整可用 |

---

## 八、常用操作速查

```bash
# Unity 批处理构建战场
unset ELECTRON_RUN_AS_NODE && "D:/Unity/Hub/Editor/6000.3.23f1/Editor/Unity.exe" \
  -batchmode -quit -projectPath "D:\4\Unity\MyGame" -executeMethod BuildArena1.BuildFromCLI -logFile -

# 渲染战场预览图（批处理下需模拟粒子）
... -executeMethod BuildArena1.RenderPreview -logFile -

# === 特效还原 ===
# 导出效果（全量导出前把 EffectExporter.Limit 改成 -1）
... -executeMethod EffectExporter.Run -logFile -
# 原版 vs 导出 并排渲染比对 → d:/4/_tmp_view/cmp/
... -executeMethod EffectCompare.Run -logFile -
# 新增/改名自建 shader 后，注册进 Always Included
... -executeMethod WarpforgeSetup.RegisterShaders -logFile -

# === 特效「能用」这一侧（第五轮新增，详见 资料/特效还原_进度与交接.md 第五节）===
# 导出集变了 → 重建索引 → 重建效果库（两步都要跑）
"D:/2/Warpforge_tools/py312/python.exe" d:/4/Unity/工具/gen_effect_index.py
... -executeMethod EffectLibraryBuilder.Run -logFile -
# 白板自检：播→消失闭环 / 渲染与台账一致 / 反复播不泄漏（grep "^WB " 看结果）
... -executeMethod WhiteboardTest.Run -logFile "d:/4/_tmp_view/whiteboard.log"
# 🆕 抓屏扭曲定位：三种纯色背景 + 一块不透明棋盘，量「相对空场景的贡献」；
#    自带 26 条断言（属性表/数值/不该有的属性/关键字）。图在 d:/4/_tmp_view/distort/
... -executeMethod DistortProbe.Run -logFile "d:/4/_tmp_view/distort.log"
#    筛输出：grep "^DP "；退出码 0 = 断言全过

# 🆕 挂上抓屏 Feature（**幂等**，重复跑不会加两份）
#    把 GrabPassTransparentFeature 加进 PC_Renderer / Mobile_Renderer 的 rendererFeatures
#    手动在 Inspector 里加也行，但那是「下个会话不知道做过没有」的改动 —— 用这个入口
... -executeMethod WarpforgeSetup.RegisterRendererFeature -logFile -
# 🆕 混合状态 + shader 解析的**常驻守卫**（2026-09-13 第三十三轮加）
#    混合：把每个材质定义真的建出来，断言 _SrcBlend/_DstBlend/_ZWrite 等于按原版 shader 名推出的值
#    shader 解析：导出报告里出现过的原版 shader 名，`WarpforgeShaderMap.TryResolve` 是不是都解析得到
#    （解析不到 = binder 会**保留占位材质**；唯一豁免的是 TMP 的文字 shader，白名单里写了理由）
... -executeMethod BlendProbe.Run -logFile "d:/4/_tmp_view/blend.log"
#    筛输出：grep "^BP "；退出码 0 = 断言全过；报告 _tmp_view/blend_report.tsv
# ⚠️ 这条踩过两次同一个坑（判据选错 → 加法发光变不透明+写深度），**改 shader/binder 之后必跑**

# 🆕 读任意原版 shader 的属性表 + pass 状态（**动手改自建 shader 之前先跑它**）
"D:/2/Warpforge_tools/py312/python.exe" d:/4/Unity/工具/dump_shader.py "Particle Distortion"
# 生成人看的白板场景 → Assets/WarpforgeArena1/Scenes/VFXWhiteboard.unity
... -executeMethod WhiteboardBuilder.Run -logFile -

# 重跑场景清单生成器
"D:/2/Warpforge_tools/py312/python.exe" d:/2/Warpforge_tools/scripts/gen_unity_arena_manifest.py

# === 规则引擎（第六轮新增，详见 资料/规则引擎_进度与交接.md 第三节）===
# 卡表精简（改了 card_stats.json 或 数据/卡牌翻译/zh_cards.json 之后重跑）
#   ⚠️ 必须带 PYTHONIOENCODING=utf-8：脚本里有 ⚠️ 这类字符，GBK 控制台会在**写文件之前**抛
#      UnicodeEncodeError（2026-09-12 撞到，当时以为脚本坏了）
#   ⚠️ 2026-09-13 起它还读 `数据/游戏数据/cardface_fixes.json`（卡面逐张核对算出的
#      subtype / keywords 修正）。**那张表要重新生成时**，四条按顺序：
#        python Unity/工具/merge_cards_new.py      # 29 批 → 资料/卡表核对_卡图提取/_合并总表.md
#        python Unity/工具/compare_cards.py        # 对账（只报差异）
#        python Unity/工具/gen_cardface_fixes.py   # 算修正表（⚠️ 基准是 card_stats.json，不是产物）
#        python Unity/工具/gen_cards_engine.py     # 盖到卡表上
#   🔴 **要手工改某张卡的 `keywords` / `subtype` 时**，**不许**直接往那两列里写 ——
#      它们是**算出来的**（源 = `_合并总表.md`），`gen_cardface_fixes.py` 一跑就**静默抹掉**。
#      走 **`_manual_keywords` / `_manual_subtype`** 那两列（脚本原样保留、**最后应用**，手工赢过算出来的），
#      说明写在同名的 `_` 键里。⚠️ 加条目要**写全**该卡的**整份**关键词表，不是增量。
#      2026-09-14 A7 用过一次（3 张督军的裸 `Flying`，见 `cardface_fixes.json` 的 `_manual_keywords._`）。
#   **改完怎么看对不对**：`gen_cards_engine.py --check` 只说「一致 / 已过期」，**不列差异** ——
#      真要核差异，生成前后拿 `git diff --numstat` + 逐字段比（⚠️ 键**要用「卡名+faction」或数组下标**，
#      光用卡名会撞上**同名跨阵营**的卡，报出一堆假差异）。
PYTHONIOENCODING=utf-8 "D:/2/Warpforge_tools/py312/python.exe" d:/4/Unity/工具/gen_cards_engine.py
#    --check 只对账不写文件
# 原版美术同步进 gitignore 的 Resources/Art/（幂等）：
#   · 卡框：**13 阵营 × 4 档 × troop/stratagem 两套**全部从新解包取（`_FRAME_OF` 里加阵营就多一套）
#   · 卡背：`素材/Warpforge原版/卡背/`（那批是切好的）
#   · **卡牌插图**：`d:/2/新解包资源/assets_full/bundle_<阵营>cardassets_assets_all/Texture2D/`
#     —— 按**卡名反查**（归一化子串 → 同长窗口相似度兜底；**按卡名从长到短认领**，防短名抢图），
#     再按同目录 `Sprite/*.json` 的 **`textureRect` 裁成 sprite**、alpha 补 255（那批图 alpha 是解码残渣）。
#     2026-09-12 起走新解包（旧解包 650 张同名图内容不对）。
"D:/2/Warpforge_tools/py312/python.exe" d:/4/Unity/工具/import_original_art.py
#   ⚠️ **导完美术必须再跑一次这个**，否则新 PNG 没开 Read/Write —— `FrameUv` 量不到 alpha 包围盒、
#      卡框的透明留白会一起铺上去（自检里那条「卡框 UV 裁到了卡本体上」会红）
... -executeMethod ArtBaker.ApplyImportSettings -logFile -
# 配不上的插图摊成人能看的（原版卡面 + 候选插图 + README）→ Unity/资料/待人工对_插图/
#   ⚠️ 那个目录在 .gitignore 里（全是原版资产）。对完把别名加进 import_original_art.py 再重跑
"D:/2/Warpforge_tools/py312/python.exe" d:/4/Unity/工具/collect_unmatched_art.py
# 规则引擎自检（纯逻辑，很快）
... -executeMethod RuleEngineTest.Run -logFile "d:/4/_tmp_view/ruleengine.log"
#    筛输出：grep "^RE " d:/4/_tmp_view/ruleengine.log
#    退出码 0 = 全过，1 = 有失败
# 🆕 **逐句解析探针**（改 `EffectText` 的判据之前/之后拿来量，2026-09-13 A4 批 1 建）
#   写 d:/4/_tmp_view/probe_in.txt（一行一句，`#` 是注释）→ 跑 → 读 probe_out.txt
#   （卡级判定 + 逐段判定 + 每条 op 摊平；**只读文件、不碰工程**，可以反复跑）
... -executeMethod EffectParseProbe.Run -logFile -
#    筛输出：grep "^EP "；退出码 0
# 卡牌基座自检（**棋盘格数改过就要复跑**）
... -executeMethod CardBaseDemo.Run -logFile -
#    筛输出：grep "^CBD "

# === 可玩对战（第六轮新增，详见 资料/规则引擎_进度与交接.md 第一节）===
# 重建「能玩的那个场景」→ Assets/CardPresentation/Scenes/Battle.unity，打开按 Play
... -executeMethod BattleScene.BuildAndSaveScene -logFile -
# 卡组编辑自检（规则/筛选/加删牌/翻页/版面；61 条断言 + 截图）
#   ⚠️ 2026-09-13 更正：这里原来写 52 条 —— 与同文档 :321 的「DeckScene.Run 61/61」打架。
#      实跑 `DeckScene.Run` 输出 `DK === 结束：61/61 全过 ✅`，61 是对的
... -executeMethod DeckScene.Run -logFile "d:/4/_tmp_view/deck.log"
#    筛输出：grep "^DK "
# 重建卡组编辑场景 → Assets/CardPresentation/Scenes/DeckEditor.unity
... -executeMethod DeckScene.BuildAndSaveScene -logFile -

# 对战自检：开局/拖拽/费用拒绝/出牌/攻击/护卫/技能与触发/打完整局/原版卡牌/卡组库那套牌/战术卡拖出去打（断言数见 `资料/阵营推进_清单与交接.md` §五；另出 16 张截图）
... -executeMethod BattleScene.Run -logFile "d:/4/_tmp_view/battle.log"
#    筛输出：grep "^BT "；图在 d:/4/_tmp_view/battle/

# === 结算「开门」视频（2026-09-12，详见 资料/规则引擎_进度与交接.md）===
# 视频元数据自检（阻塞、秒回；断言源尺寸 3840×1080 / 片长 / 帧数与原版元数据一致）
... -executeMethod VideoProbe.Run -logFile "d:/4/_tmp_view/videoprobe.log"
# 真播 + 左右拼 alpha 合成 + 像素断言 + 结算界面实拍（~30 秒）—— ⚠️ 不能带 -quit
... -executeMethod VideoProbe.Play -logFile "d:/4/_tmp_view/videoplay.log"
#    筛输出：grep "^VP "；图在 d:/4/_tmp_view/video/
#    视频本体在 gitignore 的 Resources/Art/videos/，从 素材/Warpforge原版/视频/videos/ 拷（改名 .webm）

# 从档案库同步资源（幂等）
"D:/2/Warpforge_tools/py312/python.exe" d:/4/Unity/工具/sync_from_d2.py --batch all

# === 进原版游戏（看"原版到底长什么样"的标准答案）===
# ⚠️ mod 的输出目录已经改成**归档位**（`d:/2/unity_run_ref/UserData/sjs_dump_cfg.txt` 第 3 行）：
#   `d:/4/Unity/资料/原版参照图/Unity参照管线_0825/data`
#   —— 原来指向 `_tmp_view/orig`，那个目录会被当垃圾清掉。跑一次会在那儿落：
#   `runtime_ui_dump_drive.tsv`（UI 树）+ `shot_battle_hand.png` / `shot_drive.png`（实拍）
# 最近一次跑（2026-09-12）的产物已归档成带日期的名字：
#   data/runtime_ui_dump_drive_0912.tsv（UI 树 1253 行）
#   shots_battle/arena1_0912_camera_ui.png、arena1_0912_full.png（战场实拍）
# ⚠️ 战斗 UI 的「内容」出不来（要对局数据）—— 但 3D 战场是对的，UI 树里的 rect 也全是真的。
# ⚠️ 必须先设 DOTNET_ROOT，不设起不来；副本路径必须纯 ASCII（MelonLoader 的 plthook_open 怕中文）
taskkill //IM Warpforge.exe //F
cd /d d:\2\unity_run_ref && set DOTNET_ROOT=<本机 dotnet 根> && start "" Warpforge.exe
# mod 配置：d:/2/unity_run_ref/UserData/sjs_dump_cfg.txt  四行：
#   第1行=bundle 名（写 scenes_scenes_mainmenuwarpforge.bundle 就进主菜单）
#   第2行=进场景后等几秒   第3行=输出目录
#   第4行=动作：drive＝战斗驱动全套 / 空＝只 dump 一次+截一张 / 秒数列表如 45,75,105,135＝多时间点各一份
# 产出：runtime_ui_dump_*.tsv（UI 树，带 activeSelf 和 sprite 名）+ 真渲 PNG
# ⚠️ 验不了的（2026-09-12 实测，别再撞）：手牌注入（原版关服、卡数据在远程 CCD）、
#    卡组编辑界面（实例化即黑）、主菜单只拍得到顶栏、改游戏数据、ScreenCapture 截图会崩
```

**Python 一律用 `D:/2/Warpforge_tools/py312/python.exe`** —— 系统的 Python 3.14 缺 UnityPy 纹理转换模块，导出会失败。

---

## 九、距离「完整还原原版」还差什么

> 2026-09-12 对账结果。派子代理把**反编译代码（1800+224 个带方法体的 `.c`）+ 运行时 UI dump +
> 离线规则书**挖了一遍，列出原版「战斗 + 卡组编辑」到底有哪些环节，再拿我们的代码逐条比。
> 原版那侧每条都有类名/场景对象名出处，我们这侧是实测的代码状态。**按「还差多少」排序。**

### ✅ 已经对上的（原版有、我们也有）

对局开始与先后手 · 每回合抽牌 · 能量（按每方自己的回合数 +1）· 手牌布局（原版实测的曲线/张角/间距）
· 悬停抬起与邻牌让位 · 拖拽与拖拽插槽 · 9 格棋盘落位 + 登场上场动画 · 攻击方式三选一
（原版 `Drag Attack Selector`）· 目标选择反馈（准星 + 红/紫/金弧线 + 数值格底光）· 技能卡面板
（原版 `ActiveSkillDesc`）· 关键词与触发（23 个）· 事件 → 特效（出牌/登场/近战/远程/挨打/阵亡/
发动技能/触发效果）· 回合结束与胜负 · 牌堆/弃牌堆底板与张数 · 卡面与 HUD 中文

### ✅ 原版卡牌接进对战 —— **2026-09-12 做完，别再当待办**

打开 `Battle.unity` 按 Play 打的就是**原版卡**（1130 张池按阵营凑）；中文卡名/效果、卡框/卡背、
AI 全都接上了。当时的接法与踩的坑见 `资料/规则引擎_进度与交接.md`；
**接这条链时顺手挖出的四个真 bug 已经记在 §七 的坑表里**（这里不再重抄）。
### 🟡 UI 对齐：还剩这些（2026-09-12 对账完，按「差多少」排序）

这一轮已经把**卡牌尺寸**（场卡 137.2×218.4 / 手牌 165.0×262.7，与原版逐项相等）、
**END TURN 按钮**（130.7×80.4，挪到右侧能量区）、**能量水晶**（右侧竖排三件套）、
**督军名牌**（126.3 px 高，原来小 36%）、**回合灯**（23.4 px，原来大 47%）改齐了。
出处：运行时 dump 本机重跑 + `资料/战斗规格/战斗重建_0827/战斗界面JSON权威表_0827.md` 的绝对坐标表。

**还差（都有实测值，动手时照着抄）**：

| 差什么 | 原版实测 | 我们 |
|---|---|---|
| ~~**卡面组装**~~（立绘 / 卡框 / 遮罩） | `CardImage` **2.7484²** @(0,−0.044)（**比卡还大，故意外溢**）+ `CardFrame` 2.2452×3.2572 @y−0.03（挂 `SoftMask`，`MaskArea`=CardImage、**`FlipAlphaMask=1`**）→ **立绘铺满、卡框的 alpha 当遮罩切掉多余部分** | ✅ **2026-09-12 做完**：改成「**立绘铺满卡框那块矩形 + 卡框画在上面**」（卡框自己就是遮罩，少一层）。同时：**13 个阵营的卡框全导进来了**（原来只有 4 个，其余 9 个走占位卡面**根本不画立绘**）、占位那条路也补上立绘、**卡框改成按 sprite 自身比例 fit**（原来拉满会横向撑宽 6%）、**卡框按稀有度分四档**（tier1–4，映射是对照原版卡面实测的）、**底部稀有度宝石**（原版 `Rarity`，5 张图按稀有度取色）、**战术卡另一套框**（原版 troop / stratagem 分开，共导 150 张框）。⚠️ 插图来源换成新解包（见下） |  
| **插图来源** | 原版插图 sprite = **671×1024**（宽高比 0.6553，卡本体 0.628、卡框 rect 0.689 —— 就是照着「铺满卡片」设计的） | ✅ **2026-09-12 换源**：`工具/import_original_art.py` 改从 **`d:/2/新解包资源/`** 取，**按 `Sprite/*.json` 的 `textureRect` 裁**。⚠️ 旧解包给的是 660×1024（被裁过、宽度都不对，**650 张卡图新旧内容不一致**，全在这批里）。⚠️ **alpha 统一写 255**：这批卡图的 alpha 通道是**解码残渣**（97% 像素 alpha≈0），UnityPy 直接读 bundle 也一样 —— 不是导出工具的锅 |
| ~~牌堆底板位置~~ | `PlayerDeck` 230×230 贴右下角；`EnemyDeck` 200×200 贴右上角 | ✅ 已改。⚠️ **只有一处来源**（绝对坐标表），dump 里没能二次验证 —— 看得不对就说 |
| ~~右侧能量区大底板 + 任务点~~ | `UI_Energy_Holder_big` 302.1×480.8、`QuestPointsHolder` 97.7² | ✅ 已接（图从 `battleatlasui/sliced` 同步进来）。⚠️ 2026-09-12 更正：能量底板 `Card Frame Cost Icon` 的图**本地有**（`Resources/Art/ui_deck/Card_Frame_Cost_Icon.png`），原来写「没导出的 PNG，做不了」是错的 —— 只差画上去 |
| ~~卡牌放大详情~~ | `CardDisplayWindow`：遮罩 α0.7725 + `Card Display` 752×868 + 放大卡 743 px 高 + 下面一条文字；**轻点卡牌开关**（反编译 `BasicCardUI.ToggleOpenCardDisplayOnTouch`） | ✅ **2026-09-12 做完**：`Battle/CardDisplayWindow.cs`，手牌轻点开关（再点关掉）。⚠️ 「每行 34 字硬断行」**是我们挑的**（原版 TMP 自动折行） |
| ~~**能量底板 + 任务点**~~ + **阵营资源** | `Card Frame Cost Icon` 248×244 垫在能量水晶下；`QuestPointsHolder` 97.7×97.7；`FaithHolder` / `SpiritStoneHolder`（inactive） | ✅ **2026-09-12 做完**：大底板 `UI_Energy_Holder_big`（`BattleDriver.cs:1314` 一带）、两块 `Card Frame Cost Icon` 底板、两个 `QuestPointsHolder` + 接片、**敌方那颗能量水晶**（原来一颗都没画）全部按权威表 B 节的绝对坐标摆，自检 7 条断言守着。⚠️ 这一格历史上错过两次：① 行号记成注释行；② 任务点照 `BackgroundJoin` 的偏移摆，两个图标都贴在水晶内侧。**仍然没有的只剩阵营资源**（信仰 / 灵石 —— 机制数据其实有，见 `资料/阵营推进_清单与交接.md`） |
| **右上的能量区大底板** | `UI_Energy_Holder_big` 302.1×480.8（右侧出屏 156 px，是装饰性出血） | ✅ **2026-09-12 已接**（`BattleDriver.cs:1314`，用 `HudDecorZ` 压在水晶底下）。⚠️ 2026-09-12 更正：这一格原来写「没有」，与上面两行**自相矛盾**，实际就在代码里 |
| **攻击方式选择器三钮的位置** | dump 里三钮 `activeSelf=False`：近战在槽**左下角**、Active/Range **叠在槽中心**；而 `AttackSelector.cs` 文件头写的是「以横排为准」 | 横排。**两边冲突且没有实况可判**（战斗 UI 需要对局数据才出得来）——**留作悬案，别照猜着改** |
| **护盾/信仰/任务点等阵营资源** | `FaithHolder`（117.9×149.3）、`SpiritStoneHolder`（112.1×116.6）、任务点（97.7²） | 都没有（引擎侧也没有对应机制） |
| ~~**一整套没做的界面件**~~ | `CemeteryLogPanel`（墓地日志 863×1032.5）、`Mulligan`、`ChooseCardMenu`、`Unit Chat`、`ReplayButtons`、`WaitText`、`Created By Text`。2026-09-13 全面位置核对后补的完整清单（含绝对坐标）：头衔底条 `TitleBackground`、头像块 `Avatar Item Small`、`ChatButton` / `ShowCemeteryBtn` / `CenterCameraButton` / `OffensiveButton`、任务点**数字** `'0/3'`、`Energy Accumulation`、`OvertimeIndicator` | ✅ **2026-09-13 除最后四项外全做完**：墓地日志 · 换牌 · 头衔底条 · 头像块 · 三个边角按钮 · 任务点数字 · 能量累积灯 · 加时标记（**默认关着**，机制还没做）。**还差**：`ChooseCardMenu` / `Unit Chat` / `ReplayButtons` / `WaitText`（**点开牌堆看张数**也不在这张表里、还没做）。逐条见 `资料/战斗UI_原版对账表.md` §三/§三点五/§三点七 |

### 🔴 下一件事：战术卡能打（449 张）—— **2026-09-12 已打通，还在往上推覆盖率**

**用户 2026-09-12 定的顺序**：**先把卡牌做完，再进战斗与特效**。
**现状：能打出去了**（引擎 + 牌组 + 表现层都放开），剩下的是**覆盖率**。

| 项 | 现状 | 说明 |
|---|---|---|
| **文本解析** | ⚠️ **数字只写一处**，权威在 `资料/阵营推进_清单与交接.md` §五（本文档不抄第二份）<br>⚠️ **「载荷有机制」这个数会上下动，别当退步看**：2026-09-12 从 269 掉到 235 是**口径变诚实**（五个动词解析得出但结算层没分支，以前算「有机制」）；同日晚些 `deploy` 补上后回到 273 | `Core/EffectText.cs`，handler 顺序照抄 `rule_core._resolve_text`。自检每次报这几个数 + 把**全量**缺口按频次落盘到 `_tmp_view/tactic_unparsed.txt`（分四栏：不认识的句子 / 半懂 / **缺机制** / **打得比卡面宽**）。🆕 **改判据前后用逐句探针量**（`EffectParseProbe.Run`，见 §八） |
| **结算** | ✅ 通了 | `Core/EffectResolver.cs`：deal / heal / draw / destroy / stun / give / gain / lose / recharge / refill。限时增益真的会到期（`UnitState.TempBuff`） |
| **牌组** | ✅ 放开 | `DeckBuilder.FromDeck` 收「能完整解析」的战术卡，解析不了的照样丢**并报数** |
| **出牌** | ✅ 放开 | `CanPlayCard`/`PlayCard` 统一分发；表现层能把战术卡拖到**敌方半场**的目标上 |
| **端到端自检** | ✅ 有了 | `BattleScene` 第 10 节：真鼠标拖一张原版战术卡打出去（手牌/费用/弃牌堆/目标掉血四条断言） |
| **关键词机制** | **未实现 3 个**（自检每次打印全名单） | 已做：侧翼·迅捷·无敌·易伤·星镖·爆裂·震荡·嗜血·标记光·伪装 + **猎杀标记·黑暗契约·兽群·哨戒·狙击·再生·压制·失明** + **群体·团·不稳定·残忍·天赋**（第三十四轮）+ 原有 13 个。<br>⚠️ **2026-09-13 起以自检打印的数为准**（它直接跑 `RuleCore.UnimplementedKeywords()`）；**数字只写在 `资料/阵营推进_清单与交接.md` §五 一处**。<br>⚠️ **2026-09-13 更正**：这格原来写「未实现 24 个」，且把 `mob`/`unstable`/`waystone` 列在「还没实现」里 —— 那三个第三十四轮已经做完了。剩下的三个是 `destroyer` / `ecstasy` / `sabotage`（A2/A3 之后）—— **都不是漏做**，理由逐条写在 `资料/阵营推进_清单与交接.md` §一之三 |
| **🆕 卡表数据本身** | ✅ **2026-09-13 按卡图逐张核过**（1118 张） | 查出并修掉三类：**`subtype`（卡面那行橙字）串列 40 处**（把卡的类型 `Unit`/`Troop` 填进了字段列 → **筛错卡**）· **关键词数值被吃掉 72 处**（`Armour 1. Blast 2` 只剩 `['Armour','Blast']` → 护甲不生效）· 关键词整个缺失。<br>修法：`资料/卡表核对_卡图提取/` 的独立提取 → `Unity/工具/gen_cardface_fixes.py` → `Unity/数据/游戏数据/cardface_fixes.json` → `gen_cards_engine.py` 盖上去（**不手改产物**，卡表 `version 5`）。<br>**结论与抽验见 `资料/卡表逐张核对_与对账.md`**；断言在 `RuleEngineTest.TestCardFaceFixes()`（其中一条是**不变量**：不许有单位卡的 subtype 是「卡的类型」） |
| **还没做的 handler** | ⚠️ **数字只写一处**：`_tmp_view/tactic_unparsed.txt` 的 ① 栏（自检每次重写） | ✅ **A4 整条线 2026-09-14 收工**（批 1～批 5 全做完）—— 不认的句子 **44 → 3 种**，那 3 种**都有硬理由**（见 `资料/阵营推进_清单与交接.md` §一之三·候选 H，**别当成漏做重挖**）。<br>这条线做过的：选牌 `Choose a …` · 回手/指代抽牌 · **常驻效果 / 手牌陷阱**（回合起止型 + **事件型**）· **部署时触发 + 持续改费**（含新的一层 `Core/CardCriteria.cs` —— 我们的 `TargetCriteria`）· **强行触发关键词 `triggerability`** · 受伤筛 / 静态条件降费 / 每单位星矢 / 正在祈祷 / 付费修饰 / 对称部署 / 强制攻击 / `double` / 狂暴留场 / 选效果。<br>现在**三个阵营战术卡 100% 完全解析**：**Goff 49/49 · DarkAngels 34/34 · Sautekh 31/31**。<br>🔴 **还有一笔大账**：单位卡的 `desc` 效果文字 —— ⚠️ **数字看 `_tmp_view/unit_desc_unparsed.txt`，别抄这里**；<br>⚠️ 而且那一栏**只列「解析不出来」的**，**「解析得出来但没有触发点」那一族（27 句）它看不见** —— 见 `资料/单位卡desc与光环_批次划分.md` §一⑦ |

**移植方案与实测数据**（449 张的句型普查、`_resolve_text` 的 handler 顺序表、两条决定性结论）
见 `资料/战术卡效果_移植方案.md`。

### 排在后面的（次优先，都有出处）

- **点开牌堆看张数** —— `DeckManager.ToggleCardbackPreview`（**牌背预览 + 剩余张数**，
  ⚠️ **不是逐张翻看**）。**这是本节唯一还没做的。**
- **倒计时 / 投降 / 出牌超时** —— ✅ **做完了**（含用户要求的「联网味」那几件）。
  秒数出处是知识，留着：**离线练习 600 秒、EventAI 240 秒**，换牌倒计时在本模式下没有 ——
  见 `资料/战斗规则与数值_出处.md` §二。⚠️ 只有 `clockTimeLimit` 默认值还缺（在远程包里）。
- **两处「原版有、故意不做」**（**别再翻案**）：换牌的**四种模式**
  （`MulliganOptions{normal, secondTurn, extraWarlord, whispersOfChaos}` —— 那是**服务器开关**
  `GameplayVariablesData.showMulligan` 选的，本地没有那份配置）· **换牌独立倒计时**
  （原版 `_MulliganCountdown` 在离线练习模式下**直接 return**）。

### ❌ 还差的 —— 分四类

#### 1. 玩法骨架：**做满了** —— 只剩三个边角

卡组编辑 · 对局结算（含原版那套「开门视频」）· 换牌 mulligan · 战斗/墓地日志 · 倒计时 · 投降 ·
卡牌放大详情 —— **全部做完**（逐项落点见 git log 与 `资料/规则引擎_进度与交接.md`）。

**仍然没有的**（都是「原版有、我们没做」，不是「没做完」）：

| 缺什么 | 原版叫什么 | 我们这边 |
|---|---|---|
| **卡牌放大——多张一起看** | `DisplayMultiCardView` | 没有（单张的 `CardDisplayWindow` 已有） |
| **单位语音条 `Unit Chat`** | 见 `资料/战斗UI_原版对账表.md` | 没有（与「单位语音整批没同步」是同一件事） |
| **点开牌堆看张数** | `DeckManager.ToggleCardbackPreview`（⚠️ 是**牌背预览 + 剩余张数**，**不是逐张翻看**） | 没有 |

#### 2. 🟠 表现与手感

~~发牌入场 · 攻击位移/抬手 · 命中反馈 · 阵亡消散 · 数值变化过渡 · 手牌重排补间~~
⇒ ✅ **2026-09-13 六件全做完**（第二十五轮）。
**还剩**：**AnimFX 脚本层**（原版 18 个模块 / 2346 个组件的**行为**没还原；`UnitTweenSO` 时长/缓动数据齐）、
挨打时的**镜头抖动**（`meleeHitCameraShakePreset`，查到没做）、**手牌 → 战场的落地节奏**
（`Card Hand To Board` clip 已读出来，`DeploySequence` 还没接）。

#### 3. 🟡 内容深度：能跑，但不全

⚠️ **2026-09-12 第十五轮重写本节**（原来这段停在很早的状态：写着「战术卡 v1 返回 `ErrUnimplemented`、
关键词只有 13 个、只用自设计卡 26 张」，早就不成立了）。

| 项 | 现在 | 还差 |
|---|---|---|
| **战术卡** | 449 张**都能打出去**；**四个通用动词全实现**（`create` / `deploy` / `drawtype` / `lowercost` / `repeat`）<br>⚠️ **2026-09-13 更正**：这格原来写「解析干净的 354 张 / 载荷有机制 329 / 下一件是选牌 `Choose a …`」——**三处都是旧话**。选牌（第二十六轮）、回手指代（第二十七轮）、常驻效果（第二十八轮）都已经做完；**数字只写一处，见 `资料/阵营推进_清单与交接.md` §五** | **还差什么只看 `资料/阵营推进_清单与交接.md` §一之三**（本格**不抄数**）—— 2026-09-14 更正：这格原来写「A4 还剩 20 条句子 · A5 单位卡 166 张 · A6 按阵营铺 46 张」，**三处都已过期**（A4 整条线收工；**A5 也已整条线收工（2026-09-14）**；A6 的 46 也是旧值） |
| **关键词** | ⚠️ **以自检打印的 `RuleCore.UnimplementedKeywords()` 为准**，见 §九「下一件事」那张表。<br>**2026-09-13 更正**：这格原来写「机制 31 个 / 还差 30 个」，与同文档另一处的实况数**两个说法打架** —— 删掉写死的数。<br>⚠️ **2026-09-13 第二次更正**：这条更正原来写「与同文档 908 行的『未实现 24 个』打架」，**它指的那一行已经不写这个数了** ⇒ 指针悬空，改成指向权威处（`资料/阵营推进_清单与交接.md` §五） | `DefinedTrait.cs` 有 **144 个**枚举成员可用来对账 |
| **卡池** | **原版 1130 张全在用**（`cards_engine.json`，`CardDatabase.Load()`） | 数值是 OCR 出来的，**没有第二遍核对**（`资料/卡牌数据源对账_0912.md` 有对账但仍欠） |
| **对手 AI** | `SimpleAI` 是**贪心**（只用 `ICardProvider`，喂的就是原版池） | 不搜索、不算斩杀 |

#### 4. 🔵 特效还原质量

🔴 **当前最要紧的不是「还剩多少个问题」，而是「指标为什么变差」** —— 见「下次接着做」第 2 条。
高置信问题的**张数与口径按新台账重算过**（09-11 旧台账那张 183 的数已作废）——
**权威在 `资料/特效还原_进度与交接.md` §〇·一**，`资料/特效还原台账.tsv` 是逐条数据。
**构建后 player 从来没验过**（三条线都欠，只验过编辑器）。

### 📌 美术资源（以后再说）

等原版复刻得差不多了再动手。机制早就位：画面全是原版资产（`Resources/Art/`，已 gitignore），
**换同名文件就是换美术、删目录就退回程序生成的占位美术**，做法见 `资料/原版复刻_场景与美术.md`。
版权红线到时候一并解决。**在此之前不用操心它。**

### 🧰 还需要的东西

| 要什么 | 说明 |
|---|---|
| **卡牌数值全量核对** | 全卡池要能用得先对一遍出处（见 `资料/规则引擎_进度与交接.md` 的卡表一节） |
| **构建后 player 验证** | 工程侧要先把效果库收成子集（`EffectLibraryBuilder.NameFilter` 支持，但要另开一个资产路径） |

**验收规矩**：`BattleScene.Run` 现在 断言数**只写在 `资料/阵营推进_清单与交接.md` §五 一处**，
**每改一项加一条，别只看截图**；改布局后 `CardBaseDemo.Run`（四档分辨率）**必须复跑**；
碰特效 shader 前先跑 `工具/dump_shader.py`。

### 🅱 特效线（把「能用」推到「好看」）

**🔍 P1-a0 已重查（2026-09-12）—— 结论推翻了一半**（`资料/特效还原_进度与交接.md` P1-a0）

- **原判据是「尺子的假象」**：原来那条「原版 4612 亮点 / 导出 0 ⇒ 问题在材质」，
  真相是**原版在没内容可扭曲的空场景里画的是一块恒定半透明灰方块**
  （采样到没绑定的默认灰贴图），跟背景、跟渲染队列都无关。台账里那批
  「C 组：导出整个丢了」的抓屏扭曲效果，判的是假象 —— 这类效果本来就该是空的。
- **真 bug 已修**：原版 shader 的混合**写死在 pass 状态里**（属性表里没有 `_SrcBlend`），
  而原版材质上带着内置 Standard 的残留 `_SrcBlend=1/_DstBlend=0/_ZWrite=1`；
  我们那版用 `Blend [_SrcBlend]` 间接寻址 → 被污染成**不透明覆盖 + 写深度**。
  已按 bundle 里的 pass 状态硬编码修好，属性表/默认值/关键字名一并照原版对齐，
  `DistortProbe` 加了 **26 条断言**（全过），白板自检不受影响。
- **✅ 原版读哪张屏幕贴图 —— 已查明：`_GrabPassTransparent`**（不再是悬案）。
  办法：原版 HLSL 源码确实被剥了，**但编译字节码在** —— `Shader.compressedBlob` 分块 LZ4 解压出来是 DXBC，
  资源名是明文。工具 `工具/dump_shader_blob.py`。当时我误判「字节码也剥了」，白试了 8 个候选名。
  ⚠️ **下一步（真该做的是它）**：`_GrabPassTransparent` 是 URP **没有**的东西 ——
  名字里的 *Affect Transparents* 说明拷的是**含透明物**的颜色缓冲（URP 的 `_CameraOpaqueTexture` 不含）。
  要给 URP 加个 Renderer Feature 把颜色缓冲拷进这个全局纹理，再把 `WFDistortion` 的采样源换过去。

**🆕 然后是新线索 P1-a1**：原版规则书里有 **61 个关键词的完整触发时机表**，
正好对上 W 组 189 个「两边全程空」（那批一直卡在「先解决触发条件才能判定」）。
解完能同时喂给 AnimFX 路 B。见交接文档 P1-a1 一节。

**验收注意**：指标用 `|ln(亮度比)|` 的中位/均值，**别用「对得上多少个」**；
而且**尺子已经修成确定性的了**（粒子随机种子钉死），改东西之前不用再担心噪声。

### 🗣 开新会话时怎么说

直接说「继续玩法线，<要做的事>」，让它先读对应的交接文档。**最常用的几条开场**：

- **「继续卡牌线，做 A1（相邻）」** → 先读 `资料/阵营推进_清单与交接.md` §一之三（**先跑一次自检**）
- **「继续玩法线，做卡组编辑 / 表现层动效」** → 那两件**都做完了**；要动表现层读 `资料/卡牌基座_进度与交接.md`
- **「特效线，查 sweep 退步」** → 先读 `资料/特效还原_进度与交接.md` §〇·一
- **「**最后**换美术」（其他都做完之后）** → 先读 `资料/原版复刻_场景与美术.md`

| 要什么 | 文档 |
|---|---|
| **引擎发什么事件 / 效果怎么写 / 触发与技能的规则** | `资料/规则引擎_设计.md` §6b |
| 事件名 → 特效名的对照表 | `Assets/CardPresentation/Core/VfxMap.cs` |
| 挑特效的试片台（**挑特效必须用它**） | `Assets/CardPresentation/Editor/VfxPicker.cs` → `d:/4/_tmp_view/vfx/` |
| 版面数值（为什么是这些数） | `资料/对战排版_原版数值与改造方案.md` |
| **战斗 UI 每个元素的原版实测值 / 还差什么** | **`资料/战斗UI_原版对账表.md`** |
| **原版的运行时 UI 树（最新一份）** | `资料/原版参照图/Unity参照管线_0825/data/runtime_ui_dump_drive_0912.tsv`（1253 行，2026-09-12 重跑） |
| 美术怎么接进来的、怎么换成自己的 | `资料/原版复刻_场景与美术.md` |
| **卡组编辑的规则与素材从哪来** | `资料/卡组编辑_原版数值与实现方案.md` |
| 卡组编辑场景怎么重建 | `-executeMethod DeckScene.BuildAndSaveScene` → `Assets/CardPresentation/Scenes/DeckEditor.unity` |
| 规则引擎 + 对战现状 | `资料/规则引擎_进度与交接.md` |
| 表现层（布局 / 卡 / 拖拽 / HUD） | `资料/卡牌基座_进度与交接.md` |
| 特效现状 + 逐效果判定 | `资料/特效还原_进度与交接.md` + `资料/特效还原台账.tsv` |
| **读任意原版 shader 的属性表 / pass 状态** | `工具/dump_shader.py` |
| **原版资源（首选，2026-09-12 起）** | **`d:/2/新解包资源/assets_full/`**（按 bundle 分包；`Sprite/*.json` 里有 `textureRect`）。⚠️ 旧的 `解包整理/` 里 650 张同名图内容不对 |
| **卡面各层的原版尺寸/位置** | `资料/战斗规格/战斗重建_0827/子代理读报_2dcard_0827.md`（A2 表：卡本体 2.0927×3.3313 / 卡框 2.2452×3.2572@y−0.03 / 立绘 2.7484²@y−0.044 / 稀有度宝石 0.4×0.4@(0,−1.458)） |
| **配不上立绘的 **16 张**卡**（暂时不管） | `Unity/资料/待人工对_插图/`（原版卡面 + 候选插图 + README；工具 `工具/collect_unmatched_art.py`） |
| **战斗规则 / 战斗数值去哪查（五层资料各答什么 + 反编译常量的读法）** | `资料/战斗规则与数值_出处.md` |
| **原版有哪些战斗环节 / 节点叫什么**（13 个战场各一份 2D 全树） | `资料/说明书/01_战斗_对战/` + `04_界面UI/菜单全树.md` |
| **某次排查的证据图（不是结论）** | `资料/留档_排查证据/`（`卡面组装_0912/` = 卡面改造前后对照、稀有度→tier 实测对照；`特效_抓屏扭曲/`；`卡组编辑/`） |
| **🆕 事件层（`When <事件>`）怎么设计的、还剩什么** | `资料/事件层_数据与设计.md` |
| **🆕 临时卡 `Ephemeral` + 「移出游戏」区域** | `资料/临时卡Ephemeral_设计与实现计划.md` |
| **🆕 特效 E 组 176 个的逐类根因初判**（⚠️ 开头那条「台账比一次修复还老」必读） | `资料/特效E组_根因初判.md` |
| **🆕 47 条「完全不认识」句子逐条查证** | `资料/战术卡47条_语义查证.md` |
| **🆕 236 副原版预组牌 vs 我们卡池** · 脚本 `工具/check_prebuilt_decks.py` | `资料/原版预组牌_核对.md` |
| **🆕 第三十二轮那批数值修了什么、连带挖出什么 bug** | `资料/卡表数值修正_第三十二轮.md` |
| **🆕 特效 E 组 176 个「哪个发射器/材质槽出问题」逐效果表**（按锅分组） | `资料/特效E组_逐效果根因表.md` |
| **🆕 AnimFX 18 个类逐类成表**（字段/参数分布/覆盖/建议重写顺序） | `资料/AnimFX_18类成表.md` |
| **🆕 16 张配不上立绘的卡 → 原图**（含「原版改过名」的根因） | `资料/卡表核对_卡图提取/_裁定_立绘.md` |
| **🆕 54 张「Gain N」图标被 OCR 丢掉 → 卡面原文**（含图标→规范写法对照表） | `资料/卡表核对_卡图提取/_裁定_图标丢失.md` |
| **🆕 `When <事件>` 认不出的短语全量清单**（每次自检重写） | `_tmp_view/when_unparsed.md` |

---


## 十、文档维护

**为什么单开一节**：文档是「下一个会话唯一的记忆」。一条写错的记录会一直误导下去 ——
已经吃过好几次（见 `CLAUDE.md` 铁律 5）。

**2026-09-13 第三十五轮：8 路 `general-purpose` 子代理并行，把全部交接文档按铁律 6 过了一遍** ——
删「已完成清单 / 进度叙述 / 被推翻的结论」，**坑与出处一条不动**。
**要看改了哪些、改前是多少行，用 `git log`**（本轮 23 个提交）。只记两件还会用到的：

- **搬去 `资料/历史/` 的逐轮流水账**：`规则引擎_逐轮记录_0911-0913.md` ·
  `规则引擎_第七节_已做完记录.md` · `项目任务_第七～十六轮小结.md` —— 正文里换成了指针。
- 🔴 **`原版复刻_场景与美术.md` 是「净增」的**（199 → 241 行）—— 它没有「该删」项，
  改的全是**更正 + 补出处**。**别以为瘦身就等于每份都变短。**

🔴 **第三十四轮那份「待清理清单」（`资料/文档体检_第三十四轮_待清理.md`）已结清并删除** ——
里面的「该删 / 该改」两类这一轮都消费完了。**它列的行号是旧快照，早就不该照它改。**

🔴 **一件要记住的事**：`卡牌数据源对账_0912.md` **本来就是 LF**（不是 CRLF）——
那句「这批交接文档全是 CRLF」**对这份不成立**。谁要**脚本化**改它，**不能按 CRLF 处理**（会把它整篇翻掉）。
（⚠️ **2026-09-14 更正**：这里原来写「**其余文档确实全是 CRLF**」—— **不成立**，别照那句办。
实测（`git show HEAD:<f> | file -`）：**CRLF** = `CLAUDE.md` · `资料/规则引擎_进度与交接.md` ·
`资料/战斗UI_原版对账表.md` · `资料/卡牌效果管线_计划与交接.md` · `资料/特效还原_进度与交接.md`；
**LF** = `项目任务.md` · `资料/阵营推进_清单与交接.md` · `资料/单位卡desc与光环_批次划分.md` ·
`资料/卡面图标_现状与缺口.md` · `资料/选牌与选效果面板_原版数值.md`。
⇒ **规律是「新写的多是 LF、老的是 CRLF」，但别信规律，逐个文件查**。
判断办法：`git diff --numstat` 与 `git diff --ignore-cr-at-eol --numstat` **两列相等**才算没翻。）

**还没结清的几处**（都是「两条规矩打架」或跨文件的数，**没敢自作主张**，列在这儿等拍板）：
- `排版` §三 Tooltip 原文 / §八「真未知」：体检清单说「该删」，但同一份文件的「不许动」名单又列着它们 ⇒
  里面是 Tooltip 原文与「`Z_OFFSET` 原版查不到、自定 0.004」这类**如实声明**，按铁律 6 **留了**
- **参照图张数三处不一致**：磁盘实测 **241** · `卡组` §四 写 239 · `排版` 原写 257（已改成「别当标准答案 + 指向卡组 §四」）
- `基座` §六 出处表仍称 `rule_core.gd` 为「**权威**语义来源」，与「权威 = 规则书 + 反编译、
  `rule_core.gd` 降为对照」的裁定不符
- `排版` 手牌尺寸「165×263」（battle.gd 的独立测量）与「165.0×262.7」是同一个量的两种精度，未统一
- `数据源` §二「只有我们有 24 张 / 文档有 48 个名字」是拿**旧分母**跑的，换分母没重跑

**那条规矩不变**：**该瘦，但只能删「已完成的欠账」和「同一件事的第二份摘要」——
凡带出处的行（`:行号`、`MB 5272`、反编译 `:105`）一律原样留着**，
删了结论就变成不可复核的断言。

🔴 **一条实测出来的教训（照它改你写的每一份文档）**：
**自检数 / 覆盖率 / 名单，一律只能写成指针，不能写死。**
第三十四轮核对过 17 条记忆，**5 条栽在写死的数字上**，全部在 1～2 轮内过期；
本轮又在交接文档里抓到同样的病（同一件事在 4～6 处各写一个数、互相打架）。
⇒ 写数字之前先问一句：**这个数的权威在哪一份？** 有权威就写「见 X 某节」，别抄第二份。

**别动**（删了会丢东西）：`资源使用手册.md` §3 速查 + §4 十坑 ·
`特效还原_进度与交接.md` §八 的 47 条坑表 · `规则引擎_设计.md` §6b 的规格书 ·
`战斗UI_原版对账表.md` 全篇 · `战术卡效果_移植方案.md` §一/二 的普查数据。

## 十一、悬案与「拿不到的东西」（2026-09-12 收工时整理）

> **为什么单开一节**：项目里有几件事**卡在「本地确实没有」上**，不是「没找到」。
> 这些如果散在各文档里，下一个会话会**重复挖一遍**（本轮就重复挖了两条：
> 骷髅阈值其实写在规则书、回合时限其实是字面量能读）。
> 已经查实的见 `资料/战斗规则与数值_出处.md`；**这里只放「还没解决的」**。

### A. ✅ 用户已定：这些值**去跑原版游戏** dump（新会话做）

**目标值**（都在远程包里，本地没有 —— 详见 `资料/战斗规则与数值_出处.md` §二）：

| 要什么 | 在哪个类 | 哪来 |
|---|---|---|
| `clockTimeLimit` 默认值 / `clockCountdownSec` | `ScenarioVariables` | `remotemisccontentdata_assets_all.bundle`（**远程包，本地缺**） |
| `deathTimeWarlordDuration` / `deathTimeMinionDuration` / `mulliganTimeLimit` / … 一整套动画与时限 | `VarsGlobal` | 同上 |

**做法（用户 2026-09-12 选的方案①）**：跑起原版游戏（`CLAUDE.md` 铁律 4），
**在内存里把这些 ScriptableObject dump 出来**。

- mod 源码在 `d:/2/Warpforge_tools/scenejumpshot/SceneJumpShot.cs`（924 行 + 编译环境），**能自己加探针**；
- 目标对象：**被 `BattleManager.scenarioVariables` / `globalVars` 引用的那两个单例**（`ClockManager` 也是从
  `BattleManager__get_scenarioVariables` 拿的，顺着摸得到）；
- dump 方式：`JsonUtility.ToJson(obj)` 或者反射遍历字段 —— **字段名运行时一定在**（不像磁盘上的数据被剥了类型树）；
- ⚠️ **前提是游戏能加载到它**：如果那个资产真在远程包里，**离线跑同样加载不到** ——
  **先在能跑的场景里确认它非空**（空引用就是没有），**别当已经拿到**；
- ⚠️ 拿到之后：**写进 `资料/战斗规则与数值_出处.md` §二 那张表**，并注明「从运行时 dump 的」。
  拿不到就**按规矩标成「我们挑的」**，不许写成原版数值。

### B. 还没解决的悬案（按「影响什么」排）

| # | 悬案 | 卡在哪 | 明细在哪 |
|---|---|---|---|
| 1 | **攻击方式选择器三钮的位置** | dump（三钮 `activeSelf=False`，近战在槽左下、Active/Range 叠在中心）与 `AttackSelector.cs` 文件头（「以横排为准」）**冲突**，而战斗 UI 要对局数据才出得来 | `资料/战斗UI_原版对账表.md` 第四节 |
| 2 | **七张表的数值只剩 OCR 一条来源** | 全池有多少张像 Baneblade 那样「把盾牌读成远程」，**只抽 43 张量过**（5 张错）。根治要**把四个数值圈从 1128 张卡面图里程序化读出来**（位置是固定的，可行） | `资料/卡牌数据源对账_0912.md` §六 |
| 3 | **牌堆底板的绝对位置** | 只有静态坐标表一个来源，dump 里是 stretch 读不出 —— **单一来源** | `资料/战斗UI_原版对账表.md` |
| 4 | **`special` 稀有度 → 卡框 tier 的映射** | 没实测过（原版 SO 只有 4 档），暂按 tier4 | `资料/卡组编辑_原版数值与实现方案.md` |
| 5 | **关键词字号没有原版出处** | `DescTextUnit 23.55` 是**请求值不是渲染值** → 已标「我们挑的」 | `资料/规则引擎_进度与交接.md` |
| 6 | **卡组导入导出格式 / 中文文案表** | 原版 `CardDeck.Serialize` 未反编译；中文文案是我们起的名字（版权红线） | 同上 |
| 7 | **`m_elemetsParent` 父节点变换 / `allowSelectCardLayoutOverFlow`** | 排版文档里标着「真未知」 | `资料/对战排版_原版数值与改造方案.md` |
| 8 | **奖励 / 评分数据** | **服务器侧，永久拿不到**（原版已关服）→ 只能占位或自创 | `资料/规则引擎_进度与交接.md` |
| 9 | **16 张配不上立绘的卡** | 需要人工对（用户 2026-09-12：**暂时不管**） | `Unity/资料/待人工对_插图/` |

> ⚠️ **第 2 条单独提一句**：它是唯一一条**会影响玩法正确性**的 —— 数值错了，卡的强度就错了。
> 其余几条都是「还原度」层面的。

### C. 用户说「以后再说」的

**「除了这几个，还有没有别的『我卡住了但你知道答案』的？」** —— 用户 2026-09-12：**先记着，以后再说**。
所以上面 B 表就这么留着；**真要问的时候一次问完**，别一条一条打断他。
