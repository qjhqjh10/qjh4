# 战术卡「完全不认识」47 条 · 语义查证（2026-09-13）

> 数据源：`_tmp_view/tactic_unparsed.txt` ①栏（自检产物，2026-09-13 14:04 生成，**实测 47 条**）。
> 卡名/原文另用 `Assets/RuleEngine/Resources/cards_engine.json` 与
> `资料/卡表核对_卡图提取/_合并总表.md` 对齐。阵营归属用 `_tmp_view/tactic_by_faction.md`。
> 三层权威按序查：规则书 → `d:/warpforge/scripts/rule_core.gd` → `d:/2` 反编译。

## ✅ 还剩哪些没做（2026-09-13 A4 收工时的状态）

> 🔴 **判据只认这一份**：`_tmp_view/tactic_unparsed.txt` 的 ① 栏（**自检每次跑重写**）。
> 下表是**写这一刻**的快照，只为让下一个会话一眼看到「别重复做」；**别拿它当权威**。

**本表 47 条里已经做掉的**（2026-09-13 A4 第一批，完全解析 401 → **425/449**）：
付费前缀那一族（`(5)` / `1 Also …` / `6 ☀ …` / `[icon]`）· `Give an additional +1 …` ·
`Does nothing` · `Reload the Duty abilities …` · `Lower cost by 1 when <事件>`（4 张）·
`reduce its cost **to** N` · `Lower its Health to N` · `put in play …` · `reduce` = `lower` ·
`Heal 5 and give Camouflage to a friendly unit` · `Return … and reduce its cost to 2`（**换行也是句子边界**）。
**逐条的做法与卡图实据**见 `资料/阵营推进_清单与交接.md` §一之三·候选 H（那里是正文，这里不抄）。

**还剩 20 条**（下表里没划掉的就是它）：

| 表里的 # | 句子 | 档 |
|---|---|---|
| #1 | `Trigger the abilities requiring Spirit Stones of all your troops` | 要新机制（连 A3 的 `spiritability`） |
| #2 | `Each friendly unit that is Praying heals 3` | 中等（计数） |
| #6 | `6 [Energy]: Extend effect until your next turn` | 中等（撤销 + 重结算） |
| #10 | `Each player deploys 3 troops from their deck` | 中等（对称部署） |
| #12 | `The next time it uses Ferocity this turn, it stays in play` | 中等（一次性替代） |
| #16 | `Trigger the Codex ability of a friendly unit and choose a Codicil …` | 要新机制（先数 `subtype=Codicil` 几张） |
| #22 | `Each friendly Beast gets +1 [fist] this turn and attacks a random enemy` | 中等（两个机制） |
| #27 | `This costs 1 less if you control a unit with Stealth` | 中等（`controlcount` 加关键词筛） |
| #32 | `Each of your units deals damage equal to its Shuriken to a random enemy` | 中等（通用 `_fe_count`） |
| #35 | `Take control of an enemy troop this turn and give it Fast` | 要新机制（临时控制权；反编译有真方法体） |
| #37 / #41 | `Make a damaged friendly unit attack by itself` / `Target friendly unit attacks the enemy with highest attack` | 要新机制（**强制攻击族，一起做**） |
| #38 | `Double the Melee Attack and Health of a friendly troop` | 要新机制 |
| #39 | `If it dies this turn, apply this effect to another random enemy troop` | 要新机制（挂本回合死亡监听） |
| #45 | `When you play a Stratagem, your Warlord takes 1 damage` | 缺**广播点**（事件层已建） |
| — | `Oath 4: Also destroy all damaged enemy troops` | 小（`destroy` 加「受伤的」筛选） |
| — | `For the rest of this battle, when a friendly troop uses Ferocity, …` | 同 #12 一族 |

**明确不做、也别再查第 4 遍的 3 条**（本文件 §④ 已记过一次）：`Choose an effect and give it to
a friendly troop`（`Hyper-adaptation`）· `Choose an effect and give it to all troops in your hand`
（`Infinite Biomorphologies`）· `Your Warlord heals 1 and chooses an effect`（`Exemplary Warrior`）
—— **候选效果池不在任何文本里**，原版默认成 `to_hand` 是**静默错语义**。

## 总览

| 分类 | 条数 | 说明 |
|---|---|---|
| ① 规则书有明文、可做 | 12 | 关键词表 `:161-225` / 附录 C `:310` 有对应条目，照规则书做就对 |
| ② **只有 `rule_core` 有、且它写得对** | 14 | **最便宜的一批**：规格书已写好，Unity 侧 `EffectText.cs` 正则偏窄或没串起来 |
| ③ 原版有 handler、语义清楚但要新机制 | 17 | `Resolve*` 方法名/方法体在，我们的引擎没有对应层 |
| ④ **查不到出处** | 3 | 候选池三处零命中（前轮已查过，本轮复核仍零） |
| ⑤ 单卡特例 | 1 | 卡面设计就是空效果 |

## 🔴 本批最大的发现：**14 条 `rule_core` 已经写好了，是 Unity 侧的正则太窄**

`EffectText.cs:2128` 的 `RePaid` 比 `rule_core.gd:1549` **窄两处** ——
**主对话已独立核过这两处的原文**：

| | 正则 | 差异 |
|---|---|---|
| Unity | `^(\d+)\s*(?:\[\s*(energy\|faith\|…)\s*\]\s*\|\s+)?:` | **要求方括号** `[Energy]`；**没有 `(N)` 分支** |
| `rule_core` | `(?:(?:\((\d+)\)\|(\d+))\s*\[?[Ee]nergy\]?\s*:…` | 方括号**可选**（`4 Energy:` 也收）；**有 `(N)` 分支** |

⇒ 补这两处就能收 **#8 / #23**（无括号 `N Energy:`）与 **#31 / #33**（`(N) …`）；
再加一条「`N <能量图标>` 无冒号」的规则能收 **#9 / #29**。**上限是「照 `rule_core` 抄」，几乎零风险。**

## 逐条

| # | 句子 | ×次 | 出处（文件:行号） | 建议 | 值不值得做 |
|---|---|---|---|---|---|
| 1 | `Trigger the abilities requiring Spirit Stones of all your troops` | 1 | 规则书 :210 · `rule_core.gd:3123/3471/4543` · 反编译 `BattleManager__ResolveTriggerSpiritStone` | 卡=Cosmic Serpent（`_合并总表.md:72`）。照 #31 的付费路 | 值得（灵族主题） |
| 2 | `Each friendly unit that is Praying heals 3` | 1 | 规则书 :198 · :150 · `rule_core.gd:604`（只认 `if any friendly unit is praying,`）· `:2369` | 卡=Devout Serenity（`:733`）。把 `:604` 那条从句扩成计数 | 值得 |
| 3 | `2 : Draw an additional troop` | 1 | `rule_core.gd:1567-1573`（裸 `N :` 要求换行**且** fx 含 energy/refill/gain —— 这句三者都不含 ⇒ **返回 `{}`**） | 卡=Devout Warriors（`:741`）。⚠️ **是规格书自己太紧**，不是 Unity 的锅 | 值得（1 行） |
| 4 | `4 [Energy]: Give an additional +1 [Might]` | 1 | `rule_core.gd:1618-1622`（`additional` → 保留基础 + 追加） | 卡=Fiery Conviction。**rule_core 已写好** | 值得（最便宜） |
| 5 | `8 [Faith]: Deploy an additional Battle Sister` | 1 | 规则书 :184 · `rule_core.gd:1549`（faith 变体）/`:1624` | 卡=Lead the Righteous（`:693`）。⚠️ Unity `Core/` 里 **Faith 字段 grep 零命中** —— 要先有信仰计数器 | 值得（连 #4） |
| 6 | `6 [Energy]: Extend effect until your next turn` | 1 | `rule_core.gd:1602-1604`（撤销 + 把 `this turn` 换成 `until your next turn` 重结算） | 卡=Miraculous Feat（`:750`）。**已经是对的** | 值得（最便宜） |
| 7 | `12 [Energy]: Draw 3 cards` | 1 | `rule_core.gd:1549` / `EffectText.cs:2128-2130` | 卡=Relics of Saint Katherine。⚠️ 这条**两边正则都该收** → **先跑单测确认断在哪** | 值得 |
| 8 | `4 Energy: Give +2 Attack and +2 Health instead and Heal them 1` | 1 | `rule_core.gd:1605-1617`（**连 ` and Heal them` 那个特例都写了**） | 卡=Righteous Repugnance（`:697`）。Unity 不认无括号 `4 Energy:` ⇒ **只差宽度** | 值得（最便宜） |
| 9 | `6 [icon] Give them Flank` | 1 | `关键词与图标_对照表.md:65`（`太阳`=能量）· `Sorotitas__2.md:71`（**抄录者注明卡面此处没有冒号**） | 卡=Sacred Rose（`Sorotitas__2.md:14`）。新规则：`N <能量图标> <动词>` 无冒号 = 付费激活。**先按卡图复核再动手** | 值得（1 行规则） |
| 10 | `Each player deploys 3 troops from their deck` | 1 | `rule_core.gd:2975`（`from your deck` 只给自己）· `_deploy_random_from_deck :3889` · 附录 B/C **无此卡** | 卡=Birth of a Saga（`:827`）。后半句已认 | 值得（对称部署） |
| 11 | `8 [Energy]: Give it permanently` | 1 | `rule_core.gd:1599-1601`（撤销基础临时增益 + 去时长重结算） | 卡=Daemonbreaker（`:689`）。**已经是对的** | 值得（最便宜） |
| 12 | `The next time it uses Ferocity this turn, it stays in play` | 1 | 规则书 :186 · `rule_core.gd:70`（kw 表） | 卡=Bjorn's Shrine（`:809`）。要「一次性替代」 | 中等 |
| 13 | `Lower cost by 1 when an enemy dies` | 1 | `阵营推进_清单与交接.md:81-84`（**已指名这一族**，监听器已收下、还没接 `ctx.CostMods`） | 卡=Burgeoning Empire（`:907`） | **最值得（规划已定）** |
| 14 | `Choose an effect and give it to a friendly troop` | 1 | **查不到**（`EffectText.cs:1642-1647` 前轮已记：卡面 · 卡表 · 反编译 1800 个带方法体文件**三处零命中**） | 卡=Hyper-adaptation（`:969`）。**维持判不认识**（原版默认 `to_hand` = 静默错语义） | 不做 |
| 15 | `Choose an effect and give it to all troops in your hand` | 1 | 同上 | 卡=Infinite Biomorphologies（`:970`） | 不做 |
| 16 | `Trigger the Codex ability of a friendly unit and choose a Codicil and put it in your hand` | 1 | 规则书 :175 · 反编译 `BattleManager__ResolveTriggerCodex` / `BroadcastUnitCodex` · **`Codicil` 在 `decomp_out{,2}` 零命中，但卡表 `subtype` 里有** | 卡=Author of the Codex（`:1002`）。后半句候选池 = UM 的 `subtype=Codicil` 卡。**先数有几张** | 值得（先数池） |
| 17 | `Lower cost by 1 when a friendly Troop or Stratagem trigger [Codex icon] Codex` | 1 | 规则书 :175 · `resolve_cost_act :1794` · 反编译 `AddTriggerCodex`/`BroadcastUnitCodex` | 卡=Avenging Zeal（`:874` 一带）。同 #13 | 值得（连 #13） |
| 18 | `Give +2 Health and Camouflage` | 1 | **规则书 :179 明列四种黑暗契约** · `rule_core.gd:1655-1660`（`DARK_PACT_FX["fate"]` **一字不差**就是这句） | 卡=Dark Pact of Fate。**别当普通 give**：这 4 条只经 `_grant_dark_pact :1663` 结算 | 值得（走 dark pact 通道） |
| 19 | `Give +2 Melee Attack and Vanguard` | 1 | 规则书 :179 · `rule_core.gd:1657` | 卡=Dark Pact of Blood | 值得 |
| 20 | `Give +2 Melee Attack and +2 Ranged Attack` | 1 | 规则书 :179 · `rule_core.gd:1658` · `_合并总表.md:228` | 卡=Dark Pact of Excess | 值得 |
| 21 | `Give +1 Health and Regeneration 1` | 1 | 规则书 :179 / :201 · `rule_core.gd:1659` | 卡=Dark Pact of Resilience | 值得 |
| 22 | `Each friendly Beast gets +1 [fist] this turn and attacks a random enemy` | 1 | 规则书 :145 · `rule_core.gd:1341` `_fe_count`（无 `beast` 计数字） | 卡=Let Loose（`SpaceWolves__2.md:25`）。要两个机制 | 中等 |
| 23 | `5 Energy: Draw a card` | 1 | `rule_core.gd:1549`（`\[?[Ee]nergy\]?` 括号**可选**）· `EffectText.cs:2129`（括号**必选**） | 卡=Moment of Grace。**rule_core 对、Unity 窄** | 值得（最便宜） |
| 24 | `4: Give an additional +1 Health` | 1 | `rule_core.gd:1618-1622` | 卡=Beacon of Faith（`Sorotitas__2.md:18`）。**已经是对的** | 值得（最便宜） |
| 25 | `Return a friendly troop to your hand and reduce its cost to 1` | 1 | 规则书 :231 · `EffectText.cs:2313-2319`（**已如实判不认识**）· `rule_core.gd:2896-2911` | 卡=Lying in Wait（`:5`, Genestealers）。要新原子「**设为** N 费」（不是「降 N 费」） | 值得（新原子小） |
| 26 | `Heal 5 and give Camouflage to a friendly unit` | 1 | 规则书 :173 · `rule_core.gd:2838-2861`(heal) + `:3011-3062`(give) | 卡=Evasive Manoeuvre（`:80`）。**两条都在**，只是没串联 | 值得（最便宜） |
| 27 | `This costs 1 less if you control a unit with Stealth` | 1 | 规则书 :211 · `rule_core.gd:3826` `_cost_what_matches` · `EffectText.cs:2769`（`controlcount` 只数数量、不筛关键词） | 卡=Fate Inescapable。要给 `controlcount` 加关键词过滤 | 中等 |
| 28 | `Return a friendly troop to your hand` / `Lower its cost by 2` | 1 | `_合并总表.md` 抄录注（卡面换行无标点）· `rule_core.gd:2910`（`last_returned` 就是为这张留的）/`:2939` · `EffectText.cs:2077` | 卡=Fire and Fade（`:3`）。**两边各有一半**：rule_core 只认 `costs N less`、Unity 只认 `Lower its cost by N` | 值得（最便宜） |
| 29 | `1 Also give it Shield` | 1 | `关键词与图标_对照表.md:66`（`绿圈N` = 本行费用 N）· 卡面是 `【图标:绿圈1】Also give it Shield` | 卡=Forewarned（`:93`）。同 #9 | 值得（连 #9） |
| 30 | `Spend all your Spirit Stones` | 1 | 规则书 :210 · `rule_core.gd:3126-3139` | 卡=Hosts of the Dead。**已写好但硬编码**成「每颗部署 1 Wraithguard」；建议泛化成 `For each one, <效果>` | 值得 |
| 31 | `(5) Reduce their cost by 5` | 1 | `rule_core.gd:1549`（`\((\d+)\)` 有）· `EffectText.cs:2129`（**没有 `\(`**） | 卡=Reclaim the Stars（`:83`）。**Unity 正则加一个括号分支** | 值得（1 行） |
| 32 | `Each of your units deals damage equal to its Shuriken to a random enemy` | 1 | 规则书 :207 · `rule_core.gd:2679`（只特例了 `equal to your faith`） | 卡=Sudden Assault（`:54`）。要通用 `_fe_count` | 值得（灵族主题） |
| 33 | `(1) Draw a card` | 1 | 同 #31 | 卡=Will of Asuryan（`:67`） | 值得（1 行） |
| 34 | `Lower cost by 1 when you deploy a Vehicle` | 1 | `阵营推进_清单与交接.md:81-84`（三张之一） | 卡=Mechanised Infantry（`:805`） | **最值得** |
| 35 | `Take control of an enemy troop this turn and give it Fast` | 1 | 规则书 :185 · 反编译 **`ResolveStealMinion` / `AddStealMinion` 有真方法体** | 卡=Telephatic Domination。要新机制（临时控制权） | 中等（贵，1 张） |
| 36 | `Reload the Duty abilities of all your units` | 1 | 规则书 :181 · `rule_core.gd:2912-2931`（**一字不差**） | 卡=Press the Attack（AstraMilitarum）。直接照抄 | 值得（最便宜） |
| 37 | `Make a damaged friendly unit attack by itself` | 1 | 规则书 :145 · 反编译 `ResolveForceAttackFriendly` / `ResolveForceAttack`（**有真方法体**） | 卡=Murderous Desires。和 #41 同族 | 值得（一族一起做） |
| 38 | `Double the Melee Attack and Health of a friendly troop` | 1 | 规则书 :230 · 反编译 `ResolveChangeBaseAttack` / `ResolveChangeMaxHealth` | 卡=Possession。要「当前值翻倍」+ 同步生命上限 | 中等 |
| 39 | `If it dies this turn, apply this effect to another random enemy troop` | 1 | 规则书 :237 · `rule_core.gd:2596-2608`（G7 条件 create）· `:2601` `_last_pick` | 卡=Spreading Corruption。要「挂本回合死亡监听」 | 值得（主效果已认） |
| 40 | `Lower its Health to 1` | 1 | 反编译 `ResolveChangeMaxHealth` | 卡=Eternal Servitude。要「设当前生命 = 1」原子 | 值得（便宜） |
| 41 | `Target friendly unit attacks the enemy with highest attack` | 1 | 规则书 :145 · 反编译 `ResolveScriptedTargetCardAttack` | 卡=Peerless Bladesmen。同 #37 | 值得（连 #37） |
| 42 | `If target dies, put in play one "Infractor"` | 1 | **规则书附录 C :310 明列「违规者（Infractor）」**是混沌兰德袭击者生成的 9 种之一 · `rule_core.gd:2596-2608` | 卡=Rapid Evisceration。只差动词 `put in play one` 没归到 create | 值得（便宜） |
| 43 | `Return up to 5 friendly troops that died this game to your deck and lower their cost by 3` | 1 | 规则书 :231 · `EffectText.cs:2219`（只有「从弃牌堆 Deploy」没有「回牌库」）· `rule_core.gd:2896`（只 `to hand`） | 卡=Eve of the Uprising。要「墓地→牌库」+ 批量 + 降费 | 中等 |
| 44 | `Does nothing` | 1 | `EffectText.cs:2769`（`noeffect` 条件已有，但没有「整句无事」的 op） | 卡=Improvised Barricade。**原版设计就是空效果**（规则书 :204 Sabotage 生成的假卡） | 值得（1 行，⑤ 里唯一一条） |
| 45 | `When you play a Stratagem, your Warlord takes 1 damage` | 1 | `阵营推进_清单与交接.md:86-88`（`damaged` 事件没广播）· 反编译 `BattleManager__BroadcastTacticPlayed` 有 | 卡=Jammed Communications。事件层已建（`Core/WhenEvent.cs`，31 张），缺「对手打出战术」广播点 | 值得 |
| 46 | `Lower cost by 1 when a friendly troop dies` | 1 | `阵营推进_清单与交接.md:81-84`（三张之一） | 卡=Vengeance for Cadia（`:941`） | **最值得** |
| 47 | `Your Warlord heals 1 and chooses an effect` | 1 | 规则书 :218 / :233 · **查不到候选效果池**（同 #14） | 卡=Exemplary Warrior | 不做 |

## ④ 查不到出处的（**不要编，如实列**）

**这 3 条不能当「原版就是这样」** —— 前轮已在 `EffectText.cs:1642-1647` 记过查证过程，本轮复核结论不变：

| 句子 | 卡 | 查过哪 |
|---|---|---|
| `Choose an effect and give it to a friendly troop` | Hyper-adaptation（Leviathan） | 卡面 · `cards_engine.json` · `d:/2` 反编译带方法体文件 —— **三处零命中** |
| `Choose an effect and give it to all troops in your hand` | Infinite Biomorphologies（Leviathan） | 同上 |
| `Your Warlord heals 1 and chooses an effect` | Exemplary Warrior（UM 天赋） | 同上（同一缺口：候选效果池不在任何文本里） |

⚠️ **原版在这里会默认成 `to_hand`** —— 那是**静默的错误语义**，我们宁可报不认识。
⚠️ **#16 的 `Codicil` 不算查不到** —— 它在卡表 `subtype` 字段里有（`_合并总表.md:1005`），
候选池 = UM 的 `subtype=Codicil` 卡，**先数张数**再说。

## 两条「不是查不到、但要先核」

- **#7 `12 [Energy]: Draw 3 cards`** —— 理论上两边正则都该收，**建议先跑单测确认断在哪**，别直接改。
- **#9 `6 [icon] Give them Flank`** —— **卡面本身漏印冒号**（`Sorotitas__2.md:71` 抄录者注）。**动手前按卡图复核。**

> ⚠️ `rule_core.gd` 是**我们的规格书、不是原版**；#12/#27/#32/#38/#40 给的是
> 「语义清楚但机制得新建」，**别当成「原版就是这样」**。
