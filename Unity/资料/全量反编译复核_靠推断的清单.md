# 全量反编译复核 —— 「只能靠推断」的清单（2026-09-18 建立）

> **这份讲什么**：2026-09-18 之前，游戏代码只有约 **10%** 被反编译，**而且一个协程体都没有**。
> 所以历史上大量结论只能写成「**我们挑的**」/「**查不到**」/「**按语义重写**」。
> 现在代码全反编译了（`D:/2/tools/decomp_full/`，`DECOMP DONE ok=26275 fail=7`，含 **401 个 `MoveNext`**）
> ⇒ **这份就是把那些「当时只能推断」的地方逐条拿出来，看现在能不能定案。**
>
> 🔴 **本文只做「能不能定案 + 去哪查」，不动手改。** 动手前照旧：先读原版、再改、再跑自检。
> 查法与判读前提见 `资料/全量反编译_入口与用法.md`。

---

## 〇、三条历史教训（本次又各命中一次）

1. 🔴 **「全库 0 命中」多半是搜错了目录。** 本次最典型：`战斗UI_原版对账表.md:184` 判
   「5 个卡面高亮状态色在 `d:/2/解包整理`（44.9 万文件）**全库 0 命中**」——
   **实际它们在卡牌预制体里，不在战场场景里**。这条**推翻了该文件 §三点六 一整节的前提**。
2. **「查不到」有时是「没去查」。** `AnimFX_实现与接线.md` **§八之补**记的 `*FromCamera`「查不到」，
   （⚠️ 原来这里写「`:213`」—— **行号是错的**，`:213` 是空行；真出处是 §八 的就地更正与 §八之补 那块）
   2026-09-18 一查就有（坐标是**场景里手摆的固定 Transform**）。
3. **「查不到 ≠ 没有」** —— `VarsGlobal` 那次已经付过学费。

---

## 一、最该先复核的 10 条（按「错了玩家看得见」排序）

| # | 条目 | 现在的状态 | 原版出处 | 错了会怎样 |
|---|---|---|---|---|
| 1 | **卡面高亮 5 态色 + 时长** | **自挑的 6 态色** | `08_预制体特效/战斗预制体/MonoBehaviour/MonoBehaviour_-3885077450169410624.json` | **画面错**（高亮色全错） |
| 2 | **阵营资源显隐判据** | 「有值就显示」 | `PlayerManager__ResetMana.c:107,113,140` | **静默**：该出现的资源条不出现 |
| 3 | **投降按钮 + 音量三滑块** | 只有键盘 `F`；滑块没做 | `BattleSettingsWindow`（`dump.cs:76506` `resignButton // 0x70` · `:76516-76520` 三个 `Slider`） | 缺功能 |
| 4 | **Attack→Hit 时长** | 顶替值 | `CardScript._AttackMeleeAnim_d__357__MoveNext.c`（313 行）· `._ResolveAttackRangedAnim_d__360__MoveNext.c`（182 行） | 整场战斗节奏不对 |
| 5 | **`ChoosePanel` 三条标题 + `isEnviromental` 分叉** | 文案是自己写的 | `RawCardScript__IsEnviromentaleffect.c` · `ChooseCardMenu__SetUpTitleText.c` | 文案错 |
| 6 | **`GetPushBackFactor` / `GetUnitSize`** | 顶替值 | `SupportMethods__GetPushBackFactor.c`（`2/3/else` 三分支）· `SupportMethods__GetUnitSize.c`（按 `CurrentMeleeAttack` 3/7 分档） | 手感错 |
| 7 | **强行触发是否绕 `energy==0`** | 我们挑的 | `AbilityLogic` 31 个 `.c` | **静默**：卡效果不生效 / 规则错 |
| 8 | **选牌面板卡行位置 + `CountdownTrigger`** | 部分自己摆的 | `ChooseCardMenu._SetupChooseCardsUi_d__22__MoveNext.c` · `__CountdownTrigger.c` | 面板一看就不像 |
| 9 | **`CardDeck` 导入导出格式** | **自己设计的** | `CardDeck__Serialize.c` · `CardDeck__DeserializeDeckString.c` | 玩家分享串读不了 |
| 10 | **临时卡「回合结束扫掉」的时机** | 本地查不到、规则书没写 | `grep -rl "Ephemeral" decomp_full` + `turn_end` 链路 | **卡会赖在手里不走，画面看不出来** |

---

## 二、完整清单（按「现在能不能定案」分组）

### 2.1 ✅ 现在**能定案**（原版有确切答案，可直接改）

| 条目 | 原件出处 | 严重度 |
|---|---|---|
| 卡面高亮 5 态色 | `MonoBehaviour_-3885077450169410624.json`：`ValidTarget(0,1,0.129)` · `Selected(1,1,1)` · `Playable(1,1,0)` · `SelectedTarget(1,0.518,0)` · `Regular(1,1,1,0)`；态映射 0/1/2/3/4/5 → Regular/Valid/Selected/Valid/SelTarget/Playable | 画面错 |
| `CardHighlightAnimTime` / `ScaleFactor` | 同文件：**0.1 / 1.05** | 画面错 |
| 阵营资源显隐判据 | `PlayerManager__ResetMana.c:107,113,140` —— 判据是 **`RawCardScript.Uses{SpiritStone,Faith,QuestPoints}`（按牌组能力，不是阵营表）**；任务点另看 `matchData+0x4b` 选 `scenarioVariables+0x2c/0x30` | 静默 |
| 音量三滑块 | `dump.cs:76516-76520`（music / soundFX / voiceOver `Slider`） | 缺功能 |
| `ChoosePanel` 三条标题 | `RawCardScript__IsEnviromentaleffect.c` · `ChooseCardMenu__SetUpTitleText.c` | 文案错 |
| 卡组导入导出格式 | `CardDeck__Serialize.c` · `CardDeck__DeserializeDeckString.c` | 兼容性 |
| 轻/重击幅度分档 | `SupportMethods__GetPushBackFactor.c` · `CardScript__ReceiveAttackAnim.c:28` · `BattleManager._ResolveUnitShake_d__472__MoveNext.c:144`（3 个常量需读 DLL） | 手感错 |
| 单位体型分档 | `SupportMethods__GetUnitSize.c` | 手感错 |
| 强行触发绕不绕 `energy==0` | `AbilityLogic` 31 个 `.c` | 静默 / 规则错 |
| 选牌面板卡行纵向位置 + `SetupChooseCardsUi` | `ChooseCardMenu._SetupChooseCardsUi_d__22__MoveNext.c` | 画面错 |
| 选牌倒计时为何触发 | `ChooseCardMenu__CountdownTrigger.c` | 画面错 |
| **Attack→Hit 时长** | 两个协程体全可读（313 + 182 行） | 手感/节奏错 |
| `CardChoicePanel.ToggleEye` | `MulliganManager__ToggleMulliganVisibility.c` | 画面错 |
| `CardTextsController.ChangeState` 的触发 | `CardTextsController__ChangeState.c` | 画面错 |
| 临时卡「回合结束扫掉」时机 | `grep -rl "Ephemeral" decomp_full` + `turn_end` 链路 | 静默 |
| `CosmeticItemCardback` 两个 Sprite 的区别 | `CosmeticItemCardback` 4 个 `.c` | 画面 |
| `TextTooltipController.GetFaithTooltip` | `TextTooltipController` 33 个 `.c` | 文案错 |
| 回手动画（未实现） | `BattleCardUI__PlayBackToHandAnimation.c` | 画面错 |
| **`AnimFX` 的 `sounds` 937 条** | `AnimFXController__Update.c:31-70` + `PlaySoundOnTime__Update.c:29-41`（数据在 `animfx_modules.json`） | 画面错 |
| **`AnimFX` 18 类整体** | 已复核，见 `资料/AnimFX_实现与接线.md` §十一 | 画面错 |
| `mob` 广播 645 支 / 对称部署顺序 / `DoBecome` 随机 | `grep -rl "BroadcastUnitMob" decomp_full`（⚠️「机制不同、别照改」的结论仍成立） | 静默 |
| `SetupCardInHand` / `AddHandEffect`（手牌挂效果） | `BattleManager__SetupCardInHand.c` · `__AddHandEffect.c` | 静默 |
| 奇迹骰 `Miracle` | `grep -rl "Miracle" decomp_full` ⇒ **真没有**（本作未实现） | 无所谓 |
| 13 阵营占位底色 | **原版没有「底色」字段** ⇒ 保留 | 无所谓 |

### 2.2 🟡 部分定案（机制/触发点能定，**资源或数值仍缺**）

> 🔴 **2026-09-18 用户点名：这一档「多半是还没找到，不是没有」—— 全部记成「以后再找」，不许当结论。**
> 依据是本文 §〇 第 3 条教训（「查不到 ≠ 没有」），而且本会话已经**三次**验证了这句话
> （卡面高亮那 5 个色、「全库 0 命中」是搜错目录 · `*FromCamera` · `VarsGlobal`）。
> ⏭ **下一轮再找时**：先换目录/换关键词，再用 `decomp_full` 追调用链，最后才谈「本地没有」。

| 条目 | 已定案的部分 | **仍缺什么（待再找）** |
|---|---|---|
| `Shade` 压暗层 | 类**有了**（`Shade__SwitchShade.c` · `dump.cs:101715`），带 `onAlphaLevel` / `defaultTimeToSwitch` | **色板**不在 `Shade` 类里 ⇒ 换目录找「谁给 `Shade` 赋色」 |
| 投降按钮 `rect` | 类与回调能定（`BattleSettingsWindow.resignButton // 0x70` · `BattleSettingsWindow__ResignButtonOnClick.c`） | **rect** 仍缺 ⇒ 试 `新解包资源/assets_full` 的**卡/界面预制体**（别再在战场场景里找） |
| 临时卡的 glitch 视觉 | 机制能定（`Card2DController__ToggleGlitch.c`） | **`glitchMaterial` 素材** ⇒ 按 PathID 反查，别按名字 |
| 催促表现 | 触发点已定（`ClockManager__ChangeMaterialHurryUp.c` 读 `+0xc0`；`DisplayHurryUpChatMessage` = chat **idx 2**） | **`timeRunningOutMaterial`** ⇒ 同上，按 PathID 反查 |
| `AnimFXModuleEvent` 的 UnityEvent 调什么 | dump 把 `m_Calls` 截断了 | 读 prefab **原始 JSON** 的 `m_Calls` |
| 单位语音条「事件→台词后缀」 | 各族时机 2026-09-18 已定案 | **`wp` 语义**仍未证实 |
| 中英 I2 语言表 | 字符串表只有字面量 | 换 `all_strings.txt`（**`grep -a`**）+ 查 `I2Languages` 资产 |
| `VarsDevice` 整张表 | — | 整张表没读（照 `工具/read_varsglobal.py` 的做法） |
| 关键词字号 3.75% | `DescTextUnit` 的 23.55 是**请求值不是渲染值** | 要**渲染**才量得出（属 Unity 活，不是查证） |
| PnP 62/63 个预组 id→名 | `CardDeck__DeserializeDeckString.c` | 本地没有任何 id→名的表 ⇒ 再想想是不是换了个名字 |
| `UseSpiritStoneEnergy` 无调用者 | 全量后**仍 0 调用者**；支付链已可查（`BattleManager__PayCardCost` 走 `UseMana`） | **扣石到底在哪** —— 试追 `ResolveUseWaystone` 那条协程 |
| ✅ **`zh_CN.csv` 的来源**（2026-09-18 **已定案**） | **结论：原版客户端根本没有中文表** —— 实测 `d:/2/新解包资源/assets_full` + `unity_run_ref/Warpforge_Data` 共 **246,807 个文件**，扫 `选择语言`/`游玩`/`收藏`/`你的督军本回合获得`/`眩晕` **零命中**；游戏安装目录 **84 个 bundle 里没有任何本地化包**（名字带 `local` 的两个是宣传图）；反编译查实 I2 词条表是**远端 `localization_assets_all.bundle`（CCD）**，**从来没下到本地**。⇒ **我们那份中文是自己译的** | ✅ **不用再查**。⚠️ 但**两处措辞要改**：`gen_cards_engine.py:38` 把它称作「**官方本地化**」· `cardface_fixes.json` 几处写「**官方中文…**」并当**权威判据**用 —— 这两个说法都站不住（已列入「相关措辞修正」） |

### 2.3 ❌ 仍查不到（**不是**「有意不做」）

> ⚠️ **2026-09-18 更新**：本节原来叫「仍查不到 / **有意不做**」，其中「中文文案表」那条被归成「有意偏离」。
> **现在定案：那不是「有意不做」，是「原版客户端根本没有中文表」**（见 §2.2 的 `zh_CN.csv` 那格）。
> 而且用户已**取消版权红线**（个人学习用途）⇒ **以后别再拿「版权」当「有意不做」的理由**。

| 条目 | 为什么 |
|---|---|
| `AnimFX` 的 `ChangeShapeAngle` 锥角 | **仍不能逐位还原**（97/314 实例）；保持「不改 + 警告 + 计数」可接受（线索：`BattleParticleColliderManager` 的 `playerMinionCollider(0x28)` / `enemyMinionCollider(0x40)`） |
| `ScoringCriteria` 权重值 | 值挂在卡资产、**在服务端** ⇒ 结构已定案，权重只能我们自己配 |
| `overtimeTurn` / `overtimeStartTurn` 数值 | LiveOps **全盘 0 命中**，18 个值全在服务器（判据已由用户给定，不再缺数） |
| 中文文案表 | 🔴 **2026-09-18 定案：原版客户端就没有中文表**（246,807 个文件零命中 · 84 个 bundle 无本地化包 · I2 表在远端 CCD）⇒ **只能自己译**。⚠️ **不是版权问题**（红线已取消），是**原版就没有** |

---

## 三、已经定案、**别再查**（2026-09-18 用全量反编译收口）

`cantdo` 触发点（`NotifyCantDoAction` + 14 个调用点）· `gen4..gen7`（死素材）· `backup1..6`（录音交付件）·
ChatPopup 6 钮（= 5 + 下标）· `intro`/`mirror`/`vs*` 时机 · 「语音 2s 截断」误判（实为**气泡最短停留 2s**）·
`mulliganSeconds` / `MulliganPanel` 眼睛与键盘 · `TargetReticle` 弧线 · `CardFeel.PopNumber` 纯白 ·
`BadgeCounterInk` 暖白 · `timeToPositionCard` · `AI` 整类 · `PlayBackToHandAnimation` ·
**AnimFX 18 类**（见 `AnimFX_实现与接线.md` §十一）· **`Collisions` 的 `*FromCamera`**（坐标 = 场景手摆固定 Transform）。

⇒ 明细见 `资料/全量反编译_入口与用法.md` §三 与 `资料/语音线_原版规格与ASR管道.md` §一。
⚠️ **还有一批在上面的清单里出现过、但这两节里没有的**（`mulliganSeconds` · `MulliganPanel` 眼睛与键盘 · `TargetReticle` 弧线 ·
`CardFeel.PopNumber` 纯白 · `BadgeCounterInk` 暖白 · `timeToPositionCard` 等）—— 那些明细在 **`资料/自设计清查_0917.md` §一**。
