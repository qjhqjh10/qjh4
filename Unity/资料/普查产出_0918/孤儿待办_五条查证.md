# 孤儿待办 · 五条只读查证（2026-09-18）

> 五条都出自 `项目任务.md` **之外**的交接文档尾部。全部**只读**，未改任何代码 / 数据。
> 🔴 汇总：**两条的文档状态已过期**（② 后半句 · ③ 全部）· **两条查出真 bug**（① 导出器 + 运行期接线 · ④ 抽取源）· 一条判「资产在、但不能直接用」（⑤）。

---

## ① `textureSheetAnimation` sprite 列表 —— 两件事被记混了

**结论**：**卡背为空不是导出器没导** —— 原版那批卡背粒子的序列化数据里 `sprites` **本来就是空**（单元素 `m_PathID: 0`），卡背是**运行期** `AnimFXModuleCardback.Initialize` → `BattleManager.GetCardback()` → `tsa.AddSprite` 塞的。我们的真因是 `WFModuleCardback.CardbackResolver` **全工程只声明、从未赋值** ⇒ `ResolveCardback()` 返 null ⇒ `:120` 早退，`AddSprite` 一次没跑。导出器**确实**漏了 tsa sprite 列表，但那是**另一个真 bug**，丢的**没有一个是卡背**。

**数量（自己数的）**：「30 实例 / 18 效果」**成立**（12 个效果各 2 + 6 个各 1）。受 tsa 导出缺口影响的是 **143 个 PS / 99 个效果**（原版 VFX 包 383 个 PS 带非空列表，其中 `enabled && mode=SingleRow`）。

**动哪里**：① `EffectExporter.cs:344` 后（`Export(GameObject)` 内、SpriteMask 循环下一行）补 tsa 循环，复用现成 `ImportSprite`（`:876-901`）；② `WarpforgeEffectBinder.cs` **不用改**；③ 要让卡背不空 → 给 `WFModuleCardback.CardbackResolver` 接线（`:78` 声明 / `:170-181`）。

**实据**
- `EffectExporter.cs:341-344`（只导 `SpriteRenderer`/`SpriteMask`；`textureSheetAnimation|tsa|AddSprite` 全文 **0 命中**）· `:335-340` 注释已写「不导会变 guid 全 0 伪引用」
- 死引用实证 `WarpforgeVFX/Prefabs/OrkBuff_Recall.prefab:1548` · `DutyEffect.prefab:7007`；**卡背侧无损失**：`CreateCard DA.prefab:1547` 与原版**逐字节一致**
- 原版空表实证（UnityPy 直读 battleprefabs_vfxandmisc 包）：`CreateCard` / `CreateCard Orks` / `CreateCard_Tau` / `Wild Rider Chieftain` / `DeckBuff_MoveToTop` / `CreateCard DA` **同一形状**
- `WFModuleCardback.cs:45-51`（**注释是错的，就地更正**）· `:78`（无赋值点）· `:120,130`（早退）· `:163-166`（`Apply` 本身是对的）
- `AnimFXModuleCardback__Initialize.c:37` · `dump.cs:48151-48166`（唯一字段 `particleSystemsToAddCardback`，**无 cardback 字段** ⇒ 只能来自运行期）
- 📌 **文档要改**：`资料/特效还原_进度与交接.md:224-228` 的前提（「原版带 sprite 列表」）与实测不符

**查不到的**：`CosmeticItemCardback.GetCardBackSprites` 未反编译（`Item1/Item2` 哪个是卡背）· `GetCardback` 第 3 实参语义未独立证实 · **未做运行时验证**（「99 个效果渲染异常」是从 prefab 文本死引用 + 原版 UV 状态推的）。已排除：`EffectExporter.cs` 全文 · `WarpforgeEffectBinder.cs` 全文 · 全部 84 个原版 bundle 的 `UVModule` · 全仓 `grep -rn CardbackResolver`。

---

## ② `Stealth Drone` / `Master of Ordnance` 那类 `atturn` 单位卡

**结论**：原版**没有 `atturn` 这个值** —— 是 `AbilityTrigger.TurnStart=30 / TurnStartPlayer=31 / TurnEnd=40 / TurnBeforeEnd=41 / TurnStarted=160`；四个广播器**结构一样**：先扫「场上所有卡」（过 `IsInPlay`）→ 再扫当前行动方手牌 → 再扫另一方手牌 ⇒ **原版场上单位照样吃**。我们**棋盘那半已经在**（A5 批 2，`EffectResolver.cs:3239-3262` + 真断言）⇒ **交接文档 `:501` 后半句「这一层还没有」已过期**。真坏的是**手牌那一趟没判卡类型** ⇒ **11 张单位卡被判成「手牌陷阱」**。缺的是**结算层一个类型卫**，解析层收得下。

**被判错的 11 张**（全 `type=unit`）：`Master of Ordnance`(AM15) · `Konstrictus Tormentor`(EC11) · `Wurrboy`(GOF89) · `Beast Snagga Nob`(GOF81) · `Sororitas Rhino`(SOR70) · `Gun Drone`(TAU10) · `Stealth Drone`(TAU14) · `DS8 Support Turret`(TAU18) · `Biovore`(TL14) · `Genestealer`(TL16) · `Spore Mine`(TL8)。另 8 张单位卡因 `SplitAtTurn` 三条窄化（多句 / 后缀式 / `each`）**没被误判**。

**后果三条**：① 这 11 张在手里被①排队 → `source=null`（`:3282`）→ `ctx.ActingUnit=null`（`:218-219`）→ 回落也拿不到（`:533`）⇒ `Self` 锚点失败、空过 + 进 `unresolved`；② `Coverage` 把它们记成「完全解析 + 有机制」并 `continue`（`EffectText.cs:1380`）⇒ **账虚高 11 张**；③ `DeckBuilder` **不受影响**（`:254` 先挡非战术卡）。

**动哪里**：① `EffectText.cs:3528` `IsHandTrap(CardDef)` 开头补 `if (c.IsUnit) return false;`（**照抄 `:3495`**，同一处修掉 Coverage 假账）；② `EffectResolver.cs:3228` 手牌扫描加 `if (c.IsUnit) continue;`。⚠️ **别把 ① 改成 `IsHandTrap(c)`** —— 会连带 `SplitHandTrapWhen`（`When …` 型事件陷阱），消费点是 `BroadcastHandTrapWhen`，会**双重触发**。单位那一半（②）不用新写。

**实据**
- 卡面逐字（**开图核过**）：`Stealth Drone` = `Flying. Stealth. At the end of your turn, give Stealth to an adjacent troop`（`D:/2/Warpforge部队卡片/Tau/3部队/Warpforge_14_Stealth-Drone.png`）· `Master of Ordnance` = `At the end of your turn, deal 3 damage to a random enemy for each friendly Vehicle in play.`（`.../Astra Militarum/3部队/Warpforge_15_Master-of-Ordnance.png`）—— 两条与 `cards_engine.json` 逐字一致
- 原版链：`dump.cs:20243-20246,20262` · `BattleManagerSupport__BroadcastTurnStart.c:25,29 / :33,39 / :42,51`（End / BeforeEnd / Started 三个文件**行号逐个相同**）· `CardScript__OnTurnStart.c:137,144` · `__OnTurnEnd.c:64,72,80`
- 无在场门槛：`AbilityLogic__MatchingTrigger.c`（31 行纯枚举比对）· `CardAbility__CanTriggerAbility.c`（`IsInPlay` **0 命中**）
- 我们侧：`EffectResolver.cs:3249-3262`（棋盘那半是对的：`each` 双方触发 / `you` 只在控制者）· `:3228`（手牌越界）· `EffectText.cs:3450-3456` / `:3525-3529`（都无 `IsUnit`；同族 `:3495` **有**）· 自检 `RuleEngineTest.cs:7420-7516`
- 📌 **文档要改**：`资料/阵营推进_清单与交接.md:501` 后半句

**查不到的**：原版 `CardAbility` 的 SO **不在本地**（远程 CCD）⇒「这些卡各挂 30/31/40/41 哪一个」没查到（只能证**广播范围**与枚举值）· `TurnStartPlayer` 场上那趟的 `param_2` 被 Ghidra 丢了实参，**读不准，别当结论**。已排除：`RuleCore.ResolveDeploy`（`:3581-3602` 只消费 `Trigger=="deploy"`）· `DeckBuilder`（`:51,226,254`）。

---

## ③ `Armorium Cherub` 整段载荷缺失 —— **已过期，数据与解析都不缺**

**结论**：卡面逐字 = `Flying. Rally: Give +2 ⟨紫枪⟩ to a friendly unit this turn. At the start of your turn, gain 1 ⟨金太阳⟩ and return to your hand`（`Flying`/`Rally` 是关键词，「回手」**不是** —— 规则书关键词表里没有）。`cards_engine.json` 的 `SOR67` 与卡面**一处不差**（只差图标写法：卡面画图、我们写 `Ranged`/`☀`）。**「`ReAtTurn` 整段锚定」在今天的代码里已不成立** —— 那条老正则（`60fee33^` 的 `EffectText.cs:2382`）已在 `60fee33`（2026-09-14 A5 批 2/3）换成**逐分句**的 `AtTurnClauses`（`:3383` 循环 `Split(desc)`）；`ReAtTurnPre/Suf`（`:3424`/`:3430`）虽仍带 `^…$`，但**锚的是一条分句** ⇒ 前面加 `Flying. Rally: …` **不会**打断。⇒ **只改文档，不动代码。**

**两半都不缺（探针实测）**：`_tmp_view/probe_out.txt:3891-3904`（2026-09-16，卡池 1126）判该 desc `op 2 条 · 不认识 0 · 半懂 0` —— `give +2 ranged 时长=turn`（Rally 半）+ `atturn[turn_start] → gainfaith n=1 + 尾句 return 去处=hand`（回手半）。同形状 `Grot Orderly` 一样 2 op / 0 不认识。

**实据**
- 卡图：`D:/2/Warpforge部队卡片/Sorotitas/3部队/Warpforge_03_Armorium-Cherub.png`（1 费 / 1 近战 / 0 远程 / 2 血 / Infantry）；项目内逐张表 `资料/卡表核对_卡图提取/Sorotitas__1.md:17` · `资料/PnP卡图_逐张对账_0915.md:123`（「枪 = 远程」判据）
- 规则书 `:188`（飞行）· `:200`（集结）· `:184`（信仰）· `:231`（送回牌库）；**没有**「回手」关键词
- 解析器：`EffectText.cs:802-818`（切句）· `:3324-3347`（`TryAtTurn`，宽）· `:3383-3413`（逐分句）· `:3450-3456`（`SplitAtTurn` 窄 —— Cherub 3 句 ⇒ **正确地**不判成手牌陷阱）· `:6108,6154-6167`（`return to your hand` 走 `ReReturnBare` 接住）
- 结算层活着：`EffectResolver.cs:3249-3262` · `:3293`（施放者传该单位 ⇒ 自我指代对）· `CardDef.cs:117,288,302`（Rally 走触发层）
- 上游原始表还写着错 token：`card_stats.json:21678`（`[Attack]`/`[Energy]`），被 `cardface_fixes.json` 的 `desc.Armorium Cherub` 盖掉 ⇒ 引擎读不到
- 📌 **文档要改**：`资料/阵营推进_清单与交接.md:326` · `cardface_fixes.json` 的 `_manual_desc_note/_2026-09-14_T2` 备注（都停在 09-14；数据在 `d8d7def` 2026-09-15 补进去）

**同族（多句 desc 含 at-turn 从句，7 张）**：`Grot Orderly` · `Armorium Cherub` · `Marker Drone` · `Concealed Explosives` · `Ghallaron's Champion` · `Chronomancer` · `Nemesor Zahndrekh`。前 6 张的 at-turn 半段探针**都认了**；`Nemesor Zahndrekh` 整句不认**不是因为锚点**，是正文 `adjacent remnants do not disappear` 不是已知动词。⚠️ 别把 `TryAtTurn` 退回 `SplitAtTurn`，这 7 张会一起塌。

**查不到的**：**反编译 0 命中**（`grep -ril "armorium|cherub"` = 0；`all_strings.txt` 带 `-a` 也 0；`dump.cs` 0）；**再排除一层**：`decomp_full` 里搜 `start of your turn` / `return to your hand` / `rally:` 也 0 ⇒ 原版**不解析 desc 文本**，是数据驱动的 `CardAbility`/`AbilityLogic`（卡文在 addressable 数据资产里、不在代码里）⇒「原版具体怎么结算」本次**没拿到实据**。`assets_full` 全目录搜 `Armorium` **未跑完**，不作否定证据。

---

## ④ 5 张卡的 `Stun` 是误抽 —— 确认 5 张，但根因不在 D:/4

**结论**：**确实是 5 张，一张不多一张不少**，且**全部是误抽** —— 逐张开原图，**5 张卡面都没有任何关键词行**，`Stun` 只印在效果句里。根因在 **D:/4 仓库之外**：2026-08-16 那趟视觉模型 OCR，`d:/warpforge/tools/ocr_cards.py:25-27` 的 PROMPT 直接要模型产出 `"keywords":[…]`，**没有「只读关键词行 / 碰到效果动词就停」的判据** ⇒ 效果句里带图标的 `… and ⟨图标⟩Stun it` 被顺手塞进 keywords；D:/4 侧全程只是**搬运**（`gen_cards_engine.py:710`）。**修法通道 = `cardface_fixes.json` 的 `_manual_keywords`**（人工段、最后应用、每次重跑都保留）；⚠️ **不是 `STAT_FIXES`** —— 那里的 `"keywords"` 是**死键**（`gen_cards_engine.py:679-682` 只消费 cost/attack/health/ranged）。

| 卡（阵营/类型） | `desc` 里的片段 | 卡面关键词行 | 应改成 |
|---|---|---|---|
| `Banshee Mask`（SaimHann / tactic） | `Deal 3 damage to an enemy and Stun it` | **没有**（照卡图） | `[]` |
| `Hektor Thenmann`（AstraMilitarum / hero） | `Deal 2 damage to an enemy and Stun it. Talent: …` | **没有**（照卡图） | `["Duty","Talent"]` |
| `Malicious Volleys`（BlackLegion / tactic） | `Deal 2 damage to an enemy. If it is a troop, Stun it` | **没有**（照卡图） | `[]` |
| `Snakebite Grot`（Goff / unit） | `Tide 1. Stun troops attacked.` | **没有**（照卡图） | `["Tide 1"]` |
| `Toxic Bonfire`（Goff / defence） | `Deal 2 damage to an enemy troop and Stun it` | **没有**（照卡图） | `[]` |

**它是死的**：卡表 keywords 由构造函数灌字典（`CardDef.cs:82`；`UnitState.cs:210-214` 直接复制、**不走 `AddKeyword`**），同步 `IsStunned` 的只有 `UnitState.AddKeyword`（`:359-373`，关键词那行 `:366`）。真正生效的 stun 走效果句 → `EffectText.cs:4568 TryStun` → `EffectResolver.cs:346` → `DoStun`（`:1896-1920`）。唯一现实影响 = `Has("stun")` 为真（`CardCriteria.cs:175-182`）+ 自检把它当可解析文本摊平（`RuleEngineTest.cs:8262-8266,10720-10724`）。

**实据**：卡图（**5 张逐张开过图**，均在 `D:/2/Warpforge部队卡片/` 下）：`Aeldari/4计策/Warpforge_51_Banshee-Mask.png` · `Astra Militarum/1督军/Warpforge_01_Hektor-Thenmann.png` · `Chaos/4计策/Warpforge_11_Malicious-Volleys.png` · `Orks/3部队/Warpforge_03_Snakebite-Grot.png` · `Orks/5防御卡/Warpforge_62_Toxic-Bonfire.png`。污染已落进 OCR 原表：`D:/2/Warpforge部队卡片/卡牌数据总表.md:59,135,874,982,1115`。源头链 `ocr_cards.py:21,25-27,146-152` → `merge_card_stats.py:99-104,113-116,305,347` → `sync_from_d2.py:86-91`；D:/4 侧 `gen_cards_engine.py:710,738-741` · `gen_cardface_fixes.py:139`（`newkws = list(kws)` **从源表起手 ⇒ 只会继承 Stun**）。逐卡名单见上表；通用判据 = `gen_cardface_fixes.py:129-151`（现在**只会加不会删**）。**只改数据必然回来**（`cards_engine.json:782` 自注「不要手改」，重跑从 `card_stats.json` 重灌）。

**同一趟 OCR 的另两类同类噪声（⇒ 只清这 5 张清不干净）**：⑤ 效果里**给别人**加的关键词被记成自己的（照卡图核过 `Dance of Death`、`No Respite`；数据里还有 `Master of Arms`/`Furious Reprisal`/`Helspear Assault`/`Avatar of Khaine` 的 `Blast`，**按数据判断、未看图**）；⑥ OCR 残渣（尾巴标点 `Armour 2.`/`Remnant.`/`Vanguard.`/`Stealth.` 与掉数值的裸词）。

**查不到的**：**D:/4 仓库内没有 keywords 抽取器**（全仓 grep 只有搬运/覆盖/对账脚本；`git log --all --name-only` 也从没有过 `ocr_cards.py`）⇒ 抽取器确实在 D:/4 之外。「词表扫 desc」vs「正则把 `and Stun it` 当关键词」这个二选一**不成立**（是模型自由判断，没有可指认的代码行）。除 5 张 + 上述 2 张外**未看图**；也没量全池关键词位噪声总量。

---

## ⑤ `Cardback Shadow SDF` —— 资产在、但**不能拿过来直接用**

**结论**：资产在 `D:/2/新解包资源/assets_full/bundle_cosmeticscardbacksimages_assets_all/`，**233 个** sprite，名 `Cardback_<系列>_<名>_SDF`（如 `Cardback_All_Starter_SDF`），对应 233 张**同名去后缀**的 1024×1024 贴图（每张里同时装 `_Main` 622.9×948.8 @204,51 与 `_SDF` **100×130.5 @900,447.25 —— 233 个全部一样**）。**「292×381」不是图**，是 UI 节点的 `m_SizeDelta` = 2.9212×3.8122（×100 px）= 原版**显示尺寸**；**「699」是文件数**（466 Sprite + 233 Texture2D），真 SDF sprite 只有 **233**。它是 `Cardback Container` 下的一个 UI 节点，`Image.m_Sprite = 0`（运行期才塞），材质 `Card Backs SDF` → shader **`Everguild/FX/Card Highlight And Shadow`**。**不能直接用**：它是**灰度距离场**（该区 alpha 恒 254.8、亮度铺满 0–255），当普通半透明图用会画出一整块不透明灰块；而我们**没有任何 sprite-SDF shader**（只有 TMP 文字 SDF）⇒ 要落得**自己补 shader**，或**离线把距离场烘成软 alpha** 走 `Sprites/Default`（与对账表 `:177` 对卡片那层 SDF 取同一条路）。

**它是什么**：`DeckManager__SetupDeck.c:37-44` 把 `BattleManager.GetCardback()` 的 `Item1 → cardbackImage(+0x50)`、**`Item2 → cardbackShadow(+0x58)`**，+0x58 正是这个 `Cardback Shadow SDF` 的 Image ⇒ 它是**牌堆那张牌背底下的 SDF 阴影层**；`ToggleCardbackPreview.c:11` 只 `SetActive(0x60)` 把整条链一起开关。

**实据**
- Bundle / 尺寸：`.../bundle_cosmeticscardbacksimages_assets_all/{Sprite,Texture2D}/` · `Sprite/Cardback_All_Starter_SDF.json:215-219`（textureRect 100×130.5）· `..._Main.json`（同贴图 PathID `4534546286388430470`，rect 622.9×948.8）
- 节点 / 材质 / shader：`bundle_battleprefabs_vfxandmisc_assets_all/GameObject/Cardback Shadow SDF.json:29`（Image + `UIImageMaterialColorChanger`）· `RectTransform_-6876058803722675264.json`（SizeDelta 2.9212×3.8122，父 = `Cardback Container.json`）· `bundle_duplicateassetisolation_assets_all/Material/Material_6996605638394099752.json`（`m_Name: "Card Backs SDF"`，keywords `_ALPHACHANNEL_R`/`_NOISE_CHANNEL_B`，`_ShadowColor` = 黑 a=0.533）→ `bundle_shaders_assets_all/Shader/Shader_-3135600060561652991.json`（21 个属性；**原版自定义**、不是 URP 内置）
- 引用链：`bundle_cosmeticsso_assets_all/MonoBehaviour/Cardback_All_Starter.json` 的 `imageReference` / **`cardBackSDF`**（同 AssetGUID `51881a588639f4ee9b3109e9ebb30d3c`）；字段声明 `Warpforge_code/Scripts/Assembly-CSharp/CosmeticItemCardback.cs:6`
- 字段偏移：`dump.cs:37945-37956`（0x50 / 0x58 / 0x60）· 另有 `CollectionCosmetic.cardbackSDFImage`（`dump.cs:70824,70830`）
- 工程侧：`Assets/` 下 8 个自研 shader（`CardPresentation/Shaders/{ArtOpaque,FrameCutout,VideoSplitAlpha}` + `WarpforgeVFX/Shaders/*`）+ TMP 文字 SDF，**没有** `Card Highlight And Shadow`；4 张 back = `CardPresentation/Resources/Art/cards/back_{ember,goff,tide,ultramarines}.png`（`CardArt.cs:117-121`，调用点 `BattleDriver.cs:3296,3842,3844`）—— 是**四阵营完整卡背图**，与 SDF 层**没有对应关系**（不是「缺一层阴影」，是另一层独立资产）
- 导入通道：`工具/import_original_art.py:22`（已写明按 `Sprite/*.json` 的 `textureRect` 裁）· `:46,127-133,618`；⚠️ 但 `素材/Warpforge原版/卡背/` **只有 233 张 `_Main.png`、没有 `_SDF`** ⇒ SDF 得直接从 `bundle_cosmeticscardbacksimages_assets_all/Texture2D/Cardback_*.png` 按 `textureRect` 裁（无现成 SDF 导入脚本）
- ⚠️ **别混**：同族另有 `Texture2D/Card board frame SDF.png`（**128×128**，卡**框**版，各 `<faction>cardassets` 包里 tier1-4，材质 `Card board Frame SDF`，同一个 shader）

**查不到的**：解包树里**没有 PathID→文件名索引**（靠 `Type_<PathID>.json` 命名 + `GameObject.m_Component` 反查走的）；battlearena 各场景 9 个实例 / mainmenu 4 个 / menus 47 个只验了尺寸与父节点一致，**没逐个反查组件**；`ToggleCardbackPreview` 的**点击入口**仍查不到（与对账表 `:90` 一致）；`CosmeticItemCardback.GetCardBackSprites` 方法体未反编译。已排除：`bundle_shaders_assets_all` 与 battleprefabs 的 42 个 shader 全名单里**没有别的 SDF shader**；battleprefabs 571 个材质里只有 `Card board Frame SDF` 名字带 SDF。

---

## 🔴 落地时的更正（2026-09-18 晚，主对话实测）—— **① 与 ② 的修法各只能落一半**

> 这一节是**执行结果**，不是复查结论。下面两条都以**跑过的自检**为准，别照上面 §二 的「动哪里」原样抄。

### ② 的 `IsUnit` 卫：**只落 `IsHandTrap` 那半，广播那半必须留着**

- ✅ **已落**：`EffectText.IsHandTrap` 加了 `if (c == null || c.IsUnit) return false;`
  ⇒ 它管的两处（覆盖率账 `EffectText.cs:1380` · `DeckBuilder.cs:264`）不再把单位卡当陷阱。
- ❌ **`EffectResolver.ResolveAtTurn` 的手牌广播**（原文说「`EffectResolver.cs:3228` 补 `if (c.IsUnit) continue;`」）
  **加了会被测试挡回来**：`RuleEngineTest.TestBeastbossAndPayloadSegments` 从 **3 份掉到 2 份**。
  理由：`Beast Snagga Nob`（unit，`GOF81`）那条「回合结束给**手牌里**的 Beasts +1 攻」的
  **投递通道就是那一趟**（载荷要写进 `ctx.HandBuffs`、走 `GrantHandBuffForTargets`），
  而 `ResolveAtTurn` 的文档注释里也把它列在「② 棋盘单位」那一趟 —— **两条通道对这张卡是并用的**。
  ⇒ **已撤回**，并在两处都写了「别再顺手加」的注释。
- **判据**：`RuleEngineTest.Run` → **2937/2937 全过**（加卫那版是 2936/2937）。

### ① 的 tsa 循环：**未动**（本轮只做了只读查证与两处代码修法）

✅ **2026-09-19 两条都做完了**（本节是执行结果，不是复查结论）：
`EffectExporter.cs` 的 tsa 精灵列表**已补**（导出后全库 **383** 个 tsa 列表由空变实、新增 106 张 `*_sprite.png`）；
`CardbackResolver` **已接线**（`BattleDriver.HookAnimFxCards` 里按**阵营**取 `CardArt.CardBack`，与牌堆同源）。
🔴 **落地时又量出一条语义**：`tsa.AddSprite` **第一次是「替换掉新建粒子系统自带的那条占位」、之后才追加**
（`spriteCount` 起点就是 1）⇒ **判据必须取 `GetSprite(0)` 是不是我们要的那张**，写成「`spriteCount` 涨没涨」会误判。
`AnimFXCheck` 的卡背段（49/0）逐条钉住了：回调被调到 · `isPlayer` 传下去 · **sprite 真的进了列表**（资产 sprite 与
运行时 `Sprite.Create` 各一条）· 拿不到时只记账不动粒子 · 数组为空时记账。
根因与本轮实测一致，照上面 §一 的落点做即可。

### ④ 的 5 张 `Stun`：**✅ 已落地（2026-09-18 晚）**

按上面 §四 的逐张值写进了 `数据/游戏数据/cardface_fixes.json` 的 `_manual_keywords`
（新加一节 `_2026-09-18_Stun误抽` 记来龙去脉）：

| 卡 | 覆盖值 |
|---|---|
| `Banshee Mask` | `[]` |
| `Malicious Volleys` | `[]` |
| `Toxic Bonfire` | `[]` |
| `Hektor Thenmann` | `["Duty","Talent"]`（正文真有的两个，**别连带删**）|
| `Snakebite Grot` | `["Tide 1"]`（同上）|

**管线**：`gen_cardface_fixes.py`（手工覆盖 keywords 56 张）→ `gen_cards_engine.py`（写出 376 KB）
→ `gen_icon_plan.py --write` + `gen_icon_doc.py`（**缺口 0 处**）。
**判据**：`cards_engine.json` 里**再无一张卡带 `Stun`**（全表扫过）；
那 5 张的实际值 = `[]` / `[]` / `[]` / `["Duty","Talent"]` / `["Tide 1"]`。

⚠️ **踩过的坑（留档）**：`cardface_fixes.json` 是 **CRLF**（965/965）。
我第一次用文本模式 `open(...,'r')` 数行尾，`\r\n` 被 Python 折成 `\n` ⇒ **误判成 LF**、锚点匹配 0 处。
**判据要用二进制**：`io.open(p,'rb').read().count(b'\r\n')`。

