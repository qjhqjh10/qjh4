# 原版 `AI` 类 —— 反编译成果与读法（2026-09-17）

> **背景**：对手 AI 一直是「按原版做不了」的状态 —— `AI` 类**一个方法体都没有**
> （`decomp_il2cpp_0827/` 那 2024 个 `.c` 只覆盖 71 个类，不含 `AI`）。
> 2026-09-17 重建了反编译工具链（见 `资料/反编译工具链_重建记录.md`），**把整个 `AI` 类补了下来**。
>
> 🔴 **本文件是「按原版重写 `SimpleAI`」这条线的唯一出处**（待办表第 2 行）。
> 读法 · 公式 · 常量 · 数据类 · **哪些是死代码** · 我们怎么落地，全在这里。
> ⚠️ 浮点常量不是从 `.c` 里读的（Ghidra 把标成 `void` 的函数的浮点算术**整段吞了**），
> 是从 `d:/2/Warhammer 40k Warpforge/GameAssembly.dll` 的浮点池逐条读出来的
> （`.text` file_off = RVA−0x1600，`.rdata` = RVA−0x2400）。
> **凡标 `[机器码]` 的都是这么来的**，`.c` 文本里看不到。

## 一、产物在哪 · 怎么再生

**✅ 完整性核对过（2026-09-17）**：清单 128 个方法 → 产物 **127 个 `.c`**、按名字逐个比对**缺 0**
（差的那 1 是两条方法名映射到同一文件名）；**`AI` 类 41/41 全在**；其中 29 个文件 < 300 字节，
全是 `.ctor` / getter / `OnDisable` 这类**空壳**（正常，不是失败）。反编译日志里**没有** `NOFUNC`/`DECOMPFAIL`。

| | |
|---|---|
| **方法体** | **`d:/2/Warpforge_tools/data/decomp_il2cpp_0827/decomp_out_ai/`**（**127 个 `.c`**，其中 **`AI__*.c` 41 个** = 整个 `AI` 类；其余是 `AnimFX*` 18 个类）|
| 怎么再生成 | `资料/反编译工具链_重建记录.md` §二（**注意：Ghidra 12 的 `.py` 脚本要 PyGhidra，我们用 Java 版 `Renamer.java` / `Decomp.java`**） |
| 字段偏移查表 | `d:/2/tools/il2cpp_out/dump.cs`（42 MB，**每条字段都带 `// 0x??` 偏移**）—— 反编译里那些 `+0x??` 全靠它翻成人话 |
| 字符串字面量 | `d:/2/tools/il2cpp_out/stringliteral.json` + `script.json` 的 `ScriptString`（`StringLiteral_N` = 第 N 个，从 1 数） |
| **常量真值** | `GameAssembly.dll` 浮点池（本文件已抄全，见 §五） |

## 二、已经解出来的：**AI 的换牌规则**

`AI.GetAiMulliganCards(PlayerHand hand)` → `decomp_out_ai/AI__GetAiMulliganCards.c`：

```
遍历手牌：
  · 若 card.mulliganOption == MulliganOptions.whispersOfChaos (=30)  → 保留
  · 否则若 card.rawCardData.manaCost > 4                              → 换掉
  · 其余保留
```

| 反编译里 | 字段 | 怎么翻出来的 |
|---|---|---|
| `*(int*)(item + 0x260) == 0x1e` | `CardScript.mulliganOption == 30` | `dump.cs`：`CardScript // 0x260 private MulliganOptions <mulliganOption>k__BackingField`；枚举 `MulliganOptions { normal=0, secondTurn=10, extraWarlord=20, whispersOfChaos=30 }` |
| `4 < *(int*)(*(longlong*)(item + 0x28) + 0x78)` | `item.rawCardData.manaCost > 4` | `EntityScript // 0x28 RawCardScript <rawCardData>` + `RawCardScript // 0x78 public int manaCost` |

## 三、整体形状（这是「原版长什么样」的骨架）

```
BattleManager.<PlayAi>d__399 协程（延时在这一层：GetFirstActionDelay/GetActionDelay/GetEndTurnDelay + Random.Range + WaitForSeconds）
  └─ 循环调 AI.PlayTurn(manager, mode)   ← 一次调用**只做一步**，AI 类里没有任何协程/await
       PlayTurn:
         ① playedFirstComboCard = false
         ② GetAvailableActions(manager, isPlayer:false, allowAutoActions:false)   ← 动作表，见下
         ③ 逐条 GetActionScore(manager, list, i, calculateUnitedScore:true, mode)
         ④ TweakAvailableActions(...)     ← **唯一随机源 + 唯一难度旋钮**（随机丢掉最低分的几条）
         ⑤ SelectBestAction(...)          ← 严格 `>`，平手保留先出现的
         ⑥ 最佳是 endTurn → 收手；否则 ExecuteAction（入 AutoAction 队列 + 播随机语音）
```

**`GetAvailableActions`（`BattleManager__GetAvailableActions.c`）= 动作表**：

1. 手牌：`CanPlayCard` 过的每张 → 一条 `playCard`（要选目标的按每个合法目标各来一条）
2. 场上单位：`GetAvailableAttackActions` → **近战一条、远程一条**（× 每个合法目标）
3. 有主动技能（`+0x220`）且 `CanUseActiveAbility` → `activeAbility`（要目标就每目标一条）
4. 灵族残骸 + `CanUseWaystone` → `clickWaystone`（cost 1）
5. **最后追加一条 `endTurn`**（acting/target 为 null、cost 0）= 「什么都不做」的基准

**`SelectBestAction`**：先拿 endTurn 当基准（没有 endTurn 就取 `list[0]`）；然后线性扫，
**严格大于才换**（平手保留先出现的，**选择过程无随机**）；唯一的例外 ——
若当前最佳是 `endTurn` 而候选是近战/远程攻击，则攻击胜出，**除非**出手卡是
`berzerk` 反面那四类（`stealth` / `vanguard` / `HasSpecialAttackScoring` / `cardType==Hero`）；
带 `berzerk(360)` 则**一定**胜出。

**`TweakAvailableActions`（难度就在这里）**：
按分降序排 → 从表尾（最低分）往前扫、跳过 `endTurn` → 前 `minSkips` 条**无条件删**，
之后每条掷一次 `roll = GetRandomInt(0,100)`，`roll < skipChance` 就删、否则换下一条重掷，
最多删 `maxSkips` 条。⇒ **「难度」= 有多大比例、多少条地把「AI 本来想做的事」按最低分往下砍掉。**
⚠️ 原版**只在打机器人局**才跳动作（`ShouldDisableTweakAvailableActionsByMode`：matchType ∈
{RankedBot 20, ClosedDeckBot 120, FastModeBot 210} 或「PracticeOffline 50 且非新手期」以外一律不跳）。
⚠️ **我们不分模式**（单机就是打 bot）⇒ 四档全按上表生效。
（那个函数是「被内联」还是死代码**没证死**，见 §八·3 —— 别把它当已证事实。）

## 四、打分族（`GetActionScore` 的分派）

| 动作 | 走哪个函数 | 主公式（`[机器码]` = 从 DLL 反汇编还原） |
|---|---|---|
| `playCard` | `ScoreFromPlayingCard` | `(非法术 ? GetValueInPlay : 0) + ScoreFromPlayingNow` |
| `playCard` 的 criteria 层 | `ScoreFromPlayingNow` → `ScoreFromCriteria` | 遍历卡资产上配的两张 criteria 表（`RawCardScript 0x2E8/0x2F0`），逐条累加 |
| `attackMelee` / `attackRanged` | `ScoreFromAttack` | `ScoreFromBuffedAttack(...)` ＋（特殊攻击打分 / 献祭 / blast / markerlight 的补项）；**打的是英雄且 `CombinedAttackCanKillWarlord` ⇒ +10000** |
| `activeAbility` | 技能自己的 criteria | 逐条 `ScoreFromCriteria` 求和 |
| `clickWaystone` | 固定 | **50.0** |
| `endTurn` / 其它 | —— | **0**（⇒「分 ≤0 的动作就不做，宁可结束回合」） |

**子打分器**：

| 函数 | 公式 |
|---|---|
| `GetCardValueInPlay` | `(overrideInPlay ? manualValueInPlay : manaCost) × 2 + 1 + ValueOfEnchantments`；**受伤**再 `× (0.5 × 当前血/上限 + 0.5)`；**stun/pindown** 再 `× 0.8` |
| `GetCardReferenceValueInPlay` | `manaCost × 2 + 1`（基准值，「1 张卡 ≈ 几个点」的尺子） |
| `ScoreFromDamagingBuffedUnit`（伤害的核心） | 门禁：`ambush`→0 · `longrange`→0 · `dropPod`（不忽略时）→0 · `dodge`/`shield`→只给 `攻击/3 + 1`。然后 **小兵**：打死 → `价值 × (survivor ? 0.8 : 1.0)`；没打死 → `价值 × 0.6 × 伤害 / (血上限 + bastion)`。**英雄**：打死 → **1e6**；没打死 → `伤害 × (剩余血 > 15 ? 1.2 : 1.8)` |
| `ScoreFromBuffedAttack` | `我方伤害分 − 敌方反击分`（反击要走同一个伤害分函数）`+ 0.1`（被反击后还活着）`+ rawCard.extraScoreFromAttacking + target.extraScoreFromTargetingOnAttack`；带 `poisonous` 且没打死 → `+ 0.7 × ScoreFromPoisoning(目标)` |
| `CombinedAttackCanKillWarlord` | **乐观上界和，不是搜索**：把所有「打这个目标」的攻击动作**按出手单位去重**（每单位只留伤害最高那条）后求和，`≥ 目标当前血 + CurrentBastion` ⇒ true |
| `SuicideHeroAttack` | 双方都是英雄 且 **我方当前血 ≤ 对方当前血** ⇒ true（**这版构建 0 调用点，见 §六**） |
| `ScoreFromStun` | 英雄 → `当前近战攻 × 1`；小兵 → `× 0.5`；`cantAttack`/`unstunnable` → `价值 × 0.2` |
| `ScoreFromPoisoning` | 已有毒 / `resistant` / 督军类 → 0；否则 `价值 × f`，`f` = 未晕且能打时 `血<3 ? 0.2 : 0.5`，晕了 `0.8` |
| `ScoreFromReturnToHand` | `目标当前费用 − (2×费+1 − 目标价值) × 1.5 − (rally?2:0) − (codex?2:0)` |
| `ScoreFromJam` | `ValueOfEnchantments + ValueOfTraits + (没 jam 时 rawCard.extraScoreFromJamming)` |
| `ScoreFromGivingCharge` | `不可行动 && summonSickness` ⇒ `当前近战攻`，否则 0 |
| `ScoreFromDrawingCard` | `手牌上限 − 手牌数 − 1`；**牌库空**则直接 `= −2 − 手牌上限` |
| `ScoreFromManaBuff` | 「被加费之后才付得起的手牌数」× 1.0 |
| `ScoreFromHandBuff` | 手里因 `costChange` 才打得起的牌里 `ScoreFromPlayingCard` 的**最大值**，收尾 `− 3.0 × N`（N 语义未完全解出） |
| `ValueOfTraits` | `Σ CardTrait._aIScore`（**值在数据资产上**） |
| `ValueOfEnchantments` | `addStats` → `(近战+远程+血) × 0.5`；附魔类 → `+1.0`（`SecretOrder` 派系 → `−2.0`） |
| `ScoreFromBuff(trait)` | **trait → 分值表**，见 §五·三 |
| `ScoreFromCriteria` | `baseValue(Friend/Enemy 按目标阵营选) + Σ_目标 [ value(option) × multFactor × multiplyByFriend/Enemy ]`；19 个 `ScoringOption` 见 §五·二 |
| `ShouldPlayComboCard` | `criteria.comboCard` 在手里 && 两张费用付得起 &&（本回合已打过第一张 ‖ `comboCardFirst && score ≥ 2.0`）**——这版 0 调用点** |
| `GetAiEnvEffectCard` | 从候选里**均匀随机**抽一张（不打分）—— 唯一有调用点的随机选牌 |

## 五、常量 · 数据类 · 分值表

### 五·一 关键浮点常量（`GameAssembly.dll` 浮点池，`[机器码]`）

> **本表是这些系数的唯一出处** —— §四 的公式里只写符号，别在别处再抄一遍数字。

| 值 | 用在哪 |
|---|---|
| **1e6** | 打死英雄/督军（`ScoreFromDamagingBuffedUnit`） |
| **10000** | `CombinedAttackCanKillWarlord` 的 `GetActionScore` 加分 |
| **50** | `clickWaystone` 固定分 |
| **2.0** | `ShouldPlayComboCard` 阈值 · `GetDefensiveAttackValue` 的「打督军至少 2 分」（**这两处在 §六 都是 0 调用点**） |
| **1.8 / 1.2** | 打英雄非致命：剩余血 ≤15 → 1.8，否则 1.2 |
| **1.0 / 0.8 / 0.6** | 小兵伤害分系数（打死 1.0，带 survivor 0.8，没打死 0.6 基准） |
| **0.7** | 毒分系数；**0.5 / 0.2** 毒/晕的衰减；**0.1** 被反击后存活的小奖励 |
| **1.5** | `ScoreFromReturnToHand` 系数；**10** 唯一近战候选加成；**−10** 某负出口 |

### 五·二 数据类（原文抄）

```csharp
public class AvailableAction {         // AvailableAction.cs:3-11
    public PlayerActions actionType;   // 0x10
    public CardScript actingCard;      // 0x18
    public CardScript targetCard;      // 0x20
    public float actionScore;          // 0x28  ← GetActionScore 写在这
    public int manaCost;               // 0x2C
}
public enum PlayerActions { playCard=0, attackMelee=1, attackRanged=2, useHeroPower=3,
                            endTurn=4, activeAbility=5, clickWaystone=6 }
public enum AIModes { normal=0, aggresive=1, defensive=2, combo=3 }
public enum MulliganOptions { normal=0, secondTurn=10, extraWarlord=20, whispersOfChaos=30 }
public enum DeckDifficultyLevel { SuperEasy=0, Easy=5, Normal=10, Hard=15 }   // MatchData.botDifficulty

[Serializable] public class ScoringCriteria {          // ScoringCriteria.cs
    public enum ScoringOption { FixedValue=0, Count=1, SumValueInPlay=2, SumValueOfDamage=3,
        SumValueOfHealing=4, ValueOfBuffingAttack=5, CurrentAttack=6, SumValueOfJam=7, RandomValue=8,
        FixedValueIfCountGreaterThanBaseEnemy=9, FixedValueIfCountLowerThanBaseEnemy=10,
        ValueOfBuffing=11, ValueOfBuffingCost=12, ValueOfBuffingMana=13, SumValueOfStun=14,
        SumValueOfPoison=20, SumValueOfReturnToHand=25, SumValueOfGivingCharge=30,
        ValueOfDrawingCard=35, ValueOfChangeWeapon=40 }
    public float baseValueFriend;      // 0x10   （"只要 targetCriteria 找到至少一个目标就一定加"）
    public float baseValueEnemy;       // 0x14   （option 9/10 里还被当成**计数阈值**用）
    public float multiplyByFriend;     // 0x18
    public float multiplyByEnemy;      // 0x1C
    public ScoringOption scoringOption;// 0x20
    public TargetCriteria targetCriteria, multiplierCriteria;   // 0x28 / 0x30
    public DamageCriteria damageCriteria; public HealCriteria healCriteria;  // 0x38 / 0x40
    public CardEffect buffEffect; public HandEffect handEffect;  // 0x48 / 0x50
    public bool codexEffect; public RawCardScript comboCard; public bool comboCardFirst; // 0x58/0x60/0x68
}
```

`AI` 类**只有一个字段**：`playedFirstComboCard`（`0x10`）—— **没有难度、没有随机源、没有 criteria 引用**；
模式和打分表都是**参数传进来的**。（另有**不继承 `AI`** 的 `AiScripted`，是教程/战役用的脚本 AI。）

### 五·三 `ScoreFromBuff` 的 trait → 分值表（`[机器码]`，44 行）

| 分值 | trait（原版 `DefinedTrait` 名 → 我们的 `KeywordTable` 名） |
|---|---|
| 3.0 | poisonous |
| 2.0 | shield · dodge · survivor · firstStrike · precognition · vanguard |
| 1.5 | sneakAttack · stealth · syphon · ward · sacrifice · pack · camouflage · longrange |
| 1.0 | backlash · tainted · unstoppable · coward · flying · relentless · transient · terror · psyker · reactor · requiem · landing · resolution |
| 0.5 | dropPod · berzerk |
| 0.0 | shuriken 及**未列出的任何 trait** |
| −1.0 | vulnerable · markerlight |
| −1.5 | stun · pindown · blind |
| −2.0 | cantAttack |
| −4.0 | poisoned |
| 取属性值 | bloodThirst→近战攻 · maintenance→维护费 · mutation→trait 值 · bastion→bastion · blast→`traitValue` |

## 六、🔴 **0 调用点 / 不生效的方法**（扫描全 DLL 的 `call` rel32 + 方法指针表得出的）

| 方法 | 状态 | 我们该怎么对待 |
|---|---|---|
| `SuicideHeroAttack` | **0 调用点** | **别照抄「不自杀」的调用时机** —— 原版的「不自杀」实际写在 `ScoreFromBuffedAttack` 里（伤害分 − 反击分） |
| `ScoreFromKeepingTheCard` | **0 调用点**，且默认权重 `extraScoreFromKeepingCard = 0` | **原版这版不留牌**；真正让 AI 不滥出牌的是「`endTurn` 基准 0 分」 |
| `ShouldPlayComboCard` | **0 调用点** | 连招那套资产管理功能没启用 |
| `GetDefensiveAttackValue` / `IsBetterAction` | **0 调用点**（前者可能被内联，后者确定是死代码） | 语义可参考（英雄互殴 0 分），**不要照抄调用时机** |
| `AIModes` | 穿进 `ScoreFromPlayingNow` / `ScoreFromKeepingTheCard` 但**函数体里不读它** | **原版这版「按模式调」是空的**，不要自己发明 |
| `calculateUnitedScore` | 传进来了但**全函数不读** | 忽略 |

> ⚠️ 这条清单本身就是结论：**待办表原来写的「要补：留牌 / 不自杀」两件，原版在这版构建里并没有做。**
> 我们要的是**原版实际跑的那条链**，不是原版源码里存在过的方法。

## 七、我们怎么落地（`SimpleAI` 重写）

- **搬结构**：动作表（`AvailableAction` 那一套）→ 逐个打分 → `TweakAvailableActions`（难度）→ `SelectBestAction`。
- **动作种类**：`playCard`（含战术卡目标变体）· `attackMelee` / `attackRanged`（**两条都列、各打分**，
  取代「远程攻击力更高就用远程」）· `activeAbility` · **誓约能力** · **替代行动**（`duty/pray/ferocity/agenda`）
  · `endTurn` 基准。`clickWaystone` 先留位（引擎那半还没实现 = 待办第 8 行）。
- 🔴 **criteria 那层原版数据在服务端**（`ScoringCriteria` 的**类形状**来自 `dump.cs`，但**值**是挂在卡/效果资产上的、本地没有）
  ⇒ 我们**从自己卡表 + 已解析的 `EffectOp` 推**（`deal N` → 伤害分、`give +N attack` → buff 分、
  `draw` → 抽牌分…），并在代码注释与文档里写明「**结构照原版、权重是我们配的**」。
- **随机**一律走 `System.Random(seed)`（工程铁律：一局必须可复现）。
- **难度**：照 `TweakAvailableActions` 的 `minSkips/maxSkips/skipChance` 三旋钮，玩家入口做进**对局内设置面板**
  （`Battle/SettingsPanel.cs`，用现成的 `40K_button` + `Label`）。
  🔴 **四档的取值就在这一行（唯一出处，`SimpleAI.KnobsOf` 与代码逐位一致）**：
  `SuperEasy (0,3,90)` · `Easy (0,2,60)` · `Normal (0,1,30)` · `Hard (0,0,0)` —— 语义 = `(minSkips, maxSkips, skipChance)`。
  ⚠️ **`minSkips` 一律 0**（原版那个值是「无条件先砍掉几条」）：给正数时 AI 只剩一两条动作会被**全砍光**，
  表现成「对手发呆什么都不做」。
  🔴 **那个入口是我们加的**：原版**没有**玩家可见的难度开关（那三个数在 `AIBotsConfig` 里，
  按排位/连败/牌组自动挑），所以「设置面板里能调」这一条**不是原版行为**，如实标着；
  **四档的具体取值也是我们配的**（原版那三个数的值本地没有）。
- **死代码不照抄**：`SuicideHeroAttack` / `ScoreFromKeepingTheCard` / `ShouldPlayComboCard` /
  `GetDefensiveAttackValue` / `IsBetterAction` / `AIModes`（见 §六）—— 一条都不实现，
  只在文档里记着「原版源码里有、这版没跑」。

## 七之二、落地后的验证（2026-09-17 收工）

| 尺子 | 结果 |
|---|---|
| `RuleEngineTest.Run` | **2855/2855 全过**（含新 `TestAiOriginal` 17 条：换牌规则 / 卡价值 / 斩杀分 / 合击上界 / 平手留先 / 难度旋钮可复现 / 动作表含 `duty`） |
| `BattleScene.Run` | **474/474 全过**（含设置面板难度那 6 条：默认档文案 / 命中判定 / 换档 / 转一圈回原档） |
| `DeckScene.Run` | **61/61 全过** |
| **真包**（`PlayerBuild` + `-wfdrive`） | 构建 **0 错误** · 自动对局 **10 步打完整局**（`Winner=2`）· **0 条 AI 警告** · 截图 4.1 MB |

⚠️ **没跑的**：`-wfhuman`（走 UI 入口那条自动对局）—— 它的「偶发卡死未收口」记号在 `项目任务.md` 待办表**第 1 行**（`资料/特效还原_进度与交接.md` §七 步 5 只记「怎么跑」）。
⚠️ 「0 条 AI 警告」指的是**运行时**的 AI 警告（`AI 的动作被引擎拒绝` / 步数超限）；构建期那串编译警告（shader / TMP 变体）**不在这条里**。

## 八、还没弄的 / 不确定的

1. **`ScoreFromHandBuff` 没补完**（1168 字节、大量内联浮点，手工解码到一半偏移漂移）；`−3.0 × N` 里 N 的含义没定。
2. `ScoreFromCriteria` 的浮点算术整体被 Ghidra 吞掉，`value × multFactor × multiplyBy*` 的**乘序**靠反汇编还原；
   **哪几个 option 内部乘 `multFactor`** 没逐个确认（option 3/4/11 明确，其余未定）。
3. `FeatureConfig.ShouldDisableNewMatchMaking` / `ShouldDisableTweakAvailableActionsByMode` 是「被内联」还是死代码，
   倾向内联（逻辑互补），但不能证死。
4. `GetRandomInt(0,100)` 上界是否含 100 未确认。
5. 三条日志字面量（`"Skip AI action by minsteps: "` 等）按内容匹配，未逐字面量索引比对。
6. `AnimFX*` 那 86 个方法体也一并落地了（`decomp_out_ai/`）—— 特效线 §七「路 A」要的就是它们。
