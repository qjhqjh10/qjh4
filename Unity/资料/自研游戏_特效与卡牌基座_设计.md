# 自研游戏 —— 特效接入 & 卡牌表现基座设计

> 建立：2026-09-11（第四轮）　|　第一节更新：2026-09-11（第五轮，特效接入已实现）
> 这份文档回答两件事：
> 1. 还原出来的特效**怎么在自研游戏里真正跑起来** —— 而不是只能挂在原版游戏里看
> 2. 卡牌表现基座（发牌 / 手牌 / 拖拽 / 落位 / 高亮 / 卡牌动效）**怎么设计** —— 原版只作参考蓝本，最终要自己出方案

---

## 一、特效接入：从「研究产物」变成「游戏资产」

### 1.1 现状盘点 —— 差的不是特效本身

| 项 | 现状 | 缺口 |
|---|---|---|
| 特效本体 | 958 个 prefab（`Assets/WarpforgeVFX/Prefabs/`） | 按红线发布前必须整个移出构建 |
| 材质重建 | `Runtime/WarpforgeEffectBinder` 运行时重建 | ✅ 可用 |
| shader 来源 | `StreamingAssets/WarpforgeVFX/*.bundle` | ⚠️ 里面是**原版编译字节码**，不能进发布版本 |
| **生命周期** | **无** | **特效播完不会自己消失** —— 缺 `AnimFXController` 的等价物 |
| **播放入口** | **无** | 只能手动 `Instantiate`，没有「播一个特效」的 API |
| **触发条件** | **不知道** | 很多效果是被技能/碰撞触发的，不是自播的 |

**所以「还原了 958 个效果」≠「有 958 个能用的特效」。** 差距在后三项。

### 1.2 目标形态：一个自包含的运行时包

建议做成**一个能整体拷进任何 Unity 工程的文件夹**，不依赖本工程的其它部分：

```
WarpforgeVFX_Runtime/            ← 整个文件夹拷走就能用
├── Scripts/
│   ├── WarpforgeShaderLoader.cs     从 StreamingAssets 加载 shader 包
│   ├── WarpforgeShaderMap.cs        原版 shader 名 → 本工程 shader 的映射
│   ├── WarpforgeEffectBinder.cs     运行时重建材质（已有）
│   ├── WarpforgeEffectPlayer.cs     ★新 播放入口 + 生命周期
│   └── WarpforgeEffectLibrary.cs    ★新 注册表：名字 → prefab
├── Shaders/                         5 个自建替代 shader
└── StreamingAssets/
    └── wf_shaders.bundle            发布版本必须换成**自建**的（见红线）
```

### 1.3 ✅ 已实现的两个东西（第五轮做完，这里是**实际**的接口）

**(a) `WarpforgeEffectPlayer`** —— `Assets/WarpforgeVFX/Runtime/WarpforgeEffectPlayer.cs`

```csharp
namespace WarpforgeVFX
{
    public class WarpforgeEffectPlayer : MonoBehaviour
    {
        // 播一个特效。parent 传 null 就挂在场景根上。
        public static WarpforgeEffectPlayer Play(string effectName, Transform parent = null,
            Vector3 localPosition = default(Vector3), float scale = 1f,
            float autoDestroySeconds = -1f);          // <0 = 用效果库里记的原版时长
        public static WarpforgeEffectPlayer Play(WFEffectEntry entry, ...);   // 已经有 entry 时用这个
        public static WarpforgeEffectPlayer PlayFuzzy(string keyword, ...);   // 记不清全名时用

        public static int  ActiveCount { get; }       // 当前活着的实例数（测试靠它证「播→消失」）
        public static void KillAll();                 // 切场景/退牌局时兜底
        public event Action OnFinished;               // 播完时触发（销毁之前）

        public void Exit();                           // 停发射、进入收尾（对应原版 AnimFXController.Exit）
        public void Kill();                           // 立刻销毁
        public void Tick(float dt);                   // 外部驱动（批处理/白板）；autoTick=false 时用
        public bool autoTick = true;                  // 自己跟 Update 走；外部驱动时关掉
        public float ExpectedLifetime { get; }
    }
}
```

**位置/缩放语义（这里踩过坑，别改回去）**：`Play` 会把实例**归位到 parent 的原点**，
但**绝不碰根节点的旋转和缩放**。原因：原版有一批 prefab 把根节点停在**场外 x≈100** 的「后台位」
（`Atk_GrotGrenade` 在 99.59、`Attack_Stomp` 在 100），不归位就会播到场景外面；
而旋转/缩放是效果作者定的（`Antimatter Explosion` 根节点是 X=-90°，`Atk_GrotGrenade` 是 scale 0.5），
归零会把一批效果整体转 90°。`scale` 参数是**乘数**，乘在作者设定的缩放之上。

**生命周期规则**（从原版签名桩 + 2346 个组件的参数值反推，**未经反编译核实**，实拍一比就能验）：

| 参数 | 来源 | 行为 |
|---|---|---|
| `destroyTime` | 原版 `AnimFXController.destroyTime`（912 个效果有，中位 3.0s） | 到点自动走 `Exit()` |
| `exitDestroyTime` | 同名字段（956/990 是默认值 3.0） | `Exit()` 之后再活这么久才销毁 |
| `preventDestroy` | 同名字段（97 个实例是 1） | **不自动销毁**，由玩法代码在合适时机 `Exit()`/`Kill()` |
| 粒子自然时长 | 导出时算的（`duration + 最大寿命 + 延迟`） | `destroyTime` 缺失时（63 个效果）用它 |
| `SAFE_DESTROY_TIME = 3f` | 原版常量 | 上面两个都没有时的兜底 —— **保证没有永远不死的对象** |

> 想覆盖一切时用 `Play(..., autoDestroySeconds: 2f)` —— 连 `preventDestroy` 的也照收，
> 这是玩法代码防泄漏的抓手。

**(b) `WarpforgeEffectLibrary`** —— `Assets/WarpforgeVFX/Runtime/WarpforgeEffectLibrary.cs`

```csharp
WarpforgeEffectLibrary lib = WarpforgeEffectLibrary.Instance;   // 自动从 Resources 取
WFEffectEntry e;
if (lib.TryGet("Antimatter Explosion", out e)) { ... }
List<WFEffectEntry> hit = lib.Find("Explosion");                // 模糊找，按名字长度排序
```

- 资产在 `Assets/Resources/WarpforgeVFX/WarpforgeEffectLibrary.asset`（958 条）
- 每条带：`prefab` / `destroyTime` / `exitDestroyTime` / `preventDestroy` / `natural` / `loops`
  / `verdict`（台账判定）/ `confidence`（置信度）/ `ratio`（亮度比）
- **为什么用 ScriptableObject 而不是 `Resources.LoadAll`**：后者会把整个目录无条件全打进包
  （958 prefab + 1.8 GB 贴图）；SO 的引用列表是「被引用才进包」，而且编辑器与打包后行为一致
- 数据源：`数据/游戏数据/effect_index.json`（由 `工具/gen_effect_index.py` 从
  prefab 目录 + `animfx_components.json` + 还原台账 join 出来），再用
  `EffectLibraryBuilder` 补上 Unity 侧才算得出的 `natural`/`loops` 并生成资产

### 1.4 ✅ 落地顺序（第 1–3 步已做完，第 4 步是「像原版」，第 5 步是「能发布」）

| 步 | 做什么 | 状态 |
|---|---|---|
| 1 | `WarpforgeEffectPlayer` + 兜底销毁 | ✅ |
| 2 | `WarpforgeEffectLibrary` + `effect_index.json` | ✅ |
| 3 | 白板实拍验证「播→消失」闭环 | ✅ 批处理自检 `WhiteboardTest.Run`：**闭环 21/24**（3 个 preventDestroy 本就该外部收）、**渲染与台账一致 24/24**、**5 轮无泄漏**；人看的场景 `WarpforgeArena1/Scenes/VFXWhiteboard.unity` |
| 3.5 | **构建后 player 的验证** | ⬜ 没做 —— 白板自检只在编辑器里跑过。**别把它的绿灯当发布绿灯**（做法见交接文档第八节末） |
| 4 | AnimFX P0 两个模块（`AnimFXController` / `AnimFXModuleTween`） | ⬜ 补上原版节奏；数据全在，见交接文档 P0-e |
| 5 | 发布版本前：77 个原版 shader 逐个自建替代 | ⬜ 红线要求，不急 |

**用法示例（玩法线接特效时照抄）**：

```csharp
// 卡牌攻击时在卡面位置播特效
WarpforgeEffectPlayer.Play(EffectMap.For(cardId, CardAction.Attack), parent: cardRoot);

// 战场环境类（半径上百单位的全屏叠加）挂到战场根节点上，别挂在卡牌上
WarpforgeEffectPlayer.Play("Environmental Condition Black Legion Warp Storm", parent: boardRoot);
```

---

## 二、卡牌表现基座设计

> 原版只作**参考蓝本**：学它的结构、抄它的手感数值、照搬它的设计思路，
> 但最终的布局/交互/动效要自己设计。这一节给的是**建议方案**，不是原版复刻。

### 2.1 原版有哪些现成的、可以直接拿来当参考的

| 资产 | 在哪 | 能拿来干什么 |
|---|---|---|
| `Card Hand To Board` clip（0.92s） | 原版动画数据 | 「手牌落到战场」的**完整时序蓝图**，17 条曲线把时间点拍清了 |
| **74 个 `UnitTweenSO`** | `d:/2/新解包资源/.../bundle_tweenandshakes_assets_all/MonoBehaviour/` | ease / duration / delay / loops / punch 数值**整套可搬**，含具体类型名（`PunchTween` 等） |
| `DrawCard` / `TurnCardAround` | `d:/2/Warpforge_code/`（签名桩）+ Ghidra 反编译 | 原版是 DOTween 代码驱动，`DOScale` + `SetEase` 直接写在里面 |
| `CardHighlight` 5 色态 | 原版预制体 | 可打出/不可打出/选中/目标/悬停 的状态色 |
| `card_anim_map.json` / `atk_vfx_map.json` | `Unity/数据/游戏数据/` | 卡牌 → 特效映射（591 条定义 → 400 个预制品） |
| 34 个卡牌类 / 2024 个方法名 | `d:/2/Warpforge_tools/data/decomp_il2cpp_0827/` | 当**需求规格说明书**用 —— 它告诉你「原版考虑了哪些情况」 |

**取用原则**：数值照搬（手感是调出来的，重调浪费时间），结构和交互自己定（要避版权、也要贴合自己的玩法）。

### 2.2 建议的模块划分

```
Assets/CardPresentation/
├── Core/
│   ├── CardView.cs              一张卡的视觉载体（正反面、图、文字、费用、攻防）
│   ├── CardState.cs             状态枚举 + 状态机（在牌库/在手牌/在场上/进弃牌堆）
│   └── CardAnimator.cs          动效调度器（把下面几个拼起来）
├── Hand/
│   ├── HandLayout.cs            ★手牌扇形布局（角度/间距/弧高随张数变化）
│   ├── HandHover.cs             悬停抬起 + 邻牌让位
│   └── HandDrag.cs              ★拖拽（拿起/跟随/松手回弹/出牌判定）
├── Board/
│   ├── BoardLayout.cs           ★落位（9 格：督军居中 + 两侧各 4）
│   ├── DropTargeting.cs         落点高亮与合法性判定
│   └── DeploySequence.cs        部署动画（手牌 → 战场的一条曲线）
└── Highlight/
    └── CardHighlight.cs         ★5 色态高亮
```

（★ = 你点名要的四项）

### 2.3 四个核心模块的接口草案

```csharp
// ——— 手牌扇形布局 ———
public class HandLayout : MonoBehaviour
{
    [SerializeField] float fanAngleMax = 18f;      // 满手牌时的总张角
    [SerializeField] float arcHeight   = 60f;      // 中间那张比两边高多少
    [SerializeField] float spacingMin  = 70f;      // 手牌很少时的间距
    [SerializeField] float spacingMax  = 120f;     // 间距上限，超过就压缩

    public void Refresh(IReadOnlyList<CardView> cards, int hoveredIndex = -1);
    // 需不需要让位：hoveredIndex 那张抬起，相邻牌往外挪一点（原版的"邻牌让位"手感）
}

// ——— 拖拽 ———
public class HandDrag : MonoBehaviour
{
    public event Action<CardView, Vector3> OnDropped;   // 松手时抛给落位系统判合法性
    public bool IsDragging { get; }

    // 拿起：放大 + 置顶 + 取消扇形旋转；跟随：鼠标/触摸位置 + 轻微倾斜
    // 松手：合法 → 交给 DeploySequence；不合法 → 回弹到扇形原位
}

// ——— 落位 ———
public class BoardLayout : MonoBehaviour
{
    public const int SlotCount = 9;                 // 督军居中，两侧各 4
    public int WarlordSlot => 3;

    public bool TryResolveSlot(Vector3 worldPos, out int slot);
    public Vector3 SlotPosition(int slot);
    public void HighlightValidSlots(bool[] valid);  // 拖拽时只亮合法的格子
}

// ——— 5 色态高亮 ———
public enum CardHighlightState { Normal, Playable, Unplayable, Selected, ValidTarget, Hover }
//                           常规    可打出     不可打出     已选中    合法目标    悬停
public class CardHighlight : MonoBehaviour
{
    public void Set(CardHighlightState state, float blendTime = 0.12f);
}
```

### 2.4 数值从哪来

| 要调的 | 参考来源 |
|---|---|
| 发牌手感（缩放/位移/缓动） | `DrawCard` 的 `DOScale` + `SetEase`；74 个 `UnitTweenSO` 里的 `PunchTween`（`ease` 是 DOTween 的 Ease 枚举，`6` = InOutQuad 一类） |
| 手牌到战场（0.92s 的完整时序） | `Card Hand To Board` clip 的 17 条曲线 |
| 高亮过渡时长 | `CardHighlight` 的色态切换 |
| 落位吸附 | 原版没有独立数值，自己定（建议 0.15–0.2s，`Ease.OutBack`） |

> ⚠️ **不要照搬原版的整体布局坐标** —— 那是给 1920×1080 手机横屏调的。自己的游戏先定分辨率与安全区，
> 再按自己的比例重算扇形半径和间距，只把**缓动曲线和时长**搬过来。

### 2.5 和特效怎么接

`atk_vfx_map.json` / `card_anim_map.json` 是现成的「哪个动作播哪个特效」映射表。
建议在 `CardAnimator` 里加一层：

```csharp
// 卡牌动作 → 特效名，从映射表读，不要硬编码
WarpforgeEffectPlayer.Play(EffectMap.For(cardId, CardAction.Attack), parent: cardRoot);
```

这样特效和卡牌表现是**解耦**的 —— 换特效不用改卡牌代码，换卡牌布局不用碰特效。

### 2.6 建议的落地顺序

| 步 | 内容 | 依赖 | 状态 |
|---|---|---|---|
| 1 | 定分辨率/安全区 + `BoardLayout` 的 9 格坐标 | 无 | ✅ **2026-09-11 已做**（`LayoutSpace` + `BoardLayout`） |
| 2 | `CardView` + `HandLayout` 扇形布局（先只做静态排布） | 1 | ✅ **2026-09-11 已做**（占位卡面程序生成，不依赖美术） |
| 3 | `HandHover` 悬停抬起 + 邻牌让位 | 2 | ⬜ 让位的**布局**已在 `HandLayout.Refresh(cards, hoveredIndex)` 里，缺的是鼠标拾取 |
| 4 | `HandDrag` 拖拽 + 回弹 | 3 | ⬜ 要装 DOTween |
| 5 | `BoardLayout` 落位判定 + `DeploySequence`（用 `Card Hand To Board` 的数值） | 4 | ⬜ 落位判定 `BoardLayout.TryResolveSlot` 已就绪 |
| 6 | `CardHighlight` 5 色态 | 5 | ⬜ |
| 7 | 接特效（`WarpforgeEffectPlayer`） | 一、1–3 步 | ⬜ 特效侧已就绪 |

**⚠️ 分辨率**：用户要求「1080p / 2K 这些能自由切换」，所以布局**不能用固定像素/固定世界坐标**。
现在走的是**归一化坐标（0–1）+ 自适应正交相机**（`LayoutSpace`）：

- 可见**高度恒定** 10 世界单位，宽度 = 高度 × 宽高比
- 归一化坐标相对可见区域，(0,0) 左下、(1,1) 右上
- 窄屏（4:3）可见宽度小于设计宽度时，整体乘一个 `Scale` 缩下来，不裁切
- 超宽屏多出来的宽度就是留白

自检：`Tools > CardPresentation > 生成演示场景并截图`（或 `-executeMethod CardBaseDemo.Run`），
同一份布局在 1080p / 2K / 21:9 / 4:3 各渲一张到 `d:/4/_tmp_view/cardbase/`。

**⚠️ 踩过的坑**：`cam.aspect` 一旦被显式赋值就**固定住**，不再跟着 RenderTexture 走 ——
必须在 `Render()` **之前**设，否则会拿上一个分辨率的宽高比去渲（自检图全部失真）。

**补间：DOTween 已装好**（2026-09-11，版本 1.3.030）。

- 来源：官方仓库 `Demigiant/dotween` 的 `UnityTests.Unity6000.3` 包（Unity 6 对应版本），
  走 `codeload.github.com` 下载（GitHub 官网域名不通、codeload 通）。
  **Asset Store 那条路需要编辑器 GUI + 登录，命令行做不了。**
- 重装/换机器：`工具/install_dotween.py`（幂等，带 `--check`）
- 收尾两步也做了（等价于 Utility Panel）：给用不上的模块加 `DOTWEEN_NOPHYSICS2D`
  （本工程 manifest 没装 `com.unity.modules.physics2d`，开着会编译不过）+
  建 `Assets/Resources/DOTweenSettings.asset`。
  ⚠️ 设置类的真名是 **`DG.Tweening.Core.DOTweenSettings`**，不是 `DG.Tweening.DOTweenSettings`。
- 验证：`-executeMethod DOTweenSmokeTest.Run`（批处理下用 `SetUpdate(UpdateType.Manual)`
  + `DOTween.ManualUpdate(dt)` 手动推进，不进出 play 模式）——
  实测位移精确到位、`OutQuad` 缓动生效。
  ⚠️ 判据用**位置**，别用 `IsComplete()`：默认 `autoKill` 完成后 tween 被回收，`IsComplete()` 反而返回 false。
- 仓库里没有它（`Assets/Plugins/Demigiant/` 已 gitignore），脚本一条命令装回来。

### 2.7 现在有什么（2026-09-11）

```
Assets/CardPresentation/
├── Core/
│   ├── LayoutSpace.cs     归一化 ↔ 世界坐标、自适应相机、窄屏缩放
│   └── CardView.cs        一张卡：占位卡面（程序生成贴图 + 5×7 点阵数字）+ 摆位
├── Hand/HandLayout.cs     扇形布局：张角/弧高/间距随张数变，支持悬停让位
├── Board/BoardLayout.cs   9 格（督军居中）、落位吸附、合法性判定
└── Editor/CardBaseDemo.cs 演示场景 + 多分辨率自检截图
```

**占位美术是刻意不依赖资源的**（连数字都画进贴图里）：真卡面来了只换 `FaceTexture()`，
布局、状态、动效都不用动。**别用 TextMeshPro 做占位** —— 工程里没导入 TMP Essentials 的话
一个字都不显示，是个纯浪费时间的坑（踩过）。

---

## 三、一句话总结

- **特效线已经「能用」了**（第五轮做完）：播放入口 + 生命周期 + 效果库 + 白板自检，三条全绿。
  剩 **构建后 player 没验** + AnimFX 脚本层（补原版节奏）。
- **卡牌基座**是独立的一条线，前 6 步不依赖特效，可并行起步。
  特效现在已经是可用资产，**设计文档 2.5 的「接特效」那一步可以直接做了**。
- 原版的价值是**数值和结构参考**，不是复制品 —— 布局坐标、交互细节、美术全部自己出。
