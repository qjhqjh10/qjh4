# 说明书里我们没读过的 14 份 —— 索引（2026-09-17 补）

> **怎么找出来的**：把 `D:/2/解包整理/**/*.md`（86 份）跟 `d:/4/Unity/资料/**.md` 对了一遍
> —— **14 份从没被我们任何文档引用过**。（判据是「文档里有没有出现过它的名字」，只能算近似。）
> 补读结论如下。用户 2026-09-16 说过：「**这里的资源你不使用，但是这里分好类与有文档，你以后多看看这里**」。

## 一、11 个其它战场的 2D 全树（`说明书/01_战斗_对战/2D层_battlearena*.md`）

**一句话结论：UI 段是同构的；而 `battlearena1` 那份树是「真子集」—— 它漏写了 30 个 HUD 节点名。**

- **证据 A（逐字节）**：13 份（含 arena1）的 UI 段（`Canvas` 行到文件末）分 4 簇，簇间只差
  `-0` vs `0`、`(inactive)` 标注、3 个 `CemeterySliderUI` 折叠矩形（`0x0` vs `748x43`）——
  **11 份非 arena1 的 UI 段都是 1130 行**。
- **证据 B（抽检同名同坐标，11 份全一致）**：`TurnBtn[-137,955 131x80]` · `OffensiveButton[-0,987 109x107]` ·
  `CenterCameraButton[18,1108 64x62]` · `Select Melee Button[-370,1082 118x119]` · `Replay[-540,1122 80x49]` ·
  `Voice Over Button[690,1486 89x89]` · `HideChooseButton[104,908 78x78]` …
- **证据 C**：除 arena2 多一个拼错的调试节点 `Toggle Parrticles` 外，**没有任何 arena 有独有 UI 节点**。
- 🔴 **证据 D（对我们最要紧）**：**arena1 的树少 30 个节点名**，包括
  `Match Skulls` / `Match Skulls Text` · `EnemyName`(+Text)/`NameBackground`/`TitleBackground` ·
  `ShowCemeteryBtn` · `PlayerLabel`/`NameHolder`/`TitleHolder`/`TitleLabel`/`Title Text` ·
  `Player Rating Display` · **`HideMulliganButton`/`MulliganContinueButton`** ·
  `Minion Position Highlight`/`Glow`/`Shadow` · `RightMinionArea` · `YourTurnImage` · `Energy`(多 9 处) ·
  `Item`/`Item Background`/`Item Checkmark`/`Item Label` · `SlidingArea` · `Individual rating value` ·
  `QPText` · `Shadow`。
- ✅ **但那是「说明书漏写」，不是「战场不同」** —— 2026-09-17 逐个核过
  **`07_场景/battlearena1/` 的场景 JSON 里这 12 个节点名全都在**（`MulliganContinueButton.json`、
  `ShowCemeteryBtn.json`、`YourTurnImage.json`…）。
  ⚠️ **所以：看战斗 HUD 要拿 JSON，别只信 arena1 那份树**（同一个毛病已经在 `ButtonsGroup` 上吃过一次）。
- **环境节点全部无坐标**：988 个坐标行 100% 属于 UI/RectTransform，**env 坐标行实测 = 0**。
  这些文件只给「道具清单 + 层级 + 命名 + 粒子/灯光组合」⇒ **做「战场布置」不能拿它们量坐标**。

| arena | 独有的环境件（值得抄的） |
|---|---|
| arena2 | 骷髅巨像 + `Toxic Pump` + 毒气池 |
| arena3 | `Big Monolith×7` + 绿闪电 + 圣甲虫群 |
| astramilitarum | `Valkyrie` + 起飞台（**独立根，挂在 Canvas 之后**）|
| blacklegion | `Arcs_Large/Small` + `Firepits` + 竞技环（**根顺序是倒的**）|
| darkangels | 炮塔×2 + 战舰远景分层（两段 baked）|
| emperorschildren | `Trophy Rack` / `Weapon Rack` + 大锅火 + 气泡 |
| genestealers | 铁链/吊钩/泵 + 污水（族长粉丝扇）|
| leviathan | **只有 `BattlePrefab` 一个根**，结构最干净，可当 prefab 组装范本 |
| sororitas | 教堂祭坛：**蜡烛×31** + 侧祭坛阵列 |
| spacewolves | **满月×13** + 暴雪 + 狼雕像 |
| tauviorla | 等离子发电机 + 磁轨炮塔 + 星球远景 |

## 二、`04_界面UI/菜单全树_要点_第2/3/4块.md`

| 块 | 覆盖 | 关键内容 |
|---|---|---|
| **第 2 块**（55 条明细 / 74 个根界面） | 商店与 Offer 家族 · 通行证 · 排位 · 成就 · 设置 · 收藏 · 联盟排行 · 每日奖励 · Draft | **136 个表格坐标单元** + 10 条「公共组件/贴图速查」（按钮底/窗口底/关闭钮/输入框/倒计时/进度条/选项卡的 PathID 齐备）；Offer 全体系由同一套 Drawer 拼装 ⇒ 做一套可复用 drawer 覆盖 90% 变体。缺口：11 条未实现 + 7 条待核 |
| **第 3 块**（60 个根界面） | 🔴 **`#18 Deck Editing Menu` = 全部 14 份里唯一一份完整的「卡组编辑界面」规格** | `Content 1753x1009` / `Sidebar 335x924` / `Window Options(Cards/Info/Cosmetics)` / `Deck Name 输入框` / `Deck Details 335x649` / **费用分布 drawer 9 行** / `Footer 'Done' + '30/30'` / `Card Display Scroll View 1590x924` / 筛选器（Search/Owned/Upgradable/Army/Rarity/Energy Cost/Type）。**141 个坐标单元 + 12 处 PathID**。三件套：#25 `Deck Selector Card Info button 310x56`、#28 `Card in deck container 570x620`、#33 `Deck CostQuantity Row Drawer 224x20`。⚠️ 它自评「已实现」指的是 **`d:/warpforge` 那个 Godot 工程，不是本工程** |
| **第 4 块**（82 个根界面） | 奖励抽屉 · 活动 · 公会/联盟 · 排位 · Draft · 邮箱 · 聊天 · 好友 · 成就 | 与对战准备有关的只有「卡组**选择/展示**」（#11 `DeckSelectionContinueButton 833x50`、#79 Deck Selection Popup with Tabs、#6 Draft 卡组信息面板 390x712），**不是编辑**。158 个坐标单元 + 168 处 PathID。缺口：17 条未实现 |

**「战场布置」在菜单全树里没有对应内容** —— 菜单三块纯 UI；3D 战场只在 `01_战斗_对战/` 的 arena 全树里，
而那里**没有坐标**（见上）。

## 三、优先消化哪几份

1. **任一份非 arena1 的 arena 全树**（推荐 `leviathan` 或 `astramilitarum`）—— 但**当索引看、别当坐标源**，
   要坐标去 `07_场景/battlearena1/` 的 JSON。
2. **第 3 块** —— 唯一完整的卡组编辑页规格，正对「界面/战场还原度」那条待办；第 2/4 块以商店/活动为主，
   与本项目当前范围无关，可继续挂起。
