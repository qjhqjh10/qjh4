# 特效还原 —— 进度与交接

> 建立：2026-09-10　|　最后更新：**2026-09-18**（瘦身：旧进度叙述挪 `资料/历史/`；**现状与数字见 §〇 + `资料/特效还原台账.tsv` 一处**）
> 面向：**继续这条线的新会话必读**
> 上游任务：把 Warpforge 解包特效「还原」成能在自研卡牌游戏里当素材用的 Unity 资产
> 主入口：`d:/4/项目任务.md`

---

## 〇、当前状态 —— 下次从这里接着做

| 项 | 结果 |
|---|---|
| **P0-f 悬案** | ✅ **已判掉** —— 「Z 计数掉 36」是**指标太糙**，不是资产退化。验收指标已换成 `\|ln(亮度比)\|`（见 P0-f） |
| **验收尺子** | ✅ 已换 + `analyze_sweep.py` 每次运行会打「尺子自检」表，计数变化先跟它比 |
| **播放入口** | ✅ `WarpforgeEffectPlayer` —— `Play(name, parent, pos, scale, autoDestroy)` / `Exit` / `Kill` / `ActiveCount` |
| **效果库** | ✅ `WarpforgeEffectLibrary`（SO）—— **2026-09-18 起 959 条 = 958 原版 + 1 个自制样例**（自制那批来自 `Assets/CardPresentation/Effects/`，由 `EffectLibraryBuilder.CollectUserEffects` 扫目录收进来；**这条路原来不存在**，见 `资料/自制特效_加入流程.md`）+ `数据/游戏数据/effect_index.json`（仍是 958）+ `工具/gen_effect_index.py` |
| **白板自检** | ✅ `WhiteboardTest.Run` 批处理跑（**样本 = 跨全表等距挑 20 个常用效果**）：闭环 / 渲染与台账一致 / 无泄漏 —— **数字见自检输出** |
| **白板场景** | ✅ `Assets/WarpforgeArena1/Scenes/VFXWhiteboard.unity` —— 打开按 Play 就能看（`WhiteboardBuilder.Run` 重建） |
| **🆕 P0-h 精灵没导出** | ✅ **已修** —— `SpriteRenderer`/`SpriteMask` 的 sprite 是 bundle 资产、引用落不下来；被 `VisibleInsideMask` 遮罩的粒子因此**整块渲成空**（78 个 prefab 中招）。详见 P0-h |
| **🆕⚠️ 尺子本身曾经不确定** | ✅ **已修** —— 同一份资产连跑两遍扫描，**变好/变差的行数与要测的改动一样大**。真因是粒子系统的 `useAutoRandomSeed` 每次重播换种子（见 `资料/比对基线/README.md`） |
| **全量指标** | ⚠️ **数字只写 `资料/特效还原台账.tsv` 一处**（`analyze_sweep.py` 生成），本文档不抄第二份 |
| **另开一条线** | 卡牌基座（布局/卡/悬停/拖拽/落位/状态色/接特效）→ `资料/卡牌基座_进度与交接.md` |
| **还没做** | ✅ **拖尾贴图那件 2026-09-17 已修好**（真因是 `MapProp` **无条件改名**，**不是**渲染器遍历那一层，受影响 **207 个材质**）—— 过程 · 数 · 顺带浮出来的几件见 §〇之三「三之补二」。🔴 **下一件 = 「E 组 151 条的逐效果定根因」**（2026-09-18 重扫后的新账；**三步找主因全部落空**，
两条路见下表末行与 `项目任务.md` 第 19 行） · ⚠️ **原版侧渲染会随「前面跑过什么」变**（`StrikeEffect` 实测差 **21%**，导出侧稳定）—— **尺子要修，别照旧数去改 shader**；实用规避：要查某个效果就把它写在清单第一个 |
| **C 组（导出整个丢了）** | ✅ **已清空 15 → 0**：`Explosion_Ground`（补 `Particle Premultiply` 映射）· `BlastEffect`（三张 ShaderGraph 进映射表，现归 E 组偏亮）· **`PinDownEffect`（2026-09-17 判「尺子噪声」，见 §〇之三 三）** |
| **动画脚本层（AnimFX）** | ✅ **2026-09-18 实现完毕**（15 个模块类 + 数据链 **895 效果/2313 模块** + 工厂自动登记）→ **`资料/AnimFX_实现与接线.md`**（含 §十一 全量反编译复核、§11.6 `ChangeShapeAngle` 复刻路径）。<br>⚠️ **两个数字都对、是两个口径**（2026-09-18 核）：**912 / 2346** = `数据/游戏数据/animfx_modules.json` 的**原始总数**；**895 / 2313** = 与效果库 958 条的**交集**（差的 17 个是挂在 UI / 棋盘 prefab 上的 AnimFX 控制器，**本来就不该收**）—— 自检断言按**交集**算，否则每次都红。<br>~~⬜ 没动。成表与建议顺序 → `资料/AnimFX_18类成表.md`~~ ← 2026-09-18 前的状态 |

> 🔴🔴 **E 组的当前正本 = `资料/普查产出_0918/E组根因_汇总.md` §〇之〇**（2026-09-18）：
> **重扫后 E 242 → 151 · Z 491 → 580**（全样本 `|ln|` 中位 0.170 → 0.092）·
> **三步找主因全部落空**（`Particle Dissolve Mask` 白名单其实生效 · `Sprites/Default`+`Sprites/Mask`
> 只是标记、结构差异 0 · `Matcap` 无信号 · 连「锅 H₁ `_EmissionColor`」也因**89% 的效果都有**而被排除）·
> **判据教训**：控制了两个混淆项（`分组` + shader 种数）**仍被判据**，真正的混淆是**效果家族**。
> ⏭ 两条路：**甲** 换口径再来一轮并行普查（家族 `fam` + 发射器数 + 主导槽 shader 三维）·
> **乙** 干预实验（改 `_EmissionColor` 那条加法项再扫一次，A/B 直接量，约 30 分钟、可回退）。**建议先乙。**
>
> ⚠️ 下面两份是**按旧台账（242 条）**分的组，逐效果结论**要按新账重核**，别再照它们动手：
> `资料/特效E组_根因初判.md`（E 组逐类初判 · 7 块互斥类别）· `资料/特效E组_逐效果根因表.md`（按锅分组）。
> ⚠️ **旧账里还有几条「锅」已被证伪**（真因在别处）——见汇总 §三：锅 **A**（trailMaterial 贴图全丢）已不成立
> （真因是贴图名 `_MainTexture` vs `_MainTex`，`WarpforgeEffectBinder.cs:117` **静默不设**）·
> 锅 **F** 在本块是假的（那是同 prefab 里 `m_Enabled:0` 的禁用根渲染器）·
> 「抓屏覆盖 41 条记成已收账」**方向反了**（那是**换尺子**）。

---

## 〇·一、✅ **那次退步已定位并修好（2026-09-15）—— 一句话：`_EmissionColor` 被当成乘性因子**

**真因**：第三十三轮 `4e0741d`（③-3）给自建 shader `WFParticlesExtraColor.shader` 加了一行
`col = tex * _Color * _EmissionColor * IN.color`（**乘法**）。而原版那批材质里的
`_EmissionColor` **是黑的死值**——URP / 内置粒子 shader 的属性表里它就是
`[HDR] _EmissionColor("Color", Color) = (0,0,0)`（`ParticlesUnlit.shader:9`），
没开发光的材质原样留成 `{0,0,0,1}`，而原版那边**根本不采它**（`_EmissionEnabled=0` /
没有 `_EMISSION` 关键字）。⇒ 我们这边一乘，**整个材质被乘成黑**。

**修法**：改回**加法发光项**（原版那套就是加法：URP `ParticlesLitInput.hlsl:103`
`emission = _EmissionMap * _EmissionColor.rgb`，算完是**加到**表面色上的）：

    half4 col = tex * _Color * IN.color;
    col.rgb += tex.rgb * _EmissionColor.rgb * IN.color.rgb;   // 黑 = 不加 = 什么都不发生

⚠️ 当时还有**三行「略偏亮」**（`StrikeEffect` / `VulnerableEffect` / `EnvironmentalCondition…`）已于 2026-09-15 定案 —— **不是资产缺陷**，见下。

> ⚠️ **2026-09-15 定案：这三处不是资产缺陷，是「原版侧渲染会随跑法变」—— 文档那三个数出自一批被污染的小扫。**
> ① **`CreateCard BlackLegion` 1.96 vs 台账 0.41 = 纯口径差**：文档那个是「所有原版有值的时点取中位」，台账（`analyze_sweep.py:178`）把它收在**原版有内容的时段**（原版只有 `1.50s` 一格、导出 `0.15–1.50s` 全程有 ⇒ `75/181`）⇒ 它真正的问题是**时序/寿命**，不是亮度。
> ② 🔴 **`StrikeEffect` / `VulnerableEffect` / `EnvironmentalCondition…` 的比值不可信**：连跑三次 + 全量一趟 + 单跑一趟逐时点比 —— **导出侧 13/13 逐位相同、取景缓存 md5 逐字节相同，只有原版那一侧会变**（最大偏差 20.9% / 5.5% / 4.6%），而且**不是批次大小、是「它前面跑过什么」**（`StrikeEffect` 原版 `t=0.15` 亮度和：单跑 **437** / 小批第 6 个 **344** / 全量 **435**）⇒ **全量/台账的 1.00 是干净值**。
> ③ **机制（高度可疑，但还没实证到行）**：**全局 shader 关键字泄漏** —— `_EMISSION` 在这个 shader 族里是*全局*关键字（`EffectExporter.StripGlobalKeywords` 注释里写着；原版由 `ParticleSystemRenderer` 按 Emission 模块开关，`Material.shaderKeywords` 抓不到），全局状态是**进程级**的 ⇒ 上一个效果的渲染器打开它、下一个跟着亮；导出侧因为把它剔掉了而免疫。
> ④ **下一步（别跳过）**：先钉死污染源（逐个试 `[Bore Through Intense, StrikeEffect]` 这类两元素清单）→ 再改 `EffectSweepBatch.RenderAt()`（⚠️ **别凭 shader 名推断关键字，要动必须先有实测证据**）→ 修完跑全量重出台账。
> ⑤ 📌 **跑小批前必看**：`EffectSweepBatch` 里写 `ResultPath` / `FrameCachePath` 的那几处是 `File.WriteAllText`，**小批会把全量基线 `sweep_{orig,exp}.tsv` 与取景缓存整份覆盖掉**（先 `cp` 备份并存 md5、跑完按 md5 还原）。

**全量指标与逐组计数只认 `资料/特效还原台账.tsv` 一处**（修完已重跑全量 sweep 重生；`analyze_sweep.py` 每次会打「尺子自检」表，**看计数变化前先跟它比** —— 比边界摆动还小的变化说明不了任何事）。**本节不抄第二份**。
⚠️ **`资料/特效E组_逐效果根因表.md` 的锅名单要按新台账重核**（那张表是按旧台账分的组）。

**仍然成立的三条老经验**（写在这里免得再踩）：
① 「编辑器里好、进游戏坏」是这条线最难查的一类 bug —— **编辑器里的比对图不能代表运行时的真实水平**；
② 看到「一大片效果同时坏掉」**先怀疑尺子**（这条已经救过三次）；**这次是第四次的变体：先怀疑"新增的那条通路"** ——
   一笔小改动（一行乘法）能让 30 个效果同时变暗，而它在别的效果上是对的；
③ **原版材质上带的是内置 shader 的默认值，不是「原版数据」** —— `_SrcBlend` 那批是，
   `_EmissionColor` 这批**也是**。**新往自建 shader 里接一个原版属性之前，先问它默认值是什么、
   原版在什么条件下才采它**（`工具/dump_shader.py` 读属性表）。

## 〇之三、C 组「导出整个丢了」—— ✅ **已清空（15 → 0）** + ✅ **拖尾贴图那个 bug 2026-09-17 也修好了**（见「三之补二」）

### 一、先纠一条量法：`CEmitProbe` 的工况是错的，别再用它的「导出」数

`CEmitProbe.cs` 在**同一个进程**里把原版和导出都渲了（`AssetBundle.LoadFromFile` 只做一次）。
而本文件头早就写着：**导出那侧一个源 bundle 都不能加载**，否则 `wf_shaders_extra.bundle` 被顶掉、
导出侧退回**占位材质**。它自己的输出就露馅了 —— 导出侧 `Mat_Fx_ParticleSet_apb` 报的是
`Universal Render Pipeline/Unlit`（占位），原版侧才是真 shader `Shader Graphs/Fx_ParticleDissolve_apb`。
⇒ **它那两句「导出 1113 / 4206」和「→ 结论：问题在材质」都不作数**（那是在占位材质下量的）。
`EffectSweepBatch` 两趟分进程的工况才是对的。

### 二、`Explosion_Ground`（**C 组随后清空，见 §〇之三 三**）

> ⚠️ **2026-09-17 更正：本节原来记的根因「`Everguild/FX/Particle Premultiply` 不在任何我们随包走的 shader 包里」已不成立** —— 它**就在** `wf_shaders_extra.bundle` 里（当时是拿**裸字节** grep 那个包，而它的数据块是 **LZ4 压缩**的 ⇒ 「搜出 0 命中」被当成了「不存在」）。
> 更正全文 + 「这一条该不该改走原版 shader 本体」的待办见 `WarpforgeShaderMap.cs:97-108`。
> 现状：该效果走的是**按名字挑的近似**映射（`WarpforgeShaderMap.Replacements` 与 `EffectExporter.ShaderMap` **两份要同步**），已从 C 组挪到「偏亮」，**亮度还没单独调**（用同一个 shader 的另外 12 条会跟着动）。

### 三、~~C 组最后一个 `PinDownEffect`~~ ✅ **2026-09-17 结案 —— 判「尺子噪声」**

> ⚠️ **2026-09-17 瘦身**：这一节原来有 **57 行**「跑 A 探针 → 又跑 B 探针 → 再跑 C 探针」的进度叙述，
> 按铁律 6 压成下面这条线。原始数字仍在 `资料/比对基线/sweep_{orig,exp}.tsv`，探针在 `Assets/WarpforgeArena1/Editor/`。

**起点**：台账把它判进 C 组的依据 —— 原版 lit `69→104`、导出只有 `9→43`，
但**导出侧亮度和只有 1–6 ≈ 噪声** ⇒ 判的是「**暗到看不见**」，不是「没渲」。

**排查链（四个探针逐层排除，全部通过）**：`CEmitProbe`（逐渲染器五项全同：活粒子数 / 材质 / shader / render queue / 结构）→ `MatIsoProbe`（材质层排除，基准 **0.0%**）→ **`ParticleModuleProbe`**（2026-09-17 为这条新写的「逐模块值对照」探针，**模块值也全同**）→ `EffectIso` 四方对照（图 `d:/4/_tmp_view/iso/PinDownEffect_四方.png`）。

⚠️ **顺带纠正一条**：那三个材质共用的 shader guid `0406db5a14f94604a8c57ccfbc9f3b46`
**在 `Assets/` 下没有 `.meta` 是正常的** —— 它解析成 `Universal Render Pipeline/Particles/Unlit`，
那是 **URP 包里的 shader**（在 `Packages/`）。**不是「shader 丢了」。**

⇒ **结论：找不到任何资产层差异 ⇒ 判「尺子噪声」，不要再改资产。**
依据：四个探针从四个方向收敛；§〇 早就记着**原版侧渲染不稳定**（`StrikeEffect` 实测差 21%）；
而这条的判定建立在 **69 vs 9 个亮点**这种量级上。

⚠️ **唯一还没验的一层**：那张 **8×8 精灵的像素内容** —— 材质类探针盖不到挂在
`SpriteRenderer` / `SpriteMask` 上的精灵。**下次要接就从这儿接。**

### 三之补、🔧 新工具：`ParticleModuleProbe`（逐模块逐字段对照）

`Assets/WarpforgeArena1/Editor/ParticleModuleProbe.cs` —— `-executeMethod ParticleModuleProbe.Run`，
筛 `grep "^PM "`，只看差异 `| grep "★"`。它按**层级路径**配对原版 prefab 与导出 prefab，比六层：

层级 + 变换（localPosition/Rotation/Scale）· **全部可渲染件**（enabled / material / shader / texture / mesh /
sprite / `maskInteraction`）· 材质表 + `ParticleSystemRenderer.mesh` · **`SpriteMask` 自己的字段** ·
精灵几何（rect / 纹理尺寸 / pivot / ppu / bounds）· **粒子模块值**（Main / Emission / Shape /
Color·Size·Rotation·Velocity OverLifetime / SizeBySpeed / Noise / TSA / Renderer）。
曲线与渐变按 **9 个采样点**比（形状一样才算同）。`Targets` 里已放了 5 个样本（拖尾那两个族）。

⚠️ **这支探针自己踩过三个坑，别重踩**：
1. **同名兄弟按名字配对会撞车** —— `PinDownEffect` 底下**两个渲染器都叫 `Sprite Mask`**，
   字典后写覆盖前写 ⇒ 比的是两个不同对象、报出来的差异**全是假的**。
   ⇒ 路径现在带 `#同级序号`，并且**额外无条件把两侧渲染器清单各打一遍**（顺序无关，一眼看出是不是同一批）。
2. **`SpriteMask` 不是 `SpriteRenderer`** —— `as SpriteRenderer` 对它返回 null ⇒ 那一层会被**静默跳过**。
3. **`SpriteMask.showSprite` 不存在** —— 「Show Mask Graphic」在 **`SpriteRenderer.maskInteraction`** 上（编译错过一次）。

🔴 **还有一个更根本的**：**这支探针的「差异条数」不稳定**（`GetInstanceID()`、同名副本的 ` 2` 后缀
每次加载都变；同一效果连跑两遍 14→22、另一条 64→**108**，而那些效果**根本没重导**）。
⇒ **判据要用「哪些字段不同」这个集合，不能用「几处不同」这个数。**
（⚠️ 这也回过头影响上面那条结论的**措辞**：`PinDownEffect`「14 处」这个**数**不稳，
但**逐条读出来的内容**全是「资产名 / 实例名」这一类 ⇒ **「没有资产层差异」的结论仍然成立**。）
⚠️ 另有一条噪声：`fmt=BC7`（原版 bundle）vs `fmt=DXT5`（我们重导的 PNG）会被报成「不同」——
那是**导入格式**的正常差异，**判「丢没丢」只看哪一侧是 `<null>`**。

### 三之补二、✅ **拖尾材质的贴图在导出侧丢了 —— 2026-09-17 已修好**（真因是 `MapProp` 无条件改名，**不是**「只导根渲染器」）

`Trail_fading` ×4 · `LightningTrail` ×1 —— **全是拖尾材质的贴图**
（`ParticleSystemRenderer.sharedMaterials` 的最后一项就是拖尾材质）。
拖尾没贴图 ⇒ 渲出来一块**实心拖尾** —— **这正是待办表第 3 行说的「黑方块那批」的真实成因**
（那条原来记的是「材质 alpha 没还原对」，**是错的**）。

**根因（2026-09-17 已查明并修好）—— ⚠️ 本节原来写的根因是错的，更正痕迹见下**：

⚠️ **2026-09-17 更正**：本节原来写「`EffectExporter.cs:299-307` 只对**根**渲染器 `psr0` 调
`ImportMaterial(trailMaterial)`，其余渲染器一律 `trailSlots.Add(-1)`」—— **这条不成立**。
实测：循环头就是 `GetComponentsInChildren<Renderer>(true)`，**每个渲染器都走到**（`psr0` 只是变量名起坏了）；
`FadingTrail_add*.mat` **确实导出来了**（` 1.mat` 被 **54 个 prefab** 引用，在
`Artillery Manticore down 1x.prefab` 里就是 `m_Materials` 的第 2 条）；`Trail_fading.png` **也在**
（纹理叫 `Trail_fading`、材质叫 `FadingTrail_add` —— 原记录把两个名字混着写了）。
**错因**：只读到变量名 `psr0` 就当成「根渲染器」，**没通读循环头**。

🔴 **真因**：`EffectExporter.MapProp` **无条件**把 `_MainTex`→`_BaseMap`、`_Color`→`_BaseColor`，
紧接着 `if (!mat.HasProperty(tp)) continue;` —— **目标 shader 没有映射名时静默跳过**。
而我们**自建的 shader 用的恰恰是 `_MainTex`/`_Color`**（URP 才用 `_BaseMap`）
⇒ 凡是落到自建 shader 的材质，**贴图和颜色整个丢掉、不报错**。
实测（扫 `Assets/WarpforgeVFX/Materials/*.mat`）：落在 `WarpforgeVFX/Particles/Extra Color` 上的
**207 个材质、带贴图的是 0 个**；`WFUnlitAmbient` 10/13（它声明的是 `_BaseMap`，映射命中了）；
`WFMatcap` 保全的是 `_MatCap`（名字没被改），`_MainTex` 照样空。
⇒ **这不是「5 处丢件」，是 207 个材质**，拖尾只是其中最扎眼的一处。

**修法（2026-09-17 已落地）** —— 四处，一句话都是「**让静默变可见**」：
1. `MapProp` 改成**目标感知**：映射名目标材质不认就**退回原名**，两个都不认才算真没有。
2. `trailSlots` 的记账挪到 `if (mats.Length == 0) continue;` **之前** —— 取用方按**渲染器序号**取
   `trailSlots[ri]`，原来那个 continue 会让**后面所有渲染器的拖尾槽整体错一格**（静默）。
3. `DefIndex` 里 `try { tl.Add(名); tv.Add(ImportTexture(值)); } catch { }` —— 抛异常时**名字加了值没加**，
   两个列表长度对不上；而运行时按 `i < texNames.Length && i < texVals.Length` 走
   ⇒ **后面所有贴图一起失效**。现在值先算出来、失败也 `Add(null)` + 大声报。
4. `ImportMaterial` 那个**空 `catch { }`** 与 `!HasProperty` 的**裸 `continue`** —— 前者改成把异常打出来；
   后者只在「**源上真有贴图/颜色**却搬不过去」时**按材质报一条**（源 shader 的独有属性本来就该跳过，
   全报出来会淹掉真话）。

**修完的数（2026-09-17，全量重导 958/958 成功、0 失败 → 再跑 `EffectLibraryBuilder.Run`）**：自建 shader `WarpforgeVFX/Particles/Extra Color` **带贴图 0/207 → 290/316**；`FadingTrail_add.mat` 的 `_MainTex` 从 `{fileID: 0}` 变成指向 `Trail_fading.png`（guid 逐个对上）。

⏭️ **顺带浮出来的几件**（① ④ 还没做，② ③ 已办；都不是「现在坏了」）：
① **172 条「目标 shader 认不出 N 个属性」** —— 新警告第一次把**我们替代 shader 丢掉的源属性**列了出来
   （`_SecondaryTex` 35 · `_Noise` 29 · `_Disolve` 18 · `_NoiseTex1/2` 18 · `_MatCap` 9 ·
   `_EmissionTex` 5 · `_FireNoise` 4 …）。**这批是真丢件，只是以前一声不吭**。
② ✅ **3 个材质导不出贴图 —— 2026-09-17 已修好**。真因跨两层，**都跟贴图本身没关系**：
   · **`EncodeToPNG` 对压缩格式是「返回 `null`、不抛异常」的** ⇒ 原来那句 `File.WriteAllBytes(full, png)`
     拿 null 当字节数组，抛 `ArgumentNullException: Value cannot be null. Parameter name: **bytes**`，
     **栈指向 `File.WriteAllBytes`** —— 所以它一直挂在「导贴图抛异常」名下，没人看出是格式问题。
     （⚠️ 我第一轮加的分步诊断也因此**没触发**：它包的是「抛异常」，而这里是「返回 null」。）
   · 而且 **crunch 压缩的纹理「标记成可读也没用」**：`GetPixels` 直接报
     「texture is crunch compressed while this function does not support crunched textures」。
   实测受害者：`Sororitas_VFX_Mask`（256² **DXT5**）与 `Battle Arena Space Wolves Floor`（4096² **DXT5Crunched**）；
   影响 3 个材质（`Sororitas_Flames_TwoLayer` · `Space Wolves BG First Light` · `BG Full Moon`）。
   **修法**：① `ImportTexture` 编不出就**摊成 RGBA32 再编一次**；② `ReadableCopy` 里
   **crunch 格式强制走 GPU 那条路**（`Graphics.Blit` → `ReadPixels`；GPU 采样不受 crunch 限制）。
   ⚠️ **判据是「格式」不是「大小」**：同一次导出里 8192² 的 `Battle Arena Space Wolves Props 1` 是 **RGBA32**、
   一直正常 —— **别把这条记成「大图导不出来」**。
   验证：重导后 `Sororitas_VFX_Mask.png`(22 KB) 与 `Battle Arena Space Wolves Floor.png`(7.6 MB, 4096²) 都产出，
   像素极值都是满量程（不是空白）；这 3 个材质现在有贴图，**贴图类警告 0 条**。
③ `WarpforgeShaderMap.cs` 里 `Particle Premultiply` 那条注释**原来是错的**（2026-09-17 就地更正，现 `:97-108`）。
④ 🆕 **`textureSheetAnimation` 的 sprite 列表在导出侧是空的**（2026-09-17 读 AnimFX 时顺带查出）：
   `AnimFXModuleCardback`（**30 实例 / 18 效果**）是靠 `tsa.AddSprite(cardback)` 把**卡背**塞进粒子的，
   而我们的导出器**只导 `SpriteRenderer` / `SpriteMask` 的 sprite**，**没管
   `ParticleSystem.textureSheetAnimation` 的 sprite 列表** ⇒ 那 18 个效果的卡背是空的。
   动手前先拿原版的 `CreateCard*` 对比一眼（出处 `资料/AnimFX_18类方法体_块2.md`）。

⚠️ **用过滤重导的两个坑**（当天踩过，**仍然成立**）：① 必须 `Resume=true`，否则 `ClearGenerated()` 会把其余
956 个效果的产物一起清掉；② **必须先把 `导出报告.tsv` 里那几行删掉**，否则 `IsDone()` 会当「已完成」跳过
（第一次就这么白跑了一趟）。两条都已写进 `EffectExporter` 那两个字段的注释里。
⚠️ **顺带更正一条**：`导出报告.tsv` 的第 3 段（`材质定义N个；原 shader: …`）**只有在那一行真被导出过时才有**；
Resume 续跑留下的报告里绝大多数行只有两格（实测只 2 行带第三段）⇒ **要 shader 名单别读那份报告**，
去直接枚举 bundle：原版 shader 共 **135 个**（不是 77），逐 shader 属性表见
`资料/普查产出_0917/shader属性表_汇总.md`。

### 四、下一步（按性价比）

1. ~~重跑全量 sweep 出台账~~ ✅ **2026-09-15 已做**（见上）。
2. ~~`BlastEffect` 的 `Fx_RockDissolve` 补映射~~ ✅ **已修，已收账**（C → E）。
3. ~~**`PinDownEffect`**~~ ✅ **2026-09-17 结案**（判「尺子噪声」，见 §〇之三 三）。
   ~~🔴 **现在的下一件 = 拖尾材质的贴图在导出侧丢了**~~ ✅ **2026-09-17 也已修好**（`MapProp` 那条，见 §〇之三「三之补二」）。
   🔴 **现在的下一件见 §〇 表「还没做」那一行**（= 「E 组 151 条的逐效果定根因」；`项目任务.md` 第 19 行）。
   （原写「= ① 或 ②」：② 已于 2026-09-17 修好，① 172 条仍没做，见「三之补二」末尾。）
4. 要动 `CEmitProbe` 的话：照 `EffectSweepBatch` 加 `WFPROBE_SIDE` 环境变量分两趟，
   取景复用 `资料/比对基线/sweep_frames.tsv`。

## 〇之二、接进玩法 —— 接口就一句话

特效已接进战斗：入口 `CardPresentation/Core/VfxMap.cs` —— **卡牌只报「我发生了什么事件」，播哪个特效由 VfxMap 查**（出牌 / 登场 / 近战 / 远程 / 挨打 / 阵亡 / 技能 / 触发**都接上了**；⚠️ 缺的只是「每条之间隔多久」那张表 = AnimFX 脚本层）。
逐条明细 / 批处理下特效不会自己消失的坑 / 挑特效的试片台与三张对比图 → `资料/历史/特效还原_接进玩法_0911-0912.md`。

---

## 一、一句话结论

**能还原。958 个效果已全部导出。** 当前指标与逐组计数**只看 `资料/特效还原台账.tsv` 一处**
（`analyze_sweep.py` 每次重生，还带一张「尺子自检」表）—— **本文档不抄数字**，也别引用更早的比对数字
（前两轮那批被原版渲染的品红污染过，已作废）。
离「能直接进战斗」还差两件事：**高置信的亮度/密度偏差**（E 组为主），
~~以及**没有 AnimFX 脚本**（特效不会按原版节奏自己播）。~~
✅ **2026-09-18 更正**：AnimFX 脚本层**已经做了**（15 个模块类，见 `资料/AnimFX_实现与接线.md`）。

---

## 二、三个已实测确认的事实（别再重新验证，直接引用）

### 1. 原版特效**不需要服务器**，本地就能跑

- 用 `d:/2/unity_run_ref/`（MelonLoader + SceneJumpShot mod）实测：加载 `scenes_scenes_battlearena1.bundle` → 进 Battle Arena 1，**火焰/烟/坠落碎片都是活的**
- 战斗场景带完整 UI 和调试按钮面板（`AddMana` / `AddPlayerCardButton` / `SummonCardButton` / `Card Picker`…）
- **资源层面服务器上没有东西**：catalog 里的 84 个 bundle 全部解析为本地路径，磁盘 84 个文件一个不缺。CCD 远程地址只是模板占位符（`m_CcdManagedData` 全空 + `DisableCatalogUpdateOnStart: true`）
- 未打通：手牌注入（`inj FAIL: empty Data`）。⚠️ **2026-09-13 更正**：这里原来写「卡牌数据在 `*cardassets` bundle 的 `RawCardScript` 里」，
  是**没验证的猜测，与实测不符** —— 实测：① `bundle_<阵营>cardassets_assets_all/` **压根没有 `MonoBehaviour/` 目录**（13 个包结构一致：只有 `AssetBundle/AudioClip/Sprite/Texture2D`，`_stats.json` 与对象数完全对上，**没有失败项**）；
  ② `RawCardScript` 的 MonoScript PathID `8864902669260471755` 在 `d:/2/解包整理`（44.9 万文件 / 5.35 GiB）里**0 命中** —— 即**一个 `RawCardScript` 实例都没有**；
  ③ 字段名（`summonAnimDelayed` / `cardImageReference` / `cardShortId` / `manaCost` …）逐个搜**全是 0**，而同一条命令搜 `animAdressable` 有 2198 命中（**搜索方法有效，不是漏搜**）。
  ⇒ **卡数据是运行时从服务端拿的，本地本来就没有**（和 `项目任务.md` 早先记的「客户端只有类定义、没有数据资产」一致）。
  「想拍卡牌特效的实拍，得先把这条路打通」这句仍然成立，但**不是「有个包我们没解」**。

### 2. 依赖闭包完整

加载全部 84 个 bundle（1570 MB）后统计：

| 指标 | 结果 |
|---|---|
| bundle 加载 | **84/84 成功**（UnityFS v8 / Unity 6000.2.6f2 构建，在 6.3.23f1 里正常读） |
| 粒子渲染器总数 | 13,898 |
| **shader 解析失败** | **0** |
| sharedMaterial 为空 | 148（1.07%） |
| 主贴图为空 | 120（0.86%） |

### 3. 自定义 shader 的源码**不可恢复**（bundle 原件上复核过）

用 UnityPy 直接解 `shaders_assets_all.bundle`：**45 个 Shader，0 个带 HLSL 源码**。`m_ParsedForm.m_SubShaders[].m_Passes[].progVertex.m_SubPrograms` 是空的，只剩 `compressedBlob` 里的编译字节码。

> ⚠️ 判断「源码有没有被剥离」要**直接读 bundle 原件**，别信 JSON 转储（转储可能漏字段）。

---

## 三、shader 现状：三层解析架构

```
① 自建 shader（工程资产）      ← 最优先。可改、跨平台、属性名与原版对齐
② 工程自带（URP/Sprites）      ← 次优先。标准 shader 走这条
③ 原版 shader bundle（817KB）  ← 兜底。只给工程里没有的 Everguild 自定义 shader 用
```

优先级开关：`WarpforgeShaderMap.PreferBuiltIn`（默认 true，只影响 ① 要不要抢在前面）。

> ⚠️ **② 必须排在 ③ 前面**。`shaders_assets_all.bundle` 里连 `Universal Render Pipeline/Particles/Unlit`
> 这类标准 URP shader 都有，但那份**和工程自带的同名 shader 不是同一个对象**
> （实测 `GetInstanceID()` 不同），而且**在编辑器下渲染会出故障（整片品红）**。
> 拿它当基准会让「原版 vs 导出」的比对整个失真（原版那一侧全是品红）。
> 修好后原版那侧的品红像素 15899 → 0。**bundle 里的 shader 只适合运行时用。**

**为什么自建 shader 是最优解**：它是普通 `.shader` 工程资产（所见即所得、任意平台），而**属性名与原版一一对应**，所以原版材质的数值可以原样灌进来，零转换。

### 自定义 shader 的用量分布（决定先做哪个）

13,898 个粒子渲染器里，**30.4% 用自定义 shader**，958 个效果里 **86.8% 至少依赖一个**。

**逐条「影响到多少个效果」写在 `WarpforgeShaderMap.cs` 那张表旁边**（含 2026-09-18 那批的 `// N 条效果` 注释），**本文件不抄第二份**。

**长尾那些（`Rays For Trail` / `Unlit UV scroll` / `Multi Ray` / `Particle Dissolve Mask` / `Alpha Mask One Layer` …）不用再自建** —— 它们都在 `wf_shaders_extra.bundle` 里，运行时能解析（见 P1-a）。
🔴 **2026-09-18 更正：用户已取消版权红线**（个人学习用途）⇒ 原写「进发布版本时要换成自建替代」的约束**作废**。

---

## 四、产物清单

### Unity 工程内（`d:/4/Unity/MyGame/`）

```
Assets/WarpforgeVFX/
├── Shaders/
│   ├── WFParticlesExtraColor.shader   替代 Everguild/FX/Extra Color（2789 次引用，最大头）
│   ├── WFSpritesAdditive.shader       替代 Everguild/Sprites/Sprite Additive
│   ├── WFDistortion.shader            替代 Everguild/FX/Particle Distortion Affect Transparents（233 个效果）
│   ├── WFMatcap.shader                替代 Everguild/Matcap/Matcap Full Options + With Texture（190 个效果）
│   └── WFUnlitAmbient.shader          替代 Everguild/UnlitAmbient + Emissive Flickker（场景贴图烘焙用，非粒子）
├── Runtime/
│   ├── WarpforgeShaderMap.cs              原版名 → 本工程名的映射 + 混合推断
│   ├── WarpforgeShaderLoader.cs           运行时从 StreamingAssets 加载原版 shader bundle
│   ├── WarpforgeEffectBinder.cs           挂 prefab 上，Awake 时用原版数据重建材质
│   ├── WarpforgeEffectLibrary.cs   ★新 效果库（ScriptableObject：名字 → prefab + 生命周期）
│   └── WarpforgeEffectPlayer.cs    ★新 **播放入口 + 生命周期**（Play/Exit/Kill/ActiveCount）
├── Prefabs/             效果的 prefab，根上挂 WarpforgeEffectBinder
├── Materials/           占位材质（编辑器里能看；运行时被 binder 换成真材质）
├── Textures/            导出的 PNG
├── Meshes/              含粒子专用的 ParticleSystemRenderer.mesh
├── 效果库报告.tsv        ★新 每个效果的寿命来源、有没有循环发射器、寿命风险（EffectLibraryBuilder 出）
├── 白板自检报告.tsv      ★新 白板自检的逐效果结果（WhiteboardTest 出）
└── 导出报告.tsv

Assets/Resources/WarpforgeVFX/WarpforgeEffectLibrary.asset   ★新 效果库资产（**条数见 §〇**，被引用才进包）

Assets/StreamingAssets/WarpforgeVFX/
├── wf_shaders.bundle          817 KB，44 个原版 shader（shaders_assets_all.bundle 的副本）
> 🔴 **2026-09-18 实读更正：对象数是 45 个 Shader，不是 44。** 那个包里共 76 个对象
> （**Shader 45** · Material 27 · Texture2D 3 · AssetBundle 1），45 个名字**全唯一、无重名、无空名**。
> **44 = `m_Container` 里「指向 Shader」的条目数**（容器原始 71 条 = 44 Shader + 27 Material，**44 个唯一 GUID 键**）。
> 差的那**一个**是 **`Hidden/Shader Graph/FallbackError`**（不在容器里）。
> ⇒ **45 = 对象数（`LoadAllAssets<Shader>` 拿得到），44 = 容器里可寻址的数**；
> ⚠️ UnityPy 的 `container` 是 dict，会把重复键折叠成 44，**很容易误读成「44 个 shader」**。
> 旁证：补充包 42 个 shader，两包名字交集**恰好 1 个**（又是那个 FallbackError）。
└── wf_shaders_extra.bundle   1.3 MB，42 个补充 shader（从 battleprefabs 包里抽出来重打的）
                              ← 没有它，212 个效果在**运行时**会退回占位材质
                              生成脚本：`d:/4/Unity/工具/extract_missing_shaders.py`
Assets/WarpforgeArena1/Runtime/
└── VFXWhiteboard.cs        ★新 白板：把一批效果按节奏铺开（场景里人看 / 批处理里被工具驱动）

Assets/WarpforgeArena1/Editor/
├── EffectExporter.cs     导出器
├── EffectCompare.cs      原版 vs 导出的并排渲染比对（**有 NameFilter，可以只跑一小批**）
├── EffectIso.cs          逐发射器隔离渲染 + 交叉材质对照（定位问题的主力工具）
├── EffectDiag.cs         把原版/导出 prefab 逐节点、逐渲染器、逐材质属性 dump 出来
├── EffectTimeSweep.cs    时序采样：原版/导出在多个时刻各渲一张，区分「还原失败」和「采样点不对」
├── EffectShaderProbe.cs  判定「导出的材质解析到了哪个 shader」，A/B 分离「自建 shader 缺失」
│                             和「加载器冲突」这两件事
├── BundleSmokeTest.cs    最小 bundle 加载自检，单进程单包（`-bundle <路径>`）
├── EffectSweepBatch.cs   **多时刻批量扫描**（判定逐效果的依据，两趟分进程跑，见「怎么跑」）
├── EffectLibraryBuilder.cs ★新 索引 JSON → 效果库资产（**改了导出集之后要重跑**）
├── WhiteboardTest.cs       ★新 **白板自检**：播→消失闭环 / 渲染与台账一致 / 反复播不泄漏
├── WhiteboardBuilder.cs    ★新 生成人看的白板场景
├── PlayProbe.cs            ★新 推进方式 A/B 对照探针（排查「白板渲不出来」时用）
├── CEmitProbe.cs         ★新 **C 组定位探针**：同取景同时刻并排打「每个渲染器的活粒子数 / 材质 / layer / maskInteraction」
│                             + 交叉材质（把原版材质贴到导出 prefab 上再渲一次）—— 一刀切开「没发射 / 没渲染 / 材质不对 / prefab 不对」
├── MatIsoProbe.cs        ★新 逐材质槽对照：一次只把一个槽的材质换成导出的，看哪个槽把亮度抬上去
├── MatKeyCheck.cs        ★新 查材质缓存键串号（同名同 shader 但属性不同的有多少）
├── TexCompare.cs         bundle 贴图 vs 导出 PNG 逐像素比对
├── ProbeMat.cs           把一个原版材质的全部属性打出来（做自建 shader 前先看属性表）
├── WarpforgeSetup.cs     把自建 shader 注册进 Always Included Shaders
└── ProbeBundle.cs        临时探针（当前是材质属性对比工具）
```

> `EffectIso` 的 `OutDir` 是 `d:/4/_tmp_view/iso/`，里面 `*_cross*.png` / `*_multi_*.png` 是交叉对照图
> （不是常规产物），看结论时优先看这些，比单看 `__orig` / `__exp` 可靠。

### 工程外

- `d:/4/_tmp_view/cmp/` —— 比对渲染图（`X__orig.png` / `X__exp.png`）
- `d:/4/_tmp_view/fidelity_final.png` —— 并排总览
- `d:/4/Unity/工具/remove_medallion.py` —— 卡框去盾牌那批（原先的临时脚本已固化到这里，`_tmp_view` 里的不再引用）

---

## 五、怎么跑

```bash
# Unity 路径
UNITY="D:/Unity/Hub/Editor/6000.3.23f1/Editor/Unity.exe"
# 注意：本机环境带 ELECTRON_RUN_AS_NODE，启动前要 unset

# 1. 导出效果
#    分片控制：EffectExporter 里的 SliceFrom / SliceCount / Resume / NameFilter
#      全量 958 个约 7 分钟；每导完 10 个刷一次报告，随时可中断
#      中断后续跑：Resume=true，SliceFrom 往后挪到上次的位置即可，已完成会自动跳过
#      NameFilter 填关键字可以只导关心的那批（比按字母序切前 N 个有用得多）
unset ELECTRON_RUN_AS_NODE && "$UNITY" -batchmode -quit \
  -projectPath "D:\4\Unity\MyGame" -executeMethod EffectExporter.Run -logFile -

# 2. 原版 vs 导出 的并排渲染比对 → d:/4/_tmp_view/cmp/
#    分片控制：EffectCompare 里的 NameFilter（子串匹配，空数组 = 全量）
#    ⚠️ 958 个效果全跑要 1~2 小时。验证某个改动时用 NameFilter 切一小批，几十秒就出结果
unset ELECTRON_RUN_AS_NODE && "$UNITY" -batchmode -quit \
  -projectPath "D:\4\Unity\MyGame" -executeMethod EffectCompare.Run -logFile -

# 3. 注册自建 shader 进 Always Included（新增/改名 shader 后要跑一次）
unset ELECTRON_RUN_AS_NODE && "$UNITY" -batchmode -quit \
  -projectPath "D:\4\Unity\MyGame" -executeMethod WarpforgeSetup.RegisterShaders -logFile -

# 4. 逐发射器隔离渲染 + 交叉材质对照 → d:/4/_tmp_view/iso/
#    效果名在 EffectIso.Targets 里改；这是定位「哪个发射器不对」的主力工具
unset ELECTRON_RUN_AS_NODE && "$UNITY" -batchmode -quit \
  -projectPath "D:\4\Unity\MyGame" -executeMethod EffectIso.Run -logFile -

# 5. 把某个效果的原版/导出 prefab 逐节点、逐材质属性 dump 到日志（EffectDiag.Args 里改效果名）
unset ELECTRON_RUN_AS_NODE && "$UNITY" -batchmode -quit \
  -projectPath "D:\4\Unity\MyGame" -executeMethod EffectDiag.Run -logFile "d:/4/_tmp_view/diag.log"

# 6. bundle 贴图 vs 导出 PNG 逐像素比对（TexCompare.Names 里改贴图名）
unset ELECTRON_RUN_AS_NODE && "$UNITY" -batchmode -quit \
  -projectPath "D:\4\Unity\MyGame" -executeMethod TexCompare.Run -logFile "d:/4/_tmp_view/tex.log"

# 7. 时序采样（EffectTimeSweep.Targets 里改效果名）
unset ELECTRON_RUN_AS_NODE && "$UNITY" -batchmode -quit \
  -projectPath "D:\4\Unity\MyGame" -executeMethod EffectTimeSweep.Run -logFile "d:/4/_tmp_view/sweep.log"

# 8. 探某个原版材质的属性表（ProbeMat.ShaderNames 里改 shader 名；做自建 shader 前先跑这个）
unset ELECTRON_RUN_AS_NODE && "$UNITY" -batchmode -quit \
  -projectPath "D:\4\Unity\MyGame" -executeMethod ProbeMat.Run -logFile "d:/4/_tmp_view/pm.log"
```

> **`-executeMethod` 一次只能给一个**，给两个只会跑最后一个。

### 诊断工具（EffectShaderProbe / BundleSmokeTest）

```bash
# 9. 判定「导出的材质到底解析到了哪个 shader」——A/B 两轮，唯一变量是「有没有先加载源 bundle」
#    轮 A = 干净的运行时条件；轮 B = EffectCompare 的条件。
#    效果名和要查的 shader 名分别在 EffectShaderProbe.Targets / WatchShaders 里改。
#    ⚠️ 它靠反射清 WarpforgeShaderLoader 的静态缓存来分离两轮，
#       必须连 _byName 一起清，否则第二轮会命中第一轮的缓存，负对照失效（踩过）
unset ELECTRON_RUN_AS_NODE && "$UNITY" -batchmode -quit \
  -projectPath "D:\4\Unity\MyGame" -executeMethod EffectShaderProbe.Run -logFile "d:/4/_tmp_view/probe_shader.log"
#    筛输出：grep "^WFPROBE" d:/4/_tmp_view/probe_shader.log

# 10. 最小 bundle 加载自检。**必须一个进程只测一个包** —— 把多个包放一个进程里，
#     同一个文件在不同位置会报出不同的错误（Unity 按内容去重），得不出结论
unset ELECTRON_RUN_AS_NODE && "$UNITY" -batchmode -quit \
  -projectPath "D:\4\Unity\MyGame" -executeMethod BundleSmokeTest.Run \
  -bundle "d:/4/_tmp_view/bundletest/xxx.bundle" -logFile "d:/4/_tmp_view/smoke.log"
#    筛输出：grep "^WFSMOKE" d:/4/_tmp_view/smoke.log

# 11. 重新生成补充 shader 包（换了导出集 / 补了新 shader 之后跑）
"D:/2/Warpforge_tools/py312/python.exe" "d:/4/Unity/工具/extract_missing_shaders.py"
#    加 --check 只体检不写文件

# 12. 更新 AnimFX 组件数据（导出集变了之后跑）
"D:/2/Warpforge_tools/py312/python.exe" "d:/4/Unity/工具/dump_animfx.py"
#    产出 数据/游戏数据/animfx_components.json

# 13. 多时刻批量扫描 —— **两趟，分两个进程，顺序不能反**
#     ⚠️ 2026-09-15 更正：这里原来写「工具里的 Side 常量要手动改（"orig" / "exp"），改完跑一次」
#        —— **不用改源码了**。2026-09-13（第三十四轮）起 Side 读环境变量 `WFSWEEP_SIDE`
#        （`EffectSweepBatch.cs:47`，不设 = "exp"）。第一趟设 `WFSWEEP_SIDE=orig`，第二趟不设。
#     orig 那趟加载全部源 bundle 并产出取景缓存；exp 那趟一个源包都不加载（= 运行时条件）
#     ⚠️ 为什么必须分趟、为什么顺序不能反：见 EffectSweepBatch.cs 文件头，两个方向都能把 900+ 个效果误判
WFSWEEP_SIDE=orig unset ELECTRON_RUN_AS_NODE && "$UNITY" -batchmode -quit \
  -projectPath "D:\4\Unity\MyGame" -executeMethod EffectSweepBatch.Run -logFile "d:/4/_tmp_view/sweep_o.log"
#    筛输出：grep "^WFSWEEP" d:/4/_tmp_view/sweep_o.log

# 14. 扫描结果 → 逐效果台账
"D:/2/Warpforge_tools/py312/python.exe" "d:/4/Unity/工具/analyze_sweep.py"
#    产出 资料/特效还原台账.tsv（按 根因分组 + 置信度 排序）
#    **每次运行都会打一段「尺子自检」**：Z 计数随带宽边界怎么变。看到计数变化先跟它比 ——
#    边界挪 0.05 就能摆动 22–54 个，比它小的变化说明不了任何事

# === 特效「能用」这一侧 ===
# 15. 生成效果索引（**导出集变了之后要重跑**，比如重新导过 prefab）
"D:/2/Warpforge_tools/py312/python.exe" "d:/4/Unity/工具/gen_effect_index.py"
#    产出 数据/游戏数据/effect_index.json（958 条：名字 → prefab + 生命周期 + 台账判定）
#    加 --check 只对账不写文件

# 16. 索引 → 效果库资产（Tools > Warpforge > 生成效果库）
unset ELECTRON_RUN_AS_NODE && "$UNITY" -batchmode -quit \
  -projectPath "D:\4\Unity\MyGame" -executeMethod EffectLibraryBuilder.Run -logFile -
#    产出 Assets/Resources/WarpforgeVFX/WarpforgeEffectLibrary.asset + 效果库报告.tsv
#    NameFilter / Limit 可以只装一个子集（做打包验证时用，全量会把 1.8 GB 拖进构建）
#    ⚠️ 2026-09-18 实测口径：**1.8 GB = 五个目录合计的十进制值（1.850 GB / 1.7229 GiB）**
#       （Prefabs 1.672 + Textures 0.167 + Meshes 0.008 + Materials 0.002 + Resources/WarpforgeVFX 0.002）

# 17. 白板自检 —— 播→消失闭环 / 渲染和台账一致 / 反复播不泄漏（Tools > Warpforge > 白板自检）
unset ELECTRON_RUN_AS_NODE && "$UNITY" -batchmode -quit \
  -projectPath "D:\4\Unity\MyGame" -executeMethod WhiteboardTest.Run -logFile "d:/4/_tmp_view/whiteboard.log"
#    筛输出：grep "^WB " d:/4/_tmp_view/whiteboard.log
#    NameFilter 空 = 自动跨全表挑 20 个；填关键字就只跑那几个（几十秒出结果）
#    产出 Assets/WarpforgeVFX/白板自检报告.tsv + d:/4/_tmp_view/whiteboard/whiteboard.png

# 18. 生成/重建「人看的」白板场景（Tools > Warpforge > 生成白板场景）
unset ELECTRON_RUN_AS_NODE && "$UNITY" -batchmode -quit \
  -projectPath "D:\4\Unity\MyGame" -executeMethod WhiteboardBuilder.Run -logFile -
#    产出 Assets/WarpforgeArena1/Scenes/VFXWhiteboard.unity（打开按 Play 就能看）

# === 改动的量化 ===
# 19. A/B 两份导出侧扫描：这次改动比上次好多少（|ln| 中位/均值 + 逐效果变化）
"D:/2/Warpforge_tools/py312/python.exe" "d:/4/Unity/工具/compare_sweep.py" \
  旧_sweep_exp.tsv 新_sweep_exp.tsv --top 12 --csv d:/4/_tmp_view/ab.csv
#    ⚠️ 判据是 |ln| 的中位/均值，不是 Z 计数（边界挪 0.05 就能摆动 22–54 个）

# 20. C 组定位探针（活粒子数 / 材质 / 渲染器状态 / 交叉材质）
unset ELECTRON_RUN_AS_NODE && "$UNITY" -batchmode -quit \
  -projectPath "D:\4\Unity\MyGame" -executeMethod CEmitProbe.Run -logFile "d:/4/_tmp_view/cemit.log"
#    筛输出：grep "^CE " d:/4/_tmp_view/cemit.log

# 21. 逐材质槽对照（哪个材质把亮度抬上去的）
... -executeMethod MatIsoProbe.Run -logFile "d:/4/_tmp_view/matiso.log"
# 22. 材质缓存键串号检查（纯读资产，几秒）
... -executeMethod MatKeyCheck.Run -logFile "d:/4/_tmp_view/matkey.log"
```

跑原版看效果（实拍对照用）：

```bash
taskkill //IM Warpforge.exe //F
cmd //c start "" "d:\2\unity_run_ref\Warpforge.exe"
# mod 配置：d:/2/unity_run_ref/UserData/sjs_dump_cfg.txt
#   第1行 = bundle 名，第2行 = 等几秒，第3行 = 输出目录，第4行 = 驱动命令（"drive"）
```

---

## 六、已知问题（按优先级）

### P0-a　~~sprite 类发射器导出不渲染~~ → 已修（覆盖比 0.02 → 0.65）

**结论**：旧记的两个方向都不对，真因是两个、都不在导出器里 —— ①**少了自建替代 shader**（新建 `WarpforgeVFX/Sprites/Additive`，属性表与 pass 状态照 bundle 的 `SerializedShaderState` 抄）；②**URP 粒子的「派生属性」没补**（影响面最大的一条）→ `WarpforgeEffectBinder.ApplyDerivedParticleDefaults()`。**详见 §八 坑表。**

### P0-b　Antimatter Explosion 粒子数不足（覆盖比 0.43）→ 已修

**结论**：大半靠上面那条「派生属性默认值」治好；剩下的差额是**密度**问题（粒子数 / 寿命 / 尺寸），跟 shader 无关 —— 方向是 `Emission` 的 burst 数量、`maxParticles`、`Rate over Distance`、`Sub Emitters`。**详见 §八 坑表。**

### P0-c　原版渲染的品红污染（基准问题，已修）

**结论**：根因是 bundle 里那份标准 URP shader（如 `Universal Render Pipeline/Particles/Unlit`）**和工程自带的同名 shader 不是同一个对象**（`GetInstanceID()` 不同），编辑器下整片品红 ⇒ 拿它当基准会让「原版 vs 导出」整个失真。修法：`WarpforgeShaderMap.TryResolve` 里让**工程自带 shader 排在前面**（= §三 三层解析架构）；修好后原版那侧品红像素 15899 → 0。**详见 §八 坑表。**

### P0-d　~~非粒子 shader 掉进粒子默认 shader~~ → **已修。真因不是 shader 映射，是加载器的 bundle 冲突**

**结论**：旧记的三个排查方向（`_BaseMap` 空 / 混合状态 / 顶点色）**全部不对** —— 真因是工具先把 84 个源 bundle 全量加载，于是 `StreamingAssets` 里那份**同内容副本**加载返回 **null** → 旧代码直接放弃 → shader 字典为空 → 保留占位材质 → **纯白块**。修法：`WarpforgeShaderLoader.EnsureLoaded()` 拿到 null 时改用 `LoadAllAssets<Shader>()` 扫已加载的 bundle 捡回来（实测捡回 49 个）。**通用教训**：先分清「没映射」还是「映射了却没解析到」—— 后者是加载器问题，改导出器和自建 shader 都是白费劲（用 `EffectShaderProbe` 的 A/B 两轮区分）。**详见 §八 坑表。**

### P0-e　AnimFX 脚本层：**能不能还原？—— 能。四层里三层已确认**

这是「特效能自己按原版节奏播」的关键。逐层核实过，**不存在「做半天发现做不了」的风险**：

| 层 | 需要什么 | 状态 | 证据 |
|---|---|---|---|
| 1. 类结构 | 18 个类有哪些字段/方法 | ✅ 已有 | 签名桩，连编译器生成的协程状态机字段都在（`<PlayAnimCoroutine>d__7` 里是 `UnitTweenSO[] wrap1` + `int wrap2` = 遍历 `tweenAnims`） |
| 2. 组件实例与参数 | 哪个 prefab 挂哪个模块、参数值 | ✅ 已导出 | UnityPy 读出 `actionStart=0`、`cameraShakes=[preset]`，类型名也解析 → `数据/游戏数据/animfx_components.json` |
| 3. 引用的 SO 数据 | tween 的时长/缓动/曲线 | ✅ 已导出 | 74 个 JSON，含**具体类型名** `PunchTween` + `duration/ease/punch/vibratto/loops/appendType` |
| 4. 方法体（行为） | 模块怎么用这些参数 | ⚠️ 两条路 | 见下 |

**用量分布**（源包里实测 2346 个组件，分布在 912 个效果上）→ **逐类实例数与「实现到什么程度」只看 `资料/AnimFX_实现与接线.md` §七**（那份的口径是「只算进效果库的那批」，与这里的原始数**是两个口径**）；**本文件不抄第二份**。

**第 4 层两条路**：

- **路 A：Ghidra 反编译（精确）**
  脚本现成（`il2cpp_decomp.py` 只要一个类名清单 + `script.json`），产物质量也验证过
  （`BattleManager__ResolveAction.c` 有 352 KB 真实反编译 C）。
  **⚠️ 2026-09-12 更正：产物一直都在本地，能直接读 —— 只有「重新生成」才需要工具链。**
  - **能读的**：`d:/2/Warpforge_tools/data/decomp_il2cpp_0827/decomp_out/`（**1800 个 .c**）
    + `decomp_out2/`（224 个）；字段名/枚举名在 `d:/2/Warpforge_code/Scripts/Assembly-CSharp/`
    （方法体是桩，但**名字是真的**）；DLL 里的浮点常量可用 PE 段表从
    `d:/2/unity_run_ref/GameAssembly.dll` 解出来（imagebase `0x180000000`）。
  - ✅ **2026-09-17 已经重建好了**（JDK + Ghidra 12.1.3 + Il2CppDumper 都在 `d:/2/tools/`，Ghidra 工程 1.2 GB 已分析保存 ⇒ 补类几分钟）—— 见 `资料/反编译工具链_重建记录.md`。（当初的判断：Ghidra 没装、Java 没装、`script.json` 和 Ghidra 工程没了 ——
    那只是为了**再跑一遍**分析 76 MB 的 `GameAssembly.dll`（首次数小时，估 1–2 天）。
  - 已经靠它拿到了实打实的东西（见玩法线文档第七节的事件时序）：
    `SequenceAppendType { After = 0, Same = 5 }`、`CardScript.attackStepTime`、
    `BattleManager` 的 10 个命名延迟常量。
  - ⚠️ **不是所有类都反编译了**：`CardScript__AttackMeleeAnim.c` 只有 29 行桩 ——
    要查某个方法**先确认它在不在**，别假定「有方法体」。
- **路 B：按语义重写（快，参数全在）**
  模块极小（15–126 行含空壳），字段名已经把行为说得很清楚：
  `DestroyInTime{destroyTime}` → `Destroy(gameObject, destroyTime)`；
  `Event{UnityEvent + whereToFire}` → 到时机 fire；
  `Tween{UnitTweenSO[]}` → 遍历、按 `appendType` 串、DOTween 播（**类型和参数都在 JSON 里**）。

**建议先走路 B**：风险被前置（猜错了实拍一比就露馅，损失 1–2 天而不是 5 天）。
按必要性分层补，**不要 18 个一起上**：

| 优先级 | 模块 | 为什么 |
|---|---|---|
| P0 | `AnimFXController` | 生命周期：何时 Exit / Destroy。不补的话特效可能不消失 |
| P0 | `AnimFXModuleTween` | 卡牌位移/缩放的节奏，数据全在 |
| P1 | `DestroyInTime` / `Animation` / `Event` / `DoDestroyAnimation` | 都是生命周期钩子，语义直白 |
| P2 | `ScreenShake` / `PostProcess` / `Cardback` | 表现增强，不影响特效本身 |
| P2 | `ScaleByTarget` / `MoveParticlesToTarget` / `Collisions` | 战斗中的卡牌相对定位，做玩法时才需要 |

**一个重要的缓解事实**：导出时**粒子系统模块是完整的**，效果的基本播放是有的。
AnimFX 补的是**脚本驱动的时序和额外表现** —— 所以不是「不补就全废」，而是「补了才像原版」。

**裁决手段**：`d:/2/unity_run_ref/` 有带 mod 的原版游戏，补完一个模块就实拍比一次。

### P0-f　binder 材质属性大面积丢失（**已修；「代价 36 个」已判掉 —— 是指标问题，不是资产退化**）

**结论**：`Explosion_Ground` 在全景取景下导出全空，真因是 `DefIndex` 的循环**由 bundle shader
声明的属性表驱动，而那份表残缺** —— 实测 `Everguild/FX/Particle Premultiply` 只报出 **4 个 float**，
把 `_SrcBlend` / `_DstBlend` / `_ZWrite` / `_Cull` / `_Surface` / `_CameraFadingEnabled` /
`_SoftParticlesEnabled` **全漏了**，binder 只好退到「按 shader 名猜混合」（"premultiply" 猜成
`One/OneMinusSrcAlpha`，原版实际是 `SrcAlpha/OneMinusSrcAlpha`）。修法：`EffectExporter.DefIndex`
改用 `SerializedObject` 读 `m_SavedProperties`（**名字和值都必须从序列化数据里读** —— 详见第八节坑表；
修完验证 44 个 float + 8 个 color 与原版逐项一致）。
**「修完达标数掉 36 个」判为「尺子太糙」而不是资产退化**：不带任何资产改动的尺子自检显示，
带宽边界每挪 0.05、Z 计数就摆动 **22–54 个**，36 比尺子自身的分辨率还小；也不存在「把渲染状态
改对反而更偏离原版」的机制。**验收指标因此换成 `|ln(亮度比)|` 的中位/均值**（`analyze_sweep.py`
每次运行会把那张敏感度表打成「尺子自检」段，看到计数变化先跟它比）。**判定列 Z/E/C/D/T/W
保持不变 —— 只是分组口径，不是验收依据。**

### P0-g　兜底默认值覆盖了真值（**已修**）

**结论**：把 `isParticle` 判据从复数 `"Particles"` 放宽成单数 `"Particle"` **本身是对的**（原版一大批 shader 是单数），但它让**无条件覆盖**的 `ApplyDerivedParticleDefaults` 开始作用于新的一大批材质，把刚由 P0-f 记进来的原版真值（`_SoftParticlesEnabled=1` / `_CameraFadingEnabled=1`）改写成 0 → 导出集体变亮。修法：只补没记到的（`SetF`/`SetV`）—— **兜底逻辑只该在「真缺」的时候生效**。**详见 §八 坑表。**

### P0-h　精灵（Sprite）根本没导出 → 被遮罩的粒子全部渲染为空（**已修**，C 组掉 3 个、T 组掉 4 个）

**症状 / 真因**：一批 C 组效果导出侧一个像素都没有（`ArtificeEffect` 原版是个齿轮图标、导出全黑）。真因是 `SpriteRenderer` / `SpriteMask` 的 sprite 是 **bundle 资产**，克隆成工程 prefab 时引用落不下来（**guid 全 0 的伪引用** —— YAML 里看着有值，运行时解析成 null），而 `maskInteraction = VisibleInsideMask` 的粒子**完全靠这个遮罩裁形** ⇒ 遮罩一空整块效果一个像素都画不出来；**实测 78 个 prefab 有被遮罩的粒子**。排查四步（`EffectDiag` 逐节点 → `CEmitProbe` 活粒子数 → 交叉材质 → 渲染器状态）**方法值得照抄，见 §八 坑表**。
**修法 / 效果**：`EffectExporter.ImportSprite` 把 sprite 按 `sprite.textureRect` 裁出一格写成 PNG 导成工程资产（轴心按 `sprite.pivot / sprite.rect.size` 归一化）再挂回 `SpriteRenderer.sprite` / `SpriteMask.sprite`，全量导完 **32 个精灵资产**；C 组掉 3 个、T 组掉 4 个（`EnvironmentalCondition Tau Solar Eclipse` 0 → **23994**，原版 23973）。
⚠️ **遗留**：这轮只改了**导出侧** —— `ArtificeEffect` 这类**精灵自己也显示**的效果仍偏暗（85 vs 2936），下一步查它的材质（见 E 组）。

### P1-a0　抓屏扭曲：尺子的假象 + 一个真 bug（混合被 Standard 残留值污染）—— **已收尾**

> **2026-09-13 第三十三轮收尾**：旧判据是**尺子的假象**（原版在空场景里输出的是恒定半透明灰方块，
> 不是扭曲）；真 bug（混合被 Standard 残留值污染）已修 + 26 条断言；「原版读哪张屏幕贴图」也查明了是
> `_GrabPassTransparent` 并已补上（`GrabPassTransparentFeature`，见 P1-a1 开头）。
> **仍未查清的只剩「输出的 alpha 取自哪里」**（见本节末），旧的「证据链」结论**已被推翻，不再保留**。

**重查证据**（`WarpforgeArena1/Editor/DistortProbe.cs`；同一帧、三种纯色背景 + 一块**不透明棋盘** quad，
指标 = 「相对同背景空场景的贡献」）：原版在三种背景下输出**同一个恒定值** —— 纯黑 4607 像素全 = 0.239、
中灰 0.5 下**输出正好等于背景**、纯白 4607 像素全 = 0.922；把材质**强制排进透明队列**（t=3000）
之后原版输出**一个像素都没变**。
黑/中灰/白三点唯一解：`half4(K, α)`、`K ≈ α ≈ 0.214`（线性）—— 正是**一张没被绑定的默认灰贴图**
（0.5 经 sRGB→线性）的 rgba。
- ⇒ **原版读的那张屏幕贴图，在我们这套测量环境里根本没绑上** —— 它渲出来的「4612 个亮点」
  是一块**半透明灰方块**，既不是扭曲、也不是发光。
- ⇒ 台账里那批带抓屏扭曲的「C 组」判的是**尺子的假象**：这类效果在**没有内容可扭曲**的空场景里
  本来就该是空的（和「看到一大片效果同时坏掉，先怀疑尺子」是同一类问题）。

**同一次探针查出的真 bug（已修）**：

原版 `Everguild/FX/Particle Distortion Affect Transparents` 的**属性表里没有
`_SrcBlend/_DstBlend/_ZWrite/_Surface/_Blend/_Cull`**（14 个属性，混合相关的一个没有），
混合是**写死在 pass 状态里的**（bundle 实测 `rtBlend0`：src=5 SrcAlpha / dst=10 OneMinusSrcAlpha，
`zWrite=0` / `zTest=4` / `cull=2`，`separateBlend=False`）。

而原版材质上**带着一批内置 Standard shader 的残留值**：`_SrcBlend=1`(One) / `_DstBlend=0`(Zero) /
`_ZWrite=1` / `_Surface=0`（还有 `_Glossiness` / `_BumpScale` / `_Metallic` … 一堆）。
在原版那边这些是**死值**（shader 不声明，Unity 忽略）；但我们自建的 `WFDistortion.shader`
用 `Blend [_SrcBlend] [_DstBlend]` 间接寻址 → 残留值被灌进来 → 变成**不透明覆盖 + 写深度**。
实测：棋盘背景上导出侧糊出一整块**纯黑**，把棋盘抠掉（`contrib` 全是 `(0,0,0)`）。

**修法**（照解包资源，不猜）：
1. `WFDistortion.shader` 的混合/深度/剔除**硬编码**成上面那组 pass 状态，
   删掉原版根本没有的 `_SrcBlend/_DstBlend/_ZWrite/_Surface/_Blend/_Cull/_Cutoff/_AlphaClip`；
2. 属性表和原版对齐：`_DistortionStrength` 是 **Float 默认 1.0**（我们原来写成 `Range(0,1)` 默认 0.1）、
   `_DistortTex` 默认 `"bump"`、`_Depth_And_Fallof` 默认 `(1,0.1,0,0)`（**不是** `(0,1,1,0)`）、
   `_UVSpeed` 默认 `(1,1,0,0)`、`_QueueControl` 默认 `-1`；
3. 关键字名照原版：`_SOFTPARTICLES` / `_USEMASK` / `_ANIMUVS`（**没有 `_ON` 后缀** ——
   原来写成 `_SOFTPARTICLES_ON`，导出侧 `EnableKeyword` 全部落空，等于这三个功能永远关着）；
4. `WarpforgeEffectBinder` 补混合的判据从「**原版材质**里有没有记到 `_SrcBlend`」
   改成「**我们这边的 shader** 认不认 `_SrcBlend`」—— 原来那条判据永远为真，兜底逻辑没机会生效。

**断言**：`DistortProbe.Run` 现在自带 **26 条**（属性表齐不齐、数值与原版一致不一致、
不该有的属性有没有、关键字认不认得），**26/26 全过**；白板自检不受影响（20/20 闭环、20/20 与台账一致）。

**⚠️ 这条尾巴的状态（别当成已解决）**：
- **✅ 原版采的是 `_GrabPassTransparent`**（第三十三轮已据此做了 `GrabPassTransparentFeature`，见 P1-a1）。
  查法：原版 HLSL 源码确实被剥了（`m_Script` 是 null），**但编译字节码在** ——
  `Shader.compressedBlob` 按 `compressedLengths`/`decompressedLengths` 分块 LZ4 解压出 DXBC，
  **资源名是明文**（工具 `工具/dump_shader_blob.py`）。⚠️ 这一步我一度写成「源码和字节码都被剥掉了」——
  **那是读错了属性**（`m_SubProgramBlob` 是 None，真数据在 `compressedBlob`），因此白试了 8 个候选名
  （`_CameraOpaqueTexture` / `_CameraColorTexture` / `_GrabTexture` / `_SceneColorTex` / `_ScreenTexture` /
  `_DistortionSource` / `_SceneColor` / `_MainTex`），逐个设成品红贴图**一个都没让原版输出变色**。
  "Affect Transparents" 说明它要扭曲的是**透明物**，而 URP 的 `_CameraOpaqueTexture` **不含透明物**。
- **输出的 alpha 取自哪里，两种模型都能拟合**：①取自被采样贴图的 a（→ 真实场景里是 1，
  扭曲是**全强度**的）；②取自 `_DistortTex.a × 顶点色.a`（我们现在的写法，→ 半强度）。
  黑/中灰/白三点区分不开这两者。**判别实验**：把发射器的 `startColor.a` 从 0.216 改成 1.0 再跑，
  看原版输出的 alpha 变不变 —— 变了就是顶点色。**在查清之前不要动这段**。

> 顺带一条别走错的岔路：`m_RequireOpaqueTexture: 1` 在 PC_RPAsset 里是**开着**的 ——
> 「没开不透明贴图」不是原因。

### P1-a1　✅ **已解（2026-09-16）**：这条线索**方向错了** —— 触发源不是规则书那张表，是**动画挂点**

> ✅ **2026-09-13 第三十三轮：P1-a0 已经收尾了**（这条卡了很久）。三件事都做了：
> ① **`WarpforgeEffectBinder` 的混合判据**改成「按原版 shader 名推」（`InferBlend`），
>    一处解掉 **61 条**「加法发光变成不透明+写深度」；新增 `BlendProbe.Run` 当常驻守卫
>    （混合 1099/1099 + shader 解析 70/70）。
> ② **给 URP 加了 Renderer Feature**（`GrabPassTransparentFeature`）填上原版真正读的
>    `_GrabPassTransparent` —— **抓屏扭曲那一族 41 条**现在读的是证实的、含透明物、全分辨率的那张。
> ③ **补了 10 个未映射的 shader**（原来整批保留占位材质，16 条）+ 给自建 shader 补 `_EmissionColor` 通路（11 条）。
> 逐条根因与故障模式统计见 `特效E组_根因初判.md` 末尾那一节。
> ⚠️ ③ 里那条 `_EmissionColor` 通路**当时写成了乘法、把 13 个效果一起打暗** —— 2026-09-15 已改成加法发光项，见 §〇·一。

**2026-09-11 发现**：`d:/2/Warpforge部队卡片/` 里有原版离线规则书
（`Warpforge_Offline_Rulebook_1_5-3_中文翻译.md` / 英文原本 / PDF），
其中 **4. 其他规则 → 关键词（Keywords）——完整技能表（61 个）** 一节，
**逐个写明了每个关键词的触发时机**：

> ⚠️ **2026-09-13 第三十三轮：这张表已经搬走了，只留这一处指针。**
> 原来这里只有 18 行；这一轮把**剩的 39 个也补齐**，**完整的 61 关键词「触发时机 → 对应事件」表
> 在 `关键词图标/_规则书关键词表.md` 的附录**（含「引擎缺哪个时机」的四类归纳）。
> 两边都留一份 = 迟早不一致，所以这里**只留指针**。

**为什么这有用 —— 直接命中两个未解桶：**

1. **✅ W 组「两边全程空」分类 —— 2026-09-16 做完了**（台账实测 **190 条**，不是 191）。
   **结论与逐条判据只看一处**：`资料/普查产出_0916/W组分类_汇总.md`（五块原始产出 `W组分类_块1~5.md`）。
   🔴 **三条会改做事方式的结论**（详见那份汇总 §二）：① **`vfx_wiring_j1..5.tsv` 覆盖面很窄**
   （块 1 命中 0 / 块 2 命中 4 / 块 5 命中 12）—— 真证据是**原版四张「动画 → VFX root」表**；
   ② **W 的「空」大部分是尺子造的**（挂点在动画中段，统一时间点 `Simulate` 打不到）；
   ③ **(a) 里一条「纯脚本占位/无可见粒子」都没有** —— 9 条全是「旧版·未投产资产」或「无触发源」。
   ⇒ **触发源不是规则书那张关键词表，是「动画挂点」**（单位攻击 → 播 `Atk_*` 动画 →
   动画上的 `animInfo` 在 `timeAtStartPos` 挂出效果）—— 这条**推翻了原来「对着触发表分堆」的设想**。
2. **AnimFX 路 B（P0-e）说「按语义重写模块」** —— 「什么事件 → 触发哪段效果」就是那份语义，
   而上面那条结论给了它的**输入表**（动画 → 效果映射）。

**另一份配套数据**：`数据/游戏数据/card_anim_map.json`（13 阵营，按特效名索引）里每条的字段是
**播放时机**，不是布局：

```
vfxDelayTime  startDelay  timeAtStartPos  timeMoving  timeAtEndPos
shouldMoveVFX  startPosOption  endPosOption  pointTowardsOption
```

而且**名字里就带触发语义**：`AeldariRecall_2_ability` / `Abaddon_TalentWarmaster` /
`Atk_Artillery_arc`（`_ability` / `_self` / `_quick` / `_reverse` / `Talent` / `Atk_`）。

**下一步怎么用**：✅ **三步都做完了**（2026-09-16）—— 第 1、2 步的结果在
`普查产出_0916/W组分类_汇总.md`（分类不用后缀族、改按名字在前缀族里切；**也不是**对着触发表分的），
第 3 步的交出物是**「动画 → 效果」映射表**（四张原版表），**不是**关键词表。

> 顺带：同一目录还有 `卡牌信息权威表_0824.md`（1156 行，**用户已裁决**的卡牌类型判定）
> 和 1365 张原版卡面 PNG —— 卡面数据类型/关键词的权威来源，比 OCR 的 `card_stats.json` 靠得住。

### P1-a　剩下的自建 shader（长尾）—— 范围已大幅缩小

**先纠正上一轮的判断**：`Rays For Trail` **不是「找不到」**，它在
`battleprefabs_vfxandmisc_assets_all.bundle` 里（只是不在 `shaders_assets_all.bundle` 里）。
这类「不在 shaders 包里」的 shader 一共 33 个，影响 370 处引用 ——
**已经用 `工具/extract_missing_shaders.py` 抽出来打进 `wf_shaders_extra.bundle` 了，
运行时能解析，不需要自建。**

所以现在的划分是：

| 类别 | 数量 | 处理 |
|---|---|---|
| 自建 shader 覆盖 | 8 个映射 | ✅ 已做 |
| 主 shader 包（44 个） | — | ✅ 运行时可用 |
| 补充 shader 包（+42 个） | 33 个是效果在用 | ✅ 运行时可用。⚠️ 原写「**不能进发布版本**」—— **2026-09-18 版权红线已取消**，不再有此约束 |
| 两个包里都没有的 | 见下 | ⬜ 才是真正的自建候选 |

~~要进发布版本时，这 33 个（加上主包那 44 个）都得换成自建替代 ——~~
🔴 **2026-09-18 版权红线已取消**（个人学习用途），**这一步不用做了**。下面这句保留作历史说明：
那时的做法还是照抄属性名，流程见下面。**当前个人研究阶段不用做，先把基线洗干净。**

**做法**（上一轮验证过、可照抄的流程）：
1. **读 bundle 拿属性表和状态 —— 用 `工具/dump_shader.py`**（2026-09-12 从一次性脚本固化下来）：
   ```bash
   PYTHONIOENCODING=utf-8 PYTHONUTF8=1 D:/2/Warpforge_tools/py312/python.exe \
     d:/4/Unity/工具/dump_shader.py "<shader 名片段>" [材质名片段]
   ```
   它打印的正是 `pf.m_Name`（**`Shader.m_Name` 是空的，真名在 ParsedForm 里**）、
   `pf.m_PropInfo.m_Props`（名字/类型/默认值）、
   `pf.m_SubShaders[].m_Passes[].m_State`（`rtBlend0` / `zWrite` / `zTest` / `culling` / `alphaToMask`）、
   pass tags、关键字全集，以及**用这个 shader 的材质的实际数值**。
   ⚠️ 两个必看的点：**①原版属性表里有没有 `_SrcBlend`** —— 没有就说明混合写死在 pass 状态里，
   自建 shader 里**必须硬编码、不能做间接寻址**（否则材质上内置 Standard 的残留值会被当活值用，
   见 P1-a0）；**②关键字的准确拼写** —— 原版是 `_SOFTPARTICLES` 不是 `_SOFTPARTICLES_ON`，
   写错的话 `EnableKeyword` 全部落空。
2. 照着写 `.shader`，**属性名必须与原版一字不差**（否则原版数值灌不进来）
3. **两处都要登记**：`EffectExporter.ShaderMap`（编辑器占位材质）+
   `WarpforgeShaderMap.Replacements`（运行时 binder）
4. **跑一次 `WarpforgeSetup.RegisterShaders`**（不注册的话打包后 `Shader.Find` 找不到）
5. 跑 `EffectIso` 隔离渲染核对

### P1-b　脚本层完全没动（独立缺口，与 shader 无关）
> ✅ **2026-09-18 这条已不成立** —— AnimFX 脚本层**已实现**（15 个模块类 + 数据链 + 工厂自动登记），
> 见 `资料/AnimFX_实现与接线.md`。本节保留作历史（当时的缺口描述与推理仍然成立，只是已经补上了）。

- 926/965 个物体带缺失脚本，共 **2,321 个组件** —— 这就是「特效不会自己播」的原因
- 已用**字段签名反查**精确定位 3,624/5,694 个（**0 歧义**）：`AnimFXController` 1944、`AnimFXModuleScreenShake` 866、`AnimFXModuleScaleByTarget` 628、`AnimFXModulePostProcess` 104、`AnimFXModuleCardback` 60、其余 22
> 🔴 **2026-09-18 实测更正：这一行的数全是「同一份被数了两遍」——每个恰好 ≈ 2.0×。**
> 实测 `数据/游戏数据/animfx_components.json`：**912 个效果 / 共 2346 条组件**，
> `AnimFXController` **990** · `ScreenShake` **434** · `ScaleByTarget` **314** · `PostProcess` **52** · `Cardback` **30**
> （A/实测 = 1.9636 / 1.9954 / **2.0000 / 2.0000 / 2.0000**）。
> **两处硬证据**：① 本行那个分母 **5,694 恰 = 2 × 2,847**，而 2847 正是唯一 VFX 源包
> `battleprefabs_vfxandmisc_assets_all.bundle` 的 MonoBehaviour 数；② 本行的辨识数 **3,624 > 2,847**
> ⇒ 它本身不可能是「更宽的总体」。
> **推断的成因（非实测）**：本机装了**两份**游戏，那份 bundle **逐字节相同**（md5 同为 `be3768d3…`）——
> 扫描若 glob 了两份，所有计数 ×2。
> ⚠️ **本机没有能复现这一行的脚本或产物**（全库 grep `1944|3624|5694` **零命中**）⇒ **这一行只能当历史记录读，别引用**。
> **要引用计数就用上面那组实测值**（= `数据/游戏数据/animfx_components.json` 实测，与 `资料/AnimFX_实现与接线.md` §七 同源）。
- **整层只有 18 个类 / 73 个方法 / 960 行签名**（对比：`BattleManager` 一个类就 677 个方法）
- ⚠️ **2026-09-17 更正**：那是旧数 —— 现在 **`AI` 整类 41 个方法 + `AnimFX*` 18 类共 86 个方法体都在 `decomp_out_ai/`**（原话：**一个 AnimFX 都没有**）
- 工具链现成：`d:/2/Warpforge_tools/data/decomp_il2cpp_0827/ghidra_scripts/`

### P1-c　只能拍静态帧，拍不到完整时序

`EffectCompare` 只渲染 `Simulate(1.2s)` 的单帧。要论证「时序也一致」，需要扩展成固定时间步的逐帧抓图。

---

## 七、下一步建议顺序（下次会话照这个做）

**基线**：`资料/特效还原台账.tsv`（957 行，含分组、判定、亮度比、**置信度**、峰值亮点、时段、技术构成）。
原始数据在 `资料/比对基线/`（含 README，说明怎么重新生成）。
**当前指标与各组计数只认那张台账一处**（`analyze_sweep.py` 每次重生）—— 别引用更早的比对数字。

| 步 | 内容 | 为什么排这个位置 | 预估 |
|---|---|---|---|
| **2** | 啃 E 组高置信亮度/密度（个数见台账） | 先按技术构成切子集（抓屏扭曲 / Matcap / 多发射器），同类一起看 | 2–3 天 |
| **3** | ✅ **2026-09-16 做完了** —— W 组 190 个「两边全程空」逐条分类完（**不是**用 61 关键词表，见下） | **结论与逐条判据只看一处**：`资料/普查产出_0916/W组分类_汇总.md`（分块原始产出 `W组分类_块1~5.md`）。<br>🔴 **三条跨块结论**（比清单重要）：① **`vfx_wiring_j1..5.tsv` 覆盖面很窄**（块 1 命中 0 / 块 2 命中 4 / 块 5 命中 12）—— 真证据是**原版四张「动画 → VFX root」表**（`card_vfx_tree.json` · `atk_vfx_map.json` · `anim_goid_map_0824.json` · `animinfo_lookup.json`）② **W 的「空」大部分是尺子造的**（挂点在中段动画时间轴上，独立 sweep 打不到）③ **(a) 的 9 条里一条「纯脚本占位/无可见粒子」都没有** —— 全是「旧版/未投产资产」或「无触发源」，建议把 (a) 拆成 a1/a2。<br>⚠️ 因此**喂给 AnimFX 路 B 的不是关键词表，是「动画 → 效果」映射表** | ✅ 已完 |
| **4** | ~~AnimFX 路 B：按语义重写 P0 两个模块~~ | ✅ **2026-09-18 做完了**（15 个模块类，见 `资料/AnimFX_实现与接线.md`）。**不用再做** | ~~2–3 天~~ |
| 5 | ~~构建后 player 验证~~ ✅ **2026-09-17 已收工** | 入口与跑法见下面「构建后 player 验证」那节 —— **别把白板自检的绿灯当成发布绿灯** | 1 天 |
| 6 | 按需扩 Ghidra 反编译（只在路 B 猜错了某个关键模块时才重建工具链） | 1–2 天重建 + 数小时分析 | 按需 |
| 7 | ~~（只在要进发布版本时）把主包 + 补充包那 77 个原版 shader 逐个自建替代~~ | 🔴 **2026-09-18 版权红线已取消**（个人学习用途）⇒ **这一步取消了，不用做**。原写「不能进发布版本」的约束随之作废 | **不用做** |

**做完 1–2 步 = 一个能用也能看的特效库；第 4 步做完才谈得上「能直接进战斗」。**
**当前的下一件见 §〇 表「还没做」那一行**（原来这里写的是「从第 1 步（拖尾贴图丢件）接着做」—— 那件 2026-09-17 已修好；更早写的「啃 C 组」也早已清空）。

### ✅ 构建后 player 验证 —— **入口已建、三场景跑通**（2026-09-16 第一轮 · 2026-09-17 又复核两轮）

> 一句话：**能构建、能跑、三个场景都起得来**；**原版 bundle shader 与 TMP 中文在真包里都正常**；
> **抓出并修掉 4 个「编辑器里好、真包坏」的 bug**（三个是 shader，一个是原版独有的 shader 名）。
> ⚠️ **别把白板自检的绿灯当成发布绿灯** —— 这一轮恰好证明了这句话：**4 个 bug 里没有一个是编辑器能看见的**。

#### 入口（本轮新建的两个文件）

| 文件 | 干什么 |
|---|---|
| `Assets/CardPresentation/Editor/PlayerBuild.cs` | **构建入口**：备份全量库 → 出子集库 → 建 Windows player → **把三条跑批命令打出来** |
| `Assets/CardPresentation/Core/PlayerBoot.cs` | **player 的启动参数入口**：`-wfscene` 切场景 · `-wfshot`/`-wfshotat` 截图 · `-wfquit` 自动退出。**编辑器里一律不生效** |

#### 怎么跑

```bash
U="D:/Unity/Hub/Editor/6000.3.23f1/Editor/Unity.exe"; P="d:/4/_tmp_view/player"
# ① 出子集效果库（白板那 12 个）+ 构建（全量库 1.6 GB prefab，验证用不着）
#    ⚠️ 2026-09-18 实测口径：**1.6 = 只算 `Assets/WarpforgeVFX/Prefabs` 这一个目录，且是 GiB（1.557 GiB）**；
#       它换算成十进制是 1.672 GB（会被读成「1.7」）。⇒ 与上面那条 1.8 的区别是**口径（整库 vs 只 prefab）＋单位（GB vs GiB）**，不是打架。
#       （两个口径在这台机器上几乎一样：`GetCompressedFileSizeW` 逐文件返回 == st_size，4K 簇对齐只多 +0.7%。）
unset ELECTRON_RUN_AS_NODE && "$U" -batchmode -quit -projectPath "D:\4\Unity\MyGame" \
  -executeMethod PlayerBuild.RunWithSubsetLibrary -logFile -          # 筛 "^PBLD " 看结果
# ② 三个场景各跑一遍（图 + log）。⚠️ **Battle 多一个驱动开关**（见下）
"$P/WarpforgePlayer.exe" -wfscene VFXWhiteboard -wfshot "$P/VFXWhiteboard.png" -wfshotat 10 -wfquit 24 -logFile "$P/VFXWhiteboard.log"
"$P/WarpforgePlayer.exe" -wfscene Battle -wfdrive -wfshot "$P/Battle.png" -wfshotat 12 -wfquit 240 -logFile "$P/Battle.log"
"$P/WarpforgePlayer.exe" -wfscene Battle -wfhuman -wfquit 240 -logFile "$P/human.log"   # 走 UI 入口那条路
#   ⚠️ 这条 2026-09-17 仍未跑（它的「偶发卡死未收口」记号在 `项目任务.md` 待办表第 1 行，本节只记「怎么跑」）
"$P/WarpforgePlayer.exe" -wfscene DeckEditor -wfshot "$P/DeckEditor.png" -wfshotat 15 -wfquit 30 -logFile "$P/DeckEditor.log"
# ⚠️ Battle 的 `-wfquit 240` 是**保险丝**（防这局永远打不完）—— 打完 `AutoDrive` 自己退，实测整轮约 50 s。
# ③ 不用再手动还原了：`RunWithSubsetLibrary` **建完包会自动把全量库还回去**（2026-09-16 加）。
#    ⚠️ 之前要人另跑一次 `RestoreLibrary`，忘了的后果是**编辑器里只剩 17 个特效**、
#    `BattleScene.Run` 报「VfxMap 里 14 个特效名在库里都找得到（缺 14）」—— **看着像代码回归，其实是自己的工具没收拾**。
```

> ⚠️ **跑完 `git status` 会多出几个设置文件的改动 —— 那是 Unity 自己的自动迁移，不是构建意图**
> （2026-09-16 实测：`Assets/Settings/{DefaultVolumeProfile,PC_RPAsset,UniversalRenderPipelineGlobalSettings}.asset` ·
> `ProjectSettings/{ProjectSettings,UnityConnectSettings}.asset` —— 内容是 URP prefiltering 默认值回写、
> Standalone batching 补一行、schema 补字段）。**已还原**；下次照做：**只保留有意图的那两个**
> （`ProjectSettings/GraphicsSettings.asset` 的 Always Included shaders · `Packages/{manifest,packages-lock}.json` 的 screencapture 模块）。

#### 结果（逐场景的数不抄第二份）

> 🔴 **2026-09-17 又跑了两轮**（为了把重写的 AI 与新增的音频/回放条/语音条带进真包）：都 **Succeeded** ·
> `-wfdrive` **打完** · 运行时 AI 警告 **0** · 语音表在真包里确实载入。
> **那两轮的数字只写在 `资料/AI_原版反编译_0917.md` §七之二（唯一出处）** —— 本节不抄第二份。

- **构建**：`StandaloneWindows64` · **4.5 GB** · 30–70 s · **错误 0**（修完下面 4 条之后）· **构建期警告 51–54**
  ⚠️ 「警告 51–54」是**编译期**的（shader / TMP 变体那类），与「运行时 AI 警告 = 0」**不是一回事**，别当成两条打架的数
- **产物**：`d:/4/_tmp_view/player/WarpforgePlayer.exe`（⚠️ `Resources/Art` 1.6 GB **解压后 4.3 GB** —— 这是 `resources.assets` 一个大文件）

#### 🆕 `BattleAutoDrive.cs` —— player 里的「自动打一局」

player **没有输入驱动**（原版有 `SceneJumpShot` 的 `drive`，我们没有）⇒ 加了 `-wfdrive`：
驱动方式 = **反复调现成的 `BattleDriver.SimulateAiTurn()`**（内部是 `SimpleAI.PlayTurn` + 换边）。
⚠️ **打完整局的步数随 seed 变，不是回归指标**；它会在四个节点各落一张 `auto_*.png`（接上 / 换牌之后 / 打了六步 / 结算），**打完自己退**。

⚠️ 🔴 **它是「AI 代打」，不是「模拟人类操作」** —— 人类那一侧的**拖拽 / 攻击方式选择器 / 选牌面板
不会被走到**。本轮的验收目标是「真包里的资源与渲染」，人类交互由编辑器自检（`BattleScene.Run`）覆盖。
**别把这里的绿灯读成「交互在真包里也没问题」。**

#### 🔴 修掉的 4 个真 bug（**全是「编辑器里看不出来」的**）

| # | 症状 | 根因 | 修法 |
|---|---|---|---|
| 1 | 白板 `CardPrefab` 那一格**整块洋红** | `TextMeshPro/Distance Field` **没进包**：原版材质引用的是**原版的 GUID**，本工程没有那个 GUID ⇒ 引用链拉不进 ⇒ 打包时被剥 ⇒ 运行时 `Shader.Find` 返回 **null** ⇒ 材质槽保留占位材质 | `WarpforgeSetup.RegisterShaders` 加了「**整目录扫** `Assets/TextMesh Pro/Shaders`」（+18 张）。⚠️ **编辑器的 `Shader.Find` 能在整个工程里找，不进包的也算 —— 所以编辑器永远看不见这个错** |
| 2 | 构建期 2 条 `unrecognized identifier 'fixed4'`（on d3d11） | `WFSpritesAdditive.shader` 是 **URP 的 HLSL 风格**，而 `fixed4` 由 `HLSLSupport.cginc` 提供（**只有 `CGPROGRAM` 才有**）；同工程的 `ArtOpaque` / `FrameCutout` 是 CG 风格所以没事 | `fixed4` → `half4` |
| 3 | 修完 2 之后又冒出 3 条：`UnityPixelSnap` / `_EnableExternalAlpha` / `lerp` 无三参重载 | 前两个是 `UnitySprites.cginc` 提供的，HLSL 风格这份**一件都没抄**（`_EnableExternalAlpha` 只在 Properties 里声明过、HLSL 侧没有同名 uniform，还连累了下一行的 `lerp`） | 补 `float _EnableExternalAlpha;` + 内联一份 `UnityPixelSnap()` |
| 4 | `找不到 shader 'TextMeshPro/Distance Field Offset'` | 那是**原版独有的 TMP 变体名**，本地 13 张 TMP shader 里没有（只有 `CardPrefab.prefab` 引用它） | 加进 `WarpforgeShaderMap.Replacements` → 标准 `Distance Field`（**近似**，注释标了） |

> ⚠️ 2 与 3 是**同一个 shader 的两层**：改完 `fixed4` 才露出下面那两个。
> ⇒ **「构建期 shader 报错数」是改完之后要重看的数**，不是一次能看完的。

#### 入口自己踩的两个坑（都修了，注释在 `PlayerBoot.cs`）

1. 「**切场景那条路上不起 runner**」⇒ 不截图、不自动退出，**表现是「跑批卡住」**（Battle 的日志停在「凑牌组」两行、进程一直活着）。
2. 🔴 **根因是我误解了 API**：`[RuntimeInitializeOnLoadMethod(AfterSceneLoad)]` **只在启动时跑一次** ——
   换场景**不会**再触发。⇒ 改成「**先建一个 `DontDestroyOnLoad` 的对象，切场景放在它的协程里做**」。

#### 还没做 / 还没证的（**下一轮的入口**）

> ⚠️ **2026-09-17 瘦身**：这一节原来列了十几条**已完成**的（拖拽手势、DOTween 22 条、换牌屏差异…）——
> 按「现在还成不成立」全删。**仍然成立的只剩这三条**（都是「已知、还没做/没定性」，不是坑表）：
>
> 1. **卡组编辑场景在真包里是一张静态图** —— `DeckScene.cs` 只建了一个相机、没有任何交互组件；
>    「拖拽加牌」本来就挂在待办第 10 行。**别把「卡组编辑在真包里正常」读成「交互验过了」**。
> 2. **「为什么编辑器一条 DOTween 报错都没有」仍未定位** —— 但**编辑器的 0 条已经是量出来的**
>    （`BattleScene.Run` 自己数并断言 = 0）。**别把它当「没这个问题」**；排查手段留了 `WF_NO_SETLINK=1` 做 A/B。
> 3. **D3D11 变体**：本项目 player 实际跑 D3D12，那三条 d3d11 的 shader 错误**在 D3D12 运行里看不出来**
>    —— 只有「构建时把 d3d11 也编一遍」才暴露。**别把 d3d12 跑通当成 shader 没问题**。

### 排查手法（反复用到，直接照搬）

| 症状 | 先跑什么 |
|---|---|
| 某个效果整个是纯色块 | `EffectDiag` 看 shader 名 → 大概率「没映射，掉进粒子默认 shader」 |
| 某个发射器整块不出 | `EffectIso` 隔离渲染，看是「没发射」还是「没渲染」 |
| 两边都是空 | `EffectTimeSweep` 换时间点 —— 多半是采样点不对，不是还原失败 |
| 亮度差一截但形状对 | 查「派生属性」（软粒子/相机淡出）是不是被写死成默认值了 |
| 贴图颜色不对 | `TexCompare` 逐像素比 bundle 贴图和导出的 PNG |

### P2　导出器的边角（不急）

- 纹理导入设置写死了（sRGB / Repeat / mipmap），原版可能有平台覆盖设置没搬过来
- `EffectExporter` 里 `MatCache` 是「一次运行内」的缓存，跨片跑会重复导出同名材质
  （`GenerateUniqueAssetPath` 会产出 `X 1.mat`）—— **分片导出时要注意**
- `导出报告.tsv` 每行是 `效果名<TAB>OK<TAB>说明`，按 `\t` split 拿到的第 3 段才是说明。
  🔴 **2026-09-17 更正**：这一条原来写「`LoadReport` 只取第 2 段，**不影响跳过逻辑**」——
  只说对了一半，**漏了它其实在丢数据**。`LoadReport` 只取第 2 格，而 `SaveReport()` 每 10 个效果
  就把内存那份**照原样写回** ⇒ **任何 `Resume=true` 的续跑都会把「没被重导的那些行」的第 3 格永久抹掉**。
  实测：一次续跑之后 958 行里**只剩 2 行**还带第 3 格（恰好是那趟真重导的 2 个）；而
  `工具/extract_missing_shaders.py` 按 `p[2]` 取 shader ⇒ **会静默地一个都读不到**。
  ✅ **已修**（`EffectExporter.LoadReport` 改成 `string.Join("\t", p.Skip(1))`，第 3 格以后原样留着）。
  ⚠️ 但**要 shader 名单仍然别读那份报告** —— 它是「导出过哪些效果」的流水账，不是稳定数据源；
  正本用 `资料/普查产出_0917/效果_shader对账.tsv`（958 行 5 列）+ `统计见 _小结.md`


## 八、踩过的坑（别重踩）

| 坑 | 真相 |
|---|---|
| **🆕🔴 原版材质里的「黑 `_EmissionColor`」是死值，不是数据** | 2026-09-15 把 `_EmissionColor` 接进自建粒子 shader 时写成了**乘法**（`col = tex * _Color * _EmissionColor * IN.color`），而原版那批材质的 `_EmissionColor` 是**内置 shader 的默认黑**（`{0,0,0,1}`，URP `ParticlesUnlit.shader:9`），原版那边要 `_EmissionEnabled`/`_EMISSION` 才采它 ⇒ 我们这边一乘，**整个材质被乘成黑**，13 个效果同时变暗 30 倍。**改成加法发光项**就好了（原版 URP 粒子就是加法的：`ParticlesLitInput.hlsl:103`）。⚠️ **这和上面 `_SrcBlend` 残留值是同一个坑的第二次**：原版材质带的是内置 shader 的默认值，**接一个新属性前先问「默认值是什么、原版什么条件才采它、是加法还是乘法」**。定位：把 `4e0741d`（③-3）那一笔回退掉**13 个效果就全部复原**；细节见 §〇·一 |
| **🆕⚠️ 重导 prefab 之后必须重建效果库，否则所有特效静默不播** | 效果库（ScriptableObject）存的是 **(guid + fileID)** 引用。重导 prefab 时 GUID 不变（.meta 还在），但**内部 GameObject 的 fileID 会重新生成** → 958 条引用**全部解析成 null**。实测对账：库里 958 条，fileID 对得上的 **0 条**。而它**不报错**，只是 `Play()` 返回 null、什么都不发生（本会话就是被这个坑掉的：卡牌落位该播特效，一声不响）。**规矩：跑过 `EffectExporter.Run` 之后一定要跑一次 `EffectLibraryBuilder.Run`。** 现在 `Play()` 遇到空 prefab 会打警告、构建器生成完也会自检空引用数 |
| **🆕 粒子系统的随机种子：不钉死，尺子就不确定** | `useAutoRandomSeed` 默认**开**，每次重播都换种子 → 粒子分布每次不同。实测**同一进程连渲三次**得 537/518/512 个亮点（亮度和却几乎不变 = 分布变了、能量没变），连跑两遍扫描有 ~60% 的采样行会变 —— **噪声和要测的改动一样大**。修法三件事缺一不可（`EffectSweepBatch.SeedAndReset`）：`useAutoRandomSeed=false` + 显式 `randomSeed` + 先 `Simulate(0, restart:true)` 复位再推进。修完 2968 行只剩 11 行不同（都是 1 个像素）。**顺带排除的两个错嫌疑**：`ps.Play()` 和 `fixedTimeStep` 都不是原因，别往那查 |
| **🆕 `SpriteRenderer`/`SpriteMask` 的 sprite 不会被导出** | 精灵是 bundle 资产，克隆成工程 prefab 时引用落不下来 —— Unity 写的是 **guid 全 0 的伪引用**，`EffectDiag` 里能看出是 `<null>`，但**光看 prefab YAML 会以为有值**。后果不止「精灵自己不显示」：`maskInteraction=VisibleInsideMask` 的粒子全靠它裁形，遮罩一空整块效果渲成空。修法见 P0-h |
| **🆕 判「材质不对」还是「prefab 不对」要用交叉材质** | 把 A 的材质贴到 B 上再渲一次：①导出 prefab + 原版材质 能渲出来 → 材质的锅 ②照样渲不出来 → prefab/渲染状态的锅。这一刀把排查面砍掉一半（P0-h 就是靠它锁定的） |
| **🆕 隔离实例别用 `SetActive(false)`** | 停用再启用会把粒子系统重置，活粒子直接变 0 —— 于是「换了材质也渲不出来」这种**假结论**就出来了（实测踩过）。正确做法：把另一个实例**挪到画面外**（x=10000） |
| **🆕 材质缓存键是「材质名\|shader 名」，会串** | `WarpforgeEffectBinder.Cache` 用这个名字做键，而「Glow Additive」「Smoke」这类名字在几百个效果里重复。实测 **5 个键（22 个效果）同名但属性不同**，先建的会顶替后面的。量它的工具：`MatKeyCheck` |
| **原版有一批 prefab 的根节点停在「场外 x≈100」** | 实测 `Atk_GrotGrenade` / `Atk_ExileGlaiveThrow Movement` 根节点在 **(99.59, 1.24, -0.37)**、`Attack_Stomp` 在 **(100, 0.725, 0)** —— Unity 里常见的「没上场的东西停在场外」。播放时**位置必须归位**，否则效果直接播在场景外面。但**旋转和缩放不能动**：它们是效果作者定的（`Antimatter Explosion` 根节点是 **X=-90°**，`Atk_GrotGrenade` 是 **scale 0.5**），归零会把一批效果整体转 90°。见 `WarpforgeEffectPlayer.Play` 里的注释 |
| **取景必须量「要渲染的那个实例」** | 白板自检里 10/24 个效果全程 0 亮点，台账却说它们「对得上 + 高置信」。真因：取景量的是**原样实例化的临时副本**（还停在 x≈100），而播放器把实例归位到了原点 → 相机看 x=100、效果在原点 → 一个像素都没有。**看着像还原失败，其实是尺子错了。** 修法：`FrameForInstance()` 只量要渲染的那一个实例 |
| **「一整块粉色/品红糊满屏」先分清是坏 shader 还是效果本身就是全屏类** | 实拍图里出现过一大团粉色，第一反应是品红（坏 shader）。实际是 `Environmental Condition Black Legion Warp Storm` 的 `Color change overlay` / `Color change smoke` —— 渲染器半径 **245.95 单位**，设计上就是盖满整个战场，铺进 4 单位的白板格子自然糊满屏。判据用半径：普通卡牌特效 3–15、大范围闪电 74–82、战场环境叠加 245+（`VFXWhiteboard.FullscreenRadius = 100`） |
| **`sweep_*.tsv` 的 `lit_srgb`/`sum_srgb` 两列是坏的** | 它们恒为 65536（全屏像素数）。原因：代码把像素转成 sRGB 之后，**拿去和线性空间的背景色**比阈值，背景自己就超阈值 → 每个像素都算「亮」。台账用的是 `lit`/`sum` 两列，**没受影响**；但那两列以后别再拿去算任何东西（文档里原来写的「会把比值压到 0.9 附近」还低估了它） |
| **推进方式会改变「有没有内容」的结论** | 排查白板渲不出来时写了 `PlayProbe` 做 A/B：绝对时刻法（`Stop`→`Simulate(t, restart:true)`→`Play`，即扫描工具那套）和增量法（逐步 `Simulate(dt, restart:false)`）结果**基本一致**，所以不是推进方式的锅 —— 真因是上面那条取景错位。**教训**：瞬时闪光只有 0.15s，增量步长 1/30 会错过，**渲染采样要用绝对时刻法**；而寿命检查根本不需要粒子动，**两件事分开跑**，别混在一个循环里 |
| **读材质属性：名字和「值」都要走序列化数据** | `Material.GetFloat/GetColor/GetTexture()` 对「shader 没声明」的属性返回 0/黑，而原版材质的 shader 来自 bundle、声明表是残缺的（`Shader.GetPropertyCount()` 也残缺）。只改名字不改成值 → 记下来 44 个属性但**值全是 0** → `_SrcBlend=0` → 渲染不出来。正确做法：`SerializedObject` 读 `m_SavedProperties.m_Floats/m_Colors/m_TexEnvs` |
| **兜底默认值不能覆盖已有值** | `ApplyDerivedParticleDefaults` 原本是**无条件** `SetFloat("_SoftParticlesEnabled", 0)`。当属性记录变完整之后，它会把原版真值（`=1`）改写成 0 → 导出集体变亮。改成「只补没记到的」才对。**兜底逻辑只该在「真缺」的时候生效** |
| **改判据字符串前先数清覆盖面** | `isParticle` 判据从 `"Particles"`（复数）改成 `"Particle"`（单数）本身是对的（原版一大堆 shader 是单数），但它同时让上面那个无条件覆盖作用于**新的一大批材质**。一处小改动的连带影响要靠实拍量出来 |
| **多时刻扫描：原版那趟必须加载全部源 bundle** | 原版 prefab 的材质同样要从包里解析 shader，而 `battleprefabs` 里的 shader **不是可加载资产**（`LoadAllAssets<Shader>()` 返回 0）。只加载它一个包时原版渲染成**品红**，品红很亮 → 原版亮度和虚高 → 比值假性掉到 0.2 |
| **多时刻扫描：导出那趟一个源包都不能加载** | `wf_shaders_extra.bundle` 是从 `battleprefabs` 抽出来的，那个包在场时 Unity 按「内容相同」判重、拒绝加载补充包 → 导出侧退回占位材质。日志里表现为「共 44 个原版 shader 可用」（少了 42 个） |
| **上面两条方向相反，所以扫描必须分两个进程跑** | 试过在一个进程里 `AssetBundle.Unload(false)` 卸包，**但连 prefab 一起销毁了**，不行。正确做法：两趟分别跑，取景由 orig 那趟缓存下来给 exp 用，结果按效果名合并（`工具/analyze_sweep.py`） |
| **比值要有置信度** | 两个小数字相除得出的比值没有意义。实测 `GatlingSweep` 原版只有 22 个亮点、导出 114 个，比值算出来 5.38，纯属噪声。台账里必须带「峰值亮点」和置信度，让低置信的判定自己暴露出来，别硬给结论 |
| **截图口径：`tex.GetPixels()` 和 `EncodeToPNG` 是一致的** | 一度怀疑是线性/sRGB 口径差（线性下 0.5 的差会变成 0.5^2.2≈0.22，正好对得上观察值），**查下来不是** —— 两者一致。再套一次 sRGB 变换反而会把所有比值压到 0.9 附近，掩盖真实差异。别在这上面绕 |
| **`AssetBundle.LoadFromFile` 对「内容已在进程里」的文件返回 null** | 编辑器工具先把 84 个源 bundle 全量加载后，`wf_shaders.bundle`（`shaders_assets_all.bundle` 的副本）就再也加载不进来 —— **同一份内容、两个路径也算重复**。旧代码遇到 null 直接放弃，后果是**所有只靠 bundle 兜底的原版 shader 全部解析失败**、binder 退回占位材质、渲染成纯色块。这是 P0-d 的真因。修法见 `WarpforgeShaderLoader.HarvestFromLoadedBundles()` |
| **遇到「整个变成纯色块」，先分清是「没映射」还是「映射了却没解析到」** | 两者的修法完全相反：前者要自建 shader / 改映射表，后者是加载器问题，改导出器和写 shader 都是白费劲。用 `EffectShaderProbe` 的 A/B 两轮区分（唯一变量 = 有没有先加载源 bundle） |
| **UnityPy 重打 bundle：必须保留 `AssetBundle` 对象并重写它的 `m_Container`** | 只留 Shader 对象、把 `AssetBundle` 对象一起删掉 → Unity 报 `could not be loaded because it is not compatible with this newer version of the Unity runtime`（**这句话极具误导性，跟版本毫无关系**）。保留它但不重写容器（容器里还指着已删的对象）→ 包能加载但一个资产都暴露不出来。两者都做对才正常。另外 `SerializedFile.container` 是只读属性，底层是 `ContainerHelper`（attrs+slots），要改 `.container` / `.container_dict` / `.path_dict` 三个 |
| **这些 bundle 的资产名是 32 位 GUID，不是路径** | 所以 `GetAllAssetNames()` 里按 `.shader` 后缀过滤一个都匹配不上。要拿 shader 就用 `LoadAllAssets<Shader>()`，一次调用，不依赖 container |
| **诊断脚本一次进程只测一个 bundle** | 把多个包放同一个进程里测，同一个文件在不同位置会报出**不同的**错误（`not compatible with this newer version` vs `same files already loaded`），那是 Unity 的内容去重机制在干扰，不是包的属性。`BundleSmokeTest -bundle <单个路径>` 就是为此加的 |
| **`battleprefabs` 里的 shader 用代码取不到** | `LoadAllAssets()` 988 个结果里 0 个 Shader，`GetAllAssetNames()` 983 条里没有一条含 "shader"。所以「让游戏直接加载原包」这条路走不通，只能抽出来重打。（`shaders_assets_all.bundle` 正相反，~~45 个 shader 全在容器里~~ → 🔴 **2026-09-18 实读更正：容器里是 44 个**，第 45 个 `Hidden/Shader Graph/FallbackError` **不在**容器里；见本文件 `wf_shaders.bundle` 那条的更正块） |
| **诊断日志用行首前缀，别用多行插值** | 批量模式下每条 `Debug.Log` 后面都跟一坨 Unity 堆栈。给行首加可 grep 的标记（`WFPROBE ` / `WFSMOKE `），筛日志用 `grep "^WFPROBE"` 才干净 |
| **URP 粒子的「派生属性」必须手动补，否则整块渲染成黑** | `_SoftParticleFadeParams` / `_CameraFadeParams` 这几个**原版材质里根本没序列化** —— 是 `ParticleSystemRenderer` 每次渲染现算、临时塞给材质的。binder 新建 Material 拿到默认值 0，而 shader 拿它直接乘 alpha（`col.a *= saturate(... / _SoftParticleFadeParams.y)`，y=0 除零 → alpha=0）。**这是导出后「整个发射器看不见」的头号原因**。见 `WarpforgeEffectBinder.ApplyDerivedParticleDefaults()` |
| **`_EMISSION` 是全局关键字，但会混在 `Material.shaderKeywords` 里** | URP 粒子 shader 的 `_EMISSION` 由 `ParticleSystemRenderer` 以**全局**作用域开关；可它又会出现在 bundle 材质的 `shaderKeywords` 里。导出时若照单全收烘进局部关键字，运行时 `Material.IsKeywordEnabled`（= 局部‖全局）就**恒为真** → 无条件开 Emission → 整体偏亮。**导出时必须剔掉**（`EffectExporter.StripGlobalKeywords`）。顺着「它是全局的所以导出丢了、要补上」这个直觉走会**反着修** |
| **bundle 里的 shader 在编辑器下渲染会出故障（品红）** | `shaders_assets_all.bundle` 里连标准 URP shader 都有，但那份**和工程自带的同名 shader 不是同一个对象**（`GetInstanceID` 不同），实测在编辑器里会渲染成整片品红。**只适合运行时用**。解析顺序里工程自带的同名 shader 必须排在 bundle 前面，否则「原版 vs 导出」的比对基准整个失真（原版那侧全是品红） |
| **测试环境自己的坑：同进程重复加载 bundle** | `AssetBundle.LoadFromFile` 对**已加载**的文件返回 null。诊断脚本若先把 84 个源 bundle 全加载了，会把 StreamingAssets 里那份 `wf_shaders.bundle` 的加载顶掉，binder 只能退回 `Shader.Find`。诊断时**只加载要用的那一个包** |
| **隔离渲染别用 `Renderer.forceRenderingOff` / `enabled=false`** | 两者都会让粒子**根本不发射**，隔离图假性全空。正确做法：把目标节点移到专用图层（本工程用 30 层 `WarpforgeIso`），相机 `cullingMask` 只渲染它 |
| **别分开渲染 A 和 B 再比** | 隔离渲染跑两次，任何一处不一致就会得出错误结论（本轮被这个反复误导，一度得出「导出 prefab 坏了」的错判断）。**要对照就放进同一个场景同一帧** |
| **`com.unity.modules.assetbundle` 默认没装** | 此时 Editor 程序集能用 `AssetBundle`，但**运行时程序集（Assembly-CSharp）编译不过**，报 `CS1069 ... forwarded to assembly 'UnityEngine.AssetBundleModule'`。已补进 manifest |
| **`Shader.Find` 打包后找不到未被引用的 shader** | 自建 shader 没被任何材质直接引用，必须注册进 Graphics Settings 的 Always Included Shaders（`WarpforgeSetup.RegisterShaders`）。**新增自建 shader 后一定要跑一次** |
| **`Bundle` 里的 Shader 拿不到 Properties 名字** | UnityPy 读 bundle 时 `Shader.m_Name` 是空的，真名在 `m_ParsedForm.m_Name`；属性表在 `m_ParsedForm.m_PropInfo.m_Props`，混写状态在 `m_SubShaders[].m_Passes[].m_State`（`rtBlend0.srcBlend/destBlend`、`zWrite`、`culling`）。做自建替代 shader 前**先把这张表读出来**，别靠猜 |
| **legacy 粒子 shader 在 URP 里会变粉** | `Mobile/Particles/Additive` 这类是内置管线的，`Shader.Find` 找得到但渲染错误。且混合写死在 shader 里、材质上没有 `_SrcBlend`，必须按 shader 名推断混合 |
| **不清旧产物会累积副本** | `GenerateUniqueAssetPath` 会不断产出 `X 1.mat` `X 2.mat`。已加 `ClearGenerated()` |
| **`ParticleSystemRenderer.mesh` 不是 `MeshFilter`** | 漏了这条，Mesh 模式发射的粒子（弹体/光环）导出后会整个消失 |
| **比对时相机取景必须统一** | 各自自动构图会让亮度差失去可比性，曾据此得出错误结论 |
| **`-executeMethod` 一次只能给一个** | 给两个只会跑最后一个 |
| **`rm -rf Assets/WarpforgeVFX` 会连自己写的 Runtime 脚本一起删掉** | 清理只删 `Prefabs/Materials/Textures/Meshes` 四个子目录 |
| **卡框产物曾整目录丢失** | `桌面/新建文件夹 (5)/去盾牌/` 曾被清空过（原因未查明），重跑 `d:/4/Unity/工具/remove_medallion.py` 即恢复 |
| **项目文档说 `Warpforge_code` 是「签名桩」** | 核实属实：方法体全空（`AnimFXModuleTween.Initialize(){}` / `PlayAnimCoroutine(){return null;}`），0 个文件含桩标记——是**直接留空**而非抛异常。字段名/类型/`[SerializeField]` 是完整的，这部分能用 |
| **`d:/2/Warpforge_code/Scripts/Il2CppAssemblies` 之类看着像有方法体** | 方法有 RVA 不等于有真 IL。别在这上面绕，直接看 `Warpforge_code` 的源码形态 |
| **写诊断日志别用多行插值字符串** | 批量模式下 `Debug.Log` 后面跟的是 Unity 堆栈，用 `+ "\n"` 拼接并给行首加可 grep 的前缀；筛日志时用 `grep "^\s*<前缀>"` |

---

## 九、相关文件

| 要找什么 | 去哪 |
|---|---|
| 任务总入口 | `d:/4/项目任务.md` |
| 资源怎么用 | `d:/4/Unity/资料/资源使用手册.md` |
| 场景/特效/动画评估 | `d:/4/Unity/_资源评估_场景特效动画.md` |
| 战斗权威规格 | `d:/4/Unity/资料/战斗规格/战斗重建_0827/审查更正清单_0827.md` |
| 类签名桩（18 个 AnimFX 类在这） | `d:/2/Warpforge_code/Scripts/Assembly-CSharp/` |
| Ghidra 反编译产物 | `d:/2/Warpforge_tools/data/decomp_il2cpp_0827/` |
| 原版 bundle 原件 | `d:/2/Warhammer 40k Warpforge/Warpforge_Data/StreamingAssets/aa/StandaloneWindows64/` |
| MonoBehaviour 序列化值 | `d:/2/解包整理/08_预制体特效/战斗预制体/MonoBehaviour/` |
| 原版实拍参照 | `d:/2/Unity参照管线_0825/data/` |
