# 战术卡剩余 7 条：语义查证与落点 —— **现状**（2026-09-16 收工）

> ✅ **现状一句话：这条线已经走完** —— 判过「挂起 / 待定」的卡**全部落地**，战术卡「不认识的句子」**归零**
> （🔴 **2026-09-16 下半场更正**：原来这里写「只剩 `Kustom Job` 一张是**终态**」——
> 那是**误判**，它**也做掉了**。理由与池子身份见 `普查产出_0916/静默桩家族_0916.md` §四。
> **本文件现在 `0` 条待办**。）
> **权威缺口清单仍是 `_tmp_view/tactic_unparsed.txt`**（自检每次重写）；**§十 是逐条判据的唯一出处**。
>
> 📜 **2026-09-14 那批逐卡查证原文（§二～§五 · §七 · §八）已挪进历史**
> → `资料/历史/战术卡剩余7条_逐卡查证_0914.md`（**原文照搬**，含 0916 的落地追记）。
> 本文件只留 **§九 方法论** + **§十 逐句探针复核**。

---

## 九、一条**跨轮次的方法论**（子代理本轮核出来的，值得记）

🔴 **「查不到」要区分「单文件 grep 过」和「全库零命中」**。
本轮把 4 个卡名在 `d:/warpforge` + `d:/4` + `d:/2/Warpforge_tools` **一起搜过**：
31 处命中**全部落在 `d:/4`**（本地化 / 翻译脚本 / 卡表），`d:/warpforge`（参考实现）**一处都没有**
⇒ 「权威语义层零依据」是**全工程级**的结论。

⚠️ **中文译文不是第四层权威**：`zh_cards.json` / `zh_CN.csv` 的译文是从同一份英文 `desc` 译出来的
（我们自己的翻译产物），**逐句对齐、无额外信息**。别拿它当依据。

---

## 十、逐句探针复核（**新会话接这条线从这儿起**）

工具：`EffectParseProbe.Run`（句子写进 `_tmp_view/probe_in.txt` → 读 `_tmp_view/probe_out.txt`）。
⚠️ **2026-09-16 起探针会先加载卡池**（`CardDatabase.Load()`）—— 有一族目标的判据是「池里真有一张卡叫这个名字」，不加载就量不到（`Angels of Death` 就是这么被量成「半懂」的）。
🔴 **这一节跟着自检重写的两份清单走**（`_tmp_view/tactic_unparsed.txt` / `unit_desc_unparsed.txt`），**以清单为准**。

| 句子（卡） | 现状 | 真因 / 下一步 |
|---|---|---|
| `Destroy all friendly Canoptek Scarabs`（`Self-Destruction`） | ✅ **已修（2026-09-16）** —— ④ 栏「打得比卡面宽」**1 → 0** | **同一条路**。修之前 `scarab` 映射不到 `subtype` ⇒ 落 `KindUnfilterable` ⇒ 结算层「按整个目标池打」 ⇒ **它会把自己全场都消灭**（卡面只该消灭圣甲虫）。这是「打得比卡面宽」里最重的一个 |
| `Trigger the abilities requiring Spirit Stones of all your troops`（`ASH52 Cosmic Serpent`） | ✅ **2026-09-16 做了** —— ① 栏 **1 → 0** | ① 认句：新 `EffectText.ReTriggerRequiring`（**只认这一种写法**）+ 新常量 `KeywordTable.SpiritStone`（它还必须进 `CardDef.RoutableTriggers`，否则那道闸照样挡）；② 结算：`DoTriggerAbility` 的 `SpiritStone` 分支 → `ResolveSpiritAbilityForced` —— **触发不付费**（`ctx.SuppressCostDepth` 旁路）。**裁定证据**：`AbilityTrigger.UseSpiritStone=600` · `BattleActionType.triggerSpiritStone` 的派发体**无付费调用** · 唯一的余额闸门在「玩家从手牌主动打出」那条路上、**不在这条链**。⚠️「主动激活到底在哪扣石」三层读不到 ⇒ 标**未证**。断言 `TestBatch0916` ②（真卡触发 `Wraithblade`，**一颗石头没花**） |
| `If it dies this turn, apply this effect to another random enemy troop`（`BL77 Spreading Corruption`） | ✅ **2026-09-16 做了** —— ① 栏 **1 → 0** | 🔴 **它不是条件** —— 走 `TryIf` 会在**施放当刻**求值（那一刻它还活着）⇒ 后半句**永远不发生且不报错**。新路：`TryApplyOnDeath` / `TryFillDeathWatch`（**跨句回填**，两处共用一份判据）→ `EffectOp.DeathWatchTarget` → 登记 `ctx.DeathWatches` → 死亡那一刻 `RuleCore.FlushDeathWatches` **复述一次**。⚠️ 递归语义**三层零依据** ⇒ **我们定：只转一次、不递归**。断言 `TestBatch0916` ⑦ |
| `Take control of an enemy troop this turn and give it Fast`（`GSC_Telephatic_Domination`） | ✅ **2026-09-16 做了** —— 全池唯一一张「完全解析不了」→ 现在**解析得了、也打得出** | 解析：`TryTakeControl`（`takecontrol` + `Duration=turn`，尾句 `and give it Fast` 交给 `Finish` 解成第二条 op）；结算：`DoTakeControl` —— **归属就是「待在谁的 `Board[]` 里」**，抢 = 换数组，归还 = `RuleCore.EndTurn` 那一段。⚠️ `Fast` 必须**显式清 `Exhausted`**（`AddKeyword("fast")` 只写关键词、`Exhausted` 只在构造时算一次）。⚠️ 原版把「容器 + 时长 + 翻转原语」串起来的调用点在协程里、**本次导出没有** ⇒ 「本回合末归还」是**照卡面 + 我们挑的**。断言 `TestBatch0916` ⑨ |
| `a remnant it gains shield`（`SAU61 Undying Legions`） | ✅ **2026-09-16 做了** —— ③ 栏 **3 → 0**（战术卡） | 两处都补：① 逗号要求放宽（`TryPersistent` 的 `when` 支改成**逐词边界试切、两边都解得出才算数**）；② 新事件种类 `WhenEvent.BecomesRemnant` —— **广播点原来只写日志、不广播**（`RuleCore.CleanupDeaths` 翻面那一处，全仓唯一产生点）。⚠️ **反编译里没有这个广播**（`CardScript.TransformIntoRemnant` 一条都不发）⇒ 按监听器语义补的，记成**我们的**。断言 `TestBatch0916` ① |
| `Choose an effect and give it to all troops in your hand`（`TL53 Infinite Biomorphologies`） | ✅ **2026-09-16 做了** —— ③ 栏 **2 → 0**（战术卡） | 🔴 **原来记的理由（「必须做卡实例身份」）记重了**：这张卡给的是「手牌里**所有**部队」、每一份都要给 ⇒ 按「**卡 + 份数**」记账与实例身份**语义等价**。落地三处：`BattleContext.HandBuff`（卡 + 份数 + 兑现 ops）· `EffectResolver.GrantHandBuff`（原来那个「这一版没做」的 `return false` 分支被替换）· `RuleCore.ApplyHandBuffs`（**打出时兑现**，排在 `Auras.Recompose` 之前）。断言 `TestBatch0916` ⑧ |
| `Give a Kustom Job of your choice to a friendly Vehicle`（`GOF_Mekaniak`） | ✅ **2026-09-16 下半场做掉了** —— ⚠️ **原来记的「终态」是误判**：`Kustom Job` **不是卡名**，是同一督军天赋组的**三张 0 费 `Ephemeral` 卡**（`More Dakka` / `Ramshackle` / `Wreckin' Ball`，三张目标都写 `a friendly Vehicle`）。依据是**卡面编号结构**（`Mekboy Gazmek` 25 + `Mekaniak` 25B + `25C/D/E`，与 `Titus` 00 + `Exemplary Warrior` 00a + `00b/c/d` 同形），**池子身份由用户 2026-09-16 拍板**。 | 落地：`EffectResolver.ChooseEffectPools` 登记 + `EffectText.TryChooseEffect` ③（`ReGiveKustomJob`）。正本 `普查产出_0916/静默桩家族_0916.md` §四。⚠️ **原来那条钉「终态」的断言（`TestBatch0916` ⑩：事件流里必须有「不认识」那句）已改判** —— 现在断的是「**不再报认不出**」。 |
| Oath 三条修饰句（`UM84` `Friendly [Oath] abilities apply an additional time` · `UM89` `…up to 3 times each turn` · `UM_Vico_Therbeus` `…on later turns`）+ **单位卡 `Oath N:` 整条激活路径** | ✅ **2026-09-16 做了** —— 单位卡 ① 栏 **7 → 0** | 🔴 **这三句背后是一整条从来没接过的路**：单位卡的 `Oath N:` 正文**根本收不到**（`oath` 既不在 `RoutableTriggers` 也不在 `BodyKeywords` ⇒ `TriggerOps("oath")` 恒为 null）⇒ 12 张 Ultramarines 单位的能力**既不报错、卡面也不打 `*`**。补齐五处：`CardDef.CollectOathOps` · 三句 → `MatchStaticBattleRule` 的 `OathDouble`/`OathTripleActivation`/`OathInAllTurns` · `UnitState.OathUsesThisTurn` + `DeployedTurn` · `RuleCore.CanUseOathAbility`/`UseOathAbility`/`OathExtraReplays` · `EffectResolver.ResolveOathAbility`（**多跑的那几遍不再重复扣费**）。出处：`DefinedTrait` 0x4fc/0x4fd/0x4fe + `CardScript__CanUseOathAbility.c` / `ResolveActiveAbilityPlayed.c`。断言 `TestOathAbility`（9 组） |
| `When a friendly unit triggers Mob, it triggers an additional time`（`GOF_Big_Choppa_Nob`）· `When this unit triggers Synapse, it applies the effect twice`（`TL30 Broodlord`） | ✅ **2026-09-16 做了** —— 单位卡 ① 栏 → 0 | 🔴 **原版没有「再触发一次」这个通用原语**（反编译里 `BroadcastUnitMob` 只把信号递给监听卡）⇒ **这是我们定的文法**：正文 `TryExtraTrigger` → `extratrigger` 额度（`UnitState.ExtraTriggers`，**按关键词分开记**）→ 就地消费（`RuleCore.TakeExtraTrigger`：Mob 在 `DeclareAttack`、Synapse 在 `RepeatTacticOnAdjacent` 的**广播之后**）。⚠️ **重入守卫** `ctx.ExtraTriggerDepth`（那一次额外触发会再广播一遍 ⇒ 不挡就白捡一次）。断言 `TestBatch0916` ⑤⑥ |
| `When a friendly unit uses Ferocity, it stays in play`（`SW42 Bjorn the Fell-Handed`） | ✅ **2026-09-16 做了** —— 单位卡 ① 栏 → 0 | 常驻版的**正文** `it stays in play` 原来收不下（`ReFerocityStay` 卡死 `the next time … this turn` 那个整句式）⇒ 新 `ReFerocityStayAlways` + `Payload="always"`（结算层的日志措辞分开）。**广播本来就在消费点之前** ⇒ 现成的 `ferocitystay` op 与 `RuleCore.UseAlternative` 的消费段**一行都没改** |
| `Lower cost by 2 every time a friendly unit triggers Synapse`（`TL83 Norn Emissary`） | ✅ **2026-09-16 做了** —— 单位卡 ① 栏 → 0 | 🔴 **缺的不是「一整层」**：`CostWhens` + `BroadcastCostWhen` + `CostMods` 早就齐了，卡的只是**两处正则只认 `when`**（`CardDef.AddWhenTrigger` + `EffectText.TryCostWhenStub` —— **必须一起改**，只改一处会变成「解析器认了、卡面不生效」）。断言 `TestBatch0916` ③ |

⚠️ **读法三条**：「**不认**」= 缺一条 **handler**（解析层）；「**半懂**」= 句型认了但**目标/载荷少了词**；「**认了但没机制**」= 要动的是**结算层** —— 三者改的地方完全不同，**别混**。
