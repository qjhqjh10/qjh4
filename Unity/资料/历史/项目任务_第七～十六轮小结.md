# 项目任务：第七～十六轮的轮次小结（存档）

> **2026-09-13 第三十一轮从 `项目任务.md` 搬出来的** —— 按 `CLAUDE.md` 铁律 6：
> 交接文档只留「当前状态 / 待办 / 坑 / 出处」，**旧的进度叙述是噪声、要挪走**。
> 这些小结写的是**当时**的状态，里面的数字与「下一步」**早就过期** ——
> 要现在的状态看 `资料/阵营推进_清单与交接.md` §五，要逐轮的完整历史看 `git log`。

_✅ 2026-09-12 第十三轮（for each + 兵种 + 反编译代码体检）做了五件：_
_**`for each` 计数层**（三种写法语义不同：前导/后置=重复 N 遍、追加=基础+N×增量）、_
_**兵种过滤** —— 原版数据里的 `subtype` 字段被我们丢了，接回来后「打得比卡面宽」12 → 1 张、_
_**捞回 `Dark Pact of Fate`**（源表里 cost 为 null 被误丢，四条独立证据确认是真卡）、_
_**查清反编译代码是空壳**（1891 个类 0 行方法体，规则实现不在本地）——
**连带确认了「效果是数据不是代码」、以及「黑暗契约四种效果 = 四张真卡」**、_
_**纠正回合模型误读**（一方整回合做完再换人，不是双方并行）。_
_覆盖率 **完全解析 284/449**、**载荷有机制 269**（+18）；自检 **590 条全过**。_

_✅ 2026-09-12 第十四轮（`create` 造牌 + 覆盖率口径补漏）做了三件：_
_**`create` 造牌** —— 28 个分句 / 20 张卡从「完全不认识」到能打：新解析入口 `EffectText.TryCreate`、_
_**新文件 `Core/CreatePool.cs`**（按「阵营 + 兵种 + 关键词」从全卡池筛候选 —— 附录 B/C 是对账尺）、_
_**结算 `EffectResolver.DoCreate`**（进自己手牌 / 对手手牌 / 牌库顶；卡池走 `BattleContext.CardPool`）。_
_**覆盖率口径补漏** —— 解析得出来但**动词没实现**的（`deploy`/`repeat`/`lowercost`/`reanimate`/`drawtype`）_
_以前算「有机制」，现在如实报；**并借机更正了计划文档里那张写错的「原版数据里没有这张卡」名单**_
_（`Combat Elixir`/`Sabotage` 其实是兵种、`Abaddon's Chosen`/`Sergeant Taaman`/`Extermination Protocol`
数据里都有，只是名字写法不同）。_
_覆盖率 **完全解析 304/449**（+20）、**载荷有机制 235**（口径变诚实，269 → 235）；自检 **691 条全过**。_


_✅ 2026-09-12 第十六轮（四个动词补完 + 极限战士/兽人两个阵营 + 对战检查）做了四件：_
_**四个动词补完** —— `drawtype`（定向翻找，照规则书 L461「翻到匹配的、翻过去的留在牌库、然后洗牌」）、_
_`lowercost`（降费，修正挂在 ctx 上、**按卡名**匹配 —— `CardDef` 是共享不可变对象不能改）、_
_`repeat`（**`EffectOp` 里第一种「op 之间的引用」**：`RepeatOps` 由 `Parse` 回填）、_
_`reanimate` 之外全上；**`Oath N:` / `Codex:` 复用已有的付费层与条件层**，没另开动词。_
_**三个「清单」收成一处** —— `EffectDispatch` 表，`ImplementedEffectVerbs` 从它派生（原来要手工对齐）。_
_**修好「自动凑的牌组没有战术卡」**_ —— `StarterDeck` 的 `unitsOnly` **开关从来没实现**、
_`SimpleAI` **只出单位卡** —— 合起来是「引擎自检全绿、真打起来从没碰过战术卡」。_
_**两个阵营的实战检查**：极限战士 vs 兽人 6 局（轮流先手）全完成、平均 13 回合、_
_双方共打出 **29 张战术卡**、593 条日志里**只有 6 条**「没结算」且覆盖 3 张卡（其中 1 条还是「付不起」的正确行为）。_
_覆盖率 **完全解析 354/449** · **载荷有机制 329**；自检 **825 条全过**。_


_✅ 2026-09-12 第十五轮（`deploy` 免费部署 + `it/them` 指代）做了两件：_
_**`deploy` 免费部署** —— 50 个分句从「解析得出来、结算没分支」到真能下场：_
_`EffectText.TryDeploy`（具名 / 阵营+兵种+费用 / 从牌库 / 从弃牌堆 / `and` 尾句五种写法）、_
_`RuleCore.DeployFree`（照抄原版 `_deploy_unit`：第一个空格、满场不硬塞、**不触发 Rally**）、_
_`EffectResolver.DoDeploy`（和 `create` 共用 `CreatePool` 与抽卡器）。_
_**`give it/them X` 的指代修好** —— 代词原来被吃掉，`Target` 成了 null，于是_
_① `Deploy 3 Grot and give **them** Vanguard` 给**全体**加而不是给刚部署的；_
_② `Finish` 判它「没目标」→ **六个卡白白掉出「完全解析」**。_
_修法是让代词当 `prev` 目标 + 新增 `BattleContext.LastTargets`（一批）+ `NeedsTarget` 收成一处。_
_**半懂分句 28 → 4**。_
_覆盖率 **完全解析 324/449**、**载荷有机制 273**；自检 **766 条全过**。_
_⚠️ **新会话先读 `资料/卡牌效果管线_计划与交接.md`**（每件事的**数据在哪**）_
_**和 `资料/阵营推进_清单与交接.md`**（**下一个会话从哪开始**：下一件做 `Choose a …`，
之后每会话两个阵营，附顺序表与「做一个阵营的标准流程」）。_


_✅ 2026-09-12 第十二轮（接着推战术卡覆盖率）做了四件：**关键词机制 23 → 31 个**
（战场事件系：猎杀标记 / 黑暗契约 / 兽群 / 哨戒 / 狙击 / 再生 / 压制 / 失明）、
**三选一 `Choose one:`**（6 张，按原版「单机自动选 1」用种子化随机掷）、
**`instead` 的替换语义 + 四类新条件**（原版在 `instead` 上是做坏的：条件白判、原效果照样打）、
**修掉三个静默失效**（黑暗契约因大小写失配一条增益都没加上 / `melee` 没归到 `attack` /
`Heal … to it` 被当成 `Heal them`）。
覆盖率 **完全解析 276/448**（+8）、**载荷有机制 251**（+20）；自检 **565 条全过**。
坑与出处在 `资料/战术卡效果_移植方案.md`。_


_✅ 2026-09-12 第十一轮（战术卡这一轮）做了六件：**战术卡「能打」全链路打通**
（`EffectText` 文本解析 → `EffectResolver` 结算 → 牌组收得下 → 表现层拖得出去）、
**端到端自检**（真鼠标拖一张原版战术卡打出去）、**关键词机制 13 → 23 个**、
**两条目标规则修正**（`an enemy` 含督军 / 隐身单位不能被敌方效果选中）、
**卡表按卡面核对**（6 张数值 + 9 张稀有度 + 2 类系统性稀有度错，共 167 张）、
**四份原版卡表的对账与合并可行性**。详见 `资料/规则引擎_进度与交接.md` 与
`资料/卡牌数据源对账_0912.md`。_


_✅ 2026-09-12 第七～十轮（分六次会话）做完八件：**卡组编辑界面**（含卡组库/存档/导入导出）、
**对局结算界面**、结算的**「开门」视频**（原版 `EndBattleDoors`：播 Victory/Defeat/Draw 三支片，
自建 shader 合左右拼 alpha）、**原版卡牌接进对战**（Ultramarines vs Goff + 中文卡名/效果 + 原版卡框）、
**原版插图接进来**、**战斗 UI 逐条对齐原版**（卡尺寸/卡面各层/按钮/图标/能量区）+ **卡牌放大展示窗**、
**卡组编辑器编的那套牌接进对战**（督军定阵营；不合法的明说再退回；开局提示行说明有几张没上场）、
**卡面组装按原版规格重做 + 美术全量换源**（立绘铺满/卡框分四档/底部稀有度宝石/战术卡框/
13 阵营卡框齐全；卡池清掉 16 张噪音卡 + 1 张重复条目）。_
