# 卡牌机制 · 落点索引（0913–0914）—— 从《卡牌效果管线》挪进历史

> 出处：`d:/4/Unity/资料/卡牌效果管线_计划与交接.md` 的 **§一·二～§一·十五**
> （2026-09-16 瘦身时**整块挪出、原文照搬**；铁律 6：完成清单 / 逐轮叙述不进交接正文）。
> 覆盖：`create` 造牌 · `deploy` 免费部署 · `give it/them` 指代 · 四个动词 + 两个阵营机制 ·
> 单位卡 `desc` 触发式效果 · 47 条「完全不认识」的查证 · A3/A4/A5 各批与「A4 收尾批 1~4、T1/T2/T2b/T3」的**机制名 → 落点**索引。
> ⚠️ 其中的**坑**已抽回正文 `§零④`；**数字（覆盖率 / 自检条数 / 未实现关键词）以自检输出与
> `资料/阵营推进_清单与交接.md` §一 现状表为准**，别抄这份里的旧数。
> ⚠️ 下文里指向「§一」「§四」「§五」的交叉引用，指的是**当时**那份文档的节号。

---

### 一·二、`create` 造牌（第十四轮）

**做了什么**：`Create …` 这一族 28 个分句 / 20 张卡从「完全不认识」变成能打。

| 件 | 落点 |
|---|---|
| 解析三种写法（目的地后置/前置、复制） | `EffectText.TryCreate`（`EffectOp.Dest` 记去哪儿） |
| **候选池**按阵营 + 兵种 + 关键词从**全卡池**动态筛 | **`RuleEngine/Core/CreatePool.cs`**（新文件） |
| 结算（抽 N 张 → 进手牌/对手手牌/牌库顶） | `EffectResolver.DoCreate` |
| 卡池随对局传进来 | `BattleContext.CardPool` + `RuleEngine.NewBattle(..., cardPool:)`；`BattleDriver` 传 `CardDatabase.Load()` |

**规格书**：规则书**附录 B**（`:254-287`，每张生成器「几种、各几张、上限」）+ **附录 C**（`:289-320`，候选名单）。
**两条来源互相印证**（附录 B 的「N 种」vs 卡池实算）：Ultramarines 载具 **18=18**、SaimHann 载具 **20=20**、
Goff 载具 **14=14**、钛无人机 **7=7**、锈蚀通风口 **11=11**、战斗药剂 **5 vs 6**、虫群 **11 vs 10**。
差的那两处已钉进自检（`RuleEngineTest.CheckDiceTables`）：

| 差在哪 | 事实 |
|---|---|
| `Shivversplint` | 附录 C 算它是战斗药剂，**原版数据里 `subtype` 写的是 `Upgrade`** —— 卡在池外（卡本身存在） |
| `Tyranid Prime` | 带虫群、是利维坦部队，但**附录 C 的 1d10 名单里没有它** |
| `Acolyte Hybrid` ↔ `Aberrant` | 书里那格叫 `Acolyte Hybrid`（卡池里有这张卡，但**没挂伏击关键词**），卡池里挂伏击的那张叫 `Aberrant`。**没做别名映射**，如实记着 |

**⚠️ 没写目的地就判失败**（不默认成手牌）—— 猜错目的地 = 把牌送错人，是最难查的一类。

### 一·三、`deploy` 免费部署（第十五轮）

**做了什么**：`Deploy …` 这一族 **50 个分句 / 49 种写法**从「解析得出来、结算没分支」变成真能部署。

| 件 | 落点 |
|---|---|
| 解析五种写法（具名 / 阵营+兵种+费用 / 从牌库 / 从弃牌堆 / `and` 尾句） | `EffectText.TryDeploy`（`DeployFrom` / `CostMin` / `CostMax` / `UpTo` / `Tail`） |
| **免费下场** | `RuleCore.DeployFree`（逐条照抄原版 `rule_core.gd:3871 _deploy_unit`） |
| 结算 | `EffectResolver.DoDeploy`（和 `create` **共用** `CreatePool` 与 `PickN`） |

**⚠️ 三条语义上是**故意的**，不是漏的**：
1. **不触发 Rally** —— 规则书 `:200` 写的是「集结：**从手牌**部署后触发」，原版 `rule_core.gd:2314`
   也只在 `play_card` 那条路上触发。`RuleCore.PlayCard` 照旧触发，两条路不一样是对的。
2. **满场不硬塞** —— 原版 `_deploy_unit` 返回 false，我们照办并**在日志里说「没空格了」**。
3. **`2-cost` = 恰好 2 费**（不是「≤2」）—— 三条实证：GSC 恰好 2 费 = 5 张（对上附录 C 的 1d5 名单）、
   Sautekh = 6 张（对上附录 B「次元裂隙…**6 种**」）、Leviathan = 6 张（对附录 C 的 1d5 + 1）。
   `that cost N or less` / `that costs N or more` 才是显式区间，两种分开处理。

**⚠️ 弃牌堆那条是近似**：`Deploy up to 5 friendly Infantry troops that died this game` 拿
`Discard` 里 `type == "unit"` 的充数 —— 我们**没有「本局阵亡」的账**，手牌超上限打进弃牌堆的
单位卡也会被算进去。原版有完整的阵亡流水，我们没有。**如实标着，别当它是精确的。**

### 一·四、`give it/them X` 的指代（第十五轮，顺手修的，但影响面很大）

**问题**：`TryGive` 的第三种语序 `Give it Flank` / `give them Vanguard` 把**代词本身吃掉了**，
`op.Target` 成了 null → 结算层落到「**己方全体**」那个近似（原版 `rule_core.gd:3137` 自己标着「近似」）。
后果有两层：
- **打错人**：`Deploy 3 Grot and give **them** Vanguard` 给**全体**加，而不是给刚部署的 3 个；
- **判成半懂**：`Finish` 见「没目标」就判 `Partial`，于是**六个 `Deploy … and give it/them X` 的卡
  白白掉出「完全解析」** —— 做 deploy 时撞到的。

**修法**（两处，都在一处判据上）：
- `TryGive` 的代词**留着**当目标（`ParseTarget` → `Side = "prev"`）；**单复数分开**：
  `it` / `the target` = 1 个，`them` = 一批（`Count = 0`）。
- 新增 `BattleContext.LastTargets`（**上一条效果影响到的整批**）—— 只记单个 `LastTarget`
  表示不了 `them`。`ResolveTargets` 和 `DoDeploy` 都写它。
- `Finish` 里「没目标算半懂」收成一处 `NeedsTarget(op)` —— 原来写死 `op.Verb != "draw"`，
  `create`/`deploy` 这些**本来就不带目标**的动词一进来就被判半懂。

**效果**：半懂分句 **28 → 4**（`_tmp_view/tactic_unparsed.txt` 第 ② 栏几乎清空）。

### 一·五、第十六轮：四个动词补完 + 两个阵营的机制（用户指定）

**用户定的方向**：先做简单的 → 把**极限战士 + 兽人**做到能完整对战 → 然后检查对战顺不顺。

**做了四件**：

| 件 | 落点 | 关键点 |
|---|---|---|
| **`drawtype`** | `RuleCore.DoDrawType` + `CreatePool.MatchesKind` | 语义照**规则书 L461-466**：「从牌库顶往下翻，找到匹配的拿走，翻过去的**留在牌库**，然后**洗牌**」 |
| **`lowercost`** | `EffectOp` + `RuleCore.CostOf` + `ctx.CostMods` | ⚠️ 要给**卡实例**存费用覆盖，但 `CardDef` 是共享不可变对象 ⇒ 修正只能记在 **ctx** 上。**当时按卡名匹配（同名卡一起降价）；2026-09-13 改成按卡 id**（同名卡不再一起降）—— ⚠️ **卡实例身份仍未做**，见 `资料/阵营推进_清单与交接.md` 候选 0 |
| **`repeat`** | `EffectOp.RepeatOps` + `RuleCore.DoRepeat` | **这是 `EffectOp` 里第一种「op 之间的引用」**（此前只有 `instead` 靠一个下标硬撑）。由 `Parse` 在整条 desc 解析完后回填 |
| **`Oath N:` / `Codex:`** | **复用已有的付费层和条件层** | `Oath N:` = 付费激活（和 `12 [Energy]:` 同一条路）；`Codex:` = 条件 `energyzero`（规则书 :175「能量为 0 时触发」）。**没另开动词** |
| **`Stomp`（践踏）** | `RuleCore.DeclareAttack` | 规则书 :213「溢出伤害对目标相邻**随机**敌方单位造成」；照 `rule_core:4372` 的三个判据（目标死了 / `dealt > 打之前血量` / 相邻有单位） |

**顺手修掉的四个真问题**（都是做上面那些时撞出来的）：

1. **三个「清单」收成一处** —— `ResolveOne` 的 `switch` 和 `ImplementedEffectVerbs` 原来要**手工对齐**，
   忘一边就是「能打但不报」或「报了但能打」，两种都静默。现在是一张 `EffectDispatch` 表，清单**从它派生**。
2. **覆盖率里有两条过期的误报** —— 「付费激活没接结算」这条检查是**过期的**（结算层早就实现了付费分支），
   留着它把 `Oath N:` 一族**错报成没机制**（Ultramarines 7 条）。已删。
3. **`StarterDeck` 的 `unitsOnly` 开关从来没实现** —— 函数体里写着「已经是只收 unit 了」。
   后果：**自动凑出来的牌组一张战术卡都没有**，实战里永远看不到战术。
4. **`SimpleAI` 只出单位卡** —— 同上。`NextCardToPlay` 里写着 `if (!c.IsUnit) continue`。
   ⇒ 3+4 合起来：**引擎自检里战术卡用例全绿，真打起来却从来没碰过它们**。
   现在 `NextPlay` 单位/战术都考虑，落点**由引擎说了算**（挨个格位问 `CanPlayCard`）。

**⚠️ 我自己踩的一个坑（值得记）**：`lowercost` 的正则第一版把四条写法塞进一条大正则，
其中 `of?` 被当成「`of` 里的 f 可选」，于是 `Lower the cost of all Vehicles…` 的 payload
被切成了 `f all vehicles…` —— **而且照样算解析成功**。
是「切出来的 payload 长什么样」的断言抓不住「认不认识」这类检查漏掉的错。
教训写进了 `TestLowerCost` 的注释：**只断言「认不认识」抓不住静默的错解析。**

### 三种 `for each` 写法**语义不同**，别混（`EffectOp.CountRef` / `PerCount`）
| 写法 | 例 | 语义 |
|---|---|---|
| 前导 / 后置 | `Deal 1 damage to an enemy for each friendly unit` | **重复 N 遍**（N=0 就一次都不结算） |
| 追加 | `…, and an additional +2 X for each Dark Pact on it` | **基础 + N × 增量**（当重复算会 +2 变 +6） |

### 另外四个坑（都写进代码注释了）
1. **`give` 的数值不在 `op.Amount` 里，在载荷原文里** —— 只改 `Amount` 的话增量静默丢掉
2. **`EffectOp` 列表是解析产物、会被复用** —— 直接改 `Amount` 会让同一张卡第二次打出时翻倍
3. `instead` 的替换判断读到了**上一张卡**留下的 `ctx.LastTarget`
4. 计数写 0 时不能去改载荷（会把基础值抹掉）

---

### 一·六、第三十一轮（2026-09-13）：**单位卡 `desc` 的触发式效果接进引擎**

**修之前**：触发式正文**只**走 `EffectSpec` 那个**封闭文法**（只有 `Damage` / `Heal` / `Draw`），
而原版卡面写的是 `Rally: Stun an enemy` / `Strike: Return this troop to your hand` ——
**解析失败 ⇒ 卡面标 `*` ⇒ 打起来静默不动**。实测 **89 张卡**在 `keywords` 和 `desc`
**两处都登记了触发、正文却没人认**。

**修之后**：正文交给 `EffectText`（**战术卡用的同一个解析器**，卡面本来就是同一套语法）。

| 件 | 落点 |
|---|---|
| **触发关键词 → 正文 op**（两个来源都收：`Desc` 优先、`keywords` 兜底） | `CardDef.CollectTriggerOps` + `TriggerOps()` / `TriggerText()` |
| **只有本版有时机的触发才收**（`RoutableTriggers`） | `CardDef.RoutableTriggers` = Rally / Strike / Slay / Backlash / Penitence |
| 触发时优先走 op，没有才退回封闭文法 | `RuleCore.FireTriggerAt` |
| 把一串 op 结算掉（**新入口**，和 `ResolveEffect` 并存） | `RuleCore.ResolveOps`（定义在 `Core/EffectResolver.cs` 里） |
| 查证探针（只报数、不断言） | `RuleEngineTest.ReportUnitDescCoverage` → `_tmp_view/unit_desc_unparsed.txt` |

**数字**：以自检探针 `RuleEngineTest.ReportUnitDescCoverage` → `_tmp_view/unit_desc_unparsed.txt` 为准
（每次自检重写），**本文档不抄第二份**。

**收哪些触发点**（这是结构，不是数字）：`CardDef.RoutableTriggers` 只收**本版有时机的**那几个 ——
`Rally` / `Strike` / `Slay` / `Backlash` / `Penitence`；其余（`Talent` / `Pray` / `Duty` / `Agenda` /
`Mob` / `Regiment` / `Artifice` / `Codex` …）**缺的是触发时机、不是解析** ⇒ 收了也没人消费，**故意不收**；
`When <事件>, …` 要的是**事件层**。

**踩的坑（都值得记）**：

- 🔴 **代词 `this troop` 是静默错打**。`ParseTarget` 把 `it`/`this troop`/`this unit` 这一族
  **统一归到 `prev`**（那是给战术卡的 `Choose a troop … Draw it` 用的）。
  触发式正文里**没有「上一句」** ⇒ 不处理的话 `Return this troop to your hand` 会去回手
  **上一张被指过的牌**，而场上还看不出哪里不对。
  **修法**：`ResolveOps` 在开头把**触发者自己**种进 `LastTargets`/`LastTarget`
  （借的是同一套代词机制，不另开口子），**只种一次**（后面的 `and give it …` 要能接着用），**用完还原**。
- **不能只看 `Desc`**：有 **35 张卡只在 `keywords` 里写了正文**（`Desc` 里没有）—— `CardDef` 两个来源都收。
- **别用 `KeywordTable.Normalize` 判触发名** —— 它是**前缀匹配**，`Pray: …` 会被它当面。用整词比对。
- **断言要钉两层**：① 正文被解析出来了 ② **它真的改变了局面**。
  只钉① 会漏掉「解析出来了但结算层没分支」。**打断 `FireTriggerAt` 那条新路重跑验证过**：
  断言会红（`Check` 失败 + `ResolveEffect` 空引用，两条都亮）。

**还没做的（下一件）**：`When <事件>, …` 要**事件层**；其余触发时机类关键词要的是**新的触发时机**，
不是新的解析。**现状与剩余清单看 `资料/阵营推进_清单与交接.md` §五 + 候选 A**（本文档不抄数）。

---

## 一·七、🆕 第三十二轮：那 **47 条「完全不认识」查证完了**（`资料/战术卡47条_语义查证.md`）

派子代理逐条查三层权威（规则书 → `rule_core.gd` → 反编译），分档：

| 档 | 条数 | 说明 |
|---|---|---|
| ① 规则书有明文、可做 | **12** | 照规则书做就对 |
| ② **只有 `rule_core` 有、且它写得对** | **14** | **最便宜的一批** —— 规格书已写好，是 Unity 侧没串起来 |
| ③ 原版有 handler、要新机制 | **17** | 语义清楚，引擎没有对应层 |
| ④ **查不到出处** | **3** | 候选池三处零命中（前轮已查，本轮复核不变） |
| ⑤ 单卡特例 | **1** | 卡面设计就是空效果 |

### 🔴 最大的发现：**14 条里有 6 条只差正则宽度**

`EffectText.cs:2128` 的 `RePaid` 比 `rule_core.gd:1549` **窄两处** ——
**主对话已独立核过两边的正则原文**：

| | 写法 | 后果 |
|---|---|---|
| Unity | `\[\s*(energy\|faith\|…)\s*\]` —— **方括号必选** | 收不了 `4 Energy:`（无括号）→ **#8 / #23** |
| Unity | **没有 `\((\d+)\)` 分支** | 收不了 `(5) Reduce…` / `(1) Draw…` → **#31 / #33** |
| `rule_core` | `\[?[Ee]nergy\]?` —— 括号**可选**；另有 `\((\d+)\)` 分支 | 两边它都收 |

⇒ **补这两处 + 一条「`N <能量图标>` 无冒号」的规则**（#9 / #29），上限是「照 `rule_core` 抄」，**几乎零风险**。

### 另外两条要先核、别直接改

- **#7 `12 [Energy]: Draw 3 cards`** —— 理论上两边都该收 → **先跑单测确认断在哪**。
- **#9 `6 [icon] Give them Flank`** —— **卡面本身漏印冒号**（抄录者注明）→ **先按卡图复核**。

### ⚠️ 还有一条是**规格书自己的 bug**

`rule_core.gd:1567-1573` 那条裸 `N :` 的安全校验要求正文含 `energy/refill/gain`，
于是 `2 : Draw an additional troop`（#3）**被它自己挡掉了** —— **是 `rule_core` 太紧，不是 Unity 的锅**。

---

---

## 一·八、2026-09-13 A3/A4：**新增的机制名与落点**（地图，细节不在这里抄）

> 这一节只当**索引**：每个机制一行「叫什么 / 在哪个文件 / 细节在哪份文档」。
> **数字（覆盖率 / 自检条数 / 未实现关键词）只看 `资料/阵营推进_清单与交接.md` §五**；
> **逐条的做法、卡图实据、剩下的清单**看同一份文档的 §一之三·候选 G（A3）与**候选 H（A4）**。

| 机制 | 落点 | 一句话 |
|---|---|---|
| **事件宾语** `TargetCriteria` / `TargetOwnerIs` / `ctx.EventTarget` | `WhenEvent.cs` · `EffectResolver.BroadcastWhen(..., target:)` · `BattleContext.EventTarget` | `attacks <谁>` 的宾语要**筛**、正文里的 `the target of the attack` 与裸 `adjacent` 要**指它**。原版 `TargetsAffected.target = 30` |
| **做者自指** `ActorSelf` | `WhenEvent.cs` + `WhenEvents.Matches` | 「这件事是**监听者干的**」（`this unit kills …`）—— 与 `SelfOnly`（落在谁身上）**方向相反** |
| **一句话两条事件** `WhenEvents.ParseAll` | `WhenEvent.cs` · `CardDef.AddWhenTrigger` | `create **or play** a Secret`；**两半都认得出才收** |
| **`kills` 事件** | `WhenEventKind.Kills` · `RuleCore.DeclareAttack` 里那个 `killed` | 与 `Slay:` 关键词**共用一份判据**（原版只有一个击杀时机 `Slay=120`） |
| **`spiritability` 事件** | `WhenEventKind.SpiritAbility` · `EffectResolver` 付费段 | 「花灵魂石激活」那一下；⚠️ 卡池 0 张带该前缀 ⇒ 点亮但不响 |
| **没写目标** `Subjectless` | `EffectTargetSpec.Subjectless` · `ResolveTargets` | **有施放者⇒它自己**；战术卡⇒点了谁就是谁；都没有才落到「己方全体」（老近似） |
| **付费前缀的四种写法** | `EffectText.RePaid` / **`RePaidBare`** / `CostKindOf` | `(5)` · `1 Also …` · `6 ☀ …` · `[icon]` —— **绿圈=能量、太阳=信仰**（卡图实据） |
| **「设为 N 费」** | `EffectOp.CostSetTo` · `DoLowerCost` | `reduce its cost **to** 1` 与「降 N 费」**不是一回事**（差值按当时真费用算） |
| **`sethealth`** | `TrySetHealth` · `DoSetHealth` | `Lower its Health to N`（只降不升、发 `EmitHit`、无敌挡下） |
| **`noeffect` / `reloadduty` / `costwhen`** | `EffectText.TryNoEffect/TryReloadDuty/TryCostWhenStub` + 三个 handler | 「原版就是空效果」/「装填职责」/「事件触发式降费的**标记 op**」（让那句话解析得出来） |
| **句子边界含换行** | `EffectText.Split` | 卡面印不下时后一句换行且**不带句号**（`Fire and Fade`） |
| **`SplitAndTail` 的消费者又多了几处** | `TryHeal` / `TryReturn` | `Heal 5 **and give** …`（目标只写在后半句 ⇒ `Finish` 里那条**共用目标**的规则）· `Return … **and reduce** its cost to N` |

### 一·九、2026-09-13 A4 **收尾·批 1**：新增的机制名与落点（同样只当索引）

| 机制 | 落点 | 一句话 |
|---|---|---|
| **`eachunitdeal`**（每单位按自己的值开火） | `EffectText.TryEachUnitDeal` · `EffectResolver.DoEachUnitDeal` | `Each of your units deals damage equal to its Shuriken to a random enemy` —— 伤害取自**每个单位自己**的关键词值，**不是求和**也**不是施放者的值**；⚠️ **要排在 `TryDeal` 前面**（否则被 `ReDeal` 从中间截走） |
| **`paidmod`**（付费**修饰型**激活） | `EffectText.ParseSegment` 里那段 + `EffectOp.BaseOps` · `EffectResolver.DoPaidMod` · `UnitState.RemoveBuffsFromCard` | `6 [Energy]: Extend effect until your next turn` / `8 [Energy]: Give it permanently` —— **撤销本卡的限时增益 → 按新时长重结算**（出处 `rule_core.gd:1588` 的 `undo`/`replay`）。⚠️ 撤销按 `TempBuff.SourceCard`（**真卡名**）匹配，**不是 `Src`**（战术卡那条路上恒为「战术卡」） |
| **目标筛：`DamagedOnly` / `PrayedOnly`** | `EffectTargetSpec` 两个 bool · `EffectResolver.ResolveTargets` 里两道 `RemoveAll` | `all damaged enemy troops`（`health < max_health`，`rule_core.gd:1415`）· `… that is Praying` |
| **`UnitState.Prayed`**（「正在祈祷」**状态**） | `UnitState.Prayed` · `RuleCore.UseAlternative` 置位 · `RefreshForNewTurn` 复位 | 与 `When a friendly unit Prays`（**事件**）是两件事；⚠️ 复位在**控制者的回合开始**，`RefreshForNewTurn` 只刷当前行动方 |
| **`anypraying` 条件** | `EffectCondition.Normalize` · `EffectResolver` 的 `case "anypraying"` | `if any friendly unit is Praying`（`Sororitas Rhino`）；与 `PrayedOnly` **读同一个字段** |
| **静态条件降费 `CostIfControl`** | `CardDef.CostIfControl(s)` + `CollectCostIfControl` · `RuleCore.CostOf` / `ControlsKeyword` | `This costs N less if you control a unit with <关键词>` —— **常驻条件**（人走价回），**不进 `ctx.CostMods`**；⚠️ 判据要在 `CostMods.Count == 0` 的早退**之前** |
| **对称部署 `EffectOp.EachPlayer`** | `EffectText.TryDeployEachPlayer` · `EffectResolver.DoDeploy` / `DoDeployOnce` | `Each player deploys 3 troops from their deck` —— 双方各来一次；顺序**先对手后自己**（**我们挑的**，为的是让 `LastTargets` 停在自家那批上，供下一句 `deployed this way`） |
| **`deployed this way`** | `EffectText.ParseTarget` 里那一支（复用 `spec.Deployed`） | `Your troops deployed this way gain …` —— 指**上一句刚部署的**；⚠️ 张数要补 `all ` 前缀，**直接写 `Count = 0` 会被下面「几个」那段覆盖** |
| **工具：逐句解析探针** | `RuleEngine/Editor/EffectParseProbe.cs`（入口 `EffectParseProbe.Run`） | 读 `_tmp_view/probe_in.txt` 一行一句 → 写 `probe_out.txt`（卡级判定 + 逐段判定 + op 摊平）。**改解析层判据之前/之后用它量**（那个 `\\|` 优先级 bug 就是量出来的） |

⚠️ **两个别踩回去的坑**：
1. **正则里 `|` 的优先级**：`(?<body>give|draw|…|attack.+)` 会把 `.+` 只绑到**最后一个**分支 ——
   必须写成 `(?:(?:give|draw|…|attack).+)`。第一版就是这么错的，表现是「认不出**而且不带 cost**」。
2. **黑暗契约那四张卡（`Dark Pact of Fate/Blood/Excess/Resilience`）**：它们的 desc 现在会走普通 `give`
   （没写目标 ⇒ 己方全体），但**真效果**由 `DarkPactFx` 这条通道在**授予时**加到持有者身上。
   从手牌直接打是**近似**（原版那两条数据在卡表里、我们拿不到 `targetCriteria`）。**别当成 bug 改回去**。

### 一·十、2026-09-13 A4 **收尾·批 2**：新增的机制名与落点（同样只当索引）

| 机制 | 落点 | 一句话 |
|---|---|---|
| **`forceattack`（强制攻击）** | `EffectText.TryForceAttack` · `EffectResolver.DoForceAttack` / `DefenderPool` / `TryAttackOnce` | 三种写法：`Make <攻击者> attack by itself` / `Target <攻击者> attacks <被打的>` / 裸 `Attack(s) <被打的>`。**`Target` = 攻击者、`Target2` = 被打的，两栏分开**。🔴 **一律走 `RuleCore.DeclareAttack`** —— 疲劳/配额/现身/反击/事件全由那一条路径管，**绝不在效果层另写一遍攻击** |
| **`EffectTargetSpec.PickMost`** | 字段 + `EffectResolver.StatOf` + `ResolveTargets` 里那一支 | `the enemy with **highest attack**` / `lowest Health` —— 取值 `"+attack"` / `"-health"`。⚠️ 解析时必须**抢在 `ReTargetWith` 前面**（不然 `highest attack` 会被当成「带该关键词」⇒ 筛完一个不剩、**空过**）。并列取槽号在前的（不掷骰） |
| **`EffectOp.Target2`** | 字段 | 同一句里的**第二个目标**。目前只有 `forceattack` 用。⚠️ 别塞进 `Tail` —— 那会把「谁去打」和「打谁」揉成一个 |
| **反向共用目标** | `EffectText.Finish` 里尾句那段 | 尾句**写了动词却没写目标** ⇒ 主语沿用**本句的目标**（`Your Warlord gains Concussive … **and heals 5**`）。全卡池这个形状实测 **5 句、5 句都该继承**。⚠️ 判据很紧：只在「尾句 `NeedsTarget` 却 `Target == null`」+「本句有目标」时接；`forceattack` 单列（它的「空」是语法错误） |
| **`ApplyTargetFilters`（目标筛的唯一来源）** | `EffectResolver.ApplyTargetFilters` | 兵种 / 关键词 / 已受伤 / 正在祈祷 **四道筛**，**结算（`ResolveTargets`）与「这一格能不能选」（`IsLegalPick`）都读它**。🔴 分叉的症状就是**拖拽高亮说能选、松手打出去却空过** |
| **`IsVerbWord` 补 `attack` / `ReGain` 补 `gets` / 句首 `Each`** | `EffectText.IsVerbWord` · `ReGain` · `ParseTarget` 的「几个」那段 | `gets` 也是「获得」（原来只认 `gains`）；句首 `Each ` 与 `all ` **同义**（不认就落回 `Count = 1` ⇒ **只给一个单位加**）；`attack` 进动词表才能让 `and attacks …` 被切开 |
| **`PickTarget` 跳过督军** | `EffectText.PickTarget` | 一方只有一个督军 ⇒ `Your Warlord …` 这类目标**永远不用玩家选**（不跳的话 `CanPlayTactic` 要格位 ⇒ **整张卡打出去报 `ErrSlot`**） |

✅ **强制攻击的语义：用户 2026-09-14 给了口径 —— 照这个做，别当悬案**：
「**就当做这个单位进行的普通攻击**；攻击类型**根据它近战/远程里最高的那个攻击力值**决定；
打谁**由效果决定**（随机还是确定的目标）。」
⇒ 反击伤害 / 事件广播 / 攻击类型**都不再是「查不到」**（我们走 `RuleCore.DeclareAttack`，本来就一致）。
🔴 **攻击类型不兜底**：取高的那种，打不动就**如实报「打不了」**，别偷偷换另一种（用户口径里没有这一条）。
⚠️ 仍**无原版出处**的两点（**别再查**）：① **每个单位各打一次**——原版 `AddForceAttack` 只收单张卡，
「多个单位各打一次」是效果层循环；② `by itself` 挑谁（卡面没写死，我们按「第一个打得动的」）。
⚠️ 原版 `ResolveForceAttack` 那几个只是**迭代器状态机的分配函数**，真逻辑在各自的 `MoveNext` 里，
而那些 `MoveNext` **不在这次反编译导出中**（地址清单 `d:/2/Warpforge_tools/data/decomp_il2cpp_0827/ghidra_scripts/Decomp.java:29-42`）。
唯一**有出处**的一条：攻击**计入** `attacksThisTurn`（`AddForceAttackFriendlyUnits` 没有 `dontCountAttack` 参数）。
⚠️ `d:/4/Unity/素材/Warpforge原版/卡牌/卡组数据/…/Murderous Desires.json` **是卡包不是效果定义**（`packId: BL_MurderousDesires`），别去那儿找效果。

### 一·十一、2026-09-14 **T1 / T2 / T2b / T3 / 批 3**：新增的机制名与落点（同样只当索引）

| 机制 | 落点 | 一句话 |
|---|---|---|
| 🔴 **伤害公式抽成纯函数** | `RuleCore.DamageAfterReduction(UnitState, dmg)` | 盾 / 无敌 / 易伤 / 护甲**四道**，**不产生任何副作用**。`ApplyDamage` 与 `WouldKill` **都读它** ⇒ 「预测会不会打死」与「实际结算」**不可能分叉**。**要改伤害公式就改这一处。** |
| **`WouldKill`**（这一下能不能打死） | `RuleCore.WouldKill(u, dmg)` | 纯预测。目前唯一消费者是下面的 `PreferKillable`。⚠️ `destroyer` 的规则书原文 `:180`「总是优先攻击可被摧毁的单位」是**同一句话**，但那条管**普通攻击**、**这一版没接**（要接就读这个函数，别写第二份）。 |
| **强制攻击挑「打得死的」** | `EffectResolver.PreferKillable` | **只在卡面没写打谁时**（`op.Target2 == null`）把「打得死的」排到前面，组内按**格位号**（不掷骰）。判据三处**共用**：`FieldAttack` / `WouldKill` / `IsValidTarget`。一个都打不死 ⇒ **原样返回**（退回原行为）。出处：**用户 2026-09-14 口径**。 |
| 🔴 **复数的己方部队 = 全体** | `EffectText.RePluralOwn`（`ParseTarget` 的「几个」那段） | 原版 `rule_core.gd:3966-3980`「**复数全体**」：`your troops` / `your units` / `friendly Infantry troops` ⇒ `Count = 0`（**自动全体、不要玩家点**）。判据是**名词的单复数**、**不是 `all` 这个词**。⚠️ **两处不碰**：单数（`a friendly troop` 仍是挑一个）· **相邻**短语（那一圈由 `AdjacentAll` 管，动 `Count` 会让锚点挑不出来）。实测全池 **44 句**。 |
| **`chooseeffect`**（选一个效果） | `EffectText.TryChooseEffect` · `EffectResolver.ChooseEffectPools` / `DoChooseEffect` | 池子**按「正在结算的那张卡的名字」查**（`ctx.PlayingCard`），**不写在句子里**。三种作用域：`self`（条目是整张卡 → 解析 `desc` 逐条 `ResolveOne`）· `give`（合成一条 `give` op 复用全套）· `hand`（**没做，如实报**）。⚠️ **不是 `choosecard`**：那个从卡池/牌库/手牌里筛**卡**，候选来源根本不同。⚠️ 挑法是我们的（`ctx.Rng` 等概率，UI 之后改玩家选）。 |
| **反语序治疗** | `EffectText.ReHealReverse` | `Your Warlord heals 1`（主语在动词**前**）。判据**卡死在 `(your\|the) warlord`** —— 全池只有 `Exemplary Warrior` 这 1 条；放开成一般的「名词短语 + `heals N`」会**吃掉条件从句**（`When another troop dies, heals 2` / `it heals 2`），那是**静默错语义**。 |
| **`double`**（近战攻击 + 生命翻倍） | `EffectText.TryDouble` / `ReDouble` · `EffectResolver.DoDouble` | `Double the Melee Attack and Health of a friendly troop`（`Possession`，Black Legion，8 费）。⚠️ **`ReDouble` 的组 1 卡死在 `melee attack and health` 这一个字面** —— 全池另一句 `Double` 是 `Maulerfiend` 的 `Ecstasy 5: Double this troop's [Melee] and [Ranged]`（**翻近战+远程、没有生命**，且属未实现的 `ecstasy`），用 `(.+)` 会让它被这条领走、**按错的属性组合翻倍**。⚠️ **两处是我们的取舍、写进了 `DoDouble` 的注释**：按当前显示值 ×2（**不清**增益；原版 handler 会先清）· 生命**连上限一起** ×2（依据是本工程既有的生命口径 `rule_core.gd:3317`，不是从这张卡查到的）。 |
| **`ferocitystay`**（狂暴留场） | `UnitState.FerocityStay` · `EffectText.ReFerocityStay` / `TryFerocityStay` · `EffectResolver.DoFerocityStay` · `RuleCore.UseAlternative` 的狂暴段 · `UnitState.RefreshForNewTurn` | `The next time it uses Ferocity this turn, it stays in play`（`Bjorn's Shrine`）。🔴 **反编译里有完整体**：`CardScript__UsedActiveAbility.c:52-64` 的 `has(dontReturnFerocity)`（`DefinedTrait:131 = 1270`）豁免 + `:75-88` 用完即撤 ⇒ **一次性**。⚠️ **「留在场上」和「本来就要死」是两个各自独立的短路**，别写成一个。⚠️ 消费点**必须在狂暴那一段**（用掉当场清）；只在 `RefreshForNewTurn` 清 = 「本回合每次狂暴都留场」。⚠️ 别和 `Bjorn the Fell-Handed` 的**常驻版**混（那张归事件层，今天仍判不认识）。 |
| 🔴 **常驻层的第三类触发 `when`** | `EffectOp.When` · `PersistentEffect.Ev` · `EffectResolver.DoPersist` · **`EffectResolver.BroadcastPersistentWhen`** · `EffectText` 的 `RePersistAny` 第 ③ 支 | `For the rest of this battle, when <事件>, <正文>`（`Raid Tactics`）。前两类是 `turn`（回合起止）与 `deploy`（部署时）；这一类是**任意事件**。⚠️ **`BroadcastPersistentWhen` 必须排在 `BroadcastWhen` 那句「场上没人听就 return」之前**（和 `BroadcastCostWhen` 同一条教训）。⚠️ 事件短语**复用 `WhenEvents.Parse`**；判据**复用 `WhenEvents.Matches`** —— 都别另写一份。⚠️ **容器形状是本工程自定的**：原版也是「牌在听」，而这张的监听者是**已进弃牌堆的战术卡**，那一端原版**同样找不到对应**（如实标着）。 |
| 🔴 **「这一条 op 有没有机制」只此一份** | `EffectText.OpHasMechanism` | 全局覆盖率（`Coverage`）与 `RuleEngineTest.ReportFactionCoverage`（逐阵营表）**读同一个**。⚠️ 2026-09-14 之前**两处各写了一份**，逐阵营那份少了 `create` 池子检查与 `chooseeffect` 手牌作用域 ⇒ 同一张卡在两张报表里口径不同。两类结果要分清：`false`+`imprecise=false` = **真没机制**（卡面打 `*`）；`false`+`imprecise=true` = 「**打得比卡面宽**」（会生效，只是打多/打错人）。 |

⚠️ **本轮改的是数据、不是兜底**（T2）：`GivePayload` 对**裸 `+N`** 一律判**近战**
（`GivePayload.cs` 那条 `ReBareSigned`，依据只有 `March of Vengeance` 一张卡）——
**改不成**（正则拿不到「卡面第几个图标是什么」）⇒ 回去修**卡表数据**：
`数据/游戏数据/cardface_fixes.json` 的 `desc` 列（**脚本会原样保留那一列**）→ 重跑 `工具/gen_cards_engine.py`。
⚠️ 写法用 `+1 Attack` / `+1 Ranged Attack`（该表既有约定），**不是**卡面上的 `Melee`/`Ranged` 字形。

### 一·十二、2026-09-14 **A4 批 4**：新增的机制名与落点（同样只当索引）

| 机制 | 落点 | 一句话 |
|---|---|---|
| **`triggerability`**（强行触发关键词正文） | `EffectText.TryTriggerAbility` + `ReTriggerAbilityOf` / `ReTriggerTheirAbility` · `EffectResolver.DoTriggerAbility` · `RuleCore.TriggerKeywordOf` | `Trigger the <关键词> ability/abilities/effect(s) of <目标>`（`Author of the Codex` / `Duty's End` / `Atalan Leader` / `Master Lazarus`）与**尾句** `… and trigger their <X> abilities`（5 句）。⚠️ **关键词必须在 `CardDef.RoutableTriggers` 里**，否则判不认识（不做永不生效的动词）。⚠️ 走 `RuleCore.FireTriggerAt`（所有触发唯一的出口），日志/事件/递归保护/「关键词被触发」广播全在那一处。**实测量出来的族 = 8 张卡**，详见 `资料/历史/战术卡剩余7条_逐卡查证_0914.md`。 |
| 🔴 **`codex` 进 `RoutableTriggers` 的副作用** | `EffectText.PrefixKeepsMeaning`（新） | `ParseSegment` 会把**整段开头**的可路由触发前缀剥掉。`codex` 一进去，`Codex: <正文>` 的前缀就被剥 ⇒ `Dispatch` 里**挂 `EnergyZero` 条件的分支永远进不去** ⇒ 条件丢失、**Codex 不等能量归零就触发**（自检两条红）。⇒ 两个名单**用途不同、是子集关系**：`RoutableTriggers` 管「引擎会不会调这个关键词的正文」，`PrefixKeepsMeaning` 管「整段开头的这个前缀要不要剥」。**加新前缀进 `RoutableTriggers` 之前先想一遍这一条。** |
| **强行触发绕过条件** | `BattleContext.ForcedTriggerDepth` · `ConditionHolds` 的 `"energyzero"` 分支 · `RuleCore.BeginForcedTrigger` / `EndForcedTrigger` | `Codex:` 的每个 op 都挂着 `EnergyZero`，而「强行触发」的全部意义就是「不等那个时机」⇒ 那一段里该条件**一律判成立**。⚠️ **证据只有一半**：`CardScript__TriggerOtherCardCodex.c` 整个方法体只有发一个 `TriggeredCodex=650`、**没有能量判据**（有据）；但能力自身的条件读不到（`AbilityLogic.CanPlayAbility` 方法体空）⇒ 绕过与否**是我们挑的**。⚠️ 用**计数器**不用 bool（`Duty's End` 的正文本身就是一句强行触发，会嵌套）。 |
| **事件型手牌陷阱** | `EffectText.SplitHandTrapWhen(CardDef)` / `IsHandTrap(CardDef)` · `CardDef.HandTrapWhens` / `FireHandTrapWhen` · `EffectResolver.BroadcastHandTrapWhen` | `When <事件>, <正文>` 挂在**非单位卡**（破坏卡）上 = 躺在**持有者手里**监听事件（`Jammed Communications`）。⚠️ 判据必须吃 `CardDef`：单位卡的 `When …` 是**事件层**的地盘，解析层无条件认它会让覆盖率虚报。⚠️ 广播点要排在 `BroadcastWhen` 那句「场上没人听就 return」**之前**。⚠️ `IsHandTrap(CardDef)` 盖**两族**（回合起止型 + 事件型），`CanPlayTactic` / `DeckBuilder` / 覆盖率判据读同一份。 |
| 🔴 **`a Stratagem` 的判据改判** | `CreatePool.KindWords` 的 `stratagem` 行 + `MatchesKind` 支持 `type` 多值 | 原来按 `subtype == "Stratagem"`（**只有 6 张**）——**错**。权威实现 `rule_core.gd` **三处一致**（`:950` / `:3852` / `:4695`）：**`type ∈ {tactic, defence}`**（规则书 `:72` / `:105`）。池子 **5 → 488 张**。⚠️ `spell` 那一行**没动**（同一处也说 `spell` = 战术大类，但 `Spell` 在我们数据里是 245 张的真实 `subtype`，牵动选牌那一族，留待数据专线）。 |
| **`When you play X` 的方向筛选** | `WhenEvents` 的 `play` 族解析 | `Clean` 把 `you` 剥掉之后 `SetWho("")` **直接返回 = 方向不限** ⇒ 「你打的」变成「谁打的都算」。实测受害：`Ork Nob` 的 `When you play a troop, give it Flank` **双方部署都送 Flank**；`Jammed Communications` 则是**持有者的督军被对手的战术打伤**。⇒ 补成 `OwnerIs = RelFriendly`（全池这一族只有 4 句）。 |
| 🔴 **`SplitAndTail` 往后扫** | `EffectText.SplitAndTail` | 原来只看**第一个** ` and `，而载荷里自带 ` and `（`Give +2 Attack, +2 Armor **and** +2 Health …`）⇒ **后半句被吞进目标短语**、整句还判「认了」。改成**往后扫到第一个「动词开头」的 ` and `**。实测全池只有 **4 句**受影响、4 句全是修好。⚠️ 非回归：`Deal 3 damage to an enemy and its adjacent units`（相邻）**不切**。 |
| **手工覆盖列** | `数据/游戏数据/cardface_fixes.json` 的 `_manual_subtype` / `_manual_keywords` · `工具/gen_cardface_fixes.py` | 那两列是**算出来的**（源 = `_合并总表.md`），而那张表**只覆盖 6 类卡、没有 `6破坏卡`** ⇒ 破坏卡的事实只能手工覆盖。⚠️ 直接往 `subtype` 列里加会被下一次重跑**静默抹掉**（脚本 160 行那段注释抱怨的就是这个坑）⇒ 覆盖必须走单独一列、由脚本**最后应用**（手工赢过算出来的）。 |

### 一·十三、2026-09-14 **A5 批 1**：新增的机制名与落点（同样只当索引）

| 机制 | 落点 | 一句话 |
|---|---|---|
| **`Heal N` 没写目标 = 治疗自己** | `EffectText.TryHeal` 的 `Subjectless` 那一支 | `Strike: Heals 4` / `Codex: Heal 1` / 裸 `Heals 3` 原来全是**半懂**（`heal n=N 目标[（没写）]`）⇒ 卡面打 `*`、掉出「完全解析」，而机制本来就有。判据走 `EffectTargetSpec.Subjectless`（和 `Give <内容>` 没写目标**同一条路**）：有施放者就是它自己、否则己方全体。⚠️ **只在「本句没有尾句」时套** —— 否则 `Heal 5 and give Camouflage to a friendly unit` 会从「治那个友方单位」变成「治自己」（静默错打）。⚠️ `SplitAndTail` 给 `tail` 的初值是**空串不是 null**（判据写成 `tail == null` 会**静默不生效**）。 |
| **「这一句是不是已经由别的层接手」** | `CardDef.HandledByOtherLayer(c, seg)` + `EffectText.Coverage` 里的按分句过滤 | 单位卡三族（`When <事件>, …` ≈21 次 · `Talent: <名>` 35 次 · `Companion N: <卡名>` 8 次）**机制一直在跑**，只是 `EffectText` 认不出**那句壳** ⇒ 报表虚低、下一个人会去重做已经做完的事。⚠️ 判据**全部转调**那三个采集器用的抽取函数（`TryParseWhenSentence` / `ExtractTalent` / `ExtractCompanionName`）—— **一行新文法都不写**。⚠️ 事件那一族**只对单位卡成立**（`CanListenForEvents => IsUnit`）；非单位卡的同形状句子是**手牌陷阱**（另一条路，见 §一·十二）。⚠️ `TryParseWhenSentence` 是**抽出来共用**的 —— `AddWhenTrigger` 与这个判据读同一份，别写第二份。 |

### 一·十四、2026-09-14 **A5 批 2/3**：新增的机制名与落点（同样只当索引）

| 机制 | 落点 | 一句话 |
|---|---|---|
| **Codex 的自动触发点** | `RuleCore.CheckCodex(ctx, p)`（`PlayCard` 末尾 + `PlayTactic` 末尾各调一次） | 语义照原版参考实现 `d:/warpforge/scripts/rule_core.gd:2397 _check_codex`：**打出任何一张牌之后、能量「恰好为 0」⇒ 触发「第一个」带 `Codex` 的单位的正文**（那边是 `break`，一次只一个）。**原来根本没有这条触发点** ⇒ 20 张带 Codex 的卡（10 前缀 + 10 裸写）的正文一条都不会自己发生。⚠️ 条件那一半在 op 上（`EffectText.MarkCodexCondition` 挂的 `EnergyZero`，**全仓只此一处**）；「能量不为 0 也要触发」走 `ForcedTriggerDepth`。 |
| **`codex` 进 `BodyKeywords`** | `CardDef.BodyKeywords` + `CollectBareKeywordBody` | 裸写正文的 **10 张 UM**（文档原写「9 张」，**漏了 `Sergeant Allectius`**，已就地更正）。⚠️ 守卫认得**方括号**（`EffectText.NormalizeIconPrefix`）—— 不认的话 `Death from Above` 的 `[Codex] …` 会被当成裸写、把整条 desc 收成 Codex 正文。 |
| **单位卡自己的回合起止层** | `RuleCore.ResolveAtTurn` 的**触发源②** + `EffectText.AtTurnClauses` | 参考实现 `_at_turn_effects`（`:397-442`）有**三个**触发源，我们原来只有两个 ——「**双方棋盘上的 at-turn 单位**」整层没做。语义照抄：`each`（`each|every turn`）= **双方回合都触发**、`you` = 只在控制者回合；**后缀式** `Takes 1 damage at the start of your turn` 也认。⚠️ **`AtTurnClauses`（宽）与 `SplitAtTurn`（窄）是两个问题两条判据** —— 后者只管「手牌陷阱」，放宽它会让**单位卡被 `DeckBuilder` 当陷阱排除出牌组**。 |
| **「被这一下打到的那个」** | `CardDef.AttackedOps` + `EffectText.EffectTargetSpec.AttackedBySelf` + `RuleCore.DeclareAttack` 攻击结算之后那一块 | 卡面 `<动词> … attacked [by this unit]`，**全池 6 张、一个关键词都没有**（`Venomthrope` / `Blastmaster Noise Marine` / `Sonic Blaster Noise Marine` / `Stikkbomb Boy` / `Snakebite Grot` / `Arjac Rockfist`）。原版触发点 `AbilityTrigger.UnitAttack = 50`（`CardScript__ResolveUnitAttacked.c:30` 传 `0x32`，和 Slay/Strike 同一个函数）。⚠️ **筛选条件照样判**（`with Armour` / `with Hunt Mark`）—— 判据在 `ResolveTargets` 的 `AttackedBySelf` 分支，断言里有反例。⚠️「伤害前还是之后」两路子代理取证**相反**，我们沿用本块既有的近似（伤害之后），**如实标着**。 |
| **`After receiving a Dark Pact, …`**（无 `When` 前缀） | `CardDef.TryParseAfterDarkPact` | 3 张（`Chaos Legionary` / `Meltagun Legionary` / `Aspiring Champion`）。省略主语 = **这张卡自己** ⇒ `WhenEvent.SelfOnly`，事件仍是 `GetsDarkPact`（广播方本来就传了 `subject`）。 |
| **`Whenever …` ≡ `When …`** + **缺逗号**的 `When <事件> <正文>` | `CardDef.TryParseWhenSentence` / `FinishWhenSentence` | `Whenever` 是卡面另一种写法（`Neurotyrant`）；缺逗号全池 1 条（`Eliminator Sergeant`）—— 切点靠「事件短语与正文**两边都解析得出**」判出来（用两个现成解析器当判据，**不新写文法**）。 |
| **`non-Ephemeral` 限定** | `WhenEvent.NotEphemeral` + `WhenEvents.Matches` | `When you play a non-Ephemeral Stratagem, …`（`Neurotyrant`）。判据照参考实现 `rule_core.gd:2170`：**读卡面 keyword**，不读运行时标记。拿不到 `card` 就**判不触发**（同 `SelfOnly` 口径）。 |
| **`it heals N`（代词当主语）** | `EffectText.ReHealPronounSubj` | 1 条（`Apothecary` 的 `When a friendly unit obtains [Shield], it heals 2`）。⚠️ **只放行代词** —— 一般名词短语一律不放行（那是本工程记过的坑「条件从句当主语」，断言里有反例）。 |

### 一·十五、2026-09-14 **A5 批 3 / 批 4**：新增的机制名与落点（同样只当索引）

> ⚠️ **判据与坑**（为什么这么做、踩过什么）**不在这一节** —— 在
> `资料/阵营推进_清单与交接.md` **§一之四 那 11 条**。这一节**只当索引**。

| 机制 | 落点 | 一句话 |
|---|---|---|
| **`costs N more [this turn]`**（**这张卡自己**加价） | `EffectText.TrySelfCostMore` + `EffectOp.SelfCostMoreMarker` → `EffectResolver.DoCostMore` 的分流 | `Makari` 的 `Backlash`，全池唯一。⚠️ **`cost` / `costs` 必须进 `IsVerbWord`**，否则 ` and costs …` 尾句切不开、整句不认 |
| **`next <X> … costs N less / 0`**（一次性降费） | `EffectOp.NextOnly` → `CostMod.Once` → `RuleCore.ConsumeOnceCostMods`（`PlayCard` / `PlayTactic` **各一处**）+ `EffectText.SubjectCostOp` | **8 句一族**。时长写在 `costs` **前面**（旧代码当成主语的一部分 ⇒ 5 句全没生效）；`costs 0` = **变成 0 费**（`EffectOp.CostSetTo` 哨兵从 `0` 改 **`-1`**）。⚠️ 撤销**只能在付费之后** |
| **括号选项表 `Choose and gain a bonus (A, B or C)`** | `EffectText.ReChooseBonus` → **归一**成 `choose one: …` | **不另写选项表文法**，三选一判据继续只留在 `TryChooseOne` |
| **`Each of your units deals N-M damage`** | `EffectText.TryEachUnitDeal` 第②支 + `EffectResolver.DoEachUnitDeal` 的 `fixedDmg` 分叉 | 和关键词型（`equal to its Shuriken`）**共用同一个动词** |
| **`Other friendly <筛选> deal N damage`** | `EffectOp.Subject` + `EffectOp.OtherThanSelf` + `EffectText.SubjectOf` | ⚠️ **`Subject` 这一维 A7 光环也要用**（`Your other units have +2`）—— 到时候**加宽它，别另开一栏** |
| **`a random <兵种词>` 降费** | `EffectOp.PickOne` + `EffectText.TryLowerCost` / `EffectResolver.DoLowerCost` | **`a` 与 `random` 是语义**（随机挑一张），不是噪声 |
| **`This card costs 1 less for each …`** | `EffectResolver.DoLowerCost` 的 `t == "this card"` 分支 | 同上：`this card` 也是语义（= **本卡自己**） |
| **🔴 反噬的「回手」走弃牌堆** | `EffectResolver.DoReturn` 开头那段守卫 | 反噬触发时单位**已经离场** ⇒ 不接管的话 `Subjectless` 会落到「己方全体」兜底、**把全场单位一起收回手牌** |
| **静态改战斗规则（4 条）** | `CardDef` 的 `MeleeEqualsHealth` / `Bodyguard` / `IgnoresVanguard` / `CostPerEnemyHandCard`（`MatchStaticBattleRule` 是**唯一判据**）；读点分别是 `RuleCore.FieldAttack` / `DeclareAttack` / `IsValidTarget` / `CostOf` | **不是 op、不登记任何东西** —— 只在各自那一个读点上现判 |
| **`This troop attacks it`** | `EffectText.TryForceAttack` 第⑤支 | 攻击者是**本卡自己**（`Target` 留空、打谁在 `Target2`） |
| **天赋的裸名写法** | `CardDef.ExtractBareTalentName` + `CollectTalent` + `HandledByOtherLayer` | 卡面是 `[Talent 图标] Talent: <名>`，**数据管线把图标和前缀一起剥掉了** ⇒ 只看文本认不出。两道闸，且**已知关键词要排掉** |
| **开局上手** | `CardDef.StartWithInHand`（+ `ExtractStartWith`/`CollectStartWith`）→ `RuleCore.NewBattle` 的 `SetupStartWith` | **开局长效，不是一条 op**（那一刻连回合都没开始）。牌库里有就先从牌库拿 |
| **`Stratagems in your hand become A or B`** | `EffectText.ReBecome`（0·0d 归一）→ 动词 `become` → `EffectResolver.DoBecome` | `Hrolf the Ironhowl`，全池唯一。⚠️ **`or` 的解法三层权威全没有 ⇒「每张各自随机」是我们挑的** |
| **`[Faith Icon]` 前缀** | `EffectText.RePaid` 的 `curW` 那一支**直接认 `faith icon`** | `[` `]` 在 `ParseSegment` 开头就被剥掉 ⇒ 只写 `faith` 会整条失配、**付费不记上** |
| **句首图标字形** | `EffectText.StripLeadingIcons` | `⚡ Rally: …` / `💀 Backlash: …` 会挡住 `^([a-z]+)\s*:` 那条触发前缀匹配 ⇒ 整句被 give 系 handler 从中间截走。只剥 `OtherSymbol` 与代理对（**不剥 `+` / `-`**） |
| **`deal N damage` 没写目标时的尾巴** | `EffectText.TryDeal` **先 `SplitAndTail` 再匹配** | `ReDeal` 锚了 `$` ⇒ `deal 5 damage and gain 1 Armour` 原来整句失配 |
| 🔴 **单位 ≠ 部队** | `EffectResolver.AddSide(troopOnly: spec.Kind == "troop")` —— **判据只此一处** | 规则书 `:70-75`：**单位含督军、部队不含**。解析层原来把 `for each friendly unit` 归成 `troop` ⇒ **14 张卡静默少算一个督军**。⚠️ `CreatePool.KindWords` 那张表是**卡池筛选**用的，**别改它** |
