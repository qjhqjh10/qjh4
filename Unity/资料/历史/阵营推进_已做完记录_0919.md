# 阵营推进 · 已做完记录（2026-09-19 从 `资料/阵营推进_清单与交接.md` 挪出）

> 按 `CLAUDE.md` 铁律 6：交接正文只留 ① 现状 ② 待办 ③ 坑 ④ 出处。
> 这里放的是**挪出来的已完成叙述 / 旧快照 / 被推翻的结论原文**（保留更正痕迹，便于复查），
> **不是待办**。要看某条的结论与落点，回 `资料/阵营推进_清单与交接.md`（正文里留了压缩版与指针）。
> 同目录的姊妹档：`规则引擎_逐轮记录_0911-0913.md` · `阵营推进_A4收尾批1批2.md` · `规则引擎_第七节_已做完记录.md`。

---

## 〇、旧快照：自检数字的前几版

> 这些数字**全是当时的口径**，现在一律以 `阵营推进_清单与交接.md` §一那张表为准。

- 2026-09-18 那版：`RuleEngineTest` **2855/2855**（更早于 2026-09-16）→ **2860/2860**。
  ⚠️ 两次更正的教训：0918 补 `TakeFromGraveyard` bug 时加的 5 条断言**没回填**这一格。
- 2026-09-19 早那版：`RuleEngineTest` **2937/2937** · `BattleScene` **570/0** · `DeckScene` 61/61 · `CardBaseDemo` 22/0
  （跑完「`ChoosePanel` 三条自造标题删除 + `Attack → Hit` 时长」之后）。
- 数字跳变的来历：`AnimFXCheck` 23 → **32**（新增第 ⑤ 段盯着 `ScaleByTarget.ChangeShapeAngle` 的锥角公式）；
  `BattleScene` 564 → 567（2026-09-18）→ 570（2026-09-19）。
- 更早还有 `825/825` · `837/837` · `1203/1203` 三个版本 —— 那是同一个坑（每一轮都在加断言，每一版都过期；
  当年 `规则引擎_进度与交接.md` 开头指着本节说「最新值见这里」，两个会话都被那条指针坑过）。

---

## 一、§一 自检行里挪出的三段叙述（保留原文）

### 1. 敌方手牌（2026-09-17 新增那一件）

> 🆕 **2026-09-17 新增：敌方手牌**（原版规格正本 = `资料/敌方手牌_原版规格.md`）—— 之前**这一整件都没有**，
> 所以 `VarsGlobal.timeToDrawEnemyCard` 一直无处可落。现在：敌方手牌区（`HandLayout.ConfigureForEnemy()`，
> 8 字段 + 3 曲线整套原版值）· **显示卡背**（`CardFeel.DealInQuad`）· **我方 0.3 s / 敌方 0.15 s 分开**
> （`CardFeel.DealSeconds(bool)`）。⚠️ **卡中心行的 y 是推导的**（原版那两个节点是纯 `Transform`、
> 祖先 canvas 的 `localScale` 序列化成 0 ⇒ 绝对像素读不出来）——链条写在 `HandLayout.EnemyBaselineY` 上。

### 2. 加敌方手牌时踩到的那个形状

> 🔴 **加这一件时踩到一个形状**：`BattleScene` 里有 **4 处 `Object.FindObjectOfType<HandLayout>()`** ——
> 场景里一变成两份 `HandLayout`，那个 API 返回哪一份就是**任意的**，**六条我方手牌断言一起变红**
> （马脚是「间距 1.45 实测 **0.600**」= 敌方的值）。已收敛成 `BattleScene.PlayerHand()` 一处。
> **教训：有引用就用引用，别按类型/按名字找对象**（同形状：`QuestIconPos` 的 `transform.Find`）。

### 3. `CardBaseDemo` 补断言那一件（三条真的 + 两条教训）

> 🔴 **2026-09-17 更正**：这一格原来写「`CardBaseDemo` **全过**」—— 它当时**一个 `Check(` 都没有**
> （`grep -c` = 0），全文只有 `Debug.Log` 诊断行 + 截图、**没有通过/失败汇总** ⇒ **「全过」从来没有依据**，
> 它是**截图生成器**、不是自检。**当天补上了真断言**（`Check` + 末尾 `=== 合计：N 通过 / M 失败 ===` +
> 批处理退出码，现在 **22/0**）。
>
> 🔴 **补断言当场抓到三条真的**（都长期挂着、没人管，因为**没有判定就不会红**）：
> 　① **9 格在 1080p / 2K / 4:3 下是重叠的**（空档 −0.45 / −0.45 / −0.32 世界单位）。
> 　根因 = `BoardLayout.placedScale` 的**默认值写成 0.876**，而同文件上一行的注释明写「0.876 → 卡宽 1.271 世界」
> 　—— 2.0927 × 0.876 = **1.833**，**注释和值互相打架**。正确值是 **0.607**。
> 　⚠️ **产品侧（Battle 场景）一直是对的**（`BattleScene` 显式赋了正确值，自检「场卡宽 = 原版的 137.2 px」一直绿）；
> 　**中招的是「取默认值」那条路** = `CardBaseDemo` 自建的板 ⇒ 它的场卡**大了 44%**，而它正是「四档分辨率」的版面验收图。
> 　已把判据收成**一处**（`BoardLayout.DefaultPlacedScale` / `OriginalCardWidthPx` / `OriginalSlotPitchPx`，
> 　`BattleScene` 改为引用它）。
> 　①′ 🔴 **同一个形状还有第二处**（2026-09-17 顺手查到）：`HandLayout.cardScale` 的默认值 **1.054** 是拿**旧的**
> 　`CardView.Width = 1.45` 算的死值（`1.528 ÷ 1.45`），卡宽早改成 2.0927 之后它就成了死值 ⇒ 正确值 **0.730**。
> 　同样**产品侧没事**（`BattleScene` 显式赋 `HandScale`，自检「手牌卡宽 = 165.0 px」一直绿），
> 　同样**只有取默认值的那条路中招**。已收成 `HandLayout.DefaultCardScale` 一处。
> 　　⚠️ **两条合起来的教训**：**「产品绿」不等于「那个默认值是对的」** —— 只要有一处显式赋值，错的默认值就能一直躺着不响；
> 　　**而取默认值的那些入口（演示/自检/新场景）会静悄悄画错**。以后加这类「原版尺寸」字段，**默认值必须由常量算出来**，别再手写数字。
> 　② **悬停 / 落位两条诊断一直 ❌**（`2026-09-17` 用改动前的代码复跑，输出逐字相同）—— 根因是**测试的探针点取错**：
> 　点的是卡的**中心**，而手牌是重叠扇形、层序是「**左边压上面**」（`HandLayout.Refresh` 的 `z = i * zOrderStep`，
> 　**原版就是左卡 z 更小**）⇒ 引擎判第 5 张**是对的**，错的是「点中心 = 点到这张」那个假设。
> 　改成点**右 1/4 处**后两条都转 ✅（落位距离 2.287 → **0.000**）。
> 　③ 「特效钩子被调用 0 次 ⚠️ 没触发」**是误报** —— `CardInteraction` **不发特效事件**
> 　（那一路是 `BattleDriver` → `CardEffects.FireEvent`），接线本来就在 `BattleScene.Run` 里验。
> 　已改成陈述事实、不当失败。

---

## 二、§一之三 里挪出的两句

- `> **新会话从这里开始。第一步永远是先跑一次自检**（§五）——它会把本文档里所有数重算一遍。`
  （与本节开头的「第一步永远是先跑一次自检」重复，已删。）
- 「本节不再维护」那条原来还带着「它原来写……留着只会误导」，已压成一句。

---

## 三、§一之四「四笔账」里的三笔（附原文）

### 第 1 笔 · `Infinite Biomorphologies` 的「给手牌」作用域

> 1. ✅ **`Infinite Biomorphologies` 的「给手牌」作用域 —— 2026-09-16 做完了。**
>    🔴 **原来这条的理由（「要真做得先做卡实例身份」）记重了**：这张卡给的是「手牌里**所有**部队」、
>    **每一份都要给** ⇒ 按「**卡 + 份数**」记账与「实例身份」**语义等价**（`BattleContext.HandBuff` +
>    `EffectResolver.GrantHandBuff` + `RuleCore.ApplyHandBuffs`）。**卡实例身份那一层** ——
>    ✅ **2026-09-18 也做完了**（见候选 0）。

### 第 3 笔 · `Armorium Cherub` 的整段载荷缺失（**这条是过期记录**）

> 3. ✅ **`Armorium Cherub` 的整段载荷缺失 —— 2026-09-18 复核：早就修好了，这条是过期记录。**
>    **更正**：原文写「没修 … `ReAtTurn` 是 `^…$` **锚在整条 desc 上** ⇒ 要修得先动解析层」——
>    **解析层 2026-09-14（`60fee33`）就换成逐分句了、数据 2026-09-15（`d8d7def`）也补齐了**。
>    **实据**（探针 `_tmp_view/probe_out.txt:3891-3904`）：这张卡现在是
>    **`op 2 条 · 不认识 0 · 半懂 0`**，`atturn` 的载荷完整解析成
>    `gainfaith n=1` ＋ `return 去处=hand`（含「未写主语⇒有施放者就是施放者自己」那条兜底）。
>    引擎侧卡表 `RuleEngine/Resources/cards_engine.json` 里 `desc` 也是完整那条。
>    ⚠️ **顺带核出一处数据源差异（已确认不影响管线）**：`数据/游戏数据/card_stats.json` 把这张卡记成
>    `rarity=legendary`，而**引擎侧是 `rare`**、第三方证据（`bundle_cosmeticsso_assets_all/MonoBehaviour/`
>    的头像资产里 `"cardRarity": 1`、`"cardArmy": 80`）也支持 `rare`
>    ⇒ **`card_stats.json` 那格是错的，但 `gen_cards_engine.py` 的输出已经是对的** ⇒ 无需动数据。
>    （落地复盘另见 `资料/普查产出_0918/孤儿待办_五条查证.md`。）

### 第 4 笔 · `Righteous Repugnance`（**这条早就过期了**）

> 4. ✅ **`Righteous Repugnance` —— 2026-09-15 修好了**（**这条早就过期了**）：载荷里多出来的 ` instead`
>    由 `EffectText` 的 `TryGive` **外面加一层**摘掉（记 `op.Instead`），整句现在解析得下、卡打得出去。
>    ⚠️ 「`Heal <代词> N` 这个语序全池只 1 处、不为它硬塞正则」那条结论**仍然成立**（说的是别的卡）。
>    逐条判据见 `资料/战术卡剩余7条_语义查证.md` §十。

---

## 四、§一之四「这两条 2026-09-16 都处置了」原文

> ### ✅ 这两条 2026-09-16 都处置了 —— 🔴 **其中一条原来判错了**（保留更正痕迹）
>
> - ✅ **`Cadian Honour` 那条属实、已修**：尾句 `and reload their Duty abilities` 被吞进目标短语
>   ⇒ `reload` 从来没发生过、而且**不报错**。**根因**：`SplitAndTail` 的动词白名单
>   （`EffectText.IsVerbWord`）里**没有 `reload`** ⇒ 不在 ` and ` 处切尾句。
>   **同族还有一张 `Lead by Example`（`… and reload its Duty ability.`）—— 原记录漏了它**。
>   全池含 `reload` 的卡**只有 3 张**（第 3 张 `Press the Attack` 是句首写法，不受影响）。
>   修法 = 白名单加 `reload`/`reloads`（`EffectText.cs`，附实测清单）；端到端断言见
>   `RuleEngineTest.TestA4PaidPrefixAndAtoms` ⑦（打出后 `DutyUsed` 真的复位，不只钉解析）。
>
> - 🔴 **`Thunderwolf Cavalry Pack Leader` 那条是判错了 —— 它本来就没坏**（2026-09-16 复核，
>   **不是回归、也不是修好了，是当初就没坏**）。用新写的 `CardProbe`（逐卡解剖）问**卡对象自己**：
>   · ① 事件层收对了：事件 `敌方 gethuntmark` → 正文 `give +1 to your units this turn`
>     → `give +1 目标[own/unit 「your units」]`；
>   · ② 触发层 `rally: Give Hunt Mark to an enemy troop` 也收对了。
>   **错因在「尺子」**：`EffectParseProbe` 量的是**裸解析器对整条 `desc` 的意见**，而 `CardDef`
>   会把 `When …` 开头的段**路由到事件层**（`CardDef.cs:1036` / `AddWhenTrigger`）⇒
>   探针那句 `gain 载荷「a hunt mark, give +1 to your units」 目标[enemy/any 「when an enemy」]`
>   **只是没人执行的残渣**。
>   🔴 **教训（跨轮有效）**：判「某句是不是被吞」**必须先问「哪一层会执行」** ——
>   `When …` / `Rally:` / `Slay:` / 光环 / 灵魂石 / 誓约各走各的层，
>   主解析器（④）对 `unit`/`hero` 卡的意见**基本不算数**（`tactic` 才走它）。
>   ⚠️ 同一族的另外 11 张（`Child of Torment` / `Khorne Berzerker` / `Noise Marine` / `Plague Marine` /
>   `Accursed Helbrute` / `Vengeful Brethren Bladeguard·Hellblaster·Intercessor` / `Skorpekh Lord` /
>   `Termagant Brood` / `Venerable Dreadnought`）**同样都是假警报**，别再当 bug 修。

---

## 五、逐条挪出的单行

- A6 族的收尾行（与 §一之四「数据侧的两条约定」重复，内容相同）：
  `- ✅ 顺带销账：**9 张督军 `cost` 逐张开 PnP 卡图核完**（全 0、数据本来就对），断言 `RuleEngineTest.TestBatch0916` ④。`
- 中文对账脚本那一段的**真因与计数**（正本已另有文件，正文改成指针）：
  `真因：内层 op 是 **10 空格**而旧脚本按 `^ {8}\S` 认行（**31 条内层 op 全丢**）、尾句是 **8 空格**而旧正则写死 16（**87 个尾句**永不匹配）。修完跑全池 **A 0 · B 5 · C 1090 · ? 0**；原来报的 15 条 A **全是脚本自己没读对**。`
  → 正本 = `资料/普查产出_0918/11③_中文对账脚本_修好.md`。
- 候选 F 开头那句与下面表格重复的汇总：`**做了什么**：卡面写 `adjacent` 的 **36 张** = **26 张「效果目标形」**（锚点在解析期定、结算期消费 —— **25 张已接**、1 张认不出）+ **10 张「光环型」**（`Adjacent units have Y`，走常驻层）。`（表里逐行列着同样的事。）
- 「本节踩过的坑」里那三个自检数 `825/825 · 837/837 · 1203/1203`（教训保留在正文，数字不再抄）。
