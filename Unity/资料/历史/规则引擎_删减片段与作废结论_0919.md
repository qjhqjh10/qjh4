# 规则引擎 · 从交接正文里挪出来的旧叙述与作废结论（2026-09-19）

> **2026-09-19 按 `CLAUDE.md` 铁律 6 从 `资料/规则引擎_进度与交接.md` 搬出来的。**
> 交接文档只留「当前状态 / 待办 / 坑 / 出处」，**过程叙述、已有更好出处的重复清单、被推翻的旧结论**要挪走。
> 这里存的是**被删掉的逐字原文**，用途只有一个：追溯「当时怎么写、为什么改」。
> ⚠️ **这里的一切都可能过期，别当正本用** —— 仍然成立的部分已经精简留在正文里（带指针）。
> 同族存档：`规则引擎_逐轮记录_0911-0913.md` · `规则引擎_第七节_已做完记录.md`。

---

## 一、⑥ 音量三滑块「仍未做」那段（原文）—— **2026-09-19 已落地**

> 音量滑块**仍未做**（原版三根 = music / soundFX / voiceOver；API = `AudioMixer.SetFloat("Volume"+枚举名, Log10(v)*20 或 −80)`，规格见同文件 §②）—— ⚠️ 2026-09-17 起**工程里有音频了**（`Battle/UnitChatPanel.cs`），要接随时能接。

**后来**：`Battle/WfSlider.cs`（控件）+ `Core/WarpforgeAudio.cs`（运行时按 `Volume+组名` 写 AudioMixer，dB 公式与「只存音乐」照原版）+
`Resources/Audio/Main Mixer.mixer`（工程自建，原版那份是 native 资产导不进来）。现状与两处如实标注见
`资料/战斗UI_原版对账表.md` §「2026-09-19 新增两件」；规格见 `资料/普查产出_0918/第18行_UI三小条_规格.md` §②。

---

## 二、⑤ 任务点 HUD 数字 / `Energy Accumulation` 的两条旧结论（原文）

> - 🔴 **任务点 HUD 数字**：~~原来写「`QPText` 数字**恒为 0/3**、引擎没有任务点机制」「HUD 标签写死 `0/3` 没接字段」~~ ——
>   **已作废**（2026-09-19 更正：引擎有 `PlayerState.QuestPoints`，HUD 也早就接了实时值 `SetText($"{me.QuestPoints}/3")`，
>   见 `BattleDriver.cs` `UpdateHud`）⇒ **位置与对账只看 `资料/战斗UI_原版对账表.md` §三点五**，别在本文件抄第二份。
> - ✅ **2026-09-18 更正：`Energy Accumulation` 的 ON 判据已经查到了** ——
>   判据 = **`0 < manaAccumulation`**（反编译出处 `BattleManager__SetupBoardPhase.c:181/196` + `GameplayVariablesData`），
>   见 `资料/战斗UI_原版对账表.md:156`。
>   ~~`Energy Accumulation` 固定 **OFF**（ON 的切换条件查不到）。~~ ← 这是 09-16 前的结论，**已推翻**。

**为什么现在只留一句**：两条的「现在的真值 + 出处」正文里都留了；这两段只是**当时的自我更正过程**。
（`QPText` 那条更早还翻过两次案，全过程见 `资料/战斗UI_原版对账表.md` §三点五 ③ 那一格自己记得很清楚。）

---

## 三、⑤ 位置核对的「核对方式」与「本来就对」清单（原文）

> - 核对方式：**所有可见元素按 1920×1080 绝对矩形重对一遍**（原版值取 `子代理读报_*_0827.md` 的 chain_rect；
>   **权威表有已知错行、0912 dump 是运行时被改写过的帧**）。全表见 `资料/战斗UI_原版对账表.md` **§三点五**。
> - 核对出**本来就对**的：牌堆 230² 逐像素相等、能量区四块、任务点两个 + 接片、END TURN、设置钮、已出牌数三枚、技能面板、卡牌放大窗。

**为什么删**：这两项的**正本在 `资料/战斗UI_原版对账表.md` §三点五**（同一份清单在那边分成「② 本来就对」与「④ 我们自加」两张表，
并带对照图 `资料/留档_排查证据/战斗UI位置核对_0913/`）—— 抄第二份迟早不一致。正文只留「原版值取 chain_rect + 权威表有错行」这条判据。

---

## 四、⑪ 卡牌 ID 的早期做法（原文）

> - `card_stats.json` 里 `id` 字段**1212 张全是 null**，所以早期全程**拿卡名当 id**，代价是**同名不同卡**会串（已知 5 处：Aggressor /
>   Terminator / Terminator Champion / Maulerfiend / Bladeguard Veteran；**成串的实例与修法另见 `资料/PnP卡图_逐张对账_0915.md` §四·B**）。
>   ✅ **2026-09-13 第三十三轮已治本**：卡表 `version 6`，
>   每张卡带**稳定 id**（详见 `资料/阵营推进_清单与交接.md` 候选 0）。

**为什么删**：现在的做法（稳定 id）一句话就能说完，剩下的「早期怎么错的」是过程；实例仍在 `PnP卡图_逐张对账_0915.md` §四·B。

---

## 五、出牌 / 攻击**动画时序**的三条作废结论（原文）

> 🔴 **2026-09-19 更正：本节原来那三条全部作废** ——
> ~~「近战出手 → 命中 = 0（原版近战没有飞行段）」~~ → 实为 **0.10**；
> ~~「远程出手 → 命中 = 0.75s（取 `card_anim_map` 的 `Atk_BulletImpact_Eldar_Deathspinner` 的 `timeAtStartPos`）」~~ → 实为 **0.20**
> （那个 `timeAtStartPos` 是 `GetAnimDuration()` 的**一个分量**，属于「**命中→扣血**」那一段，**位置放错了**）；
> ~~「`AttackMeleeAnim` / `ResolveAttackRangedAnim` 的两个 `MoveNext` 没被反编译 ⇒ 秒数查不到」~~ → **两份都在** `d:/2/tools/decomp_full/`
> （`_AttackMeleeAnim_d__357__MoveNext.c` / `_ResolveAttackRangedAnim_d__360__MoveNext.c`，本节全部真值就是从它俩读出来的）。
> 出处：`资料/普查产出_0918/第18行_UI三小条_规格.md` §③（含按 VA 直读 PE 的常量复核）。

**为什么删**：真值是正文那张时序表里的两格（近战 0.10 / 远程 0.20，`EventTiming.AttackStepMelee/Ranged`），出处也一样；
这段只是三条旧结论的逐条撤销记录。⚠️ 但**教训要留**：`timeAtStartPos` 那种「看着像飞行段、其实是另一段的**分量**」最容易放错位置。

---

## 六、卡框分档的两次改口（原文）

> 🔴 **2026-09-18 更正：原来这里写「卡框按稀有度分四档（原版就是）」「稀有度→tier 的映射是对照原版卡面实测的」—— 两句都已被推翻**：
> `CardTier`（4 档·**卡牌升级档**）与 `CardRarity`（5 档·含 `Special=5`）是**两个独立枚举**，而
> **PnP 卡图上五个稀有度的框逐像素相同**（`mean|d| = 0.0`）⇒ **卡图上根本没有「帧档」这个信息**，判据只能是枚举 +
> `CardFramesSO.GetClanFrame(army, cardTier, …)`（出处 `资料/卡表核对_卡图提取/_裁定_special卡框.md` §一·1.1 / §四·1）。
> ✅ **`special` 2026-09-18 已落地**：它**不是「稀有度→档位」的函数**，要**按阵营**取
> （实测 39 张 = tier1 24 / tier2 15，每阵营三张完全一致 = 印刷批次边界）——
> 判 tier2 的 5 阵营 = `AstraMilitarum`·`DarkAngels`·`Genestealers`·`Goff`·`Sororitas`（出处同文件 §三）。
> ⚠️ 原来这里还写「`special`（39 张）没实测，先按 tier4」—— **已作废**（39 张里一张 t4 都没有）。
> ⚠️ **2026-09-19 更正：本节原来分两段、自相矛盾**（前一段写 `CardArt.Frame(阵营, 稀有度, 是否战术卡)` 四档映射，后一段写「那两句已被推翻」）
> —— **现已合并成上面这一节，以「`CardArt.TierOf(rarity, faction)` + `special` 按阵营」为准。**

**为什么删**：正文已合并成一段（结论 + 出处都在），这里留的是「改了几次口」的记录。

---

## 七、头部关于 §四 的说明（原文）

> ⚠️ **§四 原来是「表现层这一轮做了什么」（已完成叙述），2026-09-16 瘦身时整段删掉** ——
> 后面的**节号保持不变**（§五 坑表 / §七 规格 / §八 出处 都还按这个号引用）。

**为什么删**：正文改成一句「§四 是空的，节号不变」就够，不必记是哪一天删的。
