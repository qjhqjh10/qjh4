# 阵营推进：清单与交接

> 2026-09-12 第十六轮结束时建立。**面向下一个会话**。
> 总入口是 `项目任务.md`；工作准则见根目录 `CLAUDE.md`（每次会话自动加载）；
> 卡牌这条线的技术细节在 `资料/卡牌效果管线_计划与交接.md`。
>
> 用户 2026-09-12 定的方针：**两个两个阵营逐步完成，不追求一次性大规模实现。**

---

## 一、上一件：**`Ephemeral`（临时）+「移出游戏」区域**（2026-09-13 第三十二轮）

规则书 `:183`（回合结束若在手牌则移除）+ `:229`（**从游戏中移除（非弃置）**）。
**卡池里出现最多的关键词（98 张）**，也是规则书审计第②条。

| 件 | 落点 |
|---|---|
| 关键词认得出 + 已实现 | `CardDef.Prefixes` + `KeywordTable.Implemented` —— 未实现清单 **26 → 25** |
| **「移出游戏」区域**（第五个区域，与弃牌堆分开） | `BattleContext.Removed` + `RemovedCard` |
| **临时身份的判据** | `BattleContext.IsEphemeral` / `MarkEphemeral` / **`TryTakeOneEphemeral`** |
| 回合结束清扫 | `RuleCore.EndTurn` → `SweepEphemeral` |
| **卡面角标** | `CardView.ShowEphemeral` + `CardArt.Trait` + **78 张原版关键词图标**已导入 |
| 断言 | `RuleEngineTest.TestEphemeral`（4 层）+ `BattleScene` 第 1e 节（含两张对照截图 `18a/18b`） |

🔑 **两个坑值得记住**：

1. 🔴 **「标记临时」不能按卡模板判** —— `CardDef` 是**共享不可变**的，手里两张同名卡
   是**同一个对象**。第一版清扫写 `foreach (手牌) if (IsEphemeral(c)) 移除`，
   结果**只标了一张却把两张都移走**（原件被当成复制一起消失，静默）。
   修法：**`TryTakeOneEphemeral`** —— 卡自己带关键词的每份都走；否则**吃掉一份标记**、其余不受影响。
   ⚠️ **这是「我们没有卡实例身份」第二次咬人**（第一次是费用修正按卡名，规则书审计第③条）。
2. **卡面标记用原版真有的关键词图标，不是 glitch** —— 原版走
   `BattleCardUI.ShowEphemeral()` + `Card2DController.ToggleGlitch()`（换 glitch 材质），
   但 **glitch 素材本地没有**（`d:/2` 全盘零命中）⇒ 改用原版图集里的
   `Atlas_trait_icon_ephemeral.png`。⚠️ **位置/大小是我们挑的**（那个 prefab 不在手上）。

**还没做的**：`Talent`（91 张，生成临时战术）/ `Companion`（9 张）/ `Tide X`（24 张）
—— 它们是临时卡的**来源**，接口（`MarkEphemeral`）已经留好；
**glitch shader**（自建，给「卡面材质抖动」的观感）。

**细节、出处、取舍全在 `资料/临时卡Ephemeral_设计与实现计划.md`。数字只写在本文档 §五。**

---

---

## 一之二、更早做过的事（只留仍然成立的结论）

- **卡表逐张核对**（第二十九轮）：1118 张卡图独立重抄一遍，修掉 **`subtype` 串列 40 处** +
  **关键词数值被吃掉 72 处**。卡表 `version 5`。修法走
  `工具/gen_cardface_fixes.py` → `数据/游戏数据/cardface_fixes.json` → `gen_cards_engine.py`（**不手改产物**）。
  结论见 `资料/卡表逐张核对_与对账.md`；自检断言 `RuleEngineTest.TestCardFaceFixes()`。
- **关键词 ↔ 图标对照表**：原版图集每个关键词一个 sprite、**文件名就是英文关键词**（78 个），
  和规则书 61 个对上 67 个。→ `资料/关键词图标/关键词与图标_对照表.md`。
  ⚠️ 里面**明确分了两节**：有证据的对照 vs **我的推测**（混沌印记那 5 条是推测，别当结论）；
  另有硬证据说明 `markOfChaos`/`markOfKhorne`/`markOfNurgle` **md5 完全相同**，光看图分不出。
- 更早三轮的机制设计文档：`资料/选牌Choose_数据与设计.md` · `资料/回手与指代_数据与设计.md` ·
  `资料/常驻效果_数据与设计.md`。

<!--
铁律 6：旧的进度叙述（「这一轮做了什么」的流水账）不留在交接正文里。
第二十六～二十九轮各做了什么见 git log（`045ac17` 那个提交）与 `资料/卡牌效果管线_计划与交接.md` §一 的表。
-->

---

## 一之三、🔜 **新会话从这开始**

**第一步永远是先跑一次自检**（命令在本文档 §五）。⚠️ 2026-09-13 第三十五轮收工时
`_tmp_view/` 已被清空（那是 gitignore 的临时目录，**跑一次自检就全重生**，别以为丢了）。
自检会重写这几份 —— **它们才是当前的工作清单**，本节写的数字都会过期：

| 产物 | 是什么 |
|---|---|
| `_tmp_view/tactic_unparsed.txt` | 战术卡的缺口，按频次排（四栏：不认识的句子 / 半懂 / 缺机制 / 打得比卡面宽） |
| `_tmp_view/tactic_by_faction.md` | 逐阵营的覆盖率（候选 D 排批次靠它） |
| `_tmp_view/when_unparsed.md` | `When <事件>` 认不出的短语 |
| `_tmp_view/adjacent_unimplemented.txt` | 🆕 卡面写了 `adjacent`、引擎不认的卡 |
| `_tmp_view/unit_desc_unparsed.txt` | 单位卡 `desc` 的缺口 |

### 🔴 **用户 2026-09-13 定的顺序：先做 A 组**（下面按此排）

| # | 做什么 | 规模 | 入口 |
|---|---|---|---|
| **A1** | 🔴 **「相邻」** | **36 张** | **下文·候选 F**（实施计划已写全：四步 + 范围切分 + 验收） |
| **A2** | **关键词补全**（16 个） | 每个 7–36 张 | **下文·候选 E** |
| **A3** | `When` 短语（11 种） | 7 种在等 A2 | `_tmp_view/when_unparsed.md` |
| **A4** | 战术卡不认识的句子（41 种） | 长尾 | `_tmp_view/tactic_unparsed.txt` ① 栏 |
| **A5** | 单位卡 `desc`（351/586） | 还差 235 张 | `_tmp_view/unit_desc_unparsed.txt` |
| **A6** | 按阵营铺 | 46 张 | 下文·候选 D |

> ⚠️ **A1 动手前必须先做一件事**：把 `RuleCore` 里 **Blast / Stomp 那两份内联的格位判据抽成一份**
> 再往上加 —— 否则就是抄第三份（铁律：两处写同一条规则 = 迟早不一致）。

### ✅ 候选 0（**第三十三轮已做完**）：**给卡发稳定 id** —— 规则书审计第③条

**结果**：卡表 `version 6`，每张卡都有唯一 id（原版优先、对不上的自造）。
落点：`RuleCore.CostOf` 的 `CostMod.Key` 从卡名改成 id ⇒ **同名卡不再一起降价**；
卡组引用解析收成一处 `CardDatabase.DeckLookup`（**先 id，找不到再退回「卡名 + 阵营」**，
第二条路留给旧存档 / 旧分享串，**别删**）。出处：`资料/规则书_实现指南对账.md` §一·③。

**两条查实的事实**（别再重新挖）：
- **id 前缀 ↔ 阵营是原版数据**：`数据/游戏数据/decklists.json`（236 副原版预组牌）真带
  `heroId:"AM3"` + `cardIds`，**13 个前缀与 13 个阵营 1:1**。
- **`card_ids.json` 的 id↔卡名是推出来的**（它自己的 note 写着「PnP 卡表编号 = 游戏卡ID 偏移 0」）⇒
  拿 PnP 卡图核过：**818 处逐字一致 · 19 处写法差异 · 159 处编号对不上**，那 19 处绝大多数是
  **同一张卡的不同写法**（`Sylar Hexcorn`↔`Sylar-Hexscorn` · `Rusted Vents`↔`Rusted-Vent`）。
  ⇒ **可用，但不是逐条验过。**

**⚠️ 还没做的（审计③的另一半）**：**卡实例身份** —— 手里两张同名卡仍然是**同一个 `CardDef` 对象**，
所以「给**这一张**降费」会落到该 id 的**所有副本**上（比按卡名好，但仍不等于原版的复制品语义）。
规则书附录那批「复制品 -1/-2」要精确，得让手牌存**实例**。**这是独立的一轮。**

### 候选 A：**事件层** —— ✅ **2026-09-13 第三十四轮全做完**

设计、判据、逐条落点见 **`资料/事件层_数据与设计.md`**（那里是正文）。**这里只留坑**：

🔴 **`reanimated` 原来是个真 bug（已修）**：只设了 `Kind`、**没设自指标记** ⇒ 按「**任何单位**被再造」收，
任意一张 SAU 在场时**别人（含敌方）被再造**都会响，`SAU72 Immortals Phalanx` 的
`deploy a copy of this troop` 会去复制**别人**。原有用例只量了「自己翻自己」，所以一直没露头。
⇒ 才有了 `WhenEvent.SelfOnly`。

🔴 **两处「已经点亮了、却一次都不响」的静默失效**（派子代理逐条核 `When` 时翻出来的）：
① 判据只收 `CardDef` —— 猎杀标记是效果在**运行时** `AddKeyword` 加的、**卡面没印**
   ⇒ `When an enemy with Hunt Mark dies` 永远 false。新增 `CardCriteria.Matches(UnitState)`；
② 关键词**没归一化** —— 判据那侧存卡面原话 `"hunt mark"`，卡表那侧存规范键 `"huntmark"`
   ⇒ 换成 `UnitState` 也还是永不相等。加 `CardCriteria.NormKeyword()` 一处归一化。

⚠️ **事件触发式降费那一族必须排在「场上没人听就 return」之前** ——
它挂在**玩家身上**、和自家场上有无单位无关，放后面会被那条早退**静默跳过**。

**原版依据（照它设计，别凭直觉）**：**牌在手上就要监听**。原版广播器**四支**
（自己 → 场上每张牌 → 当前回合方手牌 → 另一方手牌，`BattleManagerSupport__BroadcastUnitSummoned.c:23,41,53,65`）；
牌入手牌那一刻就挂（`PlayerHand__SetupCardInHand.c:60-73`）。
自指在原版靠**枚举值分「本卡 / 他卡」**（`ThisUnitSummoned=195` vs `OtherUnitSummoned=190`），
**不是**「牌自己监听自己」。

⏭️ **`When` 这一侧还剩什么**：**别抄这里的数** —— 权威是 `_tmp_view/when_unparsed.md`（自检每次重写）。
⚠️ `Ravenwing Champion`（`When you create a Secret`）实测是**「点亮但打不出来」**：
卡池里**没有任何卡会造隐秘**（18 张提到 secret/sabotage 的卡，造的全是 sabotage）—— **别把它算进「已铺完」**。

### ~~候选 B~~：**单位卡 `desc` 的效果文字** —— ✅ **2026-09-13 第三十一轮做掉了大头**

**已做**：`Rally / Strike / Slay / Backlash / Penitence` 这 **5 个触发点**的正文，**123 张卡**（见 §一）。
**还剩两族**，都归到候选 A：
- **`When <事件>, …`** —— **78 条 / 78 张**（要事件层）
- **`Talent / Pray / Duty / Agenda / Mob / Regiment / Artifice / Codex`** —— **212 条**（要新的触发时机）
  ⚠️ 其中 **`Codex` 18 条解析器早就会**（`Dispatch` 里就有 `codex:` 那条路），缺的**只是触发点**
  （「你的能量为 0 时」）。

### ~~候选 C~~：**卡面图标对真关键词** —— ✅ **2026-09-13 第三十一轮做完**

**1968 处占位符还原 1965（99.85%）**。工具 `Unity/工具/restore_card_icons.py`，
产物 `资料/卡表核对_卡图提取/_还原效果文字.md`。

⚠️ **原计划（模板匹配）被实测否掉了，别照那条做** —— 两条硬证据：
① **卡面上关键词图标旁边就印着英文词**（真卡确认），答案本来就在文本里，不用认图；
② **形状名不能当键**：78 个图标被 29 个子代理起了 **180 个不同形状名**，
`【图标:骷髅】` 130 次横跨 `backlash`/`remnant`/`strike`/`synapse`/`regiment` **五张不同的图**。
详细与「同名不同图」（`齿轮` = `artifice.png` **和** `questPointsN.png` 两张）见
`资料/关键词图标/关键词与图标_对照表.md` §零。

### 🆕 候选 E（**第三十三轮铺宽时暴露出来的，建议排在 D 前面**）：**关键词触发族**

**怎么来的**：第三十三轮把 `When <事件>` 从点亮 36 张推到 **52 张**之后，
**剩下认不出的 25 种事件短语几乎全是同一族** —— 都是「**某个关键词被触发**」：

```
a friendly unit triggers swarm ×2      this unit triggers synapse
you trigger ferocity       a friendly unit uses ferocity
a friendly unit triggers mob           an enemy gets (a) hunt mark ×2
this unit attacks an enemy with hunt mark      a friendly unit loses stealth
```

⇒ 这些事件**发不出来是因为对应的关键词还没实现**（剩下的是 `swarm` / `synapse` / `ferocity` /
`duty` / `hunt mark`……）。
**先把关键词实现掉，这些 `When` 会自动跟着亮** —— 所以它其实是「关键词补全」那件事的一部分，
只是**从 `When` 这一侧看过去才看出它的性价比**（一次实现点亮好几张卡）。

> ⚠️ **2026-09-13 更正（候选 E 做掉一批之后）**：这节原来写「全在『未实现关键词 **24** 个』那张名单里」，
> 且把 `mob` / 失去 `stealth` 也列在等它的那几种里 —— **都不成立了**：
> `mob` / `unstable` / `cruelty` / `regiment` / `talent` 第三十四轮已做，`codex`/`oath`/`stun` 是名单误报；
> 未实现关键词现在是 **16 个**。**认不出的短语也已从 14 种降到 11 种**（`played` 是误报被摘掉、
> `a friendly unit triggers mob` 与 `you gain [honour]` 已点亮）。
> **权威清单永远是 `_tmp_view/when_unparsed.md`**（每次自检重写），别抄这里的数。

⚠️ **顺序建议**：先按**频次**挑（`资料/关键词频次表.md`），
而且**优先挑那些同时能点亮 `When` 的**（即上面那几种）—— 一份工作两处收益。

**清单每次自检重写**：`_tmp_view/when_unparsed.md` · 关键词现状以 `RuleCore.UnimplementedKeywords()` 为准。

#### 📚 已查证的语义（2026-09-13 派子代理查的，**别再重查** —— 但权威仍以卡面/规则书为准）

**`ferocity`（狂暴）**
- 规则书 `:98`「部署当回合不能行动（**除非注明，如迅捷/侧翼/狂暴**）」⇒ **豁免部署当回合限制**，
  但**吃「每回合 1 次行动」的额度**；`:150` 属**替代行动**族；`:186` 表。
  英文原版 `:327` = `Fast Non-Combat Action. Triggers an effect when performed, then shuffles the unit back into the deck.`
- ⚠️ **「快速 vs 缓慢」全书没有定义段** —— `Pray` 是「缓慢，**详见战斗修女阵营指南**」，
  而**那份指南不在仓库**。唯一已知差别就是 `:98` 那条「本回合能不能动」。
- 反编译：`CardTraitFerocity` **零自有字段**；`DefinedTrait.cs:130 ferocity=1265` ·
  **`:131 dontReturnFerocity=1270`** ← 就是 Bjorn / Bjorn's Shrine 的「stays in play」要用的位；
  `AbilityTrigger.cs:97 Ferocity=725 / :98 TriggeredFerocity=726`；`BattleManager.BroadcastUnitFerocity`。
- 🔴 **卡面核对：13 张里只有 9 张真带这个关键词**，另 4 张（`Njal Stormcaller` / `Deathwolf Scout` /
  `Bjorn's Shrine` / `Raid Tactics`）**只是正文里提到它** ⇒ `卡牌完整信息库` 那几行记错了。
  ⚠️ 另有 **desc 吞前缀**的数据错：`Pack. Ferocity: …` 在卡表里只剩冒号后半句。

**`duty`（职责）**
- 规则书 `:150` 替代行动；`:152`「震荡突击兵的职责**即使在无法攻击时仍可激活**」（**与 Ferocity 相反**）；
  `:181` **一次性能力**，可由其他效果 **reload**。
  ⚠️ 规则书**没说是本局一次还是每回合一次**，也**没标速度**。
- **`[Duty]` 写法在卡池里 0 张** —— 方括号是**本工程数据层的图标占位**，**原版卡面不存在**。
- 反编译：`CardTraitDuty` · `AbilityTrigger.TriggeredDuty=661 / Duty=662` · `AbilityEffect.resetDuty=475` ·
  `BattleManager.ResolveResetDuty / BroadcastUnitDuty / AddResetDuty`。
- 🔴 **卡表两处数据错**：① **6 张卡的 `desc` 丢了 `Duty:` 前缀**（`AM41`/`AM19`/`AM10`/`AM27`/`AM24`/`AM_Hektor_Thenmann`）；
  ② **2 张的 `keywords` 误抽了 Duty**（`Commissar` · `Press the Attack`）—— 它们只是**提到** Duty，自己没那个能力。
- ⚠️ 引擎现状：`UnitState.Exhausted` 的语义是「**每回合**一次」，**没有「本局已用过」的位** ⇒
  要复刻「一次性 + reload」得**新增一个不随回合重置的单位级标记**。

**`swarm` / `synapse`**
- 规则书中文版只有表里两行；**英文原版保住了被中文截断的原句**
  （`d:/2/Warpforge部队卡片/Warpforge_Offline_Rulebook_1_5-3_英文原版.md:321-331 / :337-341`）：
  `Swarm` = 「打出在**同名部队左侧**时：**置于其下、攻血相加**」·
  `Synapse` = 「被**友方战术**选中时：对**相邻**部队/单位**重复效果**（依战术而定）」。
- **「相邻」全规则书只出现 3 次**（爆裂 `:170` · 践踏 `:213` · 突触 `:217`），**没有定义段**。
- 🔴 **原版没有 `CardTraitSynapse` / `CardTraitSwarm`** —— `CardTrait` 的子类**只有 3 个**
  （`Duty` / `Ferocity` / `Oath`）。这两个走的是**硬编码机制**路线：
  `DefinedTrait`(`:65 swarm` / `:113 synapse`) + `AbilityTrigger`(`:74-75`) + `BattleActionType`(`:81-82`)；
  `CardScript.synapseCard`(`:1526`) / `UnitSwarmSignal`(`:5,7`) 是它的状态与广播载体。
- 🔴 **卡面核对：Swarm 11 张（不是 12）· Synapse 13 张（不是 14）** —— 卡表各多算 1，
  都是「**描述里出现该词**」被 OCR 当成了关键词（`Norn Emissary` 卡面只印 `Armour 1. Blast 3.`；
  `Swarming Masses` 是**战术卡**，卡面上没有 Swarm 图标）。
- ⚠️ **11 张 Swarm 卡的卡名互不相同** ⇒ 规则书说的「同名」**只能来自同一张卡的多份**
  （规则书 `:280` 各 3 张 + `Tide` 复制同名卡）。
- ⚠️ 另有一处 desc 错：`Termagant Brood` 卡表写 `+1 [Attack]`，**卡面是 `+1 [Ranged]`**（紫枪图标）。

**四条共用一个前置**：**格位相邻已经有了** —— `RuleCore` 里 Blast / Stomp **各内联一份**
（`tgtSlot ± 1` + `BoardSpec.IsValid`）。实现时**抽成一份**，别再抄第三份。

### 🆕 候选 F（2026-09-13 候选 E 中途查出来的，**它是上面几条的共用前置**）：**「相邻」**

**怎么来的**：做候选 E 时发现 —— `EffectText.cs:2744` **认得** `adjacent` 写法
（置 `EffectTargetSpec.Adjacent = true`），但**全仓没有任何消费者**
（`ResolveTargets` 不看它；`grep -rn "Adjacent"` 在 `EffectText.cs` 之外**零命中**）。
⇒ 卡面写了「相邻」的卡**解析得出来**（所以**卡面不打 `*`**、玩家以为它在正常工作），
而「相邻」被**静默忽略**，两个方向都会错：
  · `Deal 3 damage to an enemy and its adjacent units` → 当成「一个敌方单位」⇒ **打少了**
  · `Adjacent units have +1 Attack` → 当成「己方全体」⇒ **打多了**

**实测规模（自检每次报）**：卡面写了 `adjacent` 的 **36 张** · 其中解析出的目标真带上标记的 **23 张**
· 连标记都没设上的 **13 张**。全量清单 `_tmp_view/adjacent_unimplemented.txt`。
一条**故意「还大于 0」**的断言盯着它：实现掉之后那条会红，回来更新措辞就行。

⚠️ **它卡住的不止自己** —— 实现它会**顺带解开**：
`hunt mark` 那条（`Long Fang` 的正文正是 `deal 3 damage to adjacent enemies`）、
`synapse`（「对**相邻**部队/单位重复效果」）、以及上面 36 张卡。

🔴 **动手前必须先解开一环**（不然一定改错）：`EffectTargetSpec` 里**锚点分不开** ——
两种（现在看是**至少五种**）语义都只置同一个 `Adjacent = true`。

> 🔎 **2026-09-13 派子代理查过权威语义，结论比原来那两句完整得多，别只看二分**：
> **实际形状至少 5 种**，其中两种**在文本层不可区分**：
>
> | 形状 | 判据 | 例卡 |
> |---|---|---|
> | **A 施放者自己** | 光秃秃 `adjacent X` + 给予动词，无物主代词 | `Nuadhu Fireheart`「Strike: Give Invulnerable to adjacent troops」· `Chaplain Letharius`「Heal 1 to adjacent units」 |
> | **B 被打目标** | 明写 `its` / `to the target` | `Cleansing Flames`「Deal 3 damage to an enemy and its adjacent units」 |
> | **🔴 C 督军** | `your Warlord and adjacent …` —— 锚点既不是施放者也不是被打目标 | `Ethereal Supreme` · `Holy Fire` |
> | **D 自己，明写** | `this troop and its adjacent units` | `Apothecary Polixis` |
> | **🔴 E 静默锚点（最危险）** | 文字与 A **完全同形**（裸 `adjacent units`），锚点却是**上一句的目标** | `Whirling Death`「…If **the target** dies, deal 2 damage to adjacent units」· `Methodical Destruction` · `None Must Know` · `Long Fang` |
>
> ⇒ **只加一个锚点枚举还不够**：A 与 E 文本同形，E 得靠「上一句是不是 `Deal/Destroy … to a <目标>`」
> 消歧（`EffectText.cs` 已有切句逻辑可挂）。
>
> ✅ **原版有权威枚举**（`Scripts/Assembly-CSharp/TargetsAffected.cs:17-22`，
> 我们那个 `bool` 粒度比它**粗一档**）：
> `adjacentToSelfIfMeetsCriteria=100`（≈A）· `adjacentToPreviousTarget=105` 与
> `adjacentToTargetIfMeetsCriteria=110`（≈B/E）· `adjacentToActingCard=106` ·
> **`adjacentToFriendlyWarlordIfMeetsCriteria=113`（正是 C）** · `adjacentToEnemyWarlordIfMeetsCriteria=115`。
> 锚点的**唯一入口**是 `TargetCriteria.cs:9` 的 `public TargetsAffected targetsAffected;` ——
> 它**没有任何** `Adjacent` 布尔字段。
> 相关原版签名（方法体没反编译）：`BattleManager.GetAdjacentUnits/GetAdjacentUnitRight/
> ShouldTriggerAdjacentDamage/ShouldTriggerStompDamage/ResolveTraitAdjacentDamage` ·
> `MinionManager.GetAdjacentUnits/GetNextSlotCheckingAdjacents`。
>
> 🔴 **光环那 10 张（`Adjacent units have +1 Attack` 这一族）走的是另一条路，别混进来做**：
> 原版有 `CardScript.cs:1606 HasWhileInPlayAdjacentEffect()` —— **常驻光环是另一套 API**。
> 而我们 `资料/常驻效果_数据与设计.md` **全文 grep `adjacent`/`相邻` = 零命中**，
> `RuleEngine` 里 grep `WhileInPlay|Aura|StaticBuff|Passive` **也是零** ⇒
> **常驻光环那条路我们完全没建**；这 10 张现在走的是普通一次性路径 ⇒ **给己方全体加**（「打多了」）。
> ⇒ 要新开一层，是**独立于目标解析**的另一件事。
>
> ⚠️ 一条卡面核对的副产品：**`Forged Killers` 卡面确实没写 `its`**（`Give +2 to a friendly troop and
> adjacent troops`），锚点靠语感 = 刚给的那个 troop；而**我们的中文翻译加了「其」**
> （`zh_CN.csv:1484`「给予一个友方部队**及其**相邻部队」）—— **别据中文反推英文**。
> 6 张卡面逐字核过，**没有抄错**，也没有 `neighboring` / `next to` 这类变体写法。

判据要**照解包资源 / 规则书定**（铁律 2、3，别猜）。
把「目标相邻」实现成「自己相邻」= 又一处静默错打。

#### 📋 候选 F 的实施计划（2026-09-13 记，**照着做就能开工**）

**范围切分（先想清楚这段，别一锅端）**：
- ✅ **本轮只做「26 张效果目标形」**（目标解析这一半）
- ❌ **光环那 10 张不做** —— 要新开常驻层（对应原版 `HasWhileInPlayAdjacentEffect`），是独立的一件事
- ❌ **形状 E（静默锚点）做不完就先不点亮它** —— 宁可留「认不出」，也别引入新的静默错打

**四步（按依赖顺序）**：

1. **抽共用的格位判据**（`RuleEngine/Core/`）
   现在 `RuleCore` 里 **内联写了两遍**：Stomp（`~:940-946`）与 Blast（`~:969-974`），
   都是 `for (off = -1; off <= 1; off += 2) { adj = tgtSlot + off; if (!BoardSpec.IsValid(adj)) continue; ... }`。
   ⇒ 抽成 `BoardSpec` 或 `RuleCore` 上的**一个**函数（输入：棋盘方 + 锚点槽；输出：相邻槽列表）。
   ⚠️ **先把这两处替换掉再往上加**，否则就是抄第三份（工程铁律：「两处写同一条规则 = 迟早不一致」）。
   ⚠️ 边界语义照现有两处：`BoardSpec.IsValid`（0..8）、**不排除督军格**（Blast 是在结果里再 `IsWarlord` 排除）。

2. **锚点字段**（`EffectText` 的 `EffectTargetSpec`）
   把 `public bool Adjacent` 换/加上一个**枚举**，照原版 `TargetsAffected.cs:17-22` 取名（**别自创**）：
   `Self(=100)` · `PreviousTarget(=105)` · `TargetIfMeetsCriteria(=110)` · `ActingCard(=106)` ·
   `FriendlyWarlord(=113)` · `EnemyWarlord(=115)`。
   解析期按下面 §判据 定值；**认不出/定不下来 → 保持「未设」并让那句继续认不出**。

3. **解析期判据**（`EffectText.cs:2744` 那一带）
   | 文本形状 | 锚点 |
   |---|---|
   | `its adjacent` / `adjacent to the target` | `PreviousTarget` |
   | `your Warlord and adjacent` | `FriendlyWarlord`（`Ethereal Supreme` / `Holy Fire`） |
   | `this troop and its adjacent` | `Self`（`Apothecary Polixis`） |
   | 裸 `adjacent X` + **同一段里前面刚点过目标**（`Deal/Destroy … to a <目标>`） | `PreviousTarget` ← **E，靠上一句动词消歧** |
   | 其余裸 `adjacent X` | `Self` ← A |

4. **结算期**（`EffectResolver.ResolveTargets`）
   在 pool 建好、兵种筛完之后插一段：按锚点求「锚点槽」→ 取相邻槽 → 加进目标集。
   ⚠️ **锚点单位不在场上 / 拿不到槽位时**：**别退回「全池」**（那就是「打得比卡面宽」），
      按工程惯例**如实报**并空过。

**验收（改完至少做这三件）**：
- 跑 `RuleEngineTest.Run` 看那条「还大于 0」的断言 —— **它红了才对**，然后把 `ReportAdjacentGap` 的
  措辞与 `_tmp_view/adjacent_unimplemented.txt` 一起更新（这正是当初留它的用意）。
- **正反各钉一次**：`Cleansing Flames`（目标锚点）与 `Chaplain Letharius`（自己锚点）各写一条端到端断言；
  ⚠️ 再补一条**反例**：锚点是目标时，**施放者自己的邻居不该挨打**（锚点写反了只有反例抓得到）。
- 四条自检全绿（`BattleScene` 偶发退出 segfault 的坑见 §五 那条注）。

**参考出处**：`资料/普查产出_0913/` 无此件；研究结论在**本条**（上面那张 5 形状表 + 原版枚举）。
卡面原件 `d:/2/Warpforge部队卡片/<阵营>/<分类>/*.png`。

✅ **一条现成的**：格位相邻**早就有了**，只是**内联写了两遍**
（`RuleCore` 的 Stomp 与 Blast 各一份 `tgtSlot ± 1` + `BoardSpec.IsValid`）——
实现时**抽成一个共用判据**，别再抄第三份（工程铁律：「两处写同一条规则 = 迟早不一致」）。
> ⚠️ 2026-09-13 更正：一份子代理报告说「RuleEngine 里没有任何 `slot±1` 的算法（连 Blast/Stomp
> 也还没做）」—— **是错的**，它 grep 的是「函数」，而那是内联写的。

### 候选 D：继续按阵营铺（每会话两个，按剩余升序）

| 批次 | 阵营 | 剩余 | 备注 |
|---|---|---|---|
| —— | ~~Goff · DarkAngels · Sautekh~~ | **0 ✅** | 已做完 |
| —— | ~~TauEmpire~~ | **1** | 剩 `Lower cost by 1 when an enemy dies`（= 候选 A 的第 3 条） |
| 1 | **Leviathan 2** | 2 | ⚠️ **Leviathan 那 2 张是「查不到」** |
| 2 | **SpaceWolves 3 · AstraMilitarum 3** | 3 / 3 | AstraMilitarum 那 3 条里有 2 条是候选 A |
| 3 | **BlackLegion 4 · Ultramarines 6** | 4 / 6 | Ultramarines 剩的里有 2 条是候选 A |
| 4 | **Genestealers 6 · EmperorsChildren 7** | 6 / 7 | |
| 5 | **SaimHann 9 · Sororitas 12** | 9 / 12 | Sororitas 最难（`[Faith]:` 那一族） |

（剩余 = 该阵营**未完全解析的战术卡张数**。每次自检重写 `_tmp_view/tactic_by_faction.md`，**别手抄**。）

---

## 三、「做一个阵营」的标准流程（2026-09-12 第十六轮实际走的，极限战士 + 兽人）

1. **跑一次自检**，读 `_tmp_view/tactic_by_faction.md` 里那一行 —— 那就是这张阵营的**工作清单**
   （自检每次跑都会重写它，**不用手抄**）。
2. **每个卡点先定语义**，顺序是：
   ① 规则书（`资料/规则书/…_中文翻译.md`，关键词表在 `:161-225`）
   ② `d:/warpforge/scripts/rule_core.gd`（照卡面修过的规格书，**权威语义**）
   ③ 反编译方法名（`d:/2/Warpforge_code/Scripts/Assembly-CSharp/BattleManager.cs` 的 `Resolve*`）
   **三处都没有 → 如实报「原版数据里没有」，不要猜。**
3. **分类，优先复用已有的层**（第十六轮最大的收获）：
   | 卡点长什么样 | 该动哪一层 | 例 |
   |---|---|---|
   | 新句型 / 新动词 | `EffectText` + `EffectDispatch` 表加一行 | `drawtype` / `repeat` |
   | **阵营词写法的「付费才生效」** | **复用 `op.Cost`（付费层）** | `Oath N:` = `12 [Energy]:` |
   | **阵营词写法的「某个前提」** | **复用 `EffectCondition`** | `Codex:` = 条件 `energyzero` |
   | 「给谁加什么」 | 复用 `GivePayload` 拆载荷 | `+2 Health and Vanguard` |
   | **数值修正**（降费…） | **挂在对局上、不碰 `CardDef`** | `ctx.CostMods` + `RuleCore.CostOf` |
   ⇒ **能不新开动词就不开**。第十六轮做三个阵营机制，**一个动词都没加**。
4. **加断言**：⚠️ 务必**断言「解析出来的东西长什么样」**，不只断言「认不认识」。
   第十六轮踩过：`lowercost` 的正则把 payload 切成了 `f all vehicles…`（`of?` 里那个 `?`），
   **而解析判定照样是 Ok** —— 只有「切出来是什么」的断言抓得住这类静默错解析。
   见 `RuleEngineTest.TestLowerCost` 的头注释。

   **第二十六轮（选牌）新踩的三条，下一轮会再遇到**：
   - **后续句本来就解析得了** —— `Choose a troop from your deck.` 那一族的动作写在**下一句**，
     而 `Split` 只按 `.` 切，后一段**自己就能解析**。动手前先查「这句是不是已经在未覆盖清单里」，
     别重复实现（实测 `Draw it and create a copy of it in your hand` / `It costs 2 less` /
     `Give it Stealth` 全都早就能解析）。
   - **`rule_core` 的默认值会静默错**：动作判不出时它默认 `to_hand`（`:1193`），
     还有个裸的 `" in your hand"` 兜底（`:1231`）—— 后者会把
     `Choose a card in **your opponent's** hand` 判成「进自己手牌」。
     **判不出来就报不认识，别默认。**
   - **候选筛选拿「全卡池」当判据集**（复用 `CreatePool.Resolve` 的兵种/阵营判定）：
     好处是判据只有一份，代价是**不在卡池里的卡筛不出来** ——
     自检夹具里 `new` 出来的假卡就撞到过（`TestChooseCard` 里记着）。
     ⚠️ **第二十七轮已改成逐候选直接判**（`type`/`subtype`/`faction` 是卡自己的字段），
     本节这条留作**教训**：判据集选错，报错会变成一句很难查的「候选里没有符合…的卡」。

   **第二十七轮（回手/指代）新踩的，都是「改正则之前先普查」**：
   - **`IsVerbWord` 少一个词 = 整句切不开**。`and lower …` 在 `SplitAndTail` 里切不开，
     于是锚了 `$` 的 `ReDraw` 整条失配 —— **5 张卡**一直躺在「完全不认识」里。
     动手前先 `grep` 那个形状在全仓有多少条、**是不是都该切**（这次 5 条全是）。
   - **新加的正则会把老句子的参数吃走**：`ReDrawType` 的 `([a-z]+)` 把 `Draw **it**` 的 `it`
     吃成了「兵种词」→ 静默空过，**而覆盖率两栏都算它通过**。
     ⇒ **代词分支必须排在泛型分支前面**；加新写法时一定要回头验「老句子有没有被截胡」。
   - **往词表里加写法会抢走老写法的匹配**：给 `DestPhrases` 加 `to your hand` 之前，
     必须先查「有没有 `Create … and add it to your hand` 这种句子」（这次查了，**0 条**，所以安全）。
   - **目的地类的东西要「全等」匹配，不要「包含」**：`… to your hand **and reduce its cost to 1**`
     按包含匹配会把后半句**静默吞掉**。宁可判「不认识」。

   **第二十八轮（常驻效果）新踩的**：
   - 🔴 **凡是会作用在「卡面原文」上的正则，一律加 `IgnoreCase`**。
     `SplitAtTurn` 拿原大小写的 `CardDef.Desc` 去匹小写正则 → `IsHandTrap` / `ResolveAtTurn`
     **两处静默失效**；解析那条路因为拿到的是小写 `low`，**完全看不出来**。
     （判据被两处用、一处传小写一处传原文，是这类坑的温床。）
   - ~~**注册一条永远不会被消费的效果 = 骗人**。`For the rest of this battle, give Shield to all
     Drones you deploy` 是**部署时触发**，不是回合起止 —— 宁可判「不认识」，也别注册。~~
     ⚠️ **2026-09-13 第三十轮更正**：那条**已经真做了**（建了部署触发的机制）。
     **这条教训本身仍然成立**，只是例子换了 —— 现在钉的是
     `For the rest of this battle, give Shield to all **Wizards** you deploy`（兵种词表里没有这个词）。
   - **条件从句不许当主语**：`When you play a Stratagem, your Warlord takes 1 damage`，
     不挡的话正则会把「什么时候」整段吃成目标 → 变成**任何时候都掉血**的静默错语义。
   - **测试自己的回合记账要数清楚**：`ToP1Turn` 之后当前行动方是 P1，
     所以第一次 `EndTurn` 结束的**就是 P1 的回合**（我写错过一次，查下来是断言错、实现是对的）。

   **第三十轮（部署时触发 + 持续改费）新踩的**：
   - 🔴 **断言只钉「该生效的生效了」是不够的 —— 必须同时钉「不该生效的没生效」。**
     第一版自检全绿，但复查发现它**钉不住最关键的那条**：卡面写的是
     `give Shield to all Drones you **deploy**`，不是「你场上**所有** Drone」——
     实现要是退回成「查场上所有 Drone」，那个用例**照样绿**。
     补一条「先摆一个早就在场上的 Drone，验它**不会**被补上」才钉住。
     **做法：故意打断那段分支重跑，确认断言真的会亮**（这次亮的就是补的那一条，其他都不亮）。
   - **文件名骗人**：`Core/EffectResolver.cs` 里的类**不是** `EffectResolver`，
     是 **`public static partial class RuleCore`**（全仓仅此一处这样）。写 `EffectResolver.Xxx(...)` **编译不过**。
   - **`<see cref="..."/>` 里写不存在的方法名不会报错但也没用** —— 改了方法名要回头 `grep` 一遍文档引用。
   - 🔴 **实战检查（「跑得完 ≠ 跑得对」那道关）第一次跑出来是「假的坏消息」**。
     用 `DeckBuilder.StarterDeck` 自动凑的牌组跑 4 局，结果「登记了 1 次、部署触发段 0 次」——
     看着像机制的 bug。查下来是**检查本身测错了东西**：自动凑的 TauEmpire 牌组里几乎没几张
     Drone（全区只有 **7 张**），登记之后部署的那 2 个单位**不是靶子**，**本来就不该触发**。
     ⇒ 改成**牌组里全是靶子**（22 张 Drone + `shuffle: false`）之后：登记 4 次 · 触发 **27 次** · 筛中 27 次。
     **教训：实战检查的牌组要把「靶子」塞满，否则统计量测的是噪声。**
5. **收尾**：跑四条自检（下面 §五），把这一轮写进
   `资料/卡牌效果管线_计划与交接.md`（§一 加一行、§五 更新数字）+ `项目任务.md` 的轮次小结。

---

## 四、点了名但现在**不做**的（等它自己的阵营）

| 要做的 | 等到哪个阵营 | 说明 |
|---|---|---|
| **`Remnant`（残骸）** | **Sautekh（死灵）** | 规则书 `:203`「本部队死亡时**翻面表示残骸**；残骸受伤害或控制者回合结束时被摧毁」。**28 张 Sautekh 单位带这个关键词**，是那个阵营的主题；`reanimate` 这个唯一没实现的动词就是它的一部分（`Reanimate a friendly Remnant`） |
| 替代行动族 `Duty / Ferocity / Pray / Agenda` | 对应阵营各自做 | 规则书 `:150`；原版是独立子类（`CardTraitDuty` / `CardTraitFerocity` / `CardTraitOath`） |
| 选牌 UI（表现层） | `Choose a …` 引擎侧做完之后 | 引擎侧先按「单机自动选 1」走，UI 独立一件 |
| 关键词对象化 / 阵营机制挪出通用结算器 | **等关键词批量补或第二个阵营机制开工时** | 见 `卡牌效果管线_计划与交接.md` §四·六，现在动是过度设计 |

---

## 五、跑自检（改完必须四条全绿）

```bash
cd d:/4
unset ELECTRON_RUN_AS_NODE
U="D:/Unity/Hub/Editor/6000.3.23f1/Editor/Unity.exe"
for M in RuleEngineTest.Run BattleScene.Run DeckScene.Run CardBaseDemo.Run; do
  printf "%-22s " "$M"
  "$U" -batchmode -quit -projectPath "D:\4\Unity\MyGame" -executeMethod $M -logFile - 2>&1 \
    | grep -E "^(RE|BT|DK|CBD) === (结束|合计)|error CS|✗" | tr '\n' ' '; echo
done
```

> ⚠️ **`BattleScene.Run` 偶尔会在退出时 segfault（2026-09-13 实测，别被它骗到）**：
> 本机 6 次里出现 **2 次**，**都在会话早期**；崩溃**严格发生在 Unity 关停之后** ——
> 日志里是 `Shut down.` → `Cleanup mono` → `abort_threads: Failed aborting id: …` → segfault，
> 中间还夹着一次 `Clearing [NotoSerifCJK-Regular SDF] dynamic font asset data` → **重导入字体资产**。
> **测试结果在崩溃之前就已经打印完了**（`BT === 合计：369 通过 / 0 失败`）。
> ⇒ 🔴 **判据看 `BT === 合计` 那一行，不要看进程退出码**。
> 已排除是我们的代码引起的：同一台机器上**把 `RuleEngine/` 与 `CardPresentation/` 退回改动前那一版
> （`ce16c7b`）跑，也是**干净退出**；而改动后的版本**连跑 3 次也全干净** ⇒ **偶发、与代码无关**。

**最近一次复核（2026-09-13，第三十四轮收工）**：`RuleEngineTest` **1566/1566** ·
`BattleScene` **369 通过 / 0 失败** · `DeckScene` **61/61** · `CardBaseDemo` 全过 ·
**特效**：`BlendProbe` 混合 1099/1099 + shader 解析 70/70 · `WhiteboardTest` 20/20 + 无泄漏 ·
`DistortProbe` 27/27（`_GrabPassAvailable = 1.0`）。
**卡表 `version 6`** —— 每张卡带**稳定 id**（原版 id 优先、对不上的自造，见候选 0）。
**防御卡（39 张）已接进对局**：能打、开局在手里、不参与换牌。
**三套阵营资源都接上了**：信仰 `Faith`（修女会）· 灵魂石 `SpiritStones`（灵族）· 任务点 `QuestPoints`（暗黑天使）
—— HUD 三件也摆了（位置从运行时 dump 换算，见 `BattleDriver` 里那组常量的「独立佐证」）。
**`When <事件>`**：带它的卡 **80** 张 · 真的点亮 **62** 张（62 条监听器）·
**事件触发式降费 3 张** · 认不出的**事件短语 14 种**（清单在 `_tmp_view/when_unparsed.md`，每次自检重写）。
战术卡**完全解析 403/449**（**载荷有机制** 377 · 部分 26 · 完全不懂 20；
分句 660：关键词声明 37 / 认了 575 / 半懂 6 / **不认 42** —— 第三十二轮把那 47 条查了一遍，
**其中 5 条当场修掉**：付费前缀的**方括号本来是可选的**（我们抄成必选 → 无括号 `5 Energy:` 收不了）、
`Draw an additional <类型>` 的量词位不认 `additional`；明细见 `资料/战术卡47条_语义查证.md`）。
**单位卡 desc**：完全解析 349/586（部分 120 · 完全不懂 117）。
**临时卡（`Ephemeral`）**：**98 张**卡面带这个词；「移出游戏」区域 + 回合结束清扫 + 卡面角标全做完。
**未实现关键词 16 个**（第三十四轮 24 → 16：`mob`/`regiment`/`unstable`/`cruelty`/`talent` 是**新做掉 5 个**，
`codex`/`oath`/`stun` 是**名单误报、机制一直都在**，见下面两条）
—— ⚠️ **以自检打印的 `RuleCore.UnimplementedKeywords()` 为准**，别抄这里。

**🆕 第三十四轮另外做掉的（不在上面那张「未实现」名单里的活）**：
- **`mob`（群体，Goff 15 张）与 `regiment`（团，AstraMilitarum 14 张）** —— **成对的两条**：
  规则书 `:193`「友方部队执行**近战**攻击后触发效果」/ `:202`「友方单位执行**远程**攻击**时**触发效果」。
  实测卡面两条同形，都是 `Xxx: <效果>` 挂在**自己**身上（`Skarboy Nob` = `Mob: Gain +1 Attack` ·
  `Kasrkin` = `Regiment: Deal 1 damage to a random enemy…`），语义 = 「这张卡出手打人之后做一件事」，
  和 `Strike` 同形、**只差近战 / 远程**。
  时机接在 `RuleCore.DeclareAttack` 攻击后那一段（`Slay` → `Strike` → `Mob` / `Regiment`；
  ⚠️ **这个先后是我们挑的**，三者都长在原版同一个 `ResolveUnitAttacked` 里、反编译看不到顺序）。
  ⚠️ 规则书一条写「后」一条写「时」，我们没有能分辨的依据 ⇒ **两条放同一位置**（我们挑的，如实标着）。
  ⚠️ **`mob` 只做了「触发攻击者自己的 `Mob:`」**：原版还有一支 `BroadcastUnitMob`
  （通知**除攻击者外**的所有卡，触发 645），实测听众只有 `Big Choppa Nob` 一张，
  而它要的「**再触发一次**」我们的效果文法表达不了 ⇒ 没做。
  ⇒ `When … triggers mob` 那条短语**仍然不会亮**（**不是漏做，是做不到**，别挂成待办）。
- **`unstable`（不稳定，Goff 18 张）** —— 规则书 `:221`「本单位死亡时：**对随机单位造成 1-3 伤害**」。
  ⚠️ 它**卡面没有正文**（只写裸关键词 `Unstable. Blast 3`），效果**完全由规则定义** ⇒
  **不进 `RoutableTriggers`**（没有正文可收）。目标池与伤害范围照 `rule_core.gd:4578 _unstable_blast`：
  **场上随机单位（含双方）**。结算在 `CleanupDeaths`、**排在 `Backlash` 之前**（`rule_core.gd:4529` 同序），
  连锁靠 `ctx.EffectChain` 截断。随机走 `ctx.Rng`（同一局可复现）。
- **`cruelty`（残忍，帝皇之子 9 张）** —— 规则书 `:178`「**你的回合**敌方单位受伤害未死亡时激活效果」。
  卡面写法 `Cruelty: <效果>`。🔴 **触发方是挨打方的对面**（**己方**带该词的牌响），
  和 `Penitence`（挨打那个自己触发）是**两条**，别合并。新增 `FireTriggerOnSide`（给一整排各触发一次）。

- ⏸ **`ecstasy`（狂喜 X，帝皇之子 10 张）评估后推迟，没做** —— **两条卡点**，都钉成了断言
  （`RuleEngineTest.TestUnstableEcstasyCruelty` 的块 ②，**它们红了就说明卡点解了**）：
  ① **卡表的关键词值不可靠**：实测 `Terminator` 的 `keywords` 是**裸 `Ecstasy`**（会当成 1），
     卡面写的是 **2**；`Maulerfiend` 更连 `Ecstasy` 都不在 `keywords` 里、只在 `desc` 有 `Ecstasy 5`。
  ② **`AddTriggerOp` 收不下 `Ecstasy N:` 这种正文** —— 它拿 `:` 前面**整段**和触发名**严格相等**比对，
     `"ecstasy 2"` 对不上 `"ecstasy"`。
  ⇒ **半做 = 静默用错阈值**（正是本工程红线），所以宁可标成没实现、卡面照旧打 `*`。
  时机点本身就在 `RuleCore.Hurt`（`hpBefore` 已经存好了，注释里写了「补做时用它判**跨越**」）。

- **`talent`（天赋，91 张 —— 剩下名单里最大的一个）** —— 规则书 `:218`「**回合开始时在手牌中生成临时战术**」。
  **它和别的关键词最不一样**：效果**不是卡面正文**，而是「**按名字去卡池查一张卡**」——
  实测 **80 个天赋名里 72 个在卡池里查得到同名卡**、而且**全是 `tactic`**
  （`Witchfire` / `Path of the Seer` / `Flickerjump` / `Wrath of Khaine` …）。
  ⇒ 机制 = 回合开始（`RuleCore.SpawnTalents`）去卡池找同名卡塞进手牌 **＋ `MarkEphemeral`**。
  ⚠️ **`MarkEphemeral` 那一步不能漏**：规则书 `:229` 把「天赋生成的牌」与伴生 / 潮涌复制并列为**临时**，
  而**多数天赋卡面并没有印 `Ephemeral`**（`Witchfire` 的卡面是 `Deal 1-3 damage. (1) Repeat this effect`）——
  不标记它就会**赖在手里不走**，而画面完全看不出来。
  这正是 `CardDef.Ephemeral` 注释末尾那句「三个**来源**还没做，它们生成的牌要自己调 `MarkEphemeral`」的兑现。
  ⚠️ **查不到同名卡的那 8 个会如实打日志**（名单在 `CardDef.TalentName` 的注释里），不静默。
  ⚠️ **副作用（已处理）**：天赋卡是**从卡池现拿的、不属于那 30 张卡组** ⇒
  「手牌 + 牌库 = 卡组张数」这类不变量会被顶掉一张，`BattleScene` 一次红了 **7 条计数断言**。
  修法是加了个 `Conjured(ctx, p)` 辅助，判据用 `BattleContext.MarkedCount`（**只数标记**）——
  **不能用 `IsEphemeral`**，那会把卡面自带 `Ephemeral` 的 98 张也算进来（那些本来就是卡组里的牌）。
  ⚠️ 顺带撞到一个 C# 细节：**属性和方法不能同名**（`MarkedEphemeralCount` 已经是个属性，
  新的那个只能叫 `MarkedCount`），否则 `CS0102`。

**🆕 第三十四轮查出来：「未实现」名单里有 3 个是误报** ——
派子代理逐条核了那 23 个「未实现」关键词的代码，查出 **`codex` / `oath` / `stun`** 三个
**早就有机制**，只是**没登记进 `KeywordTable.Implemented`**。后果是**名单多报 + 卡面白打 `*`**
（玩家看到的和实际能力对不上）。判据是「**代码在那个时机真的读了/做了那件事**」——
「`Prefixes` 认得出」和「`GivePayload` 能把它写进单位」**都不算**。
三个的机制分别在哪，写在 `CardDef.Implemented` 的注释里；已登记，并加了**双面断言**
`TestKeywordImplementedList`：**既不许多报，也不许把没做的登记成已做**（那比多报更糟）。

**🔴 另外 4 处「看着像有机制、其实只是近似」——别把它们当已实现**（子代理同一轮查出来的）：
| 关键词 | 假象 | 实情 |
|---|---|---|
| `duty` / `ferocity` / `agenda` / `pray` | `Prays` 广播挂在**任何** `UseAbility` 上 | `Ability` 是本工程自定关键词，和这四个原版替代行动**没有绑定**；四者的正文**都没变成能力** |
| `remnant` | `reanimate` 动词 + `Reanimated` 广播是真的 | **不读 `remnant` 关键词** —— 把墓地**全体**当残骸，也没有规则书 `:203` 的「翻面」 |
| `ambush` / `swarm` / `destroyer` | 关键词被 `CreatePool` / `CardCriteria` **当筛选器读** | 只影响「造哪张 / 能选谁」；关键词**自身的规则一行都没有** |

⚠️ **顺带查出的一条数据问题**：5 张卡（`Banshee Mask` / `Hektor Thenmann` / `Malicious Volleys` /
`Snakebite Grot` / `Toxic Bonfire`）的 `keywords` 里那个 **`Stun` 是从效果句误抽出来的**
（卡面写的是 `Deal 3 damage to an enemy and Stun it`）。而**卡面关键词是构造函数直接灌字典、
不走 `AddKeyword`** ⇒ 那个 `Stun` 本身是死的（效果照样结算，走的是 `DoStun`）。
⇒ 要根治得清卡表数据，**没做**；记在这里，别当成「卡上有个 Stun 关键词」。

⚠️ **本节踩过的坑**：这里先后写过 825/825 · 837/837 · 1203/1203 三个版本 —— 都是**写那一刻的快照**，
后来每一轮都在加断言，于是每一版都变成了误导（`规则引擎_进度与交接.md` 开头那张表指着本节说
「最新值见这里」，**两个会话都被这条指针坑过**）。
⇒ **规矩不变：数字只写一处，就是上面那一行；每次收工必须更新它。**

⚠️ **Unity 同工程同一时刻只能跑一个实例**（实测：第二个进程 `Exiting ... return code 1`，一行测试都没跑）。
所以「派子代理并行跑」在这台机器上不成立 —— 能并行的只有「读和写」。

---

## 六、两个阵营的实战检查怎么跑的（`RuleEngineTest.TestFactionBattle`）

极限战士 vs 兽人（**轮流先手**，免得「赢了是先手还是阵营强」分不清），各 6 局：
**结果 6/6 完成 · 平均 13 回合 · 极 4 : 兽 2 · 双方共打出 29 张战术卡 ·
扫 593 条事件日志，只有 6 条「没结算」，覆盖 3 张卡。**

这一节的价值在于**它抓出了两个引擎自检抓不到的问题**（第十六轮）：
1. `DeckBuilder.StarterDeck` 的 `unitsOnly` 开关**从来没实现** → 自动凑的牌组**一张战术卡都没有**；
2. `SimpleAI` **只出单位卡**（`if (!c.IsUnit) continue`）。
⇒ 合起来是「**引擎自检里战术卡用例全绿，真打起来却从来没碰过它们**」。
**新做一批卡之后，照着这个测试的思路加一局实战检查** —— 它是「跑得完 ≠ 跑得对」的那道关。

---

## 七、已知的**数据缺口**（不是我们没做，是原版数据里就没有）

| 卡 | 缺什么 | 查过哪 |
|---|---|---|
| `Mekaniak`（Goff） | `Give a Kustom Job of your choice to a friendly Vehicle` —— **没有任何叫 Kustom Job 的东西** | 卡池 1130 张、实体卡表 `卡牌信息权威表_0824.md`、反编译 1891 个类、`rule_core.gd` —— 四处零命中 |
| `Duty's End`（UM） | 嵌入效果 `"💀 Backlash: Trigger the Codex ability of all friendly units"` | 嵌入效果走 `EffectSpec` 的**封闭文法**（只有 Damage/Heal/Draw），表达不了「触发典籍能力」 |
| `Vindicare Assassin` / `Brôkhyr` / `Vitric Consul` | 整张卡 | 卡池 + 实体卡表都没有 |

⚠️ 反过来也踩过：**「查不到」和「没有」是两件事**。第十六轮之前那份「原版数据里没有这张卡」的名单里，
`Combat Elixir` / `Sabotage` 其实是**兵种**、`Abaddon's Chosen` / `Sergeant Taaman` /
`Extermination Protocol` 都存在只是**名字写法不同**。下结论前先查一遍 `CreatePool.FindNearMiss`。

---

## 八、两套阵营特色机制：**解包资料里有，而且比 UI 占位深得多**（2026-09-12 查到）

> 起因：用户问「灵族的灵魂石、修女的祈祷，解包资料里真的没有吗」。查完的结论是**有**，
> 之前那张「引擎侧也没有对应机制」的说法只对了一半 —— 缺的是**数值**，不是机制。

### ① 灵族 灵魂石 / Spirit Stone —— 档位 ⓑ⁺（机制骨架齐全，缺数值）

- **它是第二种能量货币**：`ManaType.cs:4-5` `Normal=0, SpiritStone=5`（枚举里只有这两种）。
- `PlayerManager.cs:85` `spiritStoneMana`；`:119 ResetMana(..., initialSpiritStone)`（**开局可给初始值**）；
  `:127 HasEnoughMana(int, psychic, plasma, spiritStone)`；`:142 GetCurrentSpiritStone()`；
  `:200 UseSpiritStoneEnergy()`；`:208 AddSpiritStoneMana(int)`。
- **消耗路径**：`CardScript.cs:2712 CanUseSpiritStone()`、`AbilityTrigger.cs:71 UseSpiritStone=600`、
  `BattleActionType.cs:76 useWaystone=76 / :77 triggerSpiritStone=77`、`UnitConditions.cs:17 hasUseSpiritStone`。
- **获取是「数量事件」**：`CollectSpiritStoneSignal.cs` 字段 `IsPlayer / Amount / FromWaystone`。
- **可被 buff 加**：`ManaBuff.cs:22 spiritStoneMana`；`RawCardScript.cs:87 initialSpiritStoneModifier`、`:668 UsesSpiritStone()`。
- **HUD**：`ManaManager.cs:21/33/61`（`SpiritStoneHolder` / `ToggleSpiritStoneMana` / `SetSpiritStoneMana`）；
  全树 `2D层_battlearenaaeldari全树.md:244-246` → `SpiritStoneHolder / SpiritStone[51×63] / SpiritStoneText '0'`。
- **语义（规则书中文版 :210 / :225）**：路标石单位死亡 → 生成 1 颗灵魂石；灵魂石受伤害会被摧毁（对手能清）；
  **只在自己回合收集**；收集后用于抵扣/触发。卡面佐证：`Infinity Circuit`「Gain 5 Spirit Stones」、
  `Hosts of the Dead`「**Spend all** your Spirit Stones. For each one, deploy a Wraithguard」。
- **缺口**：初始值 / 上限 / 每回合增长 / 扣减公式 —— 桩方法体全空；线索是
  `GameStaticData.cs:323 waystoneGiveMana` 那类静态配置（**下一步该去 MonoBehaviour 数据里找它的值**）。

### ② 修女会 信仰（Faith）/ 祈祷（Pray）—— 档位 ⓑ

- **信仰是每玩家一个累计值，不是货币**：`PlayerManager.cs:87 faithMana`、`:147 GetCurrentFaith()`、`:212 AddFaithMana(int)`。
  ⚠️ `ResetMana(...)` **没有 initialFaith 参数**（与灵魂石不同）—— 从 0 累积。
  `ManaType` 里**没有 Faith**，`HasEnoughMana` 也**没有 faith 参数**。
- **规则书 :184 原文**：「**信仰（Faith）** | **总信仰达到指定值时触发效果**」→ **阈值触发，不花掉**。
- **祈祷是一条独立的单位行动**：规则书 `:198`「**祈祷（Pray）** | 缓慢非战斗行动（详见战斗修女阵营指南）」、
  `:150`「职责/狂暴/祈祷 等是**替代攻击行动**，受与攻击相同的限制」。引擎侧 `EntityScript.cs:66 isPraying`、
  `CardScript.cs:3132 SetPray(bool)` / `:1672 TriggeredPray()`、`AbilityTrigger.cs:82 Pray=655 / :83 TriggeredPray=660 / :72 UseFaith=605`。
- 卡面花费形如 `N [Faith]:`（`Lead the Righteous`「**8 [Faith]**: Deploy an additional Battle Sister」等）。
- **缺口**：每次祈祷给多少信仰、阈值表 —— **规则书说的那份《战斗修女阵营指南》本地没有**；
  游戏内 hover 说明（`TextTooltipController.cs:119 GetFaithTooltip`）方法体空、I2 词条文件也没在本地。

### ③ 奇迹骰 / Miracle dice —— 档位 ⓒ 本地确实查不到
`Miracle` 在反编译类、`资料/`（含规则书）、`数据/游戏数据/` 里**命中 0 次** —— 本作没实现这个概念。

**下一步建议**：灵魂石可以按「第二种能量」做（`ManaType.SpiritStone` 那套字段就是设计图）；
信仰按「累计计数器 + 阈值触发」做。**别再说「原版没有」** —— 机制骨架是有的。

### ✅ 用户 2026-09-12 亲口给的机制口径（**比我从规则书推的准，照这个做**）

| 机制 | 用户说的 | 我原来记的（**改成用户口径**） |
|---|---|---|
| **信仰 Faith** | **没有「缓慢」这回事**（规则书那句「缓慢非战斗行动」指的是**祈祷 Pray 这个动作**，不是信仰值）· **没有上限** · **不会衰减**（现在没有任何机制/效果会衰减它） | 我原来写「缓慢/上限/衰减查不到」—— 那三个问题本身就不成立 |
| 信仰的作用 | **达到具体阈值（3 / 5 / 9 这种）时，部分卡牌获得更强的效果** —— 信仰值就是干这个的 | ✅ 与规则书「总信仰达到指定值时触发效果」一致，现在有了具体数字量级 |
| **灵魂石** | **没有初始值** · **没有上限** · **没有每回合增长**；**扣减完全由效果决定**（有些卡的效果就是「花掉灵魂石换增益」） | 我原来列的三项「缺口」全部作废 —— 本来就没有 |

⇒ 也就是说：**两套机制的数值都不是「缺」，而是「本来就没有」** —— 灵魂石的量只由
「路标石单位死亡 +1」和「效果扣减」决定；信仰只由「触发效果累加」和「阈值」决定。
落地时**别再去找初始值/上限/增长表**（那是白找）。
