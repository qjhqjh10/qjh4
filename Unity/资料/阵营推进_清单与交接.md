# 阵营推进：清单与交接

> 2026-09-12 第十六轮结束时建立。**面向下一个会话**。
> 总入口是 `项目任务.md`；工作准则见根目录 `CLAUDE.md`（每次会话自动加载）；
> 卡牌这条线的技术细节在 `资料/卡牌效果管线_计划与交接.md`。
>
> 用户 2026-09-12 定的方针：**两个两个阵营逐步完成，不追求一次性大规模实现。**

---

## 一、当前状态（2026-09-14 收工）

| | |
|---|---|
| **战术卡** | 完全解析 **442/449** · 不认的句子 **5 种** · 部分 4 · 完全不懂 3 |
| **单位卡 `desc`** | 完全解析 **425/586**（部分 102 · 完全不懂 59） |
| **未实现关键词** | **3 个**：`destroyer` / `ecstasy` / `sabotage`（各有理由，见候选 E） |
| **`When <事件>`** | 认不出的短语 **0 种** |
| **自检** | `RuleEngineTest` **2068/2068** · `BattleScene` 374/0 · `DeckScene` 61/61 · `CardBaseDemo` 全过 |

> ⚠️ **这是「收工那一刻」的快照；权威永远是自检每次重写的那几份**
> （`_tmp_view/tactic_unparsed.txt` / `tactic_by_faction.md` / `when_unparsed.md` / `unit_desc_unparsed.txt`）。
> **数字只写一处，别抄第二份。**

> 🔴 **上一轮（2026-09-14）改了 14 个文件 + 5 个新文档（`git status` 共 19 条），是脏的** ——
> 代码与文档都**已落盘、四条自检全绿**，**只是还没提交**（要按惯例「一轮一提交」就先 commit 这批）。

---

## 一之二、更早做过的事（只留仍然成立的结论）

- **`Ephemeral`（临时）+「移出游戏」区域**（第三十二轮，规则书 `:183` + `:229`）：
  「移出游戏」是**第五个区域**（`BattleContext.Removed`，与弃牌堆分开）；临时身份的判据是
  `IsEphemeral` / `MarkEphemeral` / **`TryTakeOneEphemeral`**；回合结束清扫在 `SweepEphemeral`。
  🔑 **两个坑**：
  ① **「标记临时」不能按卡模板判** —— `CardDef` 是**共享不可变**的，手里两张同名卡是**同一个对象**。
     第一版写 `foreach (手牌) if (IsEphemeral(c)) 移除` ⇒ **只标了一张却把两张都移走**（静默）。
     修法 = `TryTakeOneEphemeral`（**先看关键词、再看标记**）。⚠️ 这是「没有卡实例身份」第三次咬人。
  ② **卡面标记用原版真有的关键词图标**：原版走 `ToggleGlitch()` 换材质，但 **glitch 素材本地没有**
     ⇒ 改用图集里的 `Atlas_trait_icon_ephemeral.png`。⚠️ **位置/大小是我们挑的**（那个 prefab 不在手上）。
- **卡表逐张核对**（第二十九轮）：1118 张卡图独立重抄一遍，修掉 **`subtype` 串列 40 处** +
  **关键词数值被吃掉 72 处**。卡表 `version 5`。修法走
  `工具/gen_cardface_fixes.py` → `数据/游戏数据/cardface_fixes.json` → `gen_cards_engine.py`（**不手改产物**）。
  结论见 `资料/卡表逐张核对_与对账.md`；自检断言 `RuleEngineTest.TestCardFaceFixes()`。
- **关键词 ↔ 图标对照表**：原版图集每个关键词一个 sprite、**文件名就是英文关键词**（78 个），
  和规则书 61 个对上 67 个。→ `资料/关键词图标/关键词与图标_对照表.md`。
  ⚠️ 里面**明确分了两节**：有证据的对照 vs **我的推测**（混沌印记那 5 条是推测，别当结论）；
  另有硬证据说明 `markOfChaos`/`markOfKhorne`/`markOfNurgle` **md5 完全相同**，光看图分不出。
- 更早三轮的机制设计文档：`资料/选牌Choose_数据与设计.md` · `资料/回手与指代_数据与设计.md` ·
  `资料/常驻效果_数据与设计.md`。

<!--
铁律 6：旧的进度叙述（「这一轮做了什么」的流水账）不留在交接正文里。
第二十六～二十九轮各做了什么见 git log（`045ac17` 那个提交）与 `资料/卡牌效果管线_计划与交接.md` §一 的表。
-->

---

## 一之三、🔜 **新会话从这开始**

**第一步永远是先跑一次自检**（命令在本文档 §五）。⚠️ `_tmp_view/` 是 gitignore 的临时目录 ——
**它可能被清空，跑一次自检就全重生，别以为丢了**。
自检会重写这几份 —— **它们才是当前的工作清单**，本节写的数字都会过期：

| 产物 | 是什么 |
|---|---|
| `_tmp_view/tactic_unparsed.txt` | 战术卡的缺口，按频次排（四栏：不认识的句子 / 半懂 / 缺机制 / 打得比卡面宽） |
| `_tmp_view/tactic_by_faction.md` | 逐阵营的覆盖率（候选 D 排批次靠它） |
| `_tmp_view/when_unparsed.md` | `When <事件>` 认不出的短语 |
| `_tmp_view/adjacent_report.md` | 「相邻」**逐卡对账**（锚点定成了什么 / **光环族还没做** / 整句认不出）。⚠️ 第 3 行写「光环族（本轮不做）」是**旧的** —— 用户已定要做，见 **A7** |
| `_tmp_view/unit_desc_unparsed.txt` | 单位卡 `desc` 的缺口 |
| `_tmp_view/probe_*.txt` | 🆕 **逐句解析探针**的输入/输出（**要自己写 `probe_in.txt`**，跑 `EffectParseProbe.Run`；见 §五） |

### 🔜 **接着做（按优先级）**

| # | 做什么 | 从哪开始 |
|---|---|---|
| **A4 批 4** | 战术卡剩的 3 条（**Codex 那族**） | **`资料/战术卡剩余7条_语义查证.md` §六～§八** —— 三条的语义 / 出处(file:行号) / 落点 / **要拍板的点** 都写好了 ⇒ **照着做即可，不用重查** |
| ⛔ **A4 批 3 剩两条** | 偷控制权（`Telephatic Domination`）· 易伤死亡传染（`Spreading Corruption`）**后半** | 同一份文档 §二/§三 —— **有硬理由**（三层权威全没有 / 缺「非棋盘监听者」容器），**别当成漏做重挖** |
| **A5 批 1** | 单位卡 `desc` 缺口 —— **批 1 覆盖 [unit] ① 栏 146 次里的 ≈90 次** | **`资料/单位卡desc与光环_批次划分.md` §二** —— 161 张已**收敛成 ≈20 个句型族 → 4 批 + 3 挂起**，**按族做、别按卡做** |
| **A7 光环** | 🔴 **是 29 张，不是 10 张**（光环 10 + 同族「己方全体 have Y」19，**同一件事**） | 同一份文档 §三/§四 —— 逐卡表 + **动手前必须定的 6 件事**（含「关键词型光环没有单一读点」这个最大的设计选择）。⚠️ **设计稿 `资料/常驻效果_数据与设计.md` 对光环零覆盖**，得先给它新开一节 |
| **A6 按阵营铺** | 剩余战术卡 | `_tmp_view/tactic_by_faction.md`（自检每次重写） |

> ⚠️ **A5 / A6 / A7 三格的数会随着 A4 往前推一直变** —— **这里的数只是「量级」**，
> 真要动手时**读自检重写的那两份清单**（`unit_desc_unparsed.txt` / `tactic_by_faction.md`）。
> ⚠️ **本次收工删掉了 A1/A2/A3 的「已完成」行**（铁律 6：完成清单是噪声）——
> 它们做成了什么、踩到什么，分别在**下文的候选 E / F / G** 与
> `资料/卡牌效果管线_计划与交接.md` §一 那几张表里。

✅ **三处共用判据（**要加新东西之前先看这几处，别写第二份**）**：
- **格位相邻** = `BoardSpec.AdjacentSlots`（「谁算相邻」**全仓只此一处**）
- **伤害公式** = `RuleCore.DamageAfterReduction`（**纯函数**）—— `ApplyDamage` 与 `WouldKill` **都读它**，
  所以「预测会不会打死」与「实际结算」**不可能分叉**
- **「这条 op 有没有机制」** = `EffectText.OpHasMechanism` —— 全局覆盖率与逐阵营表**同源**

---

## 一之四、本轮（2026-09-14）的坑与出处

> 机制名 → 落点的**索引**在 `资料/卡牌效果管线_计划与交接.md` §一·十一（**不在这里抄第二份**）。
> 这一节**只留坑**（铁律 6：「坑」和「出处」是知识，要留）。

### ⏸ 三笔「如实报着、但还没做」的账

1. **`Infinite Biomorphologies` 的「给手牌」作用域** —— 手牌里是**共享不可变的 `CardDef`**，
   没有卡实例可挂加成（手里两张同名卡是**同一个对象**）。**三处都报了**：结算日志 + `unresolved`
   + 覆盖率 ③ 栏（卡面打 `*`）。**要真做得先做卡实例身份**（候选 0 的另一半，独立一轮）。
2. **`chooseeffect` 的挑法是我们的** —— UI 出来之前用 `ctx.Rng` 等概率取 1（与 `choosecard` 同口径、
   同一局可复现）；**原版是玩家从 3 项里选 1**。⚠️ **`Exemplary Warrior` 治 1 还是治 2
   用户还没裁**（它自己写 `heals 1`、`Righteous Fury` 也写）—— `TestChooseEffect` 只钉「≥1」，
   **别替用户钉死**。
3. **`Armorium Cherub` 的整段载荷缺失**没修 —— 卡面前面还应有
   `Flying. Rally: Give +2 Ranged Attack to a friendly unit this turn.`，但 `ReAtTurn` 是 `^…$`
   **锚在整条 desc 上** ⇒ 前面加东西会把「回合开始回到手牌」**直接打断**。**要修得先动解析层。**

### ⚠️ 本轮新发现、还没修的两条（**都不是本轮引入的，别当成回归**）

- `Cadian Honour` 的尾句 `and reload their Duty abilities` **被吞进目标短语里** ——
  ⇒ `reload` 从来没发生过，而且**不报错**。同族：`ReReloadDuty` 只认句首的 `Reload …`，不切尾句。
- `When an enemy gets a Hunt Mark, give +1 to your units this turn`（`Thunderwolf Cavalry Pack Leader`）——
  `gain` 把**条件从句**整段吃成了目标（`目标[enemy/any ×1 「when an enemy」]`）。
  **是这个工程明确记过的坑**（条件从句不许当主语），原来只挡了 `take(s) N damage` 那条路。

### 🔴 数据侧的两条约定 + 一条**没核完**

- **裸 `+N` 的兜底判近战**（`GivePayload`）**改不成**（正则拿不到「卡面第几个图标是什么」）
  ⇒ 只能**回去修卡表数据**。写法按 `cardface_fixes.json` 既有约定用 `+1 Attack` / `+1 Ranged Attack`，
  **不是**卡面上的 `Melee`/`Ranged` 字形。本轮修了 **10 张**（逐卡清单与实读证据在
  `cardface_fixes.json` 的 `_manual_desc_note`；⚠️ 另有 **14 处判近战是对的，不许改坏**）。
- **督军卡没有费用六边形** —— 本轮把 5 张 Aeldari 督军的费用从 **5 改成 0**（那个 5 是**阵营徽记**
  被 OCR 读成的数字），证据在 `工具/gen_cards_engine.py` 的 `STAT_FIXES`，**主对话逐张开图复核 5/5**。
  🔴 **仍有 9 张督军 cost 非 0，一张都没开图核过** —— 断言里明写着「别当成已核」。
- **数值修正表不在 `cardface_fixes.json` 里** —— 它走 `gen_cards_engine.py` 的 **`STAT_FIXES`**（每条带卡图路径）。

### 🔜 **A 组之外、用户点名要做的一件**（2026-09-14）

| 做什么 | 为什么/怎么做 | 出处 |
|---|---|---|
| **🔴 跑原版游戏，把「选牌 / 选效果面板**展开后**长什么样」拍下来** | 现有证据只有**静态快照**（`runtime_ui_dump_drive_0912.tsv`）：面板的两棵节点树都在，但**卡片是运行时生成的、dump 里一个都没有** ⇒ **展开后长什么样完全没有实况证据**。**这是表现层（批 5「选效果」）动手前必须先补的一步**（铁律 4：能跑到实况就要跑到实况） | `资料/选牌Choose_数据与设计.md` §四之二 末（🔴 那段）；跑法见 `项目任务.md` §八「进原版游戏」 |

> （**光环 / 常驻层那 10 张**已经从「A 组之外」升成正式的 **A7**，见上表。）

> ✅ **A1 动手前必须先做的那件事已做**：`RuleCore` 里 Blast / Stomp 那两份**内联**的格位判据
> 已抽成一份 —— `BoardSpec.AdjacentSlots(slot, into)`（**「谁算相邻」全仓只此一处**，加 `^` 行尾注释）。
> 三处都在读它：践踏 · 爆裂 · 新的「相邻」目标。**往上加之前先抽**这条已经兑现，别再抄第四份。
> ✅ **A3 同类的一处**：事件**宾语**的判据收在 `WhenEvents.Matches` 一处（`TargetOwnerIs` / `TargetCriteria`），
> 广播方只负责**把宾语传进来**（`BroadcastWhen(..., target:)`），不自己筛。

### ✅ 候选 G：**`When` 短语剩下的 4 条** —— **2026-09-13 A3 做完**（就是下面那 4 张卡）

| 卡（阵营） | 卡面 | 怎么接的 |
|---|---|---|
| `Long Fang`（SpaceWolves） | `When this unit attacks an enemy with Hunt Mark, deal 3 damage to adjacent enemies` | 攻击广播**带上宾语** + 事件层新增**宾语筛选** + 正文裸 `adjacent` 锚点 = **事件宾语** |
| `Sisters Repentia`（Sororitas） | `When this unit kills an enemy, gain 1 ☀` | 新事件 `kills`；**判据与 `Slay:` 关键词共用一份**（原版只有一个击杀时机） |
| `Ravenwing Ballistus Dreadnought`（DarkAngels） | `Armour 1. When you create **or play** a Secret, deal 2-4 damage to all enemies` | `WhenEvents.ParseAll` 让**一句话产出两条**监听器（两半都认得出才收） |
| `Bright Lance Vyper`（SaimHann） | `When you trigger a Spirit Stone ability, gain Sniper and +1 Ranged Attack` | 新事件 `spiritability`，落在**花灵魂石那一点**。⚠️ **卡池 0 张带 `N [Spirit Stone]:`** ⇒ 点亮但不响 |

**新增的两维（事件层）** —— 别再加第三套：
1. **宾语** `WhenEvent.TargetCriteria` / `TargetOwnerIs` —— 筛「**对谁**做的」（`attacks <谁>`）。
   与 `Criteria`（筛「**发生在谁身上**」）**不是一回事**，合并 = 拿被打者的兵种去筛攻击者。
2. **做者自指** `WhenEvent.ActorSelf` —— 要求「这件事是**监听者自己干的**」（`this unit kills …`）。
   与 `SelfOnly`（这件事**落在**监听者身上）**方向相反**；用错那个方向的后果是
   **永远不响**（`kills` 拿 `SelfOnly` 判，等于要求「被杀的**是**监听者自己」）。
   判据在 `WhenEvents.Matches` 一处；广播方**不传 `actor` / `target` 就一律不触发**（宁可收不到）。

🔴 **顺手修掉的三条真问题**（都不是这四条短语本身）：

| 症状 | 根因 | 修法 |
|---|---|---|
| `Valtus` 的 `deal 3 damage to the target of the attack` **打的是自己人** | 那个短语**不在 `IsPronoun` 一族**里 ⇒ `ParseTarget` 兜成 `Side=any/Kind=any` ⇒ 从**全场** pick 一个（实测挑中最左那个自己人）。**句子解析得干干净净、卡面不打 `*`** | 新增**事件宾语**指代（`Side="eventtarget"` → `ctx.EventTarget`，原版 `TargetsAffected.target = 30`） |
| 单位触发正文里**裸 `gain X`**（40 处里的十来张：`Farseer` 的 `gain Shield` · `Termagant Brood` 的 `gain +1 Ranged Attack` · `Sisters Repentia` 的 `Penitence: Gain +2 Attack` …）**给全队各加一份** | `TryGain` 兜底的「没写主语」被当成「写了 `your units`」⇒ 落到己方全体。`rule_core.gd:3167` 那句自己标着「**既有近似**」，**不是原版语义** | `EffectTargetSpec.Subjectless`：**有施放者（单位触发正文）⇒ 就是它自己**；没有施放者（战术卡）才落到己方全体（行为不变）。⚠️ **`TestKeywordTrigger` ④-a 的两个数因此改了**（3 / 4，见那一段的注释） |
| `破坏` / `灵魂石` / `信仰` 三个词**既不实现也不报出来**（静默丢弃，两张诊断单都看不见） | 三个词**不在 `Prefixes` 里** ⇒ `Normalize` 返 null ⇒ 构造与 `Parse` 双双丢弃 | 加进 `Prefixes`（**只做「认得出 + 报得出」**，机制仍留到对应阵营）。⚠️ **加之前量过**：只有 5 条卡表 `keywords` 会新命中（`Sabotage`），**卡面 desc 分句 0 条**受影响 |

**两条查实的原版语义**（别再重查）：
- **「kills」在原版就是 `Slay`（`AbilityTrigger.Slay = 120`）本身** —— 全枚举里**唯一**与击杀相关的值，
  没有 slain / destroyedBy 之类的第二项；击杀者由 `AddTriggerSlay(targetCard, actingCard, …)`
  **显式传参**（`BattleManager.cs:6477`），结算时判 `IsInPlayOrDying()`（`ResolveTriggerSlay.c:129`）。
  ⇒ 「`Slay:` 关键词」与「`When this unit kills an enemy`」是**同一件事的两种写法**，判据只写一份
  （`RuleCore.DeclareAttack` 里那个 `killed`）。
- **「Spirit Stone ability」= 卡面 `N [Spirit Stone]: …` 的能力**（以灵魂石计价），
  「trigger」= **花石激活**那一下 —— `AbilityTrigger.UseSpiritStone = 600` · `CardScript.CanUseSpiritStone()`
  （`CardScript.cs:2712` 要求 `HasAbilityType(card,600)` 且石头够）· `BattleActionType.useWaystone = 76`
  （付石）→ `triggerSpiritStone = 77`（激活后广播）。**规则书没有这个词条**，只能由代码 + 卡面反推
  （`Cosmic Serpent` 的 `Trigger the abilities **requiring Spirit Stones** …` 互证）。
  ⚠️ 与 `collect`（`OnCollectSpiritStone = 610`）**是两件事**，别合并。

⚠️ **两条「点亮但打不出来」**（**不是漏做**，别挂成待办、也别算进「已铺完」）：
- `you create or play a Secret`：**「play」那一半打得出来**（5 张隐秘本体是 2 费战术卡），
  但**「create」那一半没有触发源** —— 全池没有一张卡把隐秘**造进手牌**（`Quest` 与 `Secret Agenda`
  都是**往牌库加**，不走 `DoCreate` 的 hand 支）⇒ `Ravenwing Ballistus Dreadnought` 靠 play 那半能响。
- `you trigger a Spirit Stone ability`：**没有卡带灵魂石付费前缀** ⇒ `Bright Lance Vyper` 一次都不会响。

**验收**：`RuleEngineTest.TestWhenEvents` ⑬/⑭/⑮/⑯（解析层 + **真卡**监听器 + 端到端 + **反例**：
没标记的不响 · 别人打的不响 · 打不死不算 · 队友数值不许变）。**四条自检全绿**（数字见 §五）。

### 🔄 候选 H：**A4「战术卡不认识的句子」** —— 2026-09-14 收工时状态

**规格书**：`资料/战术卡剩余7条_语义查证.md`（**批 3 / 批 4 七条逐条：语义·出处·落点·硬理由**）
+ `资料/战术卡47条_语义查证.md`（第三十二轮那 47 条）。
**活清单**：`_tmp_view/tactic_unparsed.txt` ① 栏（自检每次重写）——**别抄这里的数**。

**进度口径**：分句 **44 → 5 种不认**（完全解析 **401 → 442/449**）。
**批 1（六条便宜机制）· 批 2（强制攻击族）· 批 5（选效果族）已收工**；
**批 3 做完 2 条、2 条挂起**（⚠️ 挂起有硬理由，**别重挖**）；
**批 4（Codex 那族 3 条）是下一个要做的**。

> 📜 **批 1 / 批 2 的明细（含当年顺手修掉的 7 条真问题）已挪到
> `资料/历史/阵营推进_A4收尾批1批2.md`** —— 铁律 6：完成清单不进交接正文。
> ⚠️ 机制名 → 落点看 **`资料/卡牌效果管线_计划与交接.md` §一·九 / §一·十 / §一·十一**。

**🔴 用户 2026-09-14 给的「强制攻击」口径**（**比反编译查到的准，照这个做**）：
> 「**就当做这个单位进行的普通攻击就行** —— 攻击类型**根据它近战/远程里最高的那个攻击力值**决定；
> 打谁**由效果决定**（随机还是确定的目标）。**卡面没写打谁时，优先选择可以摧毁的单位。**」

⇒ 那几条不再悬着：**反击伤害 / 事件广播**按普通攻击处理（走 `RuleCore.DeclareAttack`）·
**攻击类型取高的**、**不兜底**（打不动就如实报「打不了」，**别偷偷换另一种**）·
**打谁**：写了 `a random enemy` / `lowest Health` / `highest attack` 就照卡面，
**没写才**走「优先打得死的」（`RuleCore.WouldKill` + `EffectResolver.PreferKillable`）。

**⏸ 两笔「有理由地没做」**（**不是漏做**）：`destroyer`（规则书 `:180` 只说「优先攻击可被摧毁的单位」，
判据已现成但**没接在普通攻击上**；用户尚未裁这条要不要做）· 批 3 的偷控制权 / 死亡传染后半。

**验收**：`TestA4PaidPrefixAndAtoms` · `TestA4Batch1Atoms` · `TestA4Batch2ForceAttack`（第 ⑥ 块是本轮加的）·
`TestChooseEffect` · `TestA4Batch3`。全在 `RuleEngineTest.Run` 里跑，**数字见 §五**。
### ✅ 候选 F：**「相邻」** —— **2026-09-13 做完**（A1）

**做了什么**：卡面写 `adjacent` 的 **36 张**，现在 **26 张效果目标形全部接上**（逐卡对账见
`_tmp_view/adjacent_report.md`）·**光环那 10 张：用户 2026-09-14 定了「要做」**（要新开常驻层，见下）。收敛成：

| | 张数 | 说明 |
|---|---|---|
| **已接锚点** | **25** | 锚点在解析期定、结算期消费 |
| **锚点认不出（如实打 `*`）** | **1** | `Long Fang`（`When …` 的锚点在事件参数里，文本层定不出来） |
| **整句本来就认不出** | **2** | `Long Fang`·`Venerable Dreadnought` 的 `When` 短语还没接（是 **A3** 的账，不是「相邻」的） |
| **光环族** | 🔴 **是 29 张，不是 10 张** | **🔴 用户 2026-09-14 定：要做**（原来记的是「本轮不做」）。⚠️ **2026-09-14 更正两处（铁律 5）**：① 计数 —— 「相邻型」**10 张** + 同族「己方全体 have Y」（`Friendly/Your other/Other friendly X have Y` + `Enemies have Y`）**19 张**，**是同一件事**（筛选维度换一下而已）⇒ **合计 29**；逐卡表见 **`资料/单位卡desc与光环_批次划分.md` §三**。② **原版证据写错了一个** —— `BattleManager__ShouldTriggerAdjacentDamage.c` **不是光环**，它整个方法体是 `return 0 < get_CurrentBlast(target)` = **爆裂**的判据（我们**早就实现过**，`RuleCore.cs:1213`）。**光环的原版证据**只有 `CardScript.HasWhileInPlayAdjacentEffect`（**真有方法体**：`decomp_il2cpp_0827/decomp_out/CardScript__HasWhileInPlayAdjacentEffect.c`）——⚠️ 它是个**谓词**，而且**在 1800 个 `.c` 里没有任何调用者** ⇒ 「原版拿它干什么」**查不到**。逐卡对账见 `_tmp_view/adjacent_report.md`，**这 10 张是**：`Baneblade Tank`（AM · `Adjacent units have Armour 1`）· `Genestealer Familiar`（GS · `+1`）· `Damaged Plasmacyte`（Necron · `Regeneration 2`）· `Nemesor Zahndrekh`（Necron 督军 · `Adjacent Remnants do not disappear at the end of your turn`）· `Banner Nob`（Ork · `+1 [attack] and +1 [weapon]`）· `Makari the Grot`（Ork · `+1 Attack`）· `Wolf Guard Battle Leader`（SW · `Pack`）· `Enforcer Battlesuit`（Tau · `+1 Ranged Attack`）· `Honoured Ethereal`（Tau · `Vanguard`）· `Honour Guard`（UM · `Armour 1`）<br>⇒ **和 A5 的那一族（`Friendly X have Y` / `Your other units have Y`）是同一件事 —— 一起做，别分两笔账**；设计稿在 `资料/常驻效果_数据与设计.md`。⚠️ 两条纪律：① 光环是**持续**效果（人走效果没），**不能实现成一次性 `give`**；② 「谁算相邻」只读 `BoardSpec.AdjacentSlots`（**全仓唯一判据**） |

**落点**（四处，改哪一处都先看这四条注释）：

1. **格位判据**（唯一来源）：`BoardSpec.AdjacentSlots` —— 输入槽位、输出左右紧邻格（`IsValid` 0..8、
   **不排除督军格**）。`RuleCore` 的践踏 / 爆裂、`EffectResolver.ResolveTargets` 三处都读它。
2. **锚点枚举**：`EffectTargetSpec.Anchor`（`AdjacentAnchor`，**照抄原版 `TargetsAffected.cs:17-22` 的枚举值**，
   不再是一个 `bool`）+ `AnchorInSet`（卡面点名了锚点本体 ⇒ 它自己也在目标集里）
   + `AdjacentAll`（复数字面 ⇒ 那一圈全要）+ `AdjacentFailed`（认不出 ⇒ 整句降级成半懂）。
3. **解析期判据**：`EffectText.ParseTarget`（短语内可见的五支）+ `EffectText.FillAdjacentAnchors`
   （**要跨句上下文**的那几支：同句前文点过目标 / 上一句点过目标 / 句里有 `the target` / 都没有则「自己」）。
4. **结算期**：`EffectResolver.ResolveTargets` 里 `if (spec.Adjacent)` 那一段 —— 锚点 → 相邻槽 →
   **过同一套筛选**（`pool.Contains`）→ 取几个（复数全要 / 单数按槽号最小）。
   ⚠️ **锚点拿不到就空过并如实报**，**绝不退回整个目标池**（那正是「打得比卡面宽」）。

**两条查实的原版语义**（别再重查）：

- **相邻 = 同方棋盘行内的左右紧邻格**。原版 `BattleManager.GetAdjacentUnits`（反编译
  `decomp_out/BattleManager__GetAdjacentUnits.c`）按单位的**所属方**取那一方的
  `MinionManager.GetAdjacentUnits` —— **不跨排**。
- **相邻单位要过同一套筛选**（阵营 / 兵种 / 关键词）。原版 `CardScript.TargetedSpellPlayed`
  （`decomp_out/CardScript__TargetedSpellPlayed.c:60-70`）对相邻单位逐个调 `criteria.Matches(...)`。
  ⇒ 我们这边用 `pool.Contains(u)` 判（`pool` 正是「过了筛选的那一批」）。

**解析规则的依据 —— 卡面实际有 5 种形状**（派子代理逐张看过；**判据就是照着这张表写的**）：

| 形状 | 文本长相 | 锚点 | 例卡 |
|---|---|---|---|
| **A 施放者自己** | 裸 `adjacent X`，前面**没点过**任何目标 | `Self` | `Nuadhu Fireheart`「Strike: Give Invulnerable to adjacent troops」· `Chaplain Letharius`「Heal 1 to adjacent units」 |
| **B 被打目标** | 明写 `its` / `to the target` | `PreviousTarget` | `Cleansing Flames`「Deal 3 damage to an enemy and its adjacent units」· `Wailing Doom`「units adjacent to the target」 |
| **C 督军** | `your Warlord and adjacent …` | `FriendlyWarlord` | `Ethereal Supreme` · `Holy Fire` |
| **D 自己，明写** | `this troop and its adjacent units` | `Self`（**且锚点也在目标集里**） | `Apothecary Polixis` |
| **🔴 E 静默锚点** | 文字与 A **完全同形**（裸 `adjacent units`），锚点却是**前面那个目标** | `PreviousTarget` | `Whirling Death`「…If **the target** dies, deal 2 damage to adjacent units」· `None Must Know` · `Methodical Destruction` · `Long Fang` |

⇒ **A 与 E 文本同形**，只能靠「这一段里此前点过目标吗」（含**上一句**）消歧 ——
所以锚点判据有一部分必须放在 `Parse()` 的**回填**里（`FillAdjacentAnchors`），逐句单看是分不出来的。

⚠️ **一条卡面核对的副产品**：**`Forged Killers` 卡面确实没写 `its`**（`Give +2 to a friendly troop and
adjacent troops`），锚点靠语感 = 刚给的那个 troop；而**我们的中文翻译加了「其」**
（`zh_CN.csv:1484`「给予一个友方部队**及其**相邻部队」）—— **别据中文反推英文**。
6 张卡面逐字核过，**没有抄错**，也没有 `neighboring` / `next to` 这类变体写法。
（`Stormhawk Interception` / `Dreadnought` / `Gargantuan Squiggoth` 同形，一起被「同段前面点过目标」那条收掉。）

🔴 **顺手修掉的三处真 bug**（都是「静默少做一半 / 打错人」那一类，**都不是「相邻」本身**）：

| 症状 | 根因 | 修法 |
|---|---|---|
| `Stun adjacent units`（`None Must Know`）**眩晕的是敌方最左边那个** | `TryStun` **一个字都不解目标**（`op.Target` 恒为 null → 结算层兜底「一个敌方单位」），而且那一条**直接置 `SegKind.Ok`** ⇒ 卡面不打 `*` | 照 `TryBlind` 补上 `ParseTarget`，并改走 `Finish`（半懂要报出来） |
| `Destroy an enemy troop **and deal 2-3 damage** to adjacent units`（`Methodical Destruction`）**只做了一半** | `ReDeal` / `ReStun` / `ReDestroy` **没有 `^` 锚定** ⇒ 从句中匹配，把前面那半句**静静吃掉**（deal 抢先时吃 destroy，destroy 抢先时把整串当目标短语） | 三条正则全部 `^` 锚定 + `TryDestroy` 补 `SplitAndTail`（和 `TryDeal` 一致） |
| 单位卡覆盖率**虚高 73 张** | 覆盖率把 `Rally: Deal 3 damage …` 整句丢给解析器，**从句中匹配**报「干净」—— 而引擎真正走的是 `CardDef.AddTriggerOp` 的**冒号后那段** | `ParseSegment` 认**可路由触发前缀**（`CardDef.RoutableTriggers` 那 8 个）时改成解析正文 |

> ⚠️ **覆盖率数字因此变了，别当成退步**：单位卡 **349 → 358**（完全解析，**更诚实也更准**）、
> 战术卡 **403 → 401**（`stun` 的目标解析出来了，两句原来「假干净」的变成半懂）。
> 数字只写一处：`资料/阵营推进_清单与交接.md` §五。

> ⚠️ **2026-09-13 更正（铁律 5）**：本节原来写「光环那 10 张现在走普通一次性路径 ⇒ **给己方全体加**（打多了）」
> —— **不成立**。实测 `Makari the Grot` / `Baneblade Tank` / `Honour Guard` 的 `desc` 都**不算解析干净**
> （光环句根本没被认领），所以它们**对谁都没生效**，不是「打多了」。断言见
> `RuleEngineTest.TestAdjacent` 第 ⑤ 块（**红了就是有人把光环实现了**，回来更新本节）。

**验收**（都做了）：`RuleEngineTest` 新增 `TestAdjacent`（锚点=目标 · 锚点=自己 · **反例** · 认不出就空过 ·
单复数 · **光环如实报**）——**四条自检全绿**（数字见 §五）；「还大于 0」那条探针已换成两条**真断言**
（① 不许有卡「解析得出来、锚点却没定」② **光环族必须一直报出来，直到真做完**）。

**剩下没做的**：🔴 **光环 29 张 —— 用户 2026-09-14 定了要做**（见上面「光环族」那一行：
新开**常驻层**，和 A5 那族一起做）；`Long Fang` 的锚点要等 **A3**（`When` 短语，已做）；
`Stealth Drone` / `Master of Ordnance` 那类 **`atturn` 形态的单位卡**走的是**手牌陷阱**那条扫描
（`ResolveAtTurn` 扫手牌**不看卡类型**）—— 那时 `source` 是 null，`Self` 锚点拿不到 ⇒ **空过并打日志**
（**既有问题**，不是本轮引入；单位卡的「回合起止触发」这一层还没有）。

### ✅ 候选 0（**第三十三轮已做完**）：**给卡发稳定 id** —— 规则书审计第③条

**结果**：卡表 `version 6`，每张卡都有唯一 id（原版优先、对不上的自造）。
落点：`RuleCore.CostOf` 的 `CostMod.Key` 从卡名改成 id ⇒ **同名卡不再一起降价**；
卡组引用解析收成一处 `CardDatabase.DeckLookup`（**先 id，找不到再退回「卡名 + 阵营」**，
第二条路留给旧存档 / 旧分享串，**别删**）。出处：`资料/规则书_实现指南对账.md` §一·③。

**两条查实的事实**（别再重新挖）：
- **id 前缀 ↔ 阵营是原版数据**：`数据/游戏数据/decklists.json`（236 副原版预组牌）真带
  `heroId:"AM3"` + `cardIds`，**13 个前缀与 13 个阵营 1:1**。
- **`card_ids.json` 的 id↔卡名是推出来的**（它自己的 note 写着「PnP 卡表编号 = 游戏卡ID 偏移 0」）⇒
  拿 PnP 卡图核过：**818 处逐字一致 · 19 处写法差异 · 159 处编号对不上**，那 19 处绝大多数是
  **同一张卡的不同写法**（`Sylar Hexcorn`↔`Sylar-Hexscorn` · `Rusted Vents`↔`Rusted-Vent`）。
  ⇒ **可用，但不是逐条验过。**

**⚠️ 还没做的（审计③的另一半）**：**卡实例身份** —— 手里两张同名卡仍然是**同一个 `CardDef` 对象**，
所以「给**这一张**降费」会落到该 id 的**所有副本**上（比按卡名好，但仍不等于原版的复制品语义）。
规则书附录那批「复制品 -1/-2」要精确，得让手牌存**实例**。**这是独立的一轮。**

### 候选 A：**事件层** —— ✅ **2026-09-13 第三十四轮全做完**

设计、判据、逐条落点见 **`资料/事件层_数据与设计.md`**（那里是正文）。**这里只留坑**：

🔴 **`reanimated` 原来是个真 bug（已修）**：只设了 `Kind`、**没设自指标记** ⇒ 按「**任何单位**被再造」收，
任意一张 SAU 在场时**别人（含敌方）被再造**都会响，`SAU72 Immortals Phalanx` 的
`deploy a copy of this troop` 会去复制**别人**。原有用例只量了「自己翻自己」，所以一直没露头。
⇒ 才有了 `WhenEvent.SelfOnly`。

🔴 **两处「已经点亮了、却一次都不响」的静默失效**（派子代理逐条核 `When` 时翻出来的）：
① 判据只收 `CardDef` —— 猎杀标记是效果在**运行时** `AddKeyword` 加的、**卡面没印**
   ⇒ `When an enemy with Hunt Mark dies` 永远 false。新增 `CardCriteria.Matches(UnitState)`；
② 关键词**没归一化** —— 判据那侧存卡面原话 `"hunt mark"`，卡表那侧存规范键 `"huntmark"`
   ⇒ 换成 `UnitState` 也还是永不相等。加 `CardCriteria.NormKeyword()` 一处归一化。

⚠️ **事件触发式降费那一族必须排在「场上没人听就 return」之前** ——
它挂在**玩家身上**、和自家场上有无单位无关，放后面会被那条早退**静默跳过**。

**原版依据（照它设计，别凭直觉）**：**牌在手上就要监听**。原版广播器**四支**
（自己 → 场上每张牌 → 当前回合方手牌 → 另一方手牌，`BattleManagerSupport__BroadcastUnitSummoned.c:23,41,53,65`）；
牌入手牌那一刻就挂（`PlayerHand__SetupCardInHand.c:60-73`）。
自指在原版靠**枚举值分「本卡 / 他卡」**（`ThisUnitSummoned=195` vs `OtherUnitSummoned=190`），
**不是**「牌自己监听自己」。

⏭️ **`When` 这一侧还剩什么**：**别抄这里的数** —— 权威是 `_tmp_view/when_unparsed.md`（自检每次重写）。
⚠️ `Ravenwing Champion`（`When you create a Secret`）实测是**「点亮但打不出来」**：
卡池里**没有任何卡会造隐秘**（18 张提到 secret/sabotage 的卡，造的全是 sabotage）—— **别把它算进「已铺完」**。

### ~~候选 B~~：**单位卡 `desc` 的效果文字** —— ✅ **2026-09-13 第三十一轮做掉了大头**

**已做**：`Rally / Strike / Slay / Backlash / Penitence` 这 **5 个触发点**的正文，**123 张卡**（见 §一）。
**还剩两族**，都归到候选 A：
- **`When <事件>, …`** —— **78 条 / 78 张**（要事件层）
- **`Talent / Pray / Duty / Agenda / Mob / Regiment / Artifice / Codex`** —— **212 条**（要新的触发时机）
  ⚠️ 其中 **`Codex` 18 条解析器早就会**（`Dispatch` 里就有 `codex:` 那条路），缺的**只是触发点**
  （「你的能量为 0 时」）。

### ~~候选 C~~：**卡面图标对真关键词** —— ✅ **2026-09-13 第三十一轮做完**

**1968 处占位符还原 1965（99.85%）**。工具 `Unity/工具/restore_card_icons.py`，
产物 `资料/卡表核对_卡图提取/_还原效果文字.md`。

⚠️ **原计划（模板匹配）被实测否掉了，别照那条做** —— 两条硬证据：
① **卡面上关键词图标旁边就印着英文词**（真卡确认），答案本来就在文本里，不用认图；
② **形状名不能当键**：78 个图标被 29 个子代理起了 **180 个不同形状名**，
`【图标:骷髅】` 130 次横跨 `backlash`/`remnant`/`strike`/`synapse`/`regiment` **五张不同的图**。
详细与「同名不同图」（`齿轮` = `artifice.png` **和** `questPointsN.png` 两张）见
`资料/关键词图标/关键词与图标_对照表.md` §零。

### ✅ 候选 E：**关键词补全** —— **2026-09-13 做完（14/16）**（A2）

**做了什么**：`RuleCore.UnimplementedKeywords()` 从 **16 → 2**。逐关键词的语义、出处、落点：

| 关键词 | 语义（规则书） | 落点（代码） | 卡数 |
|---|---|---|---|
| `artifice` 巧技 | `:168` 每次打出战术时触发 | `EffectResolver.PlayTactic` 末尾 | 15 |
| `duty` 职责 | `:181` 一次性；`:152` 无法攻击时也能激活 | `RuleCore.UseAlternative`（`UnitState.DutyUsed`） | 23 |
| `pray` 祈祷 | `:198` 缓慢非战斗行动（部署当回合不能动） | 同上 + `WhenEventKind.Prays` 只在真祈祷时发 | 10 |
| `ferocity` 狂暴 | `:186` 快速；执行完**洗回牌库** | 同上 + `UnitState` 构造里与 fast/flank 并列豁免 | 11 |
| `agenda` 议程 | `:165` 以触发效果代替攻击 | 同上（每回合可用，**不是**一次性） | 12 |
| `swarm` 虫群 | `:216` 打出在**右侧同名部队**旁边时合并 | `RuleCore.TrySwarmMerge`（`UnitState.SwarmUnder`） | 11 |
| `synapse` 突触 | `:217` 被**友方战术**选中时对**相邻**重复效果 | `EffectResolver.RepeatTacticOnAdjacent`（`ctx.SynapseBusy` 防递归） | 13 |
| `uprising` 起义 | `:222` 之后每部署一个部队时触发 | `RuleCore.PlayCard` → `FireTriggerOnSide(..., except: unit)` | 9 |
| `stimulation` 激励 | `:212` 被战术选中时、**结算前**触发 | `EffectResolver.PlayTactic`（结算之前） | 4 |
| `tide` 潮涌 X | `:220` 打出时给 X 张复制（费用同首张） | `RuleCore.SpawnTideCopies`（复制品 `MarkEphemeral`） | 24 |
| `remnant` 残骸 | `:203` 死亡翻面留在场上；受伤害/回合结束被摧毁 | `CleanupDeaths` · `DestroyRemnants` · `DoReanimate` | 28 |
| `teleport` 传送 | `:219` **当回合从牌库抽到**即打出时触发 | `PlayerState.DrawnThisTurn` + `PlayCard` | 8 |
| `ambush` 伏击 | `:166` 面朝下打出；挨伤害翻开无效果、撑一轮才触发 | `UnitState.FaceDown` + `ApplyDamage` / `RevealAmbush` | 13 |
| `companion` 伴生 X | `:176` 从手牌打出时带出至多 X 张伴生部队 | `RuleCore.PlayCompanions` + `CardDef.CompanionName` | 7 |

🔴 **没做的两个 —— 都有理由，别当成漏做**：
- **`ecstasy` 狂喜 X**（10 张）：**两条卡点**没解（卡表的关键词值不可靠 + `AddTriggerOp` 收不下
  `Ecstasy N:` 这种正文）。第三十四轮就推迟了，断言还钉着（`TestUnstableEcstasyCruelty` 块②）。
- **`destroyer` 毁灭者**（10 张）：规则书中文版只说「**优先**攻击可被摧毁的单位」；
  唯一的代码证据是 `BattleManager.IsValidAttackTarget(..., bool isCheckingDestroyerTargets)`
  这个**参数名** —— 顺着查过去，那个分支实际判的是 **`frenzied`(680)**，**没拿到 destroyer 的判据**。
  ⇒ 按「**宁可认不出，别猜**」留着：猜成硬约束（「只能打打得死的」）会让单位一张牌都打不出去。
  **下次要做的**：查 `BattleManager__IsValidAttackTarget.c` 全部 345 行 + `CheckIfAttackKillsTarget` 的调用者。

**🔴 顺手挖出的一条结构性缺口（A2 里最重要的一条）**：
**卡面没写 `关键词:` 前缀时，整条 `desc` 就是那个关键词的正文**（实测 **200 张**卡是这样，例如
`Dark Apostle` 的 `kw=['Artifice']` + `desc='Give a random Dark Pact to each friendly troop'`、
`Blood Claw` 的 `kw=['Ferocity']` + `desc='Deal 3 damage to an enemy'`）。
而 `CardDef.AddTriggerOp` **要求冒号** ⇒ 这些卡的正文**一条都收不到** ⇒ 效果**静默不发生**。
修法 = `CardDef.CollectBareKeywordBody`（2026-09-13 加）：正文里没有前缀、且本卡**唯一**的
「带正文关键词」就是它 ⇒ 整条 desc 记成它的正文。**两个以上就是歧义，不收**（`Morkai Eliminator`）。
⇒ 「触发式效果收到正文的卡」**145 → 314 张**（自检每次报）。

**四条（相邻/爆裂/践踏/突触）共用一个前置**：格位相邻抽成了 **`BoardSpec.AdjacentSlots`**
（**「谁算相邻」全仓只此一处**）。**要加第四处消费者之前先看它。**

⏭️ **这一侧还剩什么**：`_tmp_view/when_unparsed.md`（自检每次重写）—— **4 种**短语认不出：
`you trigger a spirit stone ability` · `you create or play a secret` · `this unit kills an enemy` ·
`this unit attacks an enemy with hunt mark`。**都不是关键词缺机制**了，是 A3 自己的活。
⚠️ `Ravenwing Champion`（`When you create a Secret`）实测是**「点亮但打不出来」**：
卡池里**没有任何卡会造隐秘**（18 张提到 secret/sabotage 的卡，造的全是 sabotage）—— **别把它算进「已铺完」**。

**📚 已查证但已经写进代码的语义**（**别再重查**，要看细节直接读落点那几个函数，注释里带出处）：
- `ferocity` 的「快速」**没有定义段**（全书没有）：唯一已知差别是 `:98`「部署当回合能不能动」，
  而 `:98` 那句话里**点名了狂暴** ⇒ 我们让它和 fast/flank 一样豁免。
- `duty` 的「一次性」：规则书**没说是本局一次还是每回合一次** ⇒ 我们按 `:181` 的「一次性能力」
  取**本局一次**（`UnitState.DutyUsed`，不随回合重置）。**装填（reload）动词没做**（在 A4 的清单里）。
- `swarm`/`synapse` 的**原版都是硬编码机制**（`CardTrait` 的子类只有 Duty/Ferocity/Oath 三个），
  所以它们的语义只能从规则书 + 反编译两处交叉取证。


🔴 **卡表层面的数据错（还没修，做那一族时要绕开或修掉）**：
- `ferocity`：卡面核对 **13 张里只有 9 张真带**，另 4 张（`Njal Stormcaller` / `Deathwolf Scout` /
  `Bjorn's Shrine` / `Raid Tactics`）**只是正文里提到它**；另有 **desc 吞前缀**（`Pack. Ferocity: …`
  在卡表里只剩冒号后半句）。
- `duty`：**6 张卡的 `desc` 丢了 `Duty:` 前缀**（`AM41`/`AM19`/`AM10`/`AM27`/`AM24`/`AM_Hektor_Thenmann`）；
  **2 张的 `keywords` 误抽了 Duty**（`Commissar` · `Press the Attack` —— 它们只是**提到** Duty）。
  ⚠️ **`[Duty]` 写法在卡池里 0 张** —— 方括号是**本工程数据层的图标占位**，原版卡面不存在。
- `swarm`：卡面核对 **11 张（不是 12）**（`Norn Emissary` 卡面只印 `Armour 1. Blast 3.`；
  `Swarming Masses` 是**战术卡**）；`synapse` **13 张（不是 14）**—— 都是「描述里出现该词」被 OCR 当成拥有。
- `Termagant Brood` 卡表写 `+1 [Attack]`，**卡面是 `+1 [Ranged]`**（紫枪图标）。
- ⚠️ **11 张 Swarm 卡的卡名互不相同** ⇒ 规则书说的「同名」**只能来自同一张卡的多份**
  （规则书 `:280` 各 3 张 + `Tide` 复制同名卡）。

**原版侧的对照出处**（查过了，别再翻）：`DefinedTrait.cs:130 ferocity=1265` / `:131 dontReturnFerocity=1270`
（Bjorn 的「stays in play」要用）· `AbilityTrigger.cs:97 Ferocity=725` · `:88 Uprising=680*` ·
`CardTraitDuty` / `AbilityTrigger.TriggeredDuty=661` / `AbilityEffect.resetDuty=475`（**装填**，我们没做）·
`DefinedTrait:65 swarm` / `:113 synapse` · `CardScript.synapseCard:1526` / `UnitSwarmSignal`。
⚠️ **`CardTrait` 的子类只有 3 个**（Duty / Ferocity / Oath）—— swarm / synapse / remnant 这些
在原版都是**硬编码机制**，所以语义只能从规则书 + 反编译两处交叉取证。

### 📌 候选 F 的勘察记录（**已兑现**）

「相邻」这件事**2026-09-13 做完了** —— 结论、落点、顺手挖出的三个真 bug、验收，
全部搬去了 **§一之三 · ✅ 候选 F**（那里是正文）。
这里原来那张「5 种形状」表也搬过去了（它是解析规则的**依据**，不是过程记录）。

⚠️ 别再照本节旧文里的「实施计划」做 —— 四步都做完了（含第一步「抽共用的格位判据」，
落在 `BoardSpec.AdjacentSlots`）。


### 候选 D：继续按阵营铺（每会话两个，按剩余升序）

**🔴 剩余张数不写在这里** —— 权威是 `_tmp_view/tactic_by_faction.md`（**自检每次跑重写**），
按它的「完全解析 / 战术卡」两列相减排序即可。**排批次的方法**：从剩余最少的阵营开始，每会话两个。

> ⚠️ **2026-09-13 更正**：这里原来有一张**逐阵营剩余**的表（`Leviathan 2 · SpaceWolves 3 ·
> AstraMilitarum 3 · BlackLegion 4 · Ultramarines 6 · Genestealers 6 · EmperorsChildren 7 ·
> SaimHann 9 · Sororitas 12`），**整张都过期了** —— 那是 A1～A3 之前的快照，A4 做掉一批之后
> 实测是 `Leviathan 2 · SpaceWolves 4 · AstraMilitarum 0 · BlackLegion 4 · Ultramarines 5 ·
> Genestealers 3 · EmperorsChildren 1 · SaimHann 3 · Sororitas 2`。**删表留法**（铁律 6：
> 数字只留一处，而那一处是自检重写的那份）。**别再抄第二份。**

（剩余 = 该阵营**未完全解析的战术卡张数**。每次自检重写 `_tmp_view/tactic_by_faction.md`，**别手抄**。）

---

## 三、「做一个阵营」的标准流程（2026-09-12 第十六轮实际走的，极限战士 + 兽人）

1. **跑一次自检**，读 `_tmp_view/tactic_by_faction.md` 里那一行 —— 那就是这张阵营的**工作清单**
   （自检每次跑都会重写它，**不用手抄**）。
2. **每个卡点先定语义**，顺序是：
   ① 规则书（`资料/规则书/…_中文翻译.md`，关键词表在 `:161-225`）
   ② `d:/warpforge/scripts/rule_core.gd`（照卡面修过的规格书，**权威语义**）
   ③ 反编译方法名（`d:/2/Warpforge_code/Scripts/Assembly-CSharp/BattleManager.cs` 的 `Resolve*`）
   **三处都没有 → 如实报「原版数据里没有」，不要猜。**
3. **分类，优先复用已有的层**（第十六轮最大的收获）：
   | 卡点长什么样 | 该动哪一层 | 例 |
   |---|---|---|
   | 新句型 / 新动词 | `EffectText` + `EffectDispatch` 表加一行 | `drawtype` / `repeat` |
   | **阵营词写法的「付费才生效」** | **复用 `op.Cost`（付费层）** | `Oath N:` = `12 [Energy]:` |
   | **阵营词写法的「某个前提」** | **复用 `EffectCondition`** | `Codex:` = 条件 `energyzero` |
   | 「给谁加什么」 | 复用 `GivePayload` 拆载荷 | `+2 Health and Vanguard` |
   | **数值修正**（降费…） | **挂在对局上、不碰 `CardDef`** | `ctx.CostMods` + `RuleCore.CostOf` |
   ⇒ **能不新开动词就不开**。第十六轮做三个阵营机制，**一个动词都没加**。
4. **加断言**：⚠️ 务必**断言「解析出来的东西长什么样」**，不只断言「认不认识」。
   第十六轮踩过：`lowercost` 的正则把 payload 切成了 `f all vehicles…`（`of?` 里那个 `?`），
   **而解析判定照样是 Ok** —— 只有「切出来是什么」的断言抓得住这类静默错解析。
   见 `RuleEngineTest.TestLowerCost` 的头注释。

   **第二十六轮（选牌）新踩的三条，下一轮会再遇到**：
   - **后续句本来就解析得了** —— `Choose a troop from your deck.` 那一族的动作写在**下一句**，
     而 `Split` 只按 `.` 切，后一段**自己就能解析**。动手前先查「这句是不是已经在未覆盖清单里」，
     别重复实现（实测 `Draw it and create a copy of it in your hand` / `It costs 2 less` /
     `Give it Stealth` 全都早就能解析）。
   - **`rule_core` 的默认值会静默错**：动作判不出时它默认 `to_hand`（`:1193`），
     还有个裸的 `" in your hand"` 兜底（`:1231`）—— 后者会把
     `Choose a card in **your opponent's** hand` 判成「进自己手牌」。
     **判不出来就报不认识，别默认。**
   - **候选筛选拿「全卡池」当判据集**（复用 `CreatePool.Resolve` 的兵种/阵营判定）：
     好处是判据只有一份，代价是**不在卡池里的卡筛不出来** ——
     自检夹具里 `new` 出来的假卡就撞到过（`TestChooseCard` 里记着）。
     ⚠️ **第二十七轮已改成逐候选直接判**（`type`/`subtype`/`faction` 是卡自己的字段），
     本节这条留作**教训**：判据集选错，报错会变成一句很难查的「候选里没有符合…的卡」。

   **第二十七轮（回手/指代）新踩的，都是「改正则之前先普查」**：
   - **`IsVerbWord` 少一个词 = 整句切不开**。`and lower …` 在 `SplitAndTail` 里切不开，
     于是锚了 `$` 的 `ReDraw` 整条失配 —— **5 张卡**一直躺在「完全不认识」里。
     动手前先 `grep` 那个形状在全仓有多少条、**是不是都该切**（这次 5 条全是）。
   - **新加的正则会把老句子的参数吃走**：`ReDrawType` 的 `([a-z]+)` 把 `Draw **it**` 的 `it`
     吃成了「兵种词」→ 静默空过，**而覆盖率两栏都算它通过**。
     ⇒ **代词分支必须排在泛型分支前面**；加新写法时一定要回头验「老句子有没有被截胡」。
   - **往词表里加写法会抢走老写法的匹配**：给 `DestPhrases` 加 `to your hand` 之前，
     必须先查「有没有 `Create … and add it to your hand` 这种句子」（这次查了，**0 条**，所以安全）。
   - **目的地类的东西要「全等」匹配，不要「包含」**：`… to your hand **and reduce its cost to 1**`
     按包含匹配会把后半句**静默吞掉**。宁可判「不认识」。

   **第二十八轮（常驻效果）新踩的**：
   - 🔴 **凡是会作用在「卡面原文」上的正则，一律加 `IgnoreCase`**。
     `SplitAtTurn` 拿原大小写的 `CardDef.Desc` 去匹小写正则 → `IsHandTrap` / `ResolveAtTurn`
     **两处静默失效**；解析那条路因为拿到的是小写 `low`，**完全看不出来**。
     （判据被两处用、一处传小写一处传原文，是这类坑的温床。）
   - ~~**注册一条永远不会被消费的效果 = 骗人**。`For the rest of this battle, give Shield to all
     Drones you deploy` 是**部署时触发**，不是回合起止 —— 宁可判「不认识」，也别注册。~~
     ⚠️ **2026-09-13 第三十轮更正**：那条**已经真做了**（建了部署触发的机制）。
     **这条教训本身仍然成立**，只是例子换了 —— 现在钉的是
     `For the rest of this battle, give Shield to all **Wizards** you deploy`（兵种词表里没有这个词）。
   - **条件从句不许当主语**：`When you play a Stratagem, your Warlord takes 1 damage`，
     不挡的话正则会把「什么时候」整段吃成目标 → 变成**任何时候都掉血**的静默错语义。
   - **测试自己的回合记账要数清楚**：`ToP1Turn` 之后当前行动方是 P1，
     所以第一次 `EndTurn` 结束的**就是 P1 的回合**（我写错过一次，查下来是断言错、实现是对的）。

   **第三十轮（部署时触发 + 持续改费）新踩的**：
   - 🔴 **断言只钉「该生效的生效了」是不够的 —— 必须同时钉「不该生效的没生效」。**
     第一版自检全绿，但复查发现它**钉不住最关键的那条**：卡面写的是
     `give Shield to all Drones you **deploy**`，不是「你场上**所有** Drone」——
     实现要是退回成「查场上所有 Drone」，那个用例**照样绿**。
     补一条「先摆一个早就在场上的 Drone，验它**不会**被补上」才钉住。
     **做法：故意打断那段分支重跑，确认断言真的会亮**（这次亮的就是补的那一条，其他都不亮）。
   - **文件名骗人**：`Core/EffectResolver.cs` 里的类**不是** `EffectResolver`，
     是 **`public static partial class RuleCore`**（全仓仅此一处这样）。写 `EffectResolver.Xxx(...)` **编译不过**。
   - **`<see cref="..."/>` 里写不存在的方法名不会报错但也没用** —— 改了方法名要回头 `grep` 一遍文档引用。
   - 🔴 **实战检查（「跑得完 ≠ 跑得对」那道关）第一次跑出来是「假的坏消息」**。
     用 `DeckBuilder.StarterDeck` 自动凑的牌组跑 4 局，结果「登记了 1 次、部署触发段 0 次」——
     看着像机制的 bug。查下来是**检查本身测错了东西**：自动凑的 TauEmpire 牌组里几乎没几张
     Drone（全区只有 **7 张**），登记之后部署的那 2 个单位**不是靶子**，**本来就不该触发**。
     ⇒ 改成**牌组里全是靶子**（22 张 Drone + `shuffle: false`）之后：登记 4 次 · 触发 **27 次** · 筛中 27 次。
     **教训：实战检查的牌组要把「靶子」塞满，否则统计量测的是噪声。**
5. **收尾**：跑四条自检（下面 §五），把这一轮写进
   `资料/卡牌效果管线_计划与交接.md`（§一 加一行、§五 更新数字）+ `项目任务.md` 的轮次小结。

---

## 四、点了名但现在**不做**的（等它自己的阵营）

| 要做的 | 等到哪个阵营 | 说明 |
|---|---|---|
| **`Remnant`（残骸）** | **Sautekh（死灵）** | 规则书 `:203`「本部队死亡时**翻面表示残骸**；残骸受伤害或控制者回合结束时被摧毁」。**28 张 Sautekh 单位带这个关键词**，是那个阵营的主题；`reanimate` 这个唯一没实现的动词就是它的一部分（`Reanimate a friendly Remnant`） |
| 替代行动族 `Duty / Ferocity / Pray / Agenda` | 对应阵营各自做 | 规则书 `:150`；原版是独立子类（`CardTraitDuty` / `CardTraitFerocity` / `CardTraitOath`） |
| 选牌 UI（表现层） | `Choose a …` 引擎侧做完之后 | 引擎侧先按「单机自动选 1」走，UI 独立一件 |
| 关键词对象化 / 阵营机制挪出通用结算器 | **等关键词批量补或第二个阵营机制开工时** | 见 `卡牌效果管线_计划与交接.md` §四·六，现在动是过度设计 |

---

## 五、跑自检（改完必须四条全绿）

```bash
cd d:/4
unset ELECTRON_RUN_AS_NODE
U="D:/Unity/Hub/Editor/6000.3.23f1/Editor/Unity.exe"
for M in RuleEngineTest.Run BattleScene.Run DeckScene.Run CardBaseDemo.Run; do
  printf "%-22s " "$M"
  "$U" -batchmode -quit -projectPath "D:\4\Unity\MyGame" -executeMethod $M -logFile - 2>&1 \
    | grep -E "^(RE|BT|DK|CBD) === (结束|合计)|error CS|✗" | tr '\n' ' '; echo
done
```

**🆕 改解析层判据之前/之后，用逐句探针量**（2026-09-13 A4 批 1 建的工具，批 2 又用它定位了三个卡点）：

```bash
# ① 把要量的句子写进 d:/4/_tmp_view/probe_in.txt（一行一句，`#` 是注释）
# ② 跑：
unset ELECTRON_RUN_AS_NODE
"D:/Unity/Hub/Editor/6000.3.23f1/Editor/Unity.exe" -batchmode -quit \
  -projectPath "D:\4\Unity\MyGame" -executeMethod EffectParseProbe.Run -logFile -
#    筛输出：grep "^EP "（一行：几段认了 / 半懂 / 不认）
# ③ 读 d:/4/_tmp_view/probe_out.txt —— 卡级判定 + 逐段判定 + 每条 op 摊平（含目标/载荷/时长/付费）
```
> ⚠️ **别靠读代码推**：A4 那个 `|` 优先级 bug、批 2 的「`attacks` 没进动词表」「`highest attack`
> 被当成关键词」全是拿这个量出来的。**读代码只能确认你想到的那一种情况，量能发现你想不到的。**

> ⚠️ **`BattleScene.Run` 偶尔会在退出时 segfault（2026-09-13 实测，别被它骗到）**：
> 本机 6 次里出现 **2 次**，**都在会话早期**；崩溃**严格发生在 Unity 关停之后** ——
> 日志里是 `Shut down.` → `Cleanup mono` → `abort_threads: Failed aborting id: …` → segfault，
> 中间还夹着一次 `Clearing [NotoSerifCJK-Regular SDF] dynamic font asset data` → **重导入字体资产**。
> **测试结果在崩溃之前就已经打印完了**（`BT === 合计：369 通过 / 0 失败`）。
> ⇒ 🔴 **判据看 `BT === 合计` 那一行，不要看进程退出码**。
> 已排除是我们的代码引起的：同一台机器上**把 `RuleEngine/` 与 `CardPresentation/` 退回改动前那一版
> （`ce16c7b`）跑，也是**干净退出**；而改动后的版本**连跑 3 次也全干净** ⇒ **偶发、与代码无关**。

**最近一次复核（2026-09-14，T1 + T2 + T2b + T3 + 批 3 三条 + 督军费用数据修正 —— 八条机制 + 125 条新断言）**：
`RuleEngineTest` **2068/2068** ·
`BattleScene` **374 通过 / 0 失败** · `DeckScene` **61/61** · `CardBaseDemo` 全过 ·
**特效**：`BlendProbe` 混合 1099/1099 + shader 解析 70/70 · `WhiteboardTest` 20/20 + 无泄漏 ·
`DistortProbe` 27/27（`_GrabPassAvailable = 1.0`）。
**卡表 `version 6`** —— 每张卡带**稳定 id**（原版 id 优先、对不上的自造，见候选 0）。
**防御卡（39 张）已接进对局**：能打、开局在手里、不参与换牌。
**三套阵营资源都接上了**：信仰 `Faith`（修女会）· 灵魂石 `SpiritStones`（灵族）· 任务点 `QuestPoints`（暗黑天使）
—— HUD 三件也摆了（位置从运行时 dump 换算，见 `BattleDriver` 里那组常量的「独立佐证」）。
**`When <事件>`**：带它的卡 **78** 张 · 真的点亮 **72** 张（**73 条**监听器）·
**事件触发式降费 3 张** · 认不出的**事件短语 0 种**（A3 之前是 4 种）
（清单在 `_tmp_view/when_unparsed.md`，每次自检重写；A2 做完 14 个关键词之后
`triggers/uses <关键词>` 那一族**自动**跟着亮了 —— 这是 `WhenEvents` 里刻意留的机制）。
> ⚠️ **2026-09-13 A3 更正**：这格原来写「点亮 **68** 张（68 条监听器）· 认不出 **4 种**」。
> 差的那 4 张就是 A3 做的四条短语；监听器 **73** 而不是 72，是因为
> `create **or play** a secret` **一句话产两条**（`WhenEvents.ParseAll`）。
> **权威永远是那份自检重写的清单，别抄这里的数。**
战术卡**完全解析 442/449**（**载荷有机制** 418 · 部分 4 · 完全不懂 3；
分句 661：关键词声明 37 / 认了 617 / 半懂 2 / **不认 5** —— 第三十二轮把那 47 条查了一遍，
**其中 5 条当场修掉**：付费前缀的**方括号本来是可选的**（我们抄成必选 → 无括号 `5 Energy:` 收不了）、
`Draw an additional <类型>` 的量词位不认 `additional`；明细见 `资料/战术卡47条_语义查证.md`）。
> ⚠️ **2026-09-14 更正（保留一笔）**：上一格写 **436/449（载荷 413 · 部分 5 · 完全不懂 8 ·
> 不认 11）**。本轮 T2 修了裸 `+N` 的属性、T3 做掉**选效果族**（3 张卡）、批 3 做掉 **3 条**
> （`Possession` / `Bjorn's Shrine` / `Raid Tactics`）⇒ **完全解析 436 → 442 · 不认 11 → 5**；
> T2b 的复数修正**不改变「认识不认识」**（改的是解出来的张数）。
> ⚠️ 同一次更正：**`载荷有机制` 的两张报表原先口径不同**（逐阵营表少两处检查）⇒ 已抽成
> **一份** `EffectText.OpHasMechanism`。所以这轮中途那个 415 比上一轮的 413 **低**不是退步 ——
> 是把 `Infinite Biomorphologies` 的「给手牌作用域没做」**第一次如实算进来**了
> （现在 418：批 3 的三条各加 1）。
> ⚠️ **2026-09-13 A4 收尾·批 2 更正**：上一格写 **433/449（载荷 410 · 部分 5 · 完全不懂 11 ·
> 不认 14）**。批 2 做掉**强制攻击族**（`forceattack`）+ 反向共用目标 ⇒
> **完全解析 433 → 436 · 不认 14 → 11**（其中 3 条不做、1 条池子已解开但要做新机制 ⇒ **7 条可做**）。
> ⚠️ **2026-09-13 A4 收尾·批 1 更正（保留一笔）**：更早写 **425/449（载荷 401 · 部分 12 · 完全不懂 12 ·
> 认了 600 / 半懂 4 / 不认 20）** —— 批 1 做掉六条机制 + ②栏两条半懂 ⇒
> **完全解析 425 → 433 · 不认分句 20 → 14 · 半懂 4 → 2**。
> ⚠️ **2026-09-13 A4 更正（保留一笔）**：更早还写过 **401/449（载荷 377 · 部分 27 · 完全不懂 21 ·
> 认了 575 / 半懂 4 / 不认 44）** —— 那一批做的是付费前缀四种写法 + 几个小原子。
> ⚠️ **2026-09-13 候选 F 更正（保留一笔）**：更早还写过 **403/449（载荷 380 · 部分 26 · 完全不懂 20 ·
> 认了 577 / 半懂 5 / 不认 41）**。差 2 张是**口径变诚实**，不是退步：
> `TryStun` 原来**一个字都不解目标**（`op.Target` 恒为 null、结算层兜底「一个敌方单位」），
> 修成真解析之后，两句原来「假干净」的变成**半懂**。详见 §一之三·候选 F 那张「顺手修的 bug」表。
**单位卡 desc**：完全解析 **425/586**（部分 102 · 完全不懂 59）。
> ⚠️ **2026-09-13 A4 批 2 更正**：上一格写 **421/586（部分 104 · 完全不懂 61）**。批 2 顺手收下 4 张 ——
> 主要是 `Damaged Hexmark` 的 `Artifice` 正文（裸 `Attacks a random enemy with lowest Health`）。
> **A5 的主战场没动。**
> ⚠️ **2026-09-13 A4 批 1 更正（保留一笔）**：更早写 **420/586（部分 105）**，批 1 收下 1 张
> （`if any friendly unit is Praying`，`Sororitas Rhino`）。
> ⚠️ **2026-09-13 A2 更正**：原来是 **349 → 358** 再到现在 **416/586**。两个来源：
> ① 覆盖面口径修对了（`Rally: …` 那类**触发前缀**改成量**冒号后那段**，和引擎真正走的路一致）；
> ② A2 把 14 个关键词实现掉之后，它们的正文**真的被收下并结算**了
> （「触发式效果收到正文的卡」**145 → 315 张**，自检每次报）。
> **这就是 A5 的下一份工作清单，它是准的。**
**🆕 触发式效果的正文来源**（A2 挖出来的结构性缺口，已修）：**卡面没写 `关键词:` 前缀时，
整条 `desc` 就是那个关键词的正文**（实测 **200 张**）—— 原来 `AddTriggerOp` **要求冒号**，
那些卡的正文**一条都收不到、效果静默不发生**。修法是 `CardDef.CollectBareKeywordBody`
（**本卡唯一的「带正文关键词」才认**；两个以上就是歧义 ⇒ 不收）。
**临时卡（`Ephemeral`）**：**98 张**卡面带这个词；「移出游戏」区域 + 回合结束清扫 + 卡面角标全做完。
**未实现关键词 3 个：`destroyer` / `ecstasy` / `sabotage`** —— ⚠️ **以自检打印的 `RuleCore.UnimplementedKeywords()` 为准**，
别抄这里。**那 14 个做完的语义、出处、落点、卡数见 §一之三·✅ 候选 E 那张表**（不在这里抄第二份）。
没做的理由都写在候选 E / 候选 G 那一节 —— **都不是漏做**。
> ⚠️ **2026-09-13 A3 更正**：`sabotage`（**17 张**卡面带它）是 A3 才**第一次被报出来**的 ——
> 原来它**连 `Prefixes` 都不在**，`Normalize` 返 null ⇒ 既不实现、**也不报出来**（两张诊断单都看不见）。
> 现在只做到「认得出 + 报得出」，**机制仍留到基因窃取者那一轮**。

**三条还会踩的坑**（其它取舍细节都在代码注释里）：
- ⏸ **`ecstasy` 的两条卡点没解**：① 卡表的关键词值不可靠（`Terminator` 的 `keywords` 是**裸 `Ecstasy`**、
  卡面写的是 **2**；`Maulerfiend` 连关键词都没有、只在 `desc` 有 `Ecstasy 5`）；
  ② `AddTriggerOp` 收不下 `Ecstasy N:` 这种正文（**整词相等**比对，`"ecstasy 2"` ≠ `"ecstasy"`）。
  **半做 = 静默用错阈值**（本工程红线）⇒ 宁可标着没实现、卡面照旧打 `*`。
  两条都钉成了断言（`TestUnstableEcstasyCruelty` 块 ②，**它们红了就说明卡点解了**）；
  时机点就在 `RuleCore.Hurt`（`hpBefore` 已经存好，注释里写了「补做时用它判**跨越**」）。
- **`mob` 只做了「触发攻击者自己的 `Mob:`」**：原版还有一支 `BroadcastUnitMob`
  （通知**除攻击者外**的所有卡，触发 645），实测听众只有 `Big Choppa Nob` 一张，
  而它要的「**再触发一次**」我们的效果文法表达不了 ⇒ 没做。
  ⇒ `When … triggers mob` 那条短语**永远不会亮**（**不是漏做，是做不到**，别挂成待办）。
- **`talent`（91 张）的两个副作用别忘了**：① 它生成的牌**必须** `MarkEphemeral`（规则书 `:229`
  把天赋生成的牌与伴生/潮涌复制并列为临时），而**多数天赋卡面并没有印 `Ephemeral`** ——
  不标记就会**赖在手里不走**而画面看不出来；② 它让「手牌 + 牌库 = 卡组张数」这类不变量被顶掉一张，
  断言要用 `Conjured(ctx, p)`（判据 = `BattleContext.MarkedCount`，**只数标记** ——
  用 `IsEphemeral` 会把卡面自带 `Ephemeral` 的 98 张也算进来）。⚠️ 查不到同名卡的那 8 个**如实打日志**。

**「未实现」名单的两条治虫规矩**（踩过，别丢）：
- 名单要**双面**钉：`TestKeywordImplementedList` 既不许**多报**（`codex`/`oath`/`stun` 当年就是
  「机制一直在、只是没登记」），也**不许把没做的登记成已做**（那比多报更糟）。
  判据 = 「**代码在那个时机真的读了/做了那件事**」——「`Prefixes` 认得出」和
  「`GivePayload` 能把它写进单位」**都不算**。
- ⚠️ **2026-09-13 A2 起，那份「4 处近似」的旧表作废**（它列的 `duty`/`ferocity`/`agenda`/`pray`/
  `remnant`/`ambush`/`swarm` **全都真做掉了**，见候选 E）。**现在只剩 `destroyer` 一个**
  还是「名字在名单上、机制没做」，理由写在候选 E。

⚠️ **顺带查出的一条数据问题**：5 张卡（`Banshee Mask` / `Hektor Thenmann` / `Malicious Volleys` /
`Snakebite Grot` / `Toxic Bonfire`）的 `keywords` 里那个 **`Stun` 是从效果句误抽出来的**
（卡面写的是 `Deal 3 damage to an enemy and Stun it`）。而**卡面关键词是构造函数直接灌字典、
不走 `AddKeyword`** ⇒ 那个 `Stun` 本身是死的（效果照样结算，走的是 `DoStun`）。
⇒ 要根治得清卡表数据，**没做**；记在这里，别当成「卡上有个 Stun 关键词」。

⚠️ **本节踩过的坑**：这里先后写过 825/825 · 837/837 · 1203/1203 三个版本 —— 都是**写那一刻的快照**，
后来每一轮都在加断言，于是每一版都变成了误导（`规则引擎_进度与交接.md` 开头那张表指着本节说
「最新值见这里」，**两个会话都被这条指针坑过**）。
⇒ **规矩不变：数字只写一处，就是上面那一行；每次收工必须更新它。**

⚠️ **Unity 同工程同一时刻只能跑一个实例**（实测：第二个进程 `Exiting ... return code 1`，一行测试都没跑）。
所以「派子代理并行跑」在这台机器上不成立 —— 能并行的只有「读和写」。

---

## 六、两个阵营的实战检查怎么跑的（`RuleEngineTest.TestFactionBattle`）

极限战士 vs 兽人（**轮流先手**，免得「赢了是先手还是阵营强」分不清），各 6 局：
**结果 6/6 完成 · 平均 13 回合 · 极 4 : 兽 2 · 双方共打出 29 张战术卡 ·
扫 593 条事件日志，只有 6 条「没结算」，覆盖 3 张卡。**

这一节的价值在于**它抓出了两个引擎自检抓不到的问题**（第十六轮）：
1. `DeckBuilder.StarterDeck` 的 `unitsOnly` 开关**从来没实现** → 自动凑的牌组**一张战术卡都没有**；
2. `SimpleAI` **只出单位卡**（`if (!c.IsUnit) continue`）。
⇒ 合起来是「**引擎自检里战术卡用例全绿，真打起来却从来没碰过它们**」。
**新做一批卡之后，照着这个测试的思路加一局实战检查** —— 它是「跑得完 ≠ 跑得对」的那道关。

---

## 七、已知的**数据缺口**（不是我们没做，是原版数据里就没有）

| 卡 | 缺什么 | 查过哪 |
|---|---|---|
| `Mekaniak`（Goff） | `Give a Kustom Job of your choice to a friendly Vehicle` —— **没有任何叫 Kustom Job 的东西** | 卡池 1130 张、实体卡表 `卡牌信息权威表_0824.md`、反编译 1891 个类、`rule_core.gd` —— 四处零命中 |
| `Duty's End`（UM） | 嵌入效果 `"💀 Backlash: Trigger the Codex ability of all friendly units"` | 嵌入效果走 `EffectSpec` 的**封闭文法**（只有 Damage/Heal/Draw），表达不了「触发典籍能力」 |
| `Vindicare Assassin` / `Brôkhyr` / `Vitric Consul` | 整张卡 | 卡池 + 实体卡表都没有 |

⚠️ 反过来也踩过：**「查不到」和「没有」是两件事**。第十六轮之前那份「原版数据里没有这张卡」的名单里，
`Combat Elixir` / `Sabotage` 其实是**兵种**、`Abaddon's Chosen` / `Sergeant Taaman` /
`Extermination Protocol` 都存在只是**名字写法不同**。下结论前先查一遍 `CreatePool.FindNearMiss`。

---

## 八、两套阵营特色机制：**解包资料里有，而且比 UI 占位深得多**（2026-09-12 查到）

> 起因：用户问「灵族的灵魂石、修女的祈祷，解包资料里真的没有吗」。查完的结论是**有**，
> 之前那张「引擎侧也没有对应机制」的说法只对了一半 —— 缺的是**数值**，不是机制。

### ① 灵族 灵魂石 / Spirit Stone —— 档位 ⓑ⁺（机制骨架齐全，缺数值）

- **它是第二种能量货币**：`ManaType.cs:4-5` `Normal=0, SpiritStone=5`（枚举里只有这两种）。
- `PlayerManager.cs:85` `spiritStoneMana`；`:119 ResetMana(..., initialSpiritStone)`（**开局可给初始值**）；
  `:127 HasEnoughMana(int, psychic, plasma, spiritStone)`；`:142 GetCurrentSpiritStone()`；
  `:200 UseSpiritStoneEnergy()`；`:208 AddSpiritStoneMana(int)`。
- **消耗路径**：`CardScript.cs:2712 CanUseSpiritStone()`、`AbilityTrigger.cs:71 UseSpiritStone=600`、
  `BattleActionType.cs:76 useWaystone=76 / :77 triggerSpiritStone=77`、`UnitConditions.cs:17 hasUseSpiritStone`。
- **获取是「数量事件」**：`CollectSpiritStoneSignal.cs` 字段 `IsPlayer / Amount / FromWaystone`。
- **可被 buff 加**：`ManaBuff.cs:22 spiritStoneMana`；`RawCardScript.cs:87 initialSpiritStoneModifier`、`:668 UsesSpiritStone()`。
- **HUD**：`ManaManager.cs:21/33/61`（`SpiritStoneHolder` / `ToggleSpiritStoneMana` / `SetSpiritStoneMana`）；
  全树 `2D层_battlearenaaeldari全树.md:244-246` → `SpiritStoneHolder / SpiritStone[51×63] / SpiritStoneText '0'`。
- **语义（规则书中文版 :210 / :225）**：路标石单位死亡 → 生成 1 颗灵魂石；灵魂石受伤害会被摧毁（对手能清）；
  **只在自己回合收集**；收集后用于抵扣/触发。卡面佐证：`Infinity Circuit`「Gain 5 Spirit Stones」、
  `Hosts of the Dead`「**Spend all** your Spirit Stones. For each one, deploy a Wraithguard」。
- **缺口**：初始值 / 上限 / 每回合增长 / 扣减公式 —— 桩方法体全空；线索是
  `GameStaticData.cs:323 waystoneGiveMana` 那类静态配置（**下一步该去 MonoBehaviour 数据里找它的值**）。

### ② 修女会 信仰（Faith）/ 祈祷（Pray）—— 档位 ⓑ

- **信仰是每玩家一个累计值，不是货币**：`PlayerManager.cs:87 faithMana`、`:147 GetCurrentFaith()`、`:212 AddFaithMana(int)`。
  ⚠️ `ResetMana(...)` **没有 initialFaith 参数**（与灵魂石不同）—— 从 0 累积。
  `ManaType` 里**没有 Faith**，`HasEnoughMana` 也**没有 faith 参数**。
- **规则书 :184 原文**：「**信仰（Faith）** | **总信仰达到指定值时触发效果**」→ **阈值触发，不花掉**。
- **祈祷是一条独立的单位行动**：规则书 `:198`「**祈祷（Pray）** | 缓慢非战斗行动（详见战斗修女阵营指南）」、
  `:150`「职责/狂暴/祈祷 等是**替代攻击行动**，受与攻击相同的限制」。引擎侧 `EntityScript.cs:66 isPraying`、
  `CardScript.cs:3132 SetPray(bool)` / `:1672 TriggeredPray()`、`AbilityTrigger.cs:82 Pray=655 / :83 TriggeredPray=660 / :72 UseFaith=605`。
- 卡面花费形如 `N [Faith]:`（`Lead the Righteous`「**8 [Faith]**: Deploy an additional Battle Sister」等）。
- **缺口**：每次祈祷给多少信仰、阈值表 —— **规则书说的那份《战斗修女阵营指南》本地没有**；
  游戏内 hover 说明（`TextTooltipController.cs:119 GetFaithTooltip`）方法体空、I2 词条文件也没在本地。

### ③ 奇迹骰 / Miracle dice —— 档位 ⓒ 本地确实查不到
`Miracle` 在反编译类、`资料/`（含规则书）、`数据/游戏数据/` 里**命中 0 次** —— 本作没实现这个概念。

**下一步建议**：灵魂石可以按「第二种能量」做（`ManaType.SpiritStone` 那套字段就是设计图）；
信仰按「累计计数器 + 阈值触发」做。**别再说「原版没有」** —— 机制骨架是有的。

### ✅ 用户 2026-09-12 亲口给的机制口径（**比我从规则书推的准，照这个做**）

| 机制 | 用户说的 | 我原来记的（**改成用户口径**） |
|---|---|---|
| **信仰 Faith** | **没有「缓慢」这回事**（规则书那句「缓慢非战斗行动」指的是**祈祷 Pray 这个动作**，不是信仰值）· **没有上限** · **不会衰减**（现在没有任何机制/效果会衰减它） | 我原来写「缓慢/上限/衰减查不到」—— 那三个问题本身就不成立 |
| 信仰的作用 | **达到具体阈值（3 / 5 / 9 这种）时，部分卡牌获得更强的效果** —— 信仰值就是干这个的 | ✅ 与规则书「总信仰达到指定值时触发效果」一致，现在有了具体数字量级 |
| **灵魂石** | **没有初始值** · **没有上限** · **没有每回合增长**；**扣减完全由效果决定**（有些卡的效果就是「花掉灵魂石换增益」） | 我原来列的三项「缺口」全部作废 —— 本来就没有 |

⇒ 也就是说：**两套机制的数值都不是「缺」，而是「本来就没有」** —— 灵魂石的量只由
「路标石单位死亡 +1」和「效果扣减」决定；信仰只由「触发效果累加」和「阈值」决定。
落地时**别再去找初始值/上限/增长表**（那是白找）。
