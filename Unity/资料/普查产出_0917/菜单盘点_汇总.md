# 菜单 / 功能界面盘点 · 汇总（阶段二：游戏外壳）

> 2026-09-17 只读普查产出。分块表：同目录 `菜单盘点_块1.md`(139 行) · `_块2.md`(171) · `_块3.md`(144) · `_块4.md`(136)。
> **本 5 份取代** `普查产出_0913/原版菜单盘点_块12.md` 与 `_块34.md`。那两份有两处硬错，别再用：
> ① 它把 `d:/warpforge/` 写成「**原版自己**的 Godot 工程」—— **其实是我们自己的早前复刻**（README 自述「基于解包资源复刻」）；
> ② 它一整批「**建议整体不做**」（商店/卡包/锻造厂/任务/成就/排行榜）按 2026-09-17 新判据**全部作废**。
> 判据 = `CLAUDE.md` §四 + 用户 2026-09-17 三条边界：美术留原版 · 资源固定 9999／商店与卡包照常能买／日常照做／**过期活动不做**／全解锁 · 多人**做界面 + 空态提示**（点了必须如实说「暂无服务器/未实现」）。

## 一句话结论

**结构和图基本都在本地，缺的只有「画面参照尺子」与「经济数值」**；难点不是找资源，是把约 **110 个界面**在 Unity 里重搭一遍 —— 而 `d:/warpforge/` 里已有 **约 50 个大件的 GDScript 实现**可当移植蓝本（⚠️ 但 `shop.gd`/`collection.gd` 的**页签口径与原版不同**，必须按原版重排）。

## 一、规模（4 块合计）

| 项 | 数 | 说明 |
|---|---|---|
| 根界面 | **313**（`界面索引.md`）／4 块报 98+74+61+82 = 315（含 2 个跨界尾） | 要点块 4 份已全部读完 |
| 其中独立界面/弹窗 | **≈118** | 块1 24 · 块2 47 · 块3 26 · 块4 21 |
| 其中组件/抽屉/预制体 | **≈195**，按族并成 ~40 行 | 抽屉族、商品卡族、任务卡族、里程碑、导航骨架… |
| 判决 | **要建 ≈110 · 不做 ≈20 · 待用户裁决 ≈10** | 逐条见 4 份块表 |
| **Unity 主工程现状** | **0 个菜单场景** | `Assets` 下 6 个 `.unity`：`CardPresentation/Scenes/{Battle,DeckEditor,CardBase}`、`WarpforgeArena1/Scenes/{BattleArena1,VFXWhiteboard}`、`Scenes/SampleScene` |
| **Godot 原型现状** | 70 个 `.gd` / **38,952 行**（`.tscn` 是 265-326 B 空壳，UI 全代码搭） | `d:/warpforge/scripts/`；本批 118 个独立界面里**至少一半有大件实现** |
| 原版资产（结构） | **100% 在本地，0 缺口** | `bundle_menus_assets_all/`（82038 文件＝UI 层级主库，16768 GO / 16510 RT / 35014 MB）+ 4 块要点文档 |
| 原版资产（图） | 基本齐，**已点名到 bundle** | 见 4 份块表末的「bundle 速查」；**只有 2 个真缺口**（§三·3） |

**原版的场景架构（`12_主程序资源/BuildSettings`，权威）**：整个游戏只打包 **2 个场景** —— `Assets/Data/Scenes/Intro.unity` + `Reset Scene.unity`。
`assets_full/level0/` 就是 **Intro** 场景（**134 个管理器 GameObject** + `Intro UI` + `Intro Videoplayer` + `FadeBackground` + `Safe area` + `UI Camera`）。
主菜单（`bundle_scenes_scenes_mainmenuwarpforge`）与 13 个战场是 **additive/addressable 场景**；其余菜单页是 **prefab 窗口**，由 `WindowsManager`（单例）+ `GameWindow`/`GameWindowWithTabs`/`WindowHolder` 开。

## 二、阶段二要建什么（建议顺序 + 依赖）

> ⚠️ 下面是**建议**（不是原版事实的地方已标明）。原版事实只有上面那段场景架构与每行的 `[行号]` 出处。
> 建议贴原版：**新建 2 个场景**（壳 + 主菜单），**其余菜单页做 Shell 内的窗口 prefab**，`Battle.unity` 保持独立场景由菜单切入。

### 第 0 层 · 宿主（必须先做，其余全部依赖它）

| # | 建什么 | 依赖 | 关键出处 / 资产 |
|---|---|---|---|
| 0a | **新建 `Shell.unity`**（= 原版 `Intro.unity`/`level0` 的对应物）：开场动画 + 常驻件（SafeArea / UI Camera / 音频 / 淡入淡出 / Loading / Toast / Tooltip / **Console 窗口管理器**） | — | **开场动画 = `assets_full/sharedassets0/VideoClip/Warpforge Intro.mp4`**（⚠️ **不在** `bundle_videos_assets_all` 里，按 bundle 名找会漏）；菜单音乐 `bundle_menumusic_assets_all/AudioClip/Main Theme.ogg`；管理器清单 `资料/说明书/08_主程序资源/运行时管理器.md`（84 类） |
| 0b | 公共件（prefab，非场景）：`GenericPromptWindow [1139]`（**判据③「暂无服务器」的宿主**）· 抽屉模板（22 种，Godot `drawer.gd` 就是规格）· 通用弹窗底/按钮/Tab/下拉/倒计时 · `Reward Window [13701]` | 0a | 块1/块2/块4 族表；`bundle_duplicateassetisolation_assets_all/Sprite/`（147） |

### 第 1 层 · 主菜单（用户点名的「主界面」）

| # | 建什么 | 依赖 | 关键出处 / 资产 |
|---|---|---|---|
| 1 | **新建 `MainMenu.unity`**：顶栏（`Profile Player Info [4877]` + 齿轮 + `InboxBtn [1580]` + `ChatPreview [14797]`）+ 左竖 5 导航（`Menu Navigation Panel Button [9006]`）+ 模式卡区 + 3 个 PopUp Holder + 右侧聊天预览 | 0a 0b | `资料/说明书/04_界面UI/主菜单全树.md`（GO 569 / 根 6）；场景树 `bundle_scenes_scenes_mainmenuwarpforge/`（3216 件）；导航图标 `40k_main_bt_{play,collection,shop,rewards,friends}` 已进工程 `Art/原版/0_mainmenu/` |
| 1b | ⚠️ **主菜单背景是实时 3D 渲到 RT**（不是一张图）：`主菜单全树.md:18` `Background Render Texture Holder`；`bundle_scenes_scenes_mainmenuwarpforge/` 有 `Camera`/`MeshRenderer`/`Material`/`RenderSettings` | 1 | 成本要单独算 |

### 第 2 层 · 用户点名的五个入口（做完这层就「能像原版一样玩」）

| # | 建什么 | 依赖 | 关键出处 / 资产 |
|---|---|---|---|
| 2 | **日常**：`Rewards Base Submenu Variant [12171]`（左栏 Missions / Campaign / Forge / Booster Packs）→ `Missions Tab [7450]` + `Weekly Mission Container [12839]` + `Achievements Tab [4949]` + `Daily Reward Popup [810]` + `Daily Streak Popup [7107]` + `Inbox Menu [15716]` | 0 1 | 任务图 `bundle_atlasgroup_assets_all/`（Missions Atlas / 0_RewardsAtlas）；页签 `40K_rewards_bt_*` 已进工程。**最便宜、价值最高**：接 `Battle/EndPanel.cs` 的胜负产出几乎零架构改动 |
| 3 | **锻造厂**：`Forge Tab [8952]`（阵营等级/经验）**先做静态版 + 空态，粒子后补** | 2（它是 Rewards 的一页） | 装饰 6 件 + `Forge Icons` 图集在 `bundle_atlasgroup_assets_all/`；中间旋涡大图 = `40K_ArmyTrack_bg` |
| 4 | **商店**：`Shop Menu Variant [1347]` + `Card/Daily/Item Shop Tab [10696/11480/11615]` + 商品卡族 7 种 + `Booster Info Popup [13378]`（保底 100/200）+ `Booster Pack Open Window [8040]` + 报价弹窗族 8 变体（**WebShop 钮不做**）。**页签按原版 Card/Daily/Item 重排**，别沿用 Godot 的 packs/cosmetics/resources | 0 1 | 卡包自成一体：`bundle_boosterpacks_assets_all/`（84 图 + 全套动画/粒子）；`40K_shop_offer_*` 16 张；页签 `bundle_liveopsicons_assets_all/Sprite/` |
| 5 | **卡组线**：`Collection Menu Variant [10810]`（**四页签 Deck/Cards/CardBacks/Alternate Art**）→ `Deck Editing Menu [9249]`（**已半有**：`DeckEditor.unity` + `DeckEditorState.cs`，补卡片子页/万能卡计数）→ `Deck info Popup [15492]` → `Deck Selection Popup with Tabs [16712]` → `Import Deck Popup [764]`（**Unity 后端已就绪** `RuleEngine/Data/DeckLibrary.cs:134,146`） | 0 1 | 四页签图 `40k_collection_bt_*` 已进工程；卡组格 `bundle_deckselectionbuttons_assets_all/`；卡背 233 张。⚠️ 收藏页是**进卡组编辑的唯一门**，做完才有「收藏→编辑→开战」闭环 |
| 6 | **战斗入口**：`RankedEventWindowV2 [13884]` / `SkirmishModeEventWindow [15326]` / `Practice Mode Menu [2395]` + `SearchingOpponentWindow [72]`（**空态**）→ 切 `Battle.unity` | 0 1 5 | 段位图 `bundle_rankeddivisionicons_assets_all/`（13）；督军立绘 `bundle_uiwarlords_assets_all/`（65）；模式卡图 `bundle_liveopsmenuimages_assets_all/` |

### 第 3 层 · 其余页（按需，但「彻底复刻」都算在册）

| # | 建什么 | 依赖 |
|---|---|---|
| 7 | `Main Menu Settings Window [5470]` 五 Tab（**最省的做法**：把战斗里的 `Battle/SettingsPanel.cs` 抽成公共件再补四页） | 0 1 |
| 8 | `Player Profile Window [15854]` 6 页签（Profile/Avatar/Title/Battle Log/Trophies/Ranking） | 0 1 |
| 9 | 物品详情链：`Item Display Window [2842]` / `Generic Multi Card Display [380,6469]` / `WhereToGetCardsPopup [6813]` | 0 |
| 10 | 新手链：`LoadingMenu [4224]` / `ChooseNameWindow [4826]` / `Choose Army Deck FTUE Window [6576]` | 0 |
| 11 | 教程：`Tutorial Mode Menu [338]` + `Tutorial Message Window PopUp [16488]` | 0 1 |
| 12 | 社交（**界面 + 空态**）：`Social Submenu Variant [1779]`（含 `Friends Menu Demo [15605]`、公会族 6 件、`Member Options Panel [14273]`）+ `ChatPanel [8899]` + `Match Log [3219]` | 0 1 |
| 13 | 排位附属（**界面 + 空态**）：`Ranked Division Info [4920]` / 排行榜 3 件 / `Ranked Division Change Window [207]` | 0 6 |
| 14 | 奖励领取链：奖励抽屉 11 种 + `Reward Display Mission` + `Gacha Tab [15746]` | 0b 2 |
| 15 | 活动壳（**只做「欢迎/过期」两态**）：`Base Event Popup [359]` / `WelcomeScreen Alpha [1770]` / `Base Expiration Popup [3282]` / `Event Help Popup [16703]` | 0b |

### 不做（用户边界已覆盖，别再立项）

**带时效的 LiveOps 活动**（`Ranked Boost Reward Event [3148]` · `EnergySinglePlayerOnlyEvent [1583]` · `Raid Progress Tab [1745]` · `Raid Reward Container [9557]` · `Two Sides Event Window [10020]` · `Full Screen Event Announcement [8940]` · `Referral Container [10638]`）
· **赛季件**（`Ranked New Season [8934]` · `Ranked Season Ended [9545]`）
· **纯内购**（`Webshop Offer Container [6552]` · `InApp Shop Container Disabled [6134]` · `XSolla Offer Drawer [6439]` · 各种 `Purchase Premium Window [7407]`）
· **关服/调试件**（`Sunset Popup [7993]` · `Debug Reward Window [3291]` · `Mission Debug Buttons [11867]` · `Placeholder Leaderboard [6462]`）

### 待用户裁决（**别自己拍**）

1. **Draft 轮抽整套**（`Draft Mode Menu Demo [3740]` · `Timed Mode Window [4967]` · `Ongoing State [13442]` · `DraftLeaderboardPopup [7182]` · `Deck Info Panel [12964]` · `Draft Mode Expiring Popup [11847]`）—— 原版是**限时 + 付费入场**，不在五入口清单里，工作量最大的一块。
2. **Expansion Pass 套**（5 件：`Tab [6607]` / `Missions Tab [3353]` / `Points Drawer [3490]` / `Reward Container [2798]` / 两种抽屉）—— 原版按资料片卖，算不算「过期活动」无口径。
3. **`Card Shop VIP Tab Variant [16511]`** —— 原版 VIP 是付费订阅；「不做真实经济」下 VIP 从哪来？
4. **`Main Menu Offer Container` 轮播族 5 件** —— 无 LiveOps 内容，留空位还是接本地活动？
5. **`Two Side Event Select Team [12828]`** —— 既是服务器件（全服比分）又是时效活动，两条边界打架。
6. **`Rate Popup [11603]`** —— 按钮指向哪个应用商店？`Prebuilt Pack UI [6542]` —— 预组包入口原版是否真有？

## 三、🔴 风险 / 本地根本找不到的

1. **画面参照尺子基本没有**（本批最大风险）。`资料/原版参照图/Unity参照管线_0825/shots_ui/`：`ui_prefabs/` **218 张里 217 张字节完全相同（27,260 B，纯黑）**；`menu_roots/` 5 张同字节（291,390 B，同一张主菜单整屏）。
   ⇒ **全项目能当画面尺子的只有 2 张**：`menu_full_0825.png`（**仅顶栏**）与 `menu_roots/*.png`（主菜单整屏）。
   ⇒ 除主菜单外**没有「像不像」的验收手段**，只能靠节点树 + 切图还原（铁律 3 的验收方式在这批界面上不成立）。
2. **经济 / 进度数值本地读不出来**。`assets_full/_stats.json`：**84 个 `bundle_*` 全部 0 导出失败**，**905 个失败全在核心文件** —— `resources.assets` 616 · `sharedassets0` 182 · `level0` 73 · `globalgamemanagers.assets` 29 · `globalgamemanagers` 4 · `level1` 1。
   实测失败的那些 MonoBehaviour JSON **只剩 4 个头字段**（`m_GameObject/m_Enabled/m_Script/m_Name`，158 B；`level0/MonoBehaviour/` 77 个文件全是这么大），
   而**商店价格 / 卡包掉落 / 任务奖励 / 锻造厂经验 / 货币 / 点数**这些配置**恰好挂在 `level0` 的管理器 GameObject 上**（`Shop Config` `Shops` `Missions` `Forge` `Currency Config` `Points Config` `GameModes Config` `RankedScoreBoost` …）。
   ⇒ 只能走别的路（反编译字段名 + 跑实况 + 规则书），或明确标「我们自定」。
   ✅ **反例（别一竿子打翻）**：**独立 bundle 的 SO 数据是完整的** —— 卡包（`Booster Pack Ultramarines` 连 `dropTableItems` 都在）· 轮抽包 · 预组卡组 · 外观（`bundle_cosmeticsso_assets_all/` 1261 个）· 战役节点图（`Campaign Node Data` 含 graph/ports/坐标）。
3. **两件原版美术确实不在本地**（唯二真缺口）：
   `bundle_draftpacks_assets_all` **只有 882 个 MonoBehaviour，无 Texture2D/Sprite**（Draft 选包的卡包外观要从 `bundle_atlasgroup_assets_all` 的 `Draft Mode Atlas` 顶）；
   `bundle_liveopsicons_assets_all` **只有 1 张 512² 图集**。
4. **服务器件的功能本体不存在**：公会成员 / 好友 / 聊天 / 排行榜 / 匹配 / 双边事件全服比分 / 赛季。
   ⇒ 按边界**做界面 + 空态**，**点了必须如实提示「暂无服务器 / 未实现」**（红线：不许摆一个点了没反应的按钮）。
5. **少量版面在树里本来就是空的**（不是我们没找到）：`LoadingMenu [4224]`（只剩一块 inactive 巨型 Panel `[-6003,-5200 13926x11475]`）· `Generic Options Panel [4869]`（高 0＝默认收起）· `Daily Streak Popup [7107]` 与 `Deck Selection Popup [7030]`（要点块只有名字、无元素表）· `Battle Log Popup [7240]` 的**每行样式**。
   ⇒ 这些只能自定，**必须标「我们挑的，不是原版做法」**。

> ⚠️ **本批已就地更正 4 条旧记录**（铁律 5，更正痕迹留在块表里）：
> ① 块2：「`bundle_cosmeticsavatarsimages` 是空目录」→ **包名笔误**，正确 `bundle_cosmeticavatarsimages_assets_all`（cosmetic 无 s），`Texture2D/` **472 张真 PNG**；
> ② 块3：「Forge 中间旋涡查不到」→ = **`40K_ArmyTrack_bg`**（PathID `2374284676670985858`，在 `旧/03_界面UI/图集/Sprite/`）；
> ③ 块4：「宝库两件查不到」→ `旧/09_游戏数据/卡包/Sprite/` 里有（`40k_shop_popup_info_bg` / `40k_Crate_Tier5_Warp_premium`）；
> ④ 块4：「`去重资源/` = `sharedassets0/`」→ = **`bundle_duplicateassetisolation_assets_all/`**（Texture2D 51↔51 精确对上）。
> **四条错因是同一个**：只搜了一两个目录就下「查不到」—— 正是铁律 5 那条「翻过一个镜像目录就写『本地没有』＝ 一定会错」。

## 四、出处（要动手时按这张表去读）

| 要什么 | 在哪 |
|---|---|
| 313 根 → 行号/尺寸/所属块 | `资料/说明书/04_界面UI/界面索引.md` |
| 逐界面元素表（本批的源） | `资料/说明书/04_界面UI/菜单全树_要点_第{1-4}块.md`（524/531/548/602 行） |
| 主菜单整屏全树 | `资料/说明书/04_界面UI/主菜单全树.md` · `04_界面UI/主菜单聊天面板.json` |
| UI 层级原始 JSON（**坐标权威**） | `d:/2/新解包资源/assets_full/bundle_menus_assets_all/`（82038 文件）＝ 旧 `d:/2/解包整理/03_界面UI/菜单/`（文件名**带 PathID**，按 `sprite:<数字>` 反查图名要走这一份） |
| 图（真 PNG） | `assets_full/<bundle>/Texture2D/`；已切好的 5557 张在 `d:/2/Warpforge_tools/data/ui_extract/*_sprites/`；已进工程的 368 张在 `MyGame/Assets/CardPresentation/Art/原版/` |
| 移植蓝本（GDScript） | `d:/warpforge/scripts/*.gd`（70 个 / 38,952 行，注释里抄了原版绝对坐标） |
| 原版场景/管理器架构 | `assets_full/level0/GameObject/`（134 个管理器）· `资料/说明书/08_主程序资源/运行时管理器.md` |
