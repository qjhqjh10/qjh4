# 「自作主张」清查（2026-09-17）

> **起因**：用户 2026-09-17 明确要求 —— 「**一切按照原版复刻，不做多余的自我设计**」，
> 并要我查一遍「**还有什么东西是你自作主张设计的**」。
>
> **方法**（下个会话可照跑）：grep 代码里的自认标记 ——
> `我们挑的 / 我们加的 / 我们自己 / 我们起的 / 是我们排的 / 没查到 / 查不到 / 未查到 / 我们选的 / 我们定的`
> —— 逐条回解包资料核实「**原版到底有没有答案**」。本次扫出约 **80 条**（战斗面板 45 处 + Core 层 47 处，去重后）。
>
> ⚠️ **本文件的规矩**：**每一条都标「原版有没有答案」**。有答案的必须改回去（留更正痕迹）；
> 真没答案的**保留并如实标注** —— 那才是诚实的偏离，不是「自作主张」。

## 一、已改回原版（原版有答案，我们当初没查到 / 挑错了）

| # | 位置 | 原来（我们挑的） | 改回原版 | 原版出处 |
|---|---|---|---|---|
| 1 | `BattleDriver.mulliganSeconds` | 我拍的 30 s（原注释：「值拿不到」） | **25.0 s** | `VarsGlobal.mulliganTimeLimit`（资产在 `sharedassets0.assets`，见 `资料/VarsGlobal_原版数值.md`） |
| 2 | `MulliganPanel` 倒计时显示 | 纯数字 `9` | **`0:09`**（`"0:0" + 秒数`） | `SetMulliganTimer` 的字面量 `StringLiteral_20746` = `"0:0"` |
| 3 | `MulliganPanel`「眼睛」 | 只收卡片按钮 | **连压暗层一起收** | `ShowMulliganElements` → `Shade.SwitchShade` |
| 4 | `MulliganPanel` 键盘 | 注释说「原版只有按钮」 | 原版**本来就有** Space/Enter | `MulliganManager__Update`（`0x20`/`0xd` → `ProcessMulliganDone`） |
| 5 | `TargetReticle` 弧线 | 两端**渐变**（Trail→Cross） | **单色** + `GradientMode.Fixed` | `CrosshairLineEffect__GenerateGradient`（只写 1 个颜色键） |
| 6 | `CardFeel.PopNumber` | 受伤红 / 治疗绿 | **纯白**（含治疗） | `DamageText` / `HealText` 的 TMP `m_fontColor`=(1,1,1) |
| 7 | `CardView.BadgeCounterInk` | 深褐 `(46,36,28)`（注释：「本地查不到」） | **暖白 (1, 0.973, 0.910) + Bold** | `TraitCounter` 的 TMP 组件（`08_预制体特效/战斗预制体/`） |
| 8 | `BattleDriver` 能量积累图标 | 写死 OFF | **判据 = `0 < manaAccumulation`** | `BattleManager__SetupBoardPhase.c:181/196` → `GameplayVariablesData.manaAccumulation` |
| 9 | `BattleDriver.turnSeconds` | 「600 等于没压力，所以取 60」（按我们口味改的） | **照原版三步结构**：ScenarioVariables → 缩时 → **matchType 覆盖**（本局 80 ⇒ **240 s**） | `ClockManager__GetTotalTime.c`；常量 `0x1834b31c4`=240.0 复核过 |

**顺带更正的 4 处「注释写错了」**（值本来就对，是记录不对）：
`ChoosePanel.cs:29`（卡上按钮偏移早就是原版的 323 px）· `CardChoicePanel.ToggleEye`（原版是开关，
但**那个方法没被反编译**，我们是照换牌面板同款推的，已标明是推断）·
`BattleDriver` 阵营资源（原注释「grep 不到任何调用点」不准确 —— 中间层 `ManaManager__Toggle*` 一直在，
只有**最上面按阵营决定的那层**没反编译）· `BattleDriver.PlayReturnFeel`（原版**有**回手动画 =
**倒放**「手牌→战场」那条 clip，`speed = −1` 已解出常量 `0x1834b2bc8`，**尚未实现**）。

## 二、核实过「原版确实没答案」的（保留，但都如实标着）

按类别列（**这些不是「我们不查」，是资料里真没有**）：

- **表现层的颜色/时长/形状**：卡高亮 6 态色与时长（`Shade` 类不在 71 个反编译类里）· 格位底片
  idle/drag 色 · 压暗层颜色（原版只查到有个 `Shade`）· 确认按钮位置（`BattleSettingsPanel` 无 `resignButton`）·
  结算面板骷髅叠放与未点亮态 · 结果文字字号（字烘在开门视频里）· 换牌面板「每张牌的换按钮」位置与文案。
- **运行时才算得出来的**：选牌面板卡行纵向位置（anchor 只有 Transform）· 合成选项卡顺序 ·
  手牌数底板绝对位置 · 时钟数字字号。
- **只有字段名、没有值**：`VarsGlobal` 里那些**尚未逐个核对用途**的项 · `VarsDevice`（分端参数，**整张表还没读**）。
- **服务器/LiveOps 侧**：`overtimeTurn` · 奖励/评分数据（已关服）。
  🆕 **线索**：`VarsGlobal` 刚证明「按名字 grep 不到 ≠ 资源不在」（它就在 `sharedassets0.assets` 里、
  只是那个 `.assets` 没有 type tree）⇒ `overtimeTurn` **值得照同样办法再找一次**。
  （`manaAccumulation` **2026-09-17 已找到判据**：`0 < manaAccumulation` —— 见 §一 #8。）
- **原版本来就没有的**（我们加的）：自研 26 张卡（含中文名与关键词）· 「提示行写本局用哪副牌」·
  换牌面板「点卡本身也能标记」· 回车提示那半句文案 · 13 阵营占位底色。
- ~~**AI 那一块**：`AI` 类方法体一个都没有 ⇒ 打分规则、换牌规则都无从照抄。~~
  ✅ **2026-09-17 已按原版重写完**（`AI` 整类 41 个方法体反编译；公式/常量/数据类全在 **`资料/AI_原版反编译_0917.md`**，**唯一出处**）。
  ⚠️ **唯一真缺的那一层**：`ScoringCriteria` 的**权重值在服务端**（本地没有）⇒ 只能**结构照原版、权重按我们卡表配**。
  ⚠️ **「留牌」「不自杀」原版这版是死代码**（0 调用点）—— 原来把它们记成「我们缺的功能」不准确。

## 三、还没动的 / 我们配的（**当天收工的已划掉**）

| 事项 | 原版是什么 | 缺什么 |
|---|---|---|
| ~~WaitBanner 底板~~ | `40k_popup`（**九宫格**，`m_Border=(169,160,169,160)`、`Rect=359×336`）+ `Mask` + `40k_popup_texture`（**Tiled** 128×128）+ 全屏 `Dark Shade`（α0.6118） | ✅ **2026-09-17 已照原版做**：给 `ImageQuad` 补了 `CreateNineSlice`（Sliced）/ `CreateTiled`（Tiled）两个原版 `Image.Type` 的替身，`WaitBanner` 的底板就是它们 |
| **🆕 AI 那一族**（打分权重 / 难度四档 / 设置面板入口） | 权重挂在卡资产上（**在服务端**）；难度三旋钮在 `AIBotsConfig` 里自动挑；**原版没有玩家可见的难度开关** | **权重我们配**（从 `EffectOp` 推，逐条标在 `SimpleAI.ScoreOp` / `PayloadScore` / `ScoreHealing`）· **难度四档的取值与理由**见 `资料/AI_原版反编译_0917.md` §七（**唯一出处**）· **设置面板那一行是我们加的**（`Battle/SettingsPanel.cs`） |
| **催促表现** | `ClockManager.ChangeMaterialHurryUp` = **换材质**（`timeRunningOutMaterial`）+ `DisplayHurryUpChatMessage` 语音 | 那个材质在解包里 **0 命中**（仍缺）；✅ **语音现在有了** —— `_hurry` 那族台词已随 1787 条导进 `Resources/Art/audio/vo/` ⇒ 差的是**触发点**。现在仍是变色，标注着 |
| **回手动画** | `PlayBackToHandAnimation` = **倒放**「手牌→战场」clip（`Rewind` → `speed=−1` → `time=length` → `Play`） | 机制已查清、常量已解；就是**倒放我们自己的 `DeploySequence`**，未实现 |
