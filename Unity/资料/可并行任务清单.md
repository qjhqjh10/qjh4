# 可并行派子代理的任务清单（2026-09-13 盘点）

> **为什么要这份清单**：用户 2026-09-13 明确要求 ——「要利用好子代理，多派子代理去完成
> **简单、重复**的任务。这样可以**多路并行**，加快项目进度。」（已写进 `CLAUDE.md` 铁律 1·b）
>
> **判据**：能不能切成**互不依赖的 N 块**、每块产出**格式一致的结论**、**不需要动引擎代码**。
>
> ✅ **怎么读这张表**：**行首带 ✅ 的别再重做**；没打 ✅ 的才是候选。
>
> ⚠️ **两条实测教训**（真金白银换来的）：
> ① **派之前先把要发出去的数据核对清楚** —— 有一次给立绘那批的名单第一版算错了
>    （写 29 张、实际 16 张），白让子代理查了一圈；
> ② **子代理会纠正你** —— 有一次它用官方中文文本 + 像素匹配推翻了我对某个徽记的判断。
>    **它的证据比你的印象可靠时，照它的改。**
>
> **派之前必读**：
> · **同时只能跑一个 Unity 实例** —— 所有 Unity 批处理（自检四条）**必须串行**，不能派。
> · 派的时候写清四样：**要查什么 · 已知什么 · 输出格式 · 行数上限**。
> · 看一眼**有没有别的子代理正在动同一批文件** —— 撞车会写出两份不一致的结论。

---

## 一、值得派的（按「产出价值 × 可拆块数」降序）

| # | 任务 | 切几块 | 每块产出什么 | 数据在哪 | 为什么现在能做 |
|---|---|---|---|---|---|
| ① | ✅ **2026-09-13 第三十三轮做完了**（7 块并成 3 块派出去）→ 逐效果表落 `资料/特效E组_逐效果根因表.md`；<br>⚠️ **但只修了其中 4 类锅**（混合 61 / 抓屏 41 / 未映射 shader 16 / `_EmissionColor` 11），<br>锅 A（trailMaterial 35）· 锅 F（prefab 发射器不画 16）· 锅 G（Matcap 属性表 13）**还没修**<br>~~**特效 E 组 176 个「亮度/密度不对」逐效果定根因**~~ | **7 块**（按技术构成：精灵图 89 / Mesh粒子+精灵 31 / 抓屏扭曲+精灵 27 / 扭曲+Matcap+Mesh 13 / Matcap+Mesh 9 / 扭曲+Mesh 5 / Matcap+精灵 2）；或 **20 块**（按名字前缀族 Buff 28 · BulletImpact 7 · Sau 7 · Recall 7 …） | 每块一张：`效果名 ‖ 偏亮/偏暗 ‖ 亮度比中位 ‖ \|ln\| ‖ 峰值亮点 ‖ 技术构成 ‖ 嫌疑发射器/材质槽 ‖ 材质锅还是 prefab 锅` | `资料/特效还原台账.tsv`（957 行，**一个效果一行**，E 组高置信 176 行）；技术构成 `资料/比对基线/per_effect_tech.json` | ✅ **第三十三轮已做完**（见本行第一格）。台账**只有数值没有根因列**，「人工备注」列 **0/957 填写**（`工具/analyze_sweep.py:224` 恒写空串）。逐效果根因表落在 `资料/特效E组_逐效果根因表.md`。⚠️ **那张表是按「旧台账」分的组** —— sweep 第三十四轮已经重跑、而且**指标变差**（`|ln|` 中位 0.155 → 0.202），**锅名单要按新台账重核**，见 `资料/特效还原_进度与交接.md` §〇·一 |
| ② | ✅ **第三十二轮已做完**（47 条句子逐条查证过，其中 5 条当场修掉）→ `资料/战术卡47条_语义查证.md`<br>~~**战术卡 47 条「完全不认识」句子逐条查证语义**~~ | **12 块**（按阵营，`tactic_by_faction.md` 每行一块） | 每块：`句子 → 规则书行号 / rule_core.gd 行号 / 反编译 Resolve* 名 / 建议文法 / 判「该不该做」`（原版也查不到的如实标） | `_tmp_view/tactic_unparsed.txt` ①栏 + `_tmp_view/tactic_by_faction.md`；三层权威：`资料/规则书/…中文翻译.md:161-225`、`d:/warpforge/scripts/rule_core.gd`、`d:/2/Warpforge_code/Scripts/Assembly-CSharp/` | ⚠️ **分工仍然成立**：**查证可以派，改 `EffectText.cs` 留在主对话**（同一文件，块间会撞） |
| ③ | ✅ **第三十二轮做完了**（提交 `7f20f0c`）→ `资料/原版预组牌_核对.md`。<br>⚠️ 顺带产出：那 236 副牌的 `cardIds` 是**真原版 id**，第三十三轮拿它验了 id 前缀↔阵营<br>~~**236 副原版预组牌逐副核 + 顺带抽验自建 id 表**~~ | **13 块**（按阵营） | 每块：`每副牌 → heroId 查得到吗 / cardIds 全查得到吗 / 用我们 1130 张卡池能拼出吗 / 30 张合法吗` | `数据/游戏数据/decklists.json`（236 副，含 `heroId`+`cardIds`）、`prebuilt_decks_full.json`、`card_ids.json`、`warlord_ids.json`、`Assets/RuleEngine/Resources/cards_engine.json` | ✅ **第三十二轮已做完**（见本行第一格）→ `资料/原版预组牌_核对.md`。规模实测数据留在这里备查：876 个唯一 cardId **只有 796 个**能在 `card_ids.json` 查到（80 缺）；63 个 heroId 只有 57 在 `warlord_ids.json`。⚠️ 这三个 json **全仓 C# 0 引用** |
| ④ | **77 个原版 shader 逐个建「属性表+pass状态+采样纹理」清单** | **77 块**（或按导出报告里 **107 个 shader 名**切） | 每块 1 张：`属性名/类型/默认值 / rtBlend0 / zWrite / cull / 关键字全集 / 采样纹理名 / 用到它的材质与数值` | 工具 `工具/dump_shader.py:26`、`dump_shader_blob.py:69`；名字清单从 `Assets/WarpforgeVFX/导出报告.tsv` 第 3 段抽（959 行 / 107 名 / 44 个 `Everguild/`） | **没做**。全库没有任何逐 shader 清单。纯 Python 读 bundle，**不用 Unity**。⚠️ 两个工具都是「一次一个名字」不循环，**先包一层批处理脚本再切块** |
| ⑤ | **特效 W 组 191 个「两边全程空」分类：该空 vs 等事件** | **5–8 块**（按前缀族：BulletImpact 67 / Lasrifle 18 / NecronGauss 11 / Blast 6 / Ork 6 / 其余 69 个前缀归一堆） | 每块一张：`效果名 → (a) 纯脚本占位、本来就该空 / (b) 在等哪个事件`；b 那堆交回 AnimFX 清单 | `资料/特效还原台账.tsv`（分组=W，191 行）+ 规则书 `:161-225` | **没做**。⚠️ 文档里那条「按后缀族归 958 个」的线索**实测基本无效**（191 个里带 `_ability/_quick` 的只有 **3 个**），**别照它派** |
| ⑥ | 🟡 **半做**：数值那部分第三十二轮做完（`_裁定_三围/费用/稀有度.md`）；<br>**图标那份第三十三轮做完了** → `_裁定_图标丢失.md`（54 张卡）<br>⚠️ **还欠**：`special` 稀有度→卡框 tier **已实测完**（见 ⑭），**欠的是「照实测值改代码」那一步**（且改法要拍板）<br>~~**卡面独立读出的数值差异逐条裁定**~~ | **4 块**（费用 9 / 近战+远程 10 / 生命 3 / 稀有度 28） | 每块：`卡名 → 谁对 → 第三来源证据`（重看卡图 or `d:/2/Warpforge部队卡片/卡牌信息权威表_0824.md`） | `资料/卡表核对_卡图提取/_对账.md:12`（费用）、`:26`（近战）、`:34`（远程）、`:46`（生命）、`:54`（护甲 0 处）、`:58`（稀有度） | 🔴 **数值那条 2026-09-14 复核过：现状与这里原来写的不一样** —— ① **数值修正有专门的通道**：`工具/gen_cards_engine.py` 里的 `STAT_FIXES` 表（每条都带卡图路径当证据），**不是**走 `cardface_fixes.json`；② `cardface_fixes.json` 现在有 **`keywords` / `subtype` / `desc` 三个键**（`desc` 是第三十三轮加的手工列，脚本会原样保留）。③ **2026-09-14 的「数值第二轮」对账结论是「空操作」** —— 它报的 7 张**在当前引擎表里已经全对**（第一轮的 6 处已合并），价值在**独立复核**与那次旁支发现（见下）；明细 `资料/卡表核对_卡图提取/_裁定_数值第二轮.md` + `_tmp_view/stat_fixes.json`。④ **旁支发现已修**：5 张 Aeldari 督军的**费用 5 是阵营徽记被读成数字**（督军卡**没有费用六边形**）⇒ 已进 `STAT_FIXES` + 加了断言。⚠️ **仍有 9 张督军 cost 非 0，一张都没开图核过** |
| ⑦ | ✅ **2026-09-13 第三十三轮做完了**（剩的 39 个全补）→ `资料/关键词图标/_规则书关键词表.md` 附录<br>（顺带归纳出「引擎缺哪四类事件」，直接喂了 `When <事件>` 铺宽）<br>~~**规则书 61 关键词补「触发时机 → 对应事件」列**~~ | **4 块**（每块约 15 个） | 61 行表：`中文名/英文名/触发时机/对应事件/出处行号` | 61 条清单**已有**：`资料/关键词图标/_规则书关键词表.md:3`；**种子表只有 18 行**在 `资料/特效还原_进度与交接.md:673-692` | ⚠️ **更正（2026-09-13 第三十四轮，派子代理核过）**：这一格原来写「只补了 18/61、剩 43 个」是**错的**。实际 **61 个里 58 个已有「触发时机 → 对应事件」行**（A 段 31 + B 段 22 = 53 行覆盖 58 个），**真缺的只有 3 个：伏击 `Ambush` · 压制 `Pindown` · 先锋 `Vanguard`**（后两个规则书**根本没写时机**，属「选目标 / 行动被拒」那一类）。完整表在 `资料/关键词图标/_规则书关键词表.md` 附录 |
| ⑧ | **13 阵营 VO 语音建索引（1698 条 `VO_*.ogg`）** | **13 块**（按阵营） | 每块：`卡名/角色 → 语音文件 + 事件类型（attack / death / cantDo / concede …）`映射表 | `d:/2/解包整理/01_卡牌/<阵营>_*/AudioClip/VO_*.ogg`（13 个阵营目录，`find` 得 **1698** 条）；另有 `d:/2/解包整理/04_音频/督军语音` | **没做，规模已实测**：`Assets/` 下 `*.ogg` **0 个**，全仓没有 AudioSource 代码。文件名自带 `VO_<阵营>_<角色>_<事件>.ogg` 规律，纯文件名解析 |
| ⑨ | ✅ **2026-09-13 第三十三轮做完了**（18 块并成 2 块）→ `资料/AnimFX_18类成表.md`<br>（含字段/参数分布/覆盖效果数/**建议重写顺序**）<br>~~**AnimFX 18 个模块逐类成表**~~ | **18 块** | 每类 1 张：`字段名/类型/[SerializeField]/参数值分布/用它的效果清单/模块优先级` | 桩 `d:/2/Warpforge_code/Scripts/Assembly-CSharp/AnimFX*.cs`（18 个文件 907 行）；`数据/游戏数据/animfx_components.json`（2346 组件）；`数据/游戏数据/tween/`（74 个 JSON） | ✅ **第三十三轮已做完**（见本行第一格）→ `资料/AnimFX_18类成表.md`。⚠️ **别再派「读反编译方法体」** —— `decomp_il2cpp_0827/decomp_out{,2}/` 里**一个 AnimFX 类都没有**，桩文件方法体全空，只能**按语义重写** |
| ⑩ | **`_对账.md` 里「对不上」的三堆卡逐条裁定** | **3–4 块** | 每块：`卡名 → 是 OCR 名字写法不同 / 我们真缺这张 / 真重名` | `资料/卡表核对_卡图提取/_对账.md:444`（**45 张**「新表有、旧表按名字对不上」）；`资料/卡牌数据源对账_0912.md:293`（**9 张「只有文档有」**，先看 `Fire Warrior Marksman`）、`:294`（宝石表里没有的 22 张 + 模糊匹配 6 张）、`:295`（`Terminator Champion` / `Terror of Vardenghast` 两处冲突） | 三处都**只列了清单、没裁定**（脚本明写「只报差异，不做裁定」）。一卡一行，天然可切 |
| ⑪ | ✅ **2026-09-13 第三十三轮做完了** → `资料/关键词三列对账.md` 的追加节<br>🔴 顺带查出 **3 个关键词在静默丢弃**（`sabotage`/`spiritstone`/`faith` 不在 `Prefixes`，既不实现也不报出）<br>~~**`DefinedTrait` 134 个枚举 vs 规则书 61 关键词 vs `RuleCore` 三列对账**~~ | **2–3 块** | 一张表，标出「原版有、我们连词都没有」的那些 + 每条的频次 | `d:/2/Warpforge_code/Scripts/Assembly-CSharp/DefinedTrait.cs`（137 行，134 个带值成员）；`资料/关键词图标/_规则书关键词表.md`；`Core/RuleCore.cs:1255 UnimplementedKeywords()` | ✅ **第三十三轮已做完**（见本行第一格）→ `资料/关键词三列对账.md` 追加节。`卡牌效果管线_计划与交接.md` 把它点名为「低成本、高信息量」的待办 —— 那条已兑现 |
| ⑫ | **原版菜单界面盘点 vs 我们做了哪些** | **4 块**（文档**已经切好**了） | 每块：`界面名 / 干什么 / 我们有没有 / 图与结构在不在本地` | `资料/说明书/04_界面UI/菜单全树_要点_第1~4块.md`（524 / 531 / 548 / 602 行） | **没做**。我们工程里**总共只有 3 个场景**（Battle / CardBase / DeckEditor），**一个菜单场景都没有**。⚠️ 价值是给「周边功能」画地图，不是马上要做的活 |
| ⑬ | **13 个战场全树逐份盘 HUD 差异** | **13 块** | 每个战场：`与 battlearena1 的 HUD 节点差集`（重点看阵营资源件 Faith/SpiritStone） | `资料/说明书/01_战斗_对战/2D层_battlearena*全树.md`（arena1 **854** 行，其余 12 份 **1140–1380** 行） | ✅ **2026-09-13 第三十一轮已做过一次**（查出「任务点只属于暗黑天使」）。⚠️ 但**结论是「4 个 holder 每个场景都摆、靠运行时 SetActive 切」** ⇒ 纯场景 diff 会得出**错误结论**，要连**运行时 dump + 二进制**一起看。剩下的价值在「13 个 3D 战场我们只搬了 1 个」。🔴 **别按原法派** —— 纯场景 diff 这条路**已被证伪**；真要做就**连同运行时 dump + 二进制**一起看 |
| ⑭ | ✅ **2026-09-13 已经实测过**（`资料/普查产出_0913/special_tier_实测_前半.md` 20 张 + `_后半.md` 19 张，两套独立方法互验 19/19 一致）<br>~~**`special` 稀有度 → 卡框 tier 映射实测**~~ | ~~**4 块**（39 张 `special`，每块 10 张）~~ | ~~每张：卡面实际用的框档（tier1–4）~~ | 卡图 `d:/2/Warpforge部队卡片/`；卡表 `资料/卡表核对_卡图提取/_合并总表.md` | ⚠️ **2026-09-14 更正（铁律 5）：这一格原来写「没做 · `special` 没实测过 · 暂按 tier4」——三条都不成立。** 实测结论（另见 `资料/卡表核对_卡图提取/_裁定_special卡框.md`，2026-09-14 独立复核）：**39 张 = tier1 24（8 阵营）· tier2 15（5 阵营）· tier3 0 · tier4 0**，而且 **tier 是「按阵营」的常数、不是逐卡属性**（每阵营三张完全一致）。🔴 **「4 档框 ↔ 4 档稀有度」这个前提本身是错的** —— 卡图上**只有一种框按批次变**，稀有度的真正尺子是**底部菱形宝石**（浅蓝 186 / 绿 122 / 紫 279 / 金 42 / **橙红 15 = special**）。⇒ **`special = 4` 是错的**，要改的是 `CardPresentation/Core/CardArt.cs` 的 `TierOf()` 与 `工具/import_original_art.py` 的 `RARITY_TIER`；**但改法要拍板**（不是「4 改成 1」，正确形状是**按阵营/批次给档**）。⚠️ 另有两条待对齐：`special` 到底几张**三处不一致**（引擎表 53 / 39+6 / `_合并总表` 45）· `资料/分层卡演示_0827/README.md` 的 `GameData.force_frame_tier4=true` **把全卡框强拉到 tier4**，与本实测直接冲突 |
| ⑮ | **958 行「效果名 → 用了哪些原版 shader」对账表** | **2 块**（纯 Python 解析） | 958 行：`效果名 ‖ 材质数 ‖ 原 shader 列表 ‖ 是否含未映射 shader` | `Assets/WarpforgeVFX/导出报告.tsv`（959 行，每行第 3 段是 `材质定义N个；原 shader: …`） | **没做**（文档只提醒过「别漏看第 3 段」）。这是 ④ 的输入，可以一起派 |
| ⑯ | **33 个补充 shader 的引用清单落盘** | **1 块** | 一张表：`shader ‖ 影响效果数 ‖ 在不在 wf_shaders_extra.bundle ‖ 主包有没有` | `工具/extract_missing_shaders.py:116-131`（`--check` 能打印 [1]–[5] 段，**但不落盘**） | **半做**：跑得出来，只是没留档 |

---

## 二、**不建议派的**（看着能派、实际不能）

| 活 | 为什么不能派 |
|---|---|
| **任何 Unity 批处理**（`RuleEngineTest.Run` / `BattleScene.Run` / `DeckScene.Run` / `CardBaseDemo.Run` / `EffectCompare` / `EffectIso` / `MatIsoProbe` / `CEmitProbe` / `WhiteboardTest`） | **同时只能跑一个 Unity 实例**，而且 `-executeMethod` 一次只能给一个；隔离渲染还要求「同一进程同一帧」 |
| **「构建后 player 验证」** | 要建 Windows player + 跑图形设备，只能串行一次 |
| **② 那一族句子的「实现」本身 / 加时机制 / AI 加强 / 卡实例身份** | 全都改同一批 C#（`EffectText.cs` / `EffectResolver.cs` / `RuleCore.cs` / `BattleContext.cs`），块之间既撞文件又互相依赖。**查证可以派，动手留在主对话。** |
| **`EffectExporter.Run` → `EffectLibraryBuilder.Run`** | 硬顺序：重导完不跟一次建库，958 条引用会全变 null **而且不报错** |
| **`EffectSweepBatch`（两趟 orig/exp）** | 有顺序依赖，且一趟 25 分钟 |
| **悬案 #1「攻击方式选择器三钮的位置」** | dump 与代码打架、没有实况可判；只能跑原版游戏，单实例 |
| **悬案 #5「关键词字号没有原版出处」** | 要渲染才量得出来，属 Unity 活 |
| **16 张配不上立绘的卡** | 用户 2026-09-12 说过暂时不管 |
| **`资料/说明书/04_界面UI/菜单全树.md`（16771 行）整份读** | 一个代理吃不掉，必须按 ⑫ 那 4 块切 |
